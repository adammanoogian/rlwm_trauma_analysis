---
phase: 13-infrastructure-repair-hierarchical-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fitting/numpyro_models.py
  - scripts/fitting/legacy/numpyro_models.py
  - scripts/fitting/fit_bayesian.py
  - pyproject.toml
  - pytest.ini
  - environment_gpu.yml
  - scripts/16b_bayesian_regression.py
  - validation/test_pymc_integration.py
autonomous: true

must_haves:
  truths:
    - "`from scripts.fitting.numpyro_models import qlearning_hierarchical_model` succeeds on fresh checkout"
    - "`python scripts/fitting/fit_bayesian.py --model qlearning --help` prints help text without ImportError"
    - "PyMC is no longer a dependency — `pip install -e '.[bayesian]'` does NOT pull PyMC"
    - "`python scripts/16b_bayesian_regression.py --dry-run --model wmrl_m3` runs without PyMC import"
  artifacts:
    - path: "scripts/fitting/numpyro_models.py"
      provides: "NumPyro hierarchical models at canonical import path"
      contains: "def qlearning_hierarchical_model"
    - path: "pyproject.toml"
      provides: "Pinned deps without PyMC"
      contains: "numpyro==0.20.1"
  key_links:
    - from: "scripts/fitting/fit_bayesian.py"
      to: "scripts/fitting/numpyro_models.py"
      via: "import statement at line 43"
      pattern: "from scripts\\.fitting\\.numpyro_models import"
    - from: "scripts/16b_bayesian_regression.py"
      to: "numpyro"
      via: "hard require NumPyro, no PyMC fallback"
      pattern: "BACKEND = .numpyro."
---

<objective>
Fix the P0 broken import bug, pin Bayesian dependency versions, and drop PyMC entirely from the project.

Purpose: Every downstream Phase 13 plan (and all of Phases 14-18) depends on `scripts/fitting/numpyro_models.py` being importable at its canonical path. The broken import at `fit_bayesian.py:43` is a blocking P0 bug. Dependency pinning prevents silent breakage from ArviZ 1.0 (which shipped 2026-03-02 and breaks `InferenceData`). PyMC removal simplifies the dependency tree and eliminates the dual-backend maintenance burden.

Output: Working `numpyro_models.py` at canonical path, pinned deps, PyMC-free `16b_bayesian_regression.py`.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-infrastructure-repair-hierarchical-scaffolding/13-RESEARCH.md

Key files to read before starting:
- scripts/fitting/legacy/numpyro_models.py (740 lines — the file to resurrect)
- scripts/fitting/fit_bayesian.py:43 (the broken import)
- pyproject.toml:43-47 (bayesian extra), :109 (mypy pymc override), :130-133 (requires_pymc marker)
- pytest.ini:12 (requires_pymc marker)
- scripts/16b_bayesian_regression.py:58-90 (backend detection block)
- environment_gpu.yml (add netcdf4, pin numpyro/arviz)
- validation/test_pymc_integration.py (delete entire file)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resurrect numpyro_models.py at canonical path and fix imports</name>
  <files>
    scripts/fitting/numpyro_models.py
    scripts/fitting/legacy/numpyro_models.py
    scripts/fitting/fit_bayesian.py
  </files>
  <action>
    1. Copy `scripts/fitting/legacy/numpyro_models.py` to `scripts/fitting/numpyro_models.py` (the canonical path that `fit_bayesian.py:43` expects).

    2. In the new `scripts/fitting/numpyro_models.py`:
       - Add `from __future__ import annotations` at top (project convention).
       - Replace the old `sys.path` hack (lines 33-37) with a clean relative import since the module is now at `scripts/fitting/`.
       - Replace `from typing import Dict, List, Any, Optional` with Python 3.10+ native types (`dict`, `list`, `Any` from typing only if needed).
       - Verify the imports from `scripts.fitting.jax_likelihoods` still resolve correctly at the new path.
       - Do NOT rewrite the model logic — this is a move, not a rewrite. The legacy centered parameterization will be replaced in Phase 15 when HIER-01 is implemented. For now, just make it importable.

    3. In `scripts/fitting/legacy/numpyro_models.py`:
       - Add a deprecation comment at the top: `# DEPRECATED: moved to scripts/fitting/numpyro_models.py (v4.0 INFRA-01). This file retained for git history only.`
       - Leave the file otherwise untouched.

    4. Verify `fit_bayesian.py` import at line 43 resolves:
       - Run: `python -c "from scripts.fitting.numpyro_models import qlearning_hierarchical_model, wmrl_hierarchical_model, prepare_data_for_numpyro, run_inference, samples_to_arviz; print('OK')"` from project root.
       - Run: `python scripts/fitting/fit_bayesian.py --help` (should print help, not ImportError).
  </action>
  <verify>
    `python -c "from scripts.fitting.numpyro_models import qlearning_hierarchical_model; print('Import OK')"` prints "Import OK".
    `python scripts/fitting/fit_bayesian.py --help` prints usage text without error.
  </verify>
  <done>
    `scripts/fitting/numpyro_models.py` exists at the canonical path with all 5 exports that `fit_bayesian.py:43` expects. Import succeeds from project root.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pin Bayesian deps, remove PyMC from project, clean up 16b</name>
  <files>
    pyproject.toml
    pytest.ini
    environment_gpu.yml
    scripts/16b_bayesian_regression.py
    validation/test_pymc_integration.py
  </files>
  <action>
    **pyproject.toml changes:**
    1. Replace the `bayesian` extra (lines 43-47) with pinned versions:
       ```toml
       bayesian = [
           "numpyro==0.20.1",
           "arviz==0.23.4",
           "netcdf4",
       ]
       ```
       Note: `pymc>=5.0` is REMOVED. `netcdf4` is ADDED (required for `InferenceData.to_netcdf()`).

    2. Remove `"pymc.*"` from mypy overrides module list (line 109). Keep all other entries.

    3. Remove `"requires_pymc: tests that require PyMC installation"` from `[tool.pytest.ini_options]` markers (line 132).

    **pytest.ini change:**
    4. Remove `requires_pymc: tests that require PyMC installation` from markers (line 12).

    **environment_gpu.yml changes:**
    5. In the pip section, pin: `numpyro==0.20.1`, `arviz==0.23.4`. In the conda deps section, add `netcdf4`. Remove any `pymc` or `pytensor` entries if present.

    **scripts/16b_bayesian_regression.py changes:**
    6. Replace the backend detection block (lines 58-90) with a hard NumPyro require:
       ```python
       # Backend: NumPyro/JAX only (v4.0 INFRA-07: PyMC dropped)
       import jax.numpy as jnp
       import jax.random as random
       import numpyro
       import numpyro.distributions as dist
       from numpyro.infer import MCMC, NUTS
       import arviz as az
       BACKEND = "numpyro"
       ```
       Delete the `BACKEND = None` / try/except PyMC block entirely.

    7. Find and delete the `_run_pymc` function (search for `def _run_pymc`). Remove any references to PyMC in docstrings and error messages. Update the module docstring (lines 1-42) to remove PyMC references, noting: `# v4.0: PyMC backend removed (INFRA-07). NumPyro-only.`

    **validation/test_pymc_integration.py:**
    8. Delete the entire file. It tests PyMC models that no longer exist in the project.

    **Verification:**
    Run `python -c "import pyproject_metadata"` is NOT needed. Instead verify:
    - `grep -r "pymc" pyproject.toml` returns zero hits (except comments).
    - `grep -r "requires_pymc" pytest.ini pyproject.toml` returns zero hits.
    - `python scripts/16b_bayesian_regression.py --help` works (if it has --help).
    - `python -m pytest scripts/fitting/tests/ -v` still passes (no PyMC dep).
  </action>
  <verify>
    `grep -c "pymc" pyproject.toml` returns 0 (no PyMC references remain).
    `grep -c "requires_pymc" pytest.ini` returns 0.
    `python -c "from scripts.fitting import numpyro_models; print('OK')"` passes.
    `test ! -f validation/test_pymc_integration.py` returns 0 (file deleted).
  </verify>
  <done>
    PyMC fully removed from dependency tree. `pyproject.toml` pins `numpyro==0.20.1`, `arviz==0.23.4`, adds `netcdf4`. `16b_bayesian_regression.py` hard-requires NumPyro. `validation/test_pymc_integration.py` deleted.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from scripts.fitting.numpyro_models import qlearning_hierarchical_model, wmrl_hierarchical_model; print('PASS')"` prints "PASS".
2. `python scripts/fitting/fit_bayesian.py --help` prints usage text.
3. `grep -c "pymc" pyproject.toml` returns 0.
4. `grep -c "requires_pymc" pytest.ini pyproject.toml` returns 0.
5. `test ! -f validation/test_pymc_integration.py && echo DELETED` prints "DELETED".
6. `python -m pytest scripts/fitting/tests/ -v` passes (existing tests unbroken).
</verification>

<success_criteria>
- Import `from scripts.fitting.numpyro_models import ...` works on fresh checkout
- PyMC fully excised from deps and code (except legacy/ archive)
- `numpyro==0.20.1`, `arviz==0.23.4`, `netcdf4` pinned in pyproject.toml
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-infrastructure-repair-hierarchical-scaffolding/13-01-SUMMARY.md`
</output>

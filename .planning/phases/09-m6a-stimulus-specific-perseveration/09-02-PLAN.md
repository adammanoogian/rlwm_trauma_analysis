---
phase: 09-m6a-stimulus-specific-perseveration
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - scripts/fitting/model_recovery.py
  - scripts/11_run_model_recovery.py
  - scripts/14_compare_models.py
  - scripts/15_analyze_mle_by_trauma.py
  - scripts/16_regress_parameters_on_scales.py
autonomous: true

must_haves:
  truths:
    - "M6a appears in the choice-only AIC/BIC comparison table alongside M1-M3 and M5"
    - "Scripts 15 and 16 accept --model wmrl_m6a and report kappa_s parameter associations with trauma scales"
    - "Parameter recovery simulation generates synthetic data with per-stimulus last_actions tracking"
  artifacts:
    - path: "scripts/fitting/model_recovery.py"
      provides: "M6a synthetic data generation with per-stimulus dict and parameter recovery"
      contains: "wmrl_m6a"
    - path: "scripts/11_run_model_recovery.py"
      provides: "wmrl_m6a in argparse choices"
      contains: "wmrl_m6a"
    - path: "scripts/14_compare_models.py"
      provides: "M6a model comparison via --m6a argument and auto-detection"
      contains: "wmrl_m6a"
    - path: "scripts/15_analyze_mle_by_trauma.py"
      provides: "M6a trauma group analysis with kappa_s"
      contains: "kappa_s"
    - path: "scripts/16_regress_parameters_on_scales.py"
      provides: "M6a continuous regression with kappa_s"
      contains: "wmrl_m6a"
  key_links:
    - from: "scripts/fitting/model_recovery.py"
      to: "scripts/fitting/mle_utils.py"
      via: "import WMRL_M6A_BOUNDS, WMRL_M6A_PARAMS"
      pattern: "WMRL_M6A_BOUNDS|WMRL_M6A_PARAMS"
    - from: "scripts/fitting/model_recovery.py"
      to: "generate_synthetic_participant"
      via: "per-stimulus last_actions dict (not global scalar)"
      pattern: "last_actions.*stimulus|last_actions\\[stimulus\\]"
    - from: "scripts/14_compare_models.py"
      to: "output/wmrl_m6a_individual_fits.csv"
      via: "--m6a argument or auto-detection"
      pattern: "wmrl_m6a_individual_fits"
---

<objective>
Wire M6a into the downstream pipeline: parameter recovery (model_recovery.py), model comparison (script 14), trauma group analysis (script 15), and continuous regression (script 16).

Purpose: Without pipeline integration, M6a can be fit but cannot be validated (recovery), compared (AIC table), or analyzed (trauma associations). This plan completes the full M6a feature and makes it appear in the choice-only comparison table alongside M1-M3 and M5.

Output: All five downstream scripts handle wmrl_m6a correctly, parameter recovery uses per-stimulus last_actions dict (not global scalar), and the model_recovery.py argparse is also fixed to include wmrl_m5 (pre-existing gap).
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-m6a-stimulus-specific-perseveration/09-RESEARCH.md
@.planning/phases/09-m6a-stimulus-specific-perseveration/09-01-SUMMARY.md
@.planning/phases/08-m5-rl-forgetting/08-02-SUMMARY.md

@scripts/fitting/model_recovery.py
@scripts/11_run_model_recovery.py
@scripts/14_compare_models.py
@scripts/15_analyze_mle_by_trauma.py
@scripts/16_regress_parameters_on_scales.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: M6a parameter recovery simulation</name>
  <files>scripts/fitting/model_recovery.py, scripts/11_run_model_recovery.py</files>
  <action>
Add wmrl_m6a support to model_recovery.py by extending the M3/M5 patterns. The CRITICAL difference from M5 is the per-stimulus last_actions tracking in generate_synthetic_participant.

1. **Imports**: Add `WMRL_M6A_BOUNDS, WMRL_M6A_PARAMS` to the import block from `scripts.fitting.mle_utils`.

2. **get_param_names()**: Add `elif model == 'wmrl_m6a': return WMRL_M6A_PARAMS`.

3. **sample_parameters()**: Add `elif model == 'wmrl_m6a':` setting `bounds = WMRL_M6A_BOUNDS` and `param_names = WMRL_M6A_PARAMS`.

4. **generate_synthetic_participant()** -- THIS IS THE CRITICAL CHANGE:
   Add `elif model == 'wmrl_m6a':` handling with per-stimulus last_actions dict.

   a. Extract `kappa_s = params['kappa_s']` alongside other WM-RL params (alpha_pos, alpha_neg, phi, rho, capacity, epsilon). Do NOT extract kappa or phi_rl (M6a has neither).

   b. In WM initialization: add `'wmrl_m6a'` to `model in ('wmrl', 'wmrl_m3', 'wmrl_m5', 'wmrl_m6a')`.

   c. At the top of each block loop iteration (inside `for block_idx in ...`), initialize:
      ```python
      if model == 'wmrl_m6a':
          last_actions = {}  # dict: stimulus -> last action (None = never seen)
      ```
      This resets at each block boundary (M6-04).

   d. Inside the trial loop, AFTER computing hybrid_probs from Q and WM, add the M6a perseveration block:
      ```python
      elif model == 'wmrl_m6a':
          if last_actions.get(stimulus) is not None:
              hybrid_probs = hybrid_probs.copy()
              hybrid_probs[last_actions[stimulus]] += kappa_s
              hybrid_probs = hybrid_probs / np.sum(hybrid_probs)
      ```
      NOTE: This is different from M3/M5 which use a global `last_action` variable. M6a only applies the kernel if THIS SPECIFIC stimulus was seen before in this block.

   e. After action is taken, update per-stimulus tracking:
      ```python
      if model == 'wmrl_m6a':
          last_actions[stimulus] = action
      ```
      This goes alongside the existing `last_action = action` for M3/M5.

   f. The WM update block must also handle M6a: update `model in ['wmrl', 'wmrl_m3', 'wmrl_m5']` to include `'wmrl_m6a'`.

   g. The Q-learning update applies identically (no phi_rl decay for M6a).

5. **All remaining dispatch blocks**: There are dispatch points in `run_parameter_recovery()` (~line 640), `compare_behavior()` (~line 719), and the plotting/summary function (~line 818). At each location, grep for `elif model == 'wmrl_m5':` and add `elif model == 'wmrl_m6a':` COPYING THE M3 BRANCH (not M5). The reason: M5 branches extract `phi_rl` which M6a does not have. M6a has the same 7 parameters as M3 (with kappa_s replacing kappa). Specifically:
   - `run_parameter_recovery()`: Copy M3 branch using `WMRL_M6A_PARAMS` and `WMRL_M6A_BOUNDS`. Do NOT copy M5's branch (it references phi_rl).
   - `compare_behavior()`: Copy M3 branch using `WMRL_M6A_PARAMS`. Do NOT copy M5's branch.
   - Plotting/summary function: Copy M3 branch using `WMRL_M6A_PARAMS`. Do NOT copy M5's branch.

6. **model_recovery.py argparse** (line ~919): The choices list currently only has `['qlearning', 'wmrl', 'wmrl_m3']` -- this is already outdated. Fix to: `choices=['qlearning', 'wmrl', 'wmrl_m3', 'wmrl_m5', 'wmrl_m6a']`. Add both wmrl_m5 and wmrl_m6a.

7. **scripts/11_run_model_recovery.py**: Add `'wmrl_m6a'` to argparse choices and to the `'all'` expansion list. Also add `'wmrl_m5'` if missing.

**Avoid:**
- Do NOT use a global `last_action` scalar for M6a in synthetic generation. The entire point is per-stimulus tracking.
- Do NOT share the `last_actions` dict across blocks -- it must reset to `{}` at each block boundary.
- Do NOT apply M6a kernel when a stimulus is encountered for the first time in a block (dict.get returns None).
- Do NOT copy M5 branches for dispatch functions -- M5 has phi_rl which M6a lacks. Always copy M3 branches and substitute kappa_s for kappa and use M6A constants.
  </action>
  <verify>
Run a quick parameter recovery test (small N):
```bash
python scripts/11_run_model_recovery.py --model wmrl_m6a --n-subjects 10 --n-starts 10 --n-jobs 4
```
Check that:
- It completes without error
- Recovery CSV is written to output/recovery/wmrl_m6a/
- Correlations are printed (exact r values checked in later full recovery run)
  </verify>
  <done>
- model_recovery.py handles wmrl_m6a in all dispatch functions
- Synthetic data generation uses per-stimulus last_actions dict (not global scalar)
- last_actions dict resets at each block boundary
- First-presentation of each stimulus in a block: no kernel applied (dict returns None)
- Quick recovery test (N=10) completes without error
- model_recovery.py argparse updated to include wmrl_m5 and wmrl_m6a
- All M6a dispatch branches copy M3 pattern (no phi_rl), not M5 pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: M6a pipeline scripts (comparison + trauma analysis)</name>
  <files>scripts/14_compare_models.py, scripts/15_analyze_mle_by_trauma.py, scripts/16_regress_parameters_on_scales.py</files>
  <action>
**Script 14 (compare_models.py):**

1. Add `--m6a` argument to argparse (after `--m5`):
   ```python
   parser.add_argument('--m6a', type=str, default=None,
                       help='Path to M6a (WM-RL+kappa_s) individual fits CSV')
   ```

2. In the auto-detection patterns dict (find_mle_files or equivalent), add:
   ```python
   'M6a': ['wmrl_m6a_individual_fits.csv', 'wmrl_m6a_mle_results.csv'],
   ```

3. In the load block, add M6a handling:
   ```python
   if args.m6a:
       fits_dict['M6a'] = load_fits(args.m6a)
   ```
   The comparison table logic is data-driven (iterates fits_dict), so the AIC/BIC table will automatically include M6a once the key exists.

4. Also add fallback search in `output/` root for M6a (matching the M5 dual-path pattern from Phase 8).

**Script 15 (analyze_mle_by_trauma.py):**

1. Add constant: `WMRL_M6A_PARAMS = ['alpha_pos', 'alpha_neg', 'phi', 'rho', 'capacity', 'kappa_s', 'epsilon']`

2. Add `'kappa_s'` to `PARAM_NAMES` display dict: `'kappa_s': r'$\kappa_s$'`

3. In `load_data()`: Add defensive loading for wmrl_m6a (parallel to M5):
   ```python
   wmrl_m6a_path = OUTPUT_DIR / "wmrl_m6a_individual_fits.csv"
   # Also check output/mle/ path
   ```
   Use dual-path search matching the M5 pattern from 08-02-SUMMARY.

4. In `MODEL_CONFIG` dict construction, add M6a entry:
   ```python
   if wmrl_m6a is not None:
       MODEL_CONFIG['wmrl_m6a'] = {'name': 'WM-RL+M6a', 'params': WMRL_M6A_PARAMS, 'data': wmrl_m6a}
   ```

5. In column name mapping: add `'kappa_s'` handling (rename to `kappa_s` or detect dynamically like existing params).

6. Update argparse `choices`: add `'wmrl_m6a'`. Update `'all'` expansion to include `'wmrl_m6a'`.

**Script 16 (regress_parameters_on_scales.py):**

1. In `load_integrated_data()`, in the param_rename block, add kappa_s handling:
   ```python
   if 'kappa_s' in params_df.columns:
       param_rename['kappa_s'] = 'kappa_s_mean'
   ```

2. In the `format_label` dict or equivalent display name mapping, add:
   ```python
   'kappa_s_mean': r'$\kappa_s$',
   ```

3. In `main()`, in the param_cols dispatch section, add:
   ```python
   elif model == 'wmrl_m6a':
       param_cols = ['alpha_pos_mean', 'alpha_neg_mean', 'phi_mean', 'rho_mean',
                     'wm_capacity_mean', 'kappa_s_mean', 'epsilon_mean']
   ```

4. Update argparse `choices`: add `'wmrl_m6a'` to the list. Update `'all'` expansion to include `'wmrl_m6a'`.

5. Add fallback path search for M6a fits (dual-path pattern from Phase 8).

**Avoid:** Do not restructure any existing model handling. All changes are additive elif branches and new dict entries.
  </action>
  <verify>
1. `python scripts/14_compare_models.py --help` shows --m6a argument
2. `python scripts/14_compare_models.py` -- auto-detects and includes M6a in comparison table (requires wmrl_m6a_individual_fits.csv from Plan 01)
3. `python scripts/15_analyze_mle_by_trauma.py --model wmrl_m6a` -- runs without error, shows kappa_s in output
4. `python scripts/16_regress_parameters_on_scales.py --model wmrl_m6a` -- runs without error, includes kappa_s regressions
  </verify>
  <done>
- Script 14 includes M6a in AIC/BIC comparison table alongside M1-M3 and M5
- Script 15 accepts --model wmrl_m6a and reports kappa_s trauma associations
- Script 16 accepts --model wmrl_m6a and reports kappa_s regressions on continuous scales
- All existing model functionality unchanged
  </done>
</task>

</tasks>

<verification>
1. Parameter recovery quick test: `python scripts/11_run_model_recovery.py --model wmrl_m6a --n-subjects 10 --n-starts 10` completes
2. Model comparison: `python scripts/14_compare_models.py` includes M6a row in table
3. Trauma analysis: `python scripts/15_analyze_mle_by_trauma.py --model wmrl_m6a` runs without error
4. Regression: `python scripts/16_regress_parameters_on_scales.py --model wmrl_m6a` runs without error
5. Full parameter recovery (recommended post-phase): `python scripts/11_run_model_recovery.py --model wmrl_m6a --n-subjects 50 --n-starts 50 --n-jobs 8` -- all 7 parameters pass r >= 0.80
</verification>

<success_criteria>
- M6a parameter recovery simulation generates valid synthetic data with per-stimulus last_actions dict
- M6a appears in AIC/BIC comparison table (script 14)
- kappa_s associations with trauma scales are reported (scripts 15 and 16)
- Quick parameter recovery (N=10) completes without error
- No regressions in existing model pipelines
</success_criteria>

<output>
After completion, create `.planning/phases/09-m6a-stimulus-specific-perseveration/09-02-SUMMARY.md`
</output>

---
phase: 10-m6b-dual-perseveration
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/fitting/model_recovery.py
  - scripts/11_run_model_recovery.py
  - scripts/14_compare_models.py
  - scripts/15_analyze_mle_by_trauma.py
  - scripts/16_regress_parameters_on_scales.py
autonomous: true

must_haves:
  truths:
    - "M6b parameter recovery generates synthetic data with dual kernels (global + per-stimulus) and recovers kappa_total and kappa_share"
    - "M6b appears in the choice-only AIC/BIC comparison table alongside M1-M3, M5, and M6a"
    - "Scripts 15 and 16 accept --model wmrl_m6b and report kappa_total/kappa_share parameter associations"
    - "python scripts/11_run_model_recovery.py --model wmrl_m6b runs without error"
  artifacts:
    - path: "scripts/fitting/model_recovery.py"
      provides: "M6b synthetic generation with dual last_action/last_actions tracking, all dispatch blocks"
      contains: "wmrl_m6b"
    - path: "scripts/11_run_model_recovery.py"
      provides: "wmrl_m6b in choices and 'all' expansion"
      contains: "wmrl_m6b"
    - path: "scripts/14_compare_models.py"
      provides: "M6b in AIC/BIC comparison table via auto-detection + --m6b arg"
      contains: "M6b"
    - path: "scripts/15_analyze_mle_by_trauma.py"
      provides: "kappa_total/kappa_share display names, M6b load path, MODEL_CONFIG entry"
      contains: "kappa_total"
    - path: "scripts/16_regress_parameters_on_scales.py"
      provides: "wmrl_m6b param column list with kappa_total_mean, kappa_share_mean"
      contains: "kappa_total_mean"
  key_links:
    - from: "scripts/fitting/model_recovery.py"
      to: "scripts/fitting/mle_utils.py"
      via: "import WMRL_M6B_PARAMS, WMRL_M6B_BOUNDS"
      pattern: "WMRL_M6B"
    - from: "scripts/fitting/model_recovery.py generate_synthetic_participant"
      to: "dual kernel simulation"
      via: "both last_action scalar and last_actions dict tracked per block"
      pattern: "last_actions\\[stimulus\\]"
    - from: "scripts/14_compare_models.py"
      to: "output/mle/wmrl_m6b_individual_fits.csv"
      via: "patterns dict auto-detection"
      pattern: "wmrl_m6b_individual_fits"
---

<objective>
Wire M6b into all downstream scripts: parameter recovery with dual-kernel synthetic generation, model comparison (AIC/BIC table), trauma group analysis (kappa_total/kappa_share parameters), and continuous regression.

Purpose: Without downstream integration, M6b can be fitted but cannot be compared to other models, analyzed for trauma associations, or validated via parameter recovery. This plan completes the M6b pipeline.

Output: Five files modified such that M6b is fully integrated into the analysis pipeline alongside M1-M3, M5, and M6a.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-m6b-dual-perseveration/10-RESEARCH.md
@.planning/phases/10-m6b-dual-perseveration/10-01-SUMMARY.md
@.planning/phases/09-m6a-stimulus-specific-perseveration/09-02-SUMMARY.md

@scripts/fitting/model_recovery.py
@scripts/11_run_model_recovery.py
@scripts/14_compare_models.py
@scripts/15_analyze_mle_by_trauma.py
@scripts/16_regress_parameters_on_scales.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: M6b parameter recovery (model_recovery.py + script 11)</name>
  <files>scripts/fitting/model_recovery.py, scripts/11_run_model_recovery.py</files>
  <action>
Add M6b support to model_recovery.py by extending the M6a pattern. M6b has 8 params and needs DUAL kernel tracking in synthetic generation.

**In model_recovery.py:**

1. **Imports**: Add `WMRL_M6B_BOUNDS, WMRL_M6B_PARAMS` to the mle_utils import line.

2. **get_param_names()**: Add `elif model == 'wmrl_m6b': return WMRL_M6B_PARAMS` after the wmrl_m6a branch.

3. **sample_random_params() / sample_parameters()**: Add `elif model == 'wmrl_m6b': bounds = WMRL_M6B_BOUNDS` after wmrl_m6a branch (copy the M6a branch structure since bounds handling is the same).

4. **generate_synthetic_participant()**: This is the critical function. M6b needs BOTH global and per-stimulus tracking. Copy the M6a branch structure and extend:

   a. At the top of the block loop where model-specific params are extracted: add `elif model == 'wmrl_m6b':` that extracts `kappa_total = params['kappa_total']` and `kappa_share = params['kappa_share']`, then decodes: `kappa = kappa_total * kappa_share; kappa_s = kappa_total * (1 - kappa_share)`.

   b. In the model list that checks for phi/rho/capacity extraction (the `if model in ('wmrl', 'wmrl_m3', 'wmrl_m5', 'wmrl_m6a'):` block): add `'wmrl_m6b'` to the tuple.

   c. At block initialization: M6b needs BOTH `last_action = None` (global, M3-style) AND `last_actions = {}` (per-stimulus dict, M6a-style). Add this in the M6b branch after kappa decode.

   d. Inside the trial loop, after computing hybrid_probs, add `elif model == 'wmrl_m6b':`:
      ```python
      elif model == 'wmrl_m6b':
          modified = False
          if last_action is not None and kappa > 0.0:
              hybrid_probs[last_action] += kappa
              modified = True
          if last_actions.get(stimulus) is not None and kappa_s > 0.0:
              hybrid_probs[last_actions[stimulus]] += kappa_s
              modified = True
          if modified:
              hybrid_probs = hybrid_probs / np.sum(hybrid_probs)
      ```

   e. After action is taken (where M3/M5 update `last_action = action`): add `if model == 'wmrl_m6b': last_actions[stimulus] = action` alongside the existing `last_action = action` update for M6b. ALSO ensure M6b is included in the model list that does `last_action = action` (the global scalar update).

5. **All remaining dispatch blocks**: Find every `elif model == 'wmrl_m6a':` in the file and add a corresponding `elif model == 'wmrl_m6b':` after it. This includes:
   - `compute_recovery_metrics()`
   - `plot_recovery_scatter()`
   - `plot_distribution_comparison()`
   - Any other dispatch locations

**In scripts/11_run_model_recovery.py:**

1. Add `'wmrl_m6b'` to the argparse `choices` list.
2. Add `'wmrl_m6b'` to the `'all'` models expansion list.
  </action>
  <verify>
Run a quick parameter recovery test:
`python scripts/11_run_model_recovery.py --model wmrl_m6b --n-subjects 2 --n-datasets 1 --n-starts 3 --n-jobs 1`
Should complete without error and write files to `output/recovery/wmrl_m6b/`.
Check that `output/recovery/wmrl_m6b/recovery_results.csv` has columns including `true_kappa_total`, `recovered_kappa_total`, `true_kappa_share`, `recovered_kappa_share`.
  </verify>
  <done>
- M6b synthetic data generation uses BOTH global last_action and per-stimulus last_actions dict
- Both kernels reset at block boundaries (last_action=None, last_actions={})
- Stick-breaking decode (kappa = kappa_total * kappa_share) in generation matches likelihood
- Quick recovery (N=2, 1 dataset) completes without error
- Recovery CSV contains true/recovered columns for all 8 parameters including kappa_total, kappa_share
  </done>
</task>

<task type="auto">
  <name>Task 2: M6b downstream pipeline (scripts 14, 15, 16)</name>
  <files>scripts/14_compare_models.py, scripts/15_analyze_mle_by_trauma.py, scripts/16_regress_parameters_on_scales.py</files>
  <action>
Wire M6b into all downstream analysis scripts, following the M6a extension pattern from 09-02-SUMMARY.md.

**In scripts/14_compare_models.py:**

1. Add to `patterns` dict (or equivalent file-finding structure):
   `'M6b': ['wmrl_m6b_individual_fits.csv', 'wmrl_m6b_mle_results.csv'],`

2. Add `--m6b` argparse argument (same structure as `--m6a` argument): `parser.add_argument('--m6b', type=str, help='Path to M6b fits file')`

3. Add the M6b load block parallel to M6a (read the existing M6a load pattern and copy for M6b).

**In scripts/15_analyze_mle_by_trauma.py:**

1. Add display names for M6b parameters:
   `'kappa_total': r'$\kappa_{total}$'` and `'kappa_share': r'$\kappa_{share}$'`

2. Add M6b load path detection (parallel to M6a pattern).

3. Add `MODEL_CONFIG` entry for wmrl_m6b with 8 parameters:
   ```python
   'wmrl_m6b': {
       'params': WMRL_M6B_PARAMS,  # or inline the list
       'display_name': 'WM-RL M6b (Dual Perseveration)',
   }
   ```

4. Add `'wmrl_m6b'` to `--model` choices and `'all'` expansion.

5. Add column rename for `kappa_total` and `kappa_share` columns (same pattern as kappa_s rename in M6a).

6. Add wmrl_m6b to `load_data()` return tuple (parallel to M6a addition in 09-02).

**In scripts/16_regress_parameters_on_scales.py:**

1. Add `'wmrl_m6b'` to `--model` choices and `'all'` expansion.

2. Add `elif model == 'wmrl_m6b':` param column list:
   ```python
   ['alpha_pos_mean', 'alpha_neg_mean', 'phi_mean', 'rho_mean',
    'wm_capacity_mean', 'kappa_total_mean', 'kappa_share_mean', 'epsilon_mean']
   ```

3. Add dynamic column rename for `kappa_total_mean` and `kappa_share_mean` in the format_label function or rename dict.

**Avoid:**
- Do NOT change any M3/M5/M6a code paths. All changes are additive.
- Do NOT reference non-existent functions (get_display_names, get_param_bounds_dict).
- Do NOT forget to add M6b to BOTH the patterns dict AND the argparse in script 14 (missing either causes silent omission from AIC table).
  </action>
  <verify>
1. Run: `python scripts/14_compare_models.py` -- should complete (may show "M6b not found" if no fits yet, but no crash).
2. Run: `python scripts/15_analyze_mle_by_trauma.py --model wmrl_m6b` -- should complete (may warn about missing data if no fits yet, but no crash/KeyError).
3. Run: `python scripts/16_regress_parameters_on_scales.py --model wmrl_m6b` -- should complete (may warn about missing data if no fits yet, but no crash/KeyError).
4. Verify `--model all` still works for scripts 15 and 16 (no regressions).
  </verify>
  <done>
- M6b in AIC/BIC comparison table via auto-detection of wmrl_m6b_individual_fits.csv
- Script 15 displays kappa_total and kappa_share with proper LaTeX labels
- Script 16 includes kappa_total_mean and kappa_share_mean in regression output
- All downstream scripts accept --model wmrl_m6b without error
- --model all includes M6b in all scripts
- No regressions in existing M1/M2/M3/M5/M6a pipeline
  </done>
</task>

</tasks>

<verification>
1. `python scripts/11_run_model_recovery.py --model wmrl_m6b --n-subjects 2 --n-datasets 1 --n-starts 3 --n-jobs 1` -- completes, writes recovery CSVs
2. `python scripts/14_compare_models.py` -- runs without error (M6b included if fits exist)
3. `python scripts/15_analyze_mle_by_trauma.py --model wmrl_m6b` -- runs without crash
4. `python scripts/16_regress_parameters_on_scales.py --model wmrl_m6b` -- runs without crash
5. `python scripts/11_run_model_recovery.py --model wmrl_m6a --n-subjects 2 --n-datasets 1 --n-starts 3 --n-jobs 1` -- still works (regression check)
6. Recovery CSV has columns: true_kappa_total, recovered_kappa_total, true_kappa_share, recovered_kappa_share
</verification>

<success_criteria>
- M6b parameter recovery generates synthetic data with BOTH global and per-stimulus kernels
- Recovery CSV stores kappa_total and kappa_share (the free parameters, not decoded kappa/kappa_s)
- M6b appears in choice-only AIC/BIC comparison table alongside M1-M3, M5, M6a
- Scripts 15 and 16 handle kappa_total and kappa_share without KeyError
- No regressions in existing M6a/M5/M3 pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/10-m6b-dual-perseveration/10-02-SUMMARY.md`
</output>

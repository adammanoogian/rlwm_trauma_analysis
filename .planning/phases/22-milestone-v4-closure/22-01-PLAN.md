---
phase: 22-milestone-v4-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: true

must_haves:
  truths:
    - "git diff .planning/STATE.md returns empty after plan completes; committed STATE.md narrative matches Phase 21 COMPLETE 11/11 + Phase 22 active"
    - "ROADMAP.md Progress Table shows Phases 14, 15, 20, 21 with true completion dates and cluster-pending status; no shipped phase still reads 'Not started'"
    - "PROJECT.md 'Active (v4.0)' requirements list is empty of shipped items; all shipped items migrated to 'Validated' with v4 suffix"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Current-position narrative matching working-tree reality"
      contains: "Phase 21 of 21 (Principled Bayesian Model Selection Pipeline) — COMPLETE"
    - path: ".planning/ROADMAP.md"
      provides: "Progress Table rows for Phases 14, 15, 20, 21 refreshed"
      contains: "| 14. Collins K"
    - path: ".planning/PROJECT.md"
      provides: "Validated-section migration for v4.0 shipped items"
      contains: "### Validated"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 22 active block consistent with Progress Table row"
      pattern: "Phase 22.*Milestone v4.0 Closure"
    - from: ".planning/PROJECT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Validated list mirrors REQUIREMENTS traceability statuses (Phases 13, 16, 17, 18, 19, 20, 21)"
      pattern: "HIER-02.*Complete|CMP-01.*Complete|PSCAN-01.*Complete"
---

<objective>
Refresh the three status-tracking documents (STATE.md, ROADMAP.md Progress Table, PROJECT.md
requirements list) so they describe what is actually on disk. Right now all three
documents lag behind: STATE.md still claims Phase 20 has plan 20-03 outstanding
(contradicted by 20-03-SUMMARY.md on disk), ROADMAP Progress Table shows Phases 14,
15, 21 as "Not started" even though every plan has a SUMMARY.md, and PROJECT.md's
"Active (v4.0)" list shows shipped items as unchecked.

Purpose: Closes `tech_debt.stale_docs` from `.planning/v4.0-MILESTONE-AUDIT.md`.
Delivers Phase 22 success criteria #1, #2, #3. Without these refreshes, downstream
Phase 22 work (traceability extension, reproducibility guard) would be validating
against stale ground truth.

Output: Three committed status docs consistent with on-disk phase artifacts; the
reproducibility guard in plan 22-04 can then cross-check Progress Table counts
against `ls .planning/phases/*/SUMMARY.md` without spurious mismatches.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v4.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit STATE.md narrative refresh for Phase 21 completion + Phase 22 active</name>
  <files>.planning/STATE.md</files>
  <action>
The working tree already has the 21-10 completion narrative written into STATE.md
(the 2026-04-18 shell crash left it uncommitted). This task commits the pending
update and layers on the Phase 22 active marker.

Concrete edits:
1. Change `Phase: 21 of 21 (Principled Bayesian Model Selection Pipeline) — COMPLETE`
   to `Phase: 22 of 22 (Milestone v4.0 Closure — Verification, Traceability,
   Reproducibility) — ACTIVE`. Preserve the 11/11 Phase 21 narrative as the most
   recent prior activity.
2. Remove the stale fragment `Phase 20 still has plan 20-03 outstanding` wherever
   it appears in the Current Position block. Plan 20-03 has a SUMMARY and the
   Phase 20 folder has 20-VERIFICATION.md; verify first with
   `ls .planning/phases/20-*/20-*-SUMMARY.md` before deleting the fragment.
3. Add a new `### Phase 22 Decisions (2026-04-19)` block at the top of the decisions
   ladder (above `### Phase 21 Decisions`) capturing the three user directives
   from the 2026-04-19 /gsd:plan-phase invocation verbatim:
   (a) Thesis doc must be gitignored (not just untracked);
   (b) Every cluster-pending item frames to `bash cluster/21_submit_pipeline.sh`
       as the canonical cold-start entry — not piecemeal sbatch calls;
   (c) No one-off verification checks — every closure claim must cite a pytest
       test, a grep invariant, or the closure-state script as its evidence.
4. Do NOT rewrite any other sections of STATE.md. Phase 21 narrative, Phase 14-20
   decisions, Hierarchical Bayesian Architecture coverage tables all stay exactly
   as-is.

DO NOT append a "Last activity" line dated in the future or invent new narrative
beyond what the 2026-04-19 invocation captured. The purpose is to make the
committed tree match disk state, not to manufacture progress.
  </action>
  <verify>
`git diff .planning/STATE.md` prints zero lines AFTER the commit. Before the
commit, `git diff .planning/STATE.md` prints the combined 21-10 narrative +
Phase 22 active block + Phase 22 decisions block.

`grep -n "Phase 20 still has plan 20-03 outstanding" .planning/STATE.md` returns
no matches.

`grep -n "Phase 22 Decisions" .planning/STATE.md` returns exactly one match
positioned above the Phase 21 Decisions block.
  </verify>
  <done>
STATE.md's Current Position block reports Phase 22 active; the stale Phase 20
fragment is gone; the three 2026-04-19 user directives appear in a Phase 22
Decisions block; the file is committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh ROADMAP.md Progress Table rows for Phases 14, 15, 20, 21</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit only the Progress Table at the tail of ROADMAP.md (currently lines ~325-353).
Update the rows so they reflect on-disk SUMMARY.md counts. Determine the true
values by listing the phase directories first:

  ls .planning/phases/14-*/14-0*-SUMMARY.md   # expect 14-01, 14-02, 14-03
  ls .planning/phases/15-*/15-0*-SUMMARY.md   # expect 15-01, 15-02; 15-03 status set by 22-02
  ls .planning/phases/20-*/20-0*-SUMMARY.md   # expect 20-01, 20-02, 20-03
  ls .planning/phases/21-*/21-*-SUMMARY.md    # expect 11 files 21-01 through 21-11

Row updates (do each as a separate Edit call so diffs stay clean):

- Phase 14 row: change `0/TBD Not started | -` to `3/3 Complete (cluster refit pending) | 2026-04-12`.
  Rationale: all three PLANs have SUMMARY.md; 14-03-SUMMARY.md explicitly documents
  status:partial with cluster refit deferred. Completion date = most recent
  SUMMARY.md mtime (2026-04-12).
- Phase 15 row: change `0/3 Not started | -` to `2/3 Complete (15-03 absorbed — see 15-VERIFICATION.md) | 2026-04-12`.
  Note the slash notation `2/3` intentionally signals the 15-03 absorption — plan
  22-02 resolves the absorption with either a 15-03-SUMMARY.md or a 15-VERIFICATION.md
  note; this row's wording survives either resolution path.
- Phase 20 row: already reads `3/3 Complete | 2026-04-14` per audit table, but if
  the text in ROADMAP still shows otherwise, fix it. Primary check: `grep "^| 20\." .planning/ROADMAP.md`.
- Phase 21 row: if it reads `0/TBD Not started`, change to `11/11 Complete | 2026-04-18`.
  Audit already verified the orchestrator ran on the current tree, but confirm with
  `ls .planning/phases/21-*/21-*-SUMMARY.md | wc -l` = 11.
- Phase 22 row: already reads `0/4 In progress | -` per current tree; leave alone.

Do NOT edit the Phase header narrative sections (`#### Phase 14:` ... `#### Phase 21:`)
in this plan — those are content, not status. Only the terminal Progress Table.
  </action>
  <verify>
`grep -c "Not started" .planning/ROADMAP.md` is ≤ 2 (deferred Phase 6 and Phase 7
from v2.0 are the only legitimate "Not started" rows).

`grep -c "^| 1[4-9]\.\|^| 2[01]\." .planning/ROADMAP.md` returns 8 (one row per
phase from 14 through 21 inclusive).

`python -c "import re,sys; t=open('.planning/ROADMAP.md',encoding='utf8').read();
rows=re.findall(r'^\| 1[4-9]\..*|^\| 2[01]\..*', t, re.M); assert
sum('Complete' in r for r in rows) == 8, rows; print('OK')"` prints OK.

Additionally, count consistency with on-disk summaries:
`bash -c 'for p in 14 15 20 21; do echo -n "$p summary count: "; ls .planning/phases/$p-*/$p-*-SUMMARY.md 2>/dev/null | wc -l; done'`
should agree with the `N/M` values placed in the rows (modulo the 15-03
absorption convention, which stays 2/3 intentionally).
  </verify>
  <done>
Every phase in ROADMAP.md Progress Table whose code has shipped reports `Complete`
(or `Complete (cluster refit pending)` for Phase 14) with a completion date;
no row for a shipped phase still reads `Not started`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate PROJECT.md v4.0 Active requirements to Validated section</name>
  <files>.planning/PROJECT.md</files>
  <action>
Open `.planning/PROJECT.md`. The document has three sections of interest:
`## Current Milestone: v4.0` (around line 36), `### Validated` (around line 57),
`### Active (v4.0)` (around line 82). Goal: every shipped requirement moves
from `Active (v4.0)` to `Validated` with a v4 suffix; only genuinely
cluster-pending items remain in `Active (v4.0)`.

Determine what counts as "shipped" from `.planning/REQUIREMENTS.md` traceability
table. A requirement is shipped iff its status column currently reads `Complete`.
As of 2026-04-19 this is:

- INFRA-01..08 (Phase 13, 8 items)
- K-01 (Phase 13, 1 item)
- HIER-02..06 (Phase 16, 5 items; HIER-01/07..10 remain Active — see below)
- L2-02..08 (Phase 16, 7 items)
- M4H-01..06 (Phase 17, 6 items)
- CMP-01..04 (Phase 18, 4 items)
- MIG-01..05 (Phase 18, 5 items)
- DOC-01..04 (Phase 18, 4 items)
- PSCAN-01..06 (Phase 19, 6 items)

That is 46 Complete items. Remaining Active-with-cluster-pending items:

- K-02, K-03 (Phase 14, cluster refit pending)
- GPU-01, GPU-02, GPU-03 (Phase 14, M4 GPU fit pending)
- HIER-01, HIER-07..10, L2-01 (Phase 15 — will be resolved in plan 22-02 via
  15-VERIFICATION.md absorption note; leave Active with an annotation
  "(absorbed into Phase 16+18+21 — see 15-VERIFICATION.md)" for now)

Concrete edits to PROJECT.md:
1. In `### Validated`, append a subheader `#### v4.0 (milestone)` under any
   existing version subheaders, then list the 46 Complete requirement IDs as
   bulleted lines `- REQ-ID (Phase N — SUMMARY.md)`. Group by family for readability
   (INFRA, K, HIER, L2, M4H, CMP, MIG, DOC, PSCAN).
2. In `### Active (v4.0)`, remove every checkbox line whose REQ-ID is in the
   46-Complete list. Leave K-02, K-03, GPU-01..03 as checkboxes with the
   annotation `(cluster refit via bash cluster/21_submit_pipeline.sh — cold start)`.
   Leave HIER-01/07..10/L2-01 with the annotation `(absorbed into Phases 16/18/21
   — see 15-VERIFICATION.md once plan 22-02 lands)`.
3. Update the `Last updated` footer to `2026-04-19 — v4.0 closure audit`.

Do NOT edit any text outside the Validated / Active blocks. The project description,
Research Context, Target Audience, etc. stay exactly as-is.
  </action>
  <verify>
`python -c "import re; t=open('.planning/PROJECT.md',encoding='utf8').read();
active=re.search(r'### Active \(v4\.0\)(.*?)(?=### |\Z)', t, re.S).group(1);
shipped_ids=['INFRA-0','HIER-02','HIER-03','HIER-04','HIER-05','HIER-06',
'L2-02','L2-03','L2-04','L2-05','L2-06','L2-07','L2-08','M4H-0','CMP-0','MIG-0',
'DOC-0','PSCAN-0']; bad=[i for i in shipped_ids if i in active]; assert not bad,
f'shipped IDs still in Active: {bad}'; print('OK')"` prints OK.

`grep -c "v4.0 (milestone)" .planning/PROJECT.md` returns 1.

`grep -c "bash cluster/21_submit_pipeline.sh" .planning/PROJECT.md` returns at
least 1 (the K-02 / K-03 / GPU annotation line).
  </verify>
  <done>
PROJECT.md's `Active (v4.0)` section contains only cluster-pending items (K-02,
K-03, GPU-01..03) + Phase 15 absorption notes; all 46 shipped v4.0 requirements
are listed under `Validated > v4.0 (milestone)`. Last-updated footer refreshed.
  </done>
</task>

</tasks>

<verification>
Wave-level checks (run after all three tasks land):

1. `git diff --stat .planning/` shows STATE.md, ROADMAP.md, PROJECT.md each
   modified exactly once with the edits above (other planning docs untouched).
2. Cross-consistency: every row in ROADMAP Progress Table that reads `Complete`
   for a v4.0 phase has its corresponding requirements listed under
   PROJECT.md Validated > v4.0 (milestone), and STATE.md mentions that phase
   as shipped rather than pending.
3. `grep -c "Phase 20 still has plan 20-03 outstanding" .planning/STATE.md` = 0.
4. `grep -c "Active (v4.0)" .planning/PROJECT.md` = 1 (section header only,
   no stale cross-references).
</verification>

<success_criteria>
- STATE.md: `git diff` returns empty after commit; Phase 22 active block
  present; Phase 20 plan-20-03 stale fragment deleted; Phase 22 Decisions block
  captures the three 2026-04-19 user directives verbatim.
- ROADMAP.md Progress Table: Phases 14, 15, 20, 21 rows reflect on-disk SUMMARY.md
  counts and completion dates; no shipped phase reads "Not started".
- PROJECT.md: `Validated > v4.0 (milestone)` subsection contains 46 shipped
  REQ-IDs; `Active (v4.0)` contains only cluster-pending K/GPU + Phase 15
  absorption notes; cluster-pending annotations reference
  `bash cluster/21_submit_pipeline.sh` by name.
- Downstream: plan 22-04 closure-state script can grep these three docs and
  find no inconsistencies.
</success_criteria>

<output>
After completion, create `.planning/phases/22-milestone-v4-closure/22-01-SUMMARY.md`
following the template at `@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md`.
</output>

## Downstream Consumers

- **Plan 22-02:** Reads refreshed STATE.md to cross-reference Phase 15 absorption
  decision; needs Progress Table values stable before writing 15-VERIFICATION.md.
- **Plan 22-03:** Appends DEER + BMS rows to REQUIREMENTS.md traceability table
  and updates the Coverage Summary — the Progress Table in ROADMAP.md (updated
  here) is the source of truth for phase-level counts that REQUIREMENTS.md
  Coverage Summary must match.
- **Plan 22-04:** Closure-state script asserts STATE.md `git diff` is empty,
  ROADMAP Progress Table consistency, and PROJECT.md Validated coverage.

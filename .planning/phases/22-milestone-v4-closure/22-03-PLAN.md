---
phase: 22-milestone-v4-closure
plan: 03
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true

must_haves:
  truths:
    - "REQUIREMENTS.md gains a ### DEER — Non-Linear Parallelization Research section with 4 items (DEER-01..04)"
    - "REQUIREMENTS.md gains a ### BMS — Bayesian Model Selection Pipeline section with 10 items (BMS-01..BMS-10)"
    - "Traceability table grows from 57 to >= 71 rows with new phase mappings"
    - "Coverage summary table adds Phase 20 (DEER 4 items) and Phase 21 (BMS 10 items); Total changes from 57 to >= 71"
    - "Every new REQ-ID row carries a one-line satisfaction evidence citing a SUMMARY.md path, VERIFICATION.md path, or grep-invariant checked into the repo"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Extended requirements catalog with DEER + BMS families and updated traceability + coverage summary"
      contains: "### BMS — Bayesian Model Selection Pipeline"
      contains_alt: "### DEER — Non-Linear Parallelization Research"
  key_links:
    - from: ".planning/REQUIREMENTS.md DEER section"
      to: ".planning/phases/20-deer-nonlinear-parallelization/20-VERIFICATION.md"
      via: "Satisfaction evidence column cites 20-VERIFICATION.md observable-truth rows"
      pattern: "20-VERIFICATION.md|20-0[1-3]-SUMMARY.md"
    - from: ".planning/REQUIREMENTS.md BMS section"
      to: ".planning/phases/21-principled-bayesian-model-selection-pipeline/21-VERIFICATION.md"
      via: "Satisfaction evidence column cites 21-VERIFICATION.md Observable Truths + per-plan SUMMARY.md"
      pattern: "21-VERIFICATION.md|21-[01][0-9]-SUMMARY.md"
    - from: ".planning/REQUIREMENTS.md Coverage summary"
      to: ".planning/ROADMAP.md Progress Table"
      via: "Total requirement count updated to match new phase totals (Phase 20 = 4, Phase 21 = 10)"
      pattern: "Total.*7[1-9]"
---

<objective>
Extend `.planning/REQUIREMENTS.md` with the two missing requirement families:
DEER-01..04 (Phase 20 DEER non-linear parallelization research) and BMS-01..BMS-10
(Phase 21 Principled Bayesian Model Selection Pipeline nine-step workflow + RFX-BMS
module). Update the traceability table and coverage summary so that every shipped
requirement — including the 14 new IDs — has a phase mapping and satisfaction
evidence.

Purpose: Closes `tech_debt.requirements_traceability` from the v4.0 milestone audit
and satisfies Phase 22 success criterion #5. Without BMS-* IDs, the entire 11-plan
Phase 21 workflow is tracked only via plan SUMMARYs — invisible to
`/gsd:audit-milestone` requirement-coverage checks.

Output: REQUIREMENTS.md with 57 → 71 IDs in the traceability table (4 DEER +
10 BMS), updated Coverage Summary row totals, and satisfaction-evidence citations
pointing at the VERIFICATION.md files written by plan 22-02.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/v4.0-MILESTONE-AUDIT.md
@.planning/phases/20-deer-nonlinear-parallelization/20-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DEER and BMS requirement family sections to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Insert two new sections into REQUIREMENTS.md. The file currently has family
sections in this order: INFRA, K, GPU, HIER, L2, M4H, CMP, MIG, DOC, PSCAN.
The new families land immediately after PSCAN (preserving chronological phase
order: Phase 13 → 14 → 15 → 16 → 17 → 18 → 19 → 20 → 21).

**Section 1: DEER — Non-Linear Parallelization Research**

Insert immediately after the PSCAN block (after `### PSCAN — ...` closes).

  ### DEER — Non-Linear Parallelization Research (Phase 20)

  - [x] **DEER-01**: Research document `docs/DEER_NONLINEAR_PARALLELIZATION.md`
    (or equivalent section in `docs/PARALLEL_SCAN_LIKELIHOOD.md`) covering DEER
    fixed-point iteration with worked RLWM mixing non-linearity example, convergence
    analysis (is softmax-mixing a contraction mapping?), comparison with Picard/Newton
    alternatives, Unifying Framework (TMLR 2025) taxonomy placement, and explicit
    go/no-go recommendation with justification.
  - [x] **DEER-02**: Go-path implementation `wmrl_m3_multiblock_likelihood_stacked_deer()`
    fully parallelizing all components; numerical agreement with sequential
    implementation to < 1e-3 relative error; benchmark shows additional speedup
    over Phase 19 scan+sequential hybrid. Or no-go documentation (Phase 20 outcome
    was no-go per 20-VERIFICATION.md; DEER-02 is satisfied by the documented
    no-go decision).
  - [x] **DEER-03**: Empirical go/no-go evidence: either benchmark data showing
    DEER speedup OR convergence/speedup failure evidence showing why Phase 19
    hybrid remains the final GPU path.
  - [x] **DEER-04**: Project-utils guide `JAX_GPU_BAYESIAN_FITTING.md` updated
    with DEER findings + reproducible benchmark script at
    `validation/benchmark_parallel_scan.py`.

**Section 2: BMS — Bayesian Model Selection Pipeline**

Insert immediately after the new DEER section.

  ### BMS — Bayesian Model Selection Pipeline (Phase 21, Baribault & Collins 2023 / Hess 2025 anchored)

  - [x] **BMS-01**: Prior-predictive gate (Phase 21 step 21.1) — orchestrator
    `scripts/21_run_prior_predictive.py` + `cluster/21_1_prior_predictive.slurm`
    simulates choices from priors alone for all 6 choice-only models; accuracy
    curves cover plausible [0.4, 0.9] asymptote range; exit 1 on FAIL blocks
    downstream MCMC.
  - [x] **BMS-02**: RFX-BMS + PXP module `scripts/fitting/bms.py` — per Rigoux
    et al. (2014) / Stephan et al. (2009); unit tests at
    `scripts/fitting/tests/test_bms.py`; usable from step 21.5 orchestrator.
  - [x] **BMS-03**: Bayesian parameter recovery (step 21.2) —
    `scripts/21_run_bayesian_recovery.py` single-subject + aggregate CLI modes;
    SLURM array `cluster/21_2_recovery.slurm` (1-50) + aggregator
    `cluster/21_2_recovery_aggregate.slurm`; kappa-family pass criterion
    r >= 0.80 AND 95% HDI coverage >= 0.90.
  - [x] **BMS-04**: Baseline hierarchical fit runner (step 21.3) —
    `scripts/21_fit_baseline.py` delegates to fit_bayesian.main() with forced
    `--output-subdir 21_baseline`; SLURM `cluster/21_3_fit_baseline.slurm`;
    load-bearing exit-1 shim converts silent gate-fail into SLURM failure.
  - [x] **BMS-05**: Baseline convergence + PPC audit (step 21.4) —
    `scripts/21_baseline_audit.py` applies Baribault & Collins (2023) four-criterion
    gate (R-hat <= 1.05, ESS_bulk >= 400, divergences == 0, BFMI >= 0.2); exit 1
    when n_passing < 2 blocks step 21.5; SLURM `cluster/21_4_baseline_audit.slurm`.
  - [x] **BMS-06**: PSIS-LOO + stacking + RFX-BMS winner gate (step 21.5) —
    `scripts/21_compute_loo_stacking.py` primary stacking weights (Yao 2018)
    + secondary rfx_bms (Rigoux 2014); soft Pareto-k gate; three-tier winner
    verdict (DOMINANT_SINGLE / TOP_TWO / INCONCLUSIVE_MULTIPLE); tri-state exit
    code; SLURM `cluster/21_5_loo_stacking_bms.slurm`.
  - [x] **BMS-07**: Winner L2 refit orchestrator (step 21.6) —
    `scripts/21_fit_with_l2.py` three-branch dispatch (copy for M1/M2;
    2-cov for M3/M5/M6a; subscale for M6b); post-fit beta-site verification;
    SLURM `cluster/21_6_fit_with_l2.slurm` + dispatcher `cluster/21_dispatch_l2_winners.sh`
    + wrapper `cluster/21_6_dispatch_l2.slurm` (--time=14:00:00).
  - [x] **BMS-08**: Scale-fit audit (step 21.7) — `scripts/21_scale_audit.py`
    pattern-match beta-site enumeration handles all three L2 tiers; longest-
    prefix-first covariate/target parser; FDR-BH per-winner; unified exit-0 for
    PROCEED_TO_AVERAGING and NULL_RESULT; SLURM `cluster/21_7_scale_audit.slurm`.
  - [x] **BMS-09**: Stacking-weighted model averaging (step 21.8) —
    `scripts/21_model_averaging.py` canonical-key matching with _total suffix
    stripping (unifies 2-cov and subscale betas); three short-circuit paths
    (NULL_RESULT, single-winner, multi-winner averaging); optional M6b-subscale
    arm via launch_subscale.flag marker; SLURM `cluster/21_8_model_averaging.slurm`.
  - [x] **BMS-10**: Manuscript tables + master orchestrator (step 21.9) —
    `scripts/21_manuscript_tables.py` three table generators with shared
    TableArtefact dataclass; Figure 1 forest plot delegates to Phase 18 script;
    paper.qmd patcher inserts Methods subsection; master orchestrator
    `cluster/21_submit_pipeline.sh` (+x) chains all 9 steps via afterok exclusively
    with local pre-flight pytest gate; SLURM `cluster/21_9_manuscript_tables.slurm`.

Every new REQ-ID above gets the `[x]` marker because every plan is code-satisfied
on disk per the audit. Cluster-execution pending is an orthogonal concern tracked
at the VERIFICATION.md level, not the REQ-ID level.
  </action>
  <verify>
`grep -c "^### DEER\b" .planning/REQUIREMENTS.md` returns 1.

`grep -c "^### BMS\b" .planning/REQUIREMENTS.md` returns 1.

`grep -c "^- \[x\] \*\*DEER-" .planning/REQUIREMENTS.md` returns 4.

`grep -c "^- \[x\] \*\*BMS-" .planning/REQUIREMENTS.md` returns 10.

`python -c "import re; t=open('.planning/REQUIREMENTS.md',encoding='utf8').read();
deer=re.search(r'### DEER.*?(?=### |\Z)', t, re.S).group(0);
bms=re.search(r'### BMS.*?(?=### |\Z)', t, re.S).group(0);
assert 'DEER-04' in deer and 'BMS-10' in bms; print('OK')"` prints OK.
  </verify>
  <done>
REQUIREMENTS.md gains two new requirement-family sections (DEER with 4 items,
BMS with 10 items) inserted after PSCAN. Every item has an `[x]` marker and a
one-paragraph description tied to concrete on-disk artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend traceability table and Coverage Summary</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
The traceability table currently ends at PSCAN-06 (line ~207) with a 3-column
schema `| Requirement | Phase | Status |`. Append 14 new rows preserving the
same schema. Each row's Status column must cite a one-line satisfaction evidence
pointing at the VERIFICATION.md written by plan 22-02 or the plan SUMMARY.md.

**New traceability rows** (append in this exact order after PSCAN-06):

    | DEER-01 | Phase 20 | Complete (evidence: 20-VERIFICATION.md Observable Truth #1; docs/PARALLEL_SCAN_LIKELIHOOD.md §DEER) |
    | DEER-02 | Phase 20 | Complete — no-go outcome (evidence: 20-VERIFICATION.md §Gaps Summary; 20-03-SUMMARY.md) |
    | DEER-03 | Phase 20 | Complete (evidence: 20-VERIFICATION.md Observable Truth on empirical evidence; validation/benchmark_parallel_scan.py) |
    | DEER-04 | Phase 20 | Complete (evidence: 20-03-SUMMARY.md §Accomplishments; validation/benchmark_parallel_scan.py exists) |
    | BMS-01 | Phase 21 | Complete (evidence: 21-01-SUMMARY.md; 21-VERIFICATION.md SC#1 row) |
    | BMS-02 | Phase 21 | Complete (evidence: 21-02-SUMMARY.md; scripts/fitting/tests/test_bms.py passes) |
    | BMS-03 | Phase 21 | Complete (evidence: 21-03-SUMMARY.md; 21-VERIFICATION.md SC#5 row) |
    | BMS-04 | Phase 21 | Complete (evidence: 21-04-SUMMARY.md; scripts/21_fit_baseline.py expected.exists() shim) |
    | BMS-05 | Phase 21 | Complete (evidence: 21-05-SUMMARY.md; 21-VERIFICATION.md SC#2 row) |
    | BMS-06 | Phase 21 | Complete (evidence: 21-06-SUMMARY.md; 21-VERIFICATION.md SC#3 row) |
    | BMS-07 | Phase 21 | Complete (evidence: 21-07-SUMMARY.md; 21-VERIFICATION.md SC#4 row) |
    | BMS-08 | Phase 21 | Complete (evidence: 21-08-SUMMARY.md; 21-VERIFICATION.md SC#4 row) |
    | BMS-09 | Phase 21 | Complete (evidence: 21-09-SUMMARY.md; averaged_scale_effects.csv schema) |
    | BMS-10 | Phase 21 | Complete — cluster execution pending (evidence: 21-10-SUMMARY.md; 21-VERIFICATION.md SC#1 cluster_execution_pending block; master orchestrator at cluster/21_submit_pipeline.sh) |

**Coverage Summary updates**: the block that currently reads:

    | Phase | Requirements | Count |
    |---|---|---|
    | Phase 13 — Infrastructure Repair & Hierarchical Scaffolding | INFRA-01..08, K-01 | 9 |
    | ...
    | Phase 19 — Associative Scan Likelihood Parallelization | PSCAN-01..06 | 6 |
    | **Total** | | **57** |

Add two rows BEFORE the Total row:

    | Phase 20 — DEER Non-Linear Parallelization Research | DEER-01..04 | 4 |
    | Phase 21 — Principled Bayesian Model Selection Pipeline | BMS-01..10 | 10 |

Change the Total to `**71**`.

Then update the narrative immediately after the Coverage Summary table. The
current narrative reads:

    - v1 requirements: 57 unique REQ-IDs (INFRA 8 + K 3 + GPU 3 + HIER 10 + L2 8 + M4H 6 + CMP 4 + MIG 5 + DOC 4 + PSCAN 6). L2-08 is the P2-optional member...
    - Mapped to phases: 57
    - Unmapped: 0 ✓

Replace with:

    - v1 requirements: 71 unique REQ-IDs (INFRA 8 + K 3 + GPU 3 + HIER 10 + L2 8 + M4H 6 + CMP 4 + MIG 5 + DOC 4 + PSCAN 6 + DEER 4 + BMS 10). L2-08 is the P2-optional member of the L2 family; DEER-02 is satisfied by the documented no-go outcome per 20-VERIFICATION.md.
    - Mapped to phases: 71
    - Unmapped: 0 ✓
    - Each requirement appears in exactly one phase (no duplicates verified).

Finally, update the `*Requirements defined:*` footer at the very bottom:

    *Requirements defined: 2026-04-11 (v1 catalog)*
    *Extended: 2026-04-19 (DEER + BMS families added during Phase 22 closure audit)*
    *Roadmap created: 2026-04-11*
    *Source research: .planning/research/SUMMARY.md*
  </action>
  <verify>
`grep -c "^| DEER-\|^| BMS-" .planning/REQUIREMENTS.md` returns >= 14 (this is
the exact invariant in Phase 22 success criterion #5).

`grep -c "^\| Phase 20 .*DEER-01\|^\| Phase 21 .*BMS-01" .planning/REQUIREMENTS.md`
returns 2 (Coverage Summary rows).

`grep -c "\*\*Total\*\* | | \*\*71\*\*" .planning/REQUIREMENTS.md` returns 1.

`grep -c "21-VERIFICATION.md\|20-VERIFICATION.md" .planning/REQUIREMENTS.md`
returns >= 10 (satisfaction-evidence citations).

`python -c "t=open('.planning/REQUIREMENTS.md',encoding='utf8').read();
import re; rows=re.findall(r'^\| (?:INFRA|K|GPU|HIER|L2|M4H|CMP|MIG|DOC|PSCAN|DEER|BMS)-[0-9]+.*', t, re.M);
assert len(rows)==71, f'expected 71 rows, got {len(rows)}'; print('OK')"` prints OK.
  </verify>
  <done>
REQUIREMENTS.md traceability table has 71 rows (57 original + 4 DEER + 10 BMS);
Coverage Summary has new Phase 20 and Phase 21 rows totaling 71; narrative and
footer reflect the 2026-04-19 extension. Every new REQ-ID row cites concrete
satisfaction evidence (VERIFICATION.md or SUMMARY.md path).
  </done>
</task>

</tasks>

<verification>
Wave-level checks:

1. **Row count invariant from Phase 22 SC#5**:
   `grep -c "^| DEER-\|^| BMS-" .planning/REQUIREMENTS.md` returns >= 14.

2. **Total consistency**:
   `grep -oP '\*\*Total\*\* \| \| \*\*\K\d+' .planning/REQUIREMENTS.md` returns 71.

3. **Phase-level consistency with ROADMAP Progress Table**:
   Every row that REQUIREMENTS.md marks `Complete` for a v4.0 phase should
   correspond to a ROADMAP Progress Table row showing that phase as `Complete`
   (per plan 22-01). The closure-state script (plan 22-04) will assert this
   cross-doc invariant.

4. **No dangling references**: every VERIFICATION.md path cited in a Status
   cell must exist on disk. Verify with:
   `python -c "import re; t=open('.planning/REQUIREMENTS.md',encoding='utf8').read();
   import os; paths=re.findall(r'(\d{2}-VERIFICATION\.md)', t);
   for p in set(paths): phase=p[:2]; folder=[d for d in os.listdir('.planning/phases') if d.startswith(phase+'-')];
       assert folder, f'no folder for {phase}';
       assert os.path.exists(os.path.join('.planning/phases', folder[0], p)), f'missing: {p}';
   print('OK')"` prints OK.
</verification>

<success_criteria>
- REQUIREMENTS.md gains ### DEER and ### BMS sections with 4 + 10 items
  respectively. Every item has [x] completion marker and a one-paragraph
  description citing concrete on-disk artifacts.
- Traceability table grows from 57 to 71 rows. Every new row has (a) a phase
  mapping, (b) a Status column citing satisfaction evidence that resolves to a
  real file on disk.
- Coverage Summary total updated from 57 to 71 with Phase 20 and Phase 21 rows.
- Narrative and footer updated to reflect 2026-04-19 extension.
- `grep -c "^| DEER-\|^| BMS-" .planning/REQUIREMENTS.md` returns >= 14 (Phase 22
  SC#5 invariant).
</success_criteria>

<output>
After completion, create `.planning/phases/22-milestone-v4-closure/22-03-SUMMARY.md`.
</output>

## Downstream Consumers

- **Plan 22-04:** Closure-state script asserts:
  - `grep -c "^| DEER-\|^| BMS-" .planning/REQUIREMENTS.md >= 14` (SC#5 invariant)
  - Every VERIFICATION.md path cited in a REQUIREMENTS.md Status column resolves
    to a real file (cross-doc integrity)
  - Coverage Summary totals match sum of per-phase requirement counts
  - Traceability row count matches declared Total in narrative
- **`/gsd:audit-milestone` re-run (post Phase 22):** Sees 71 requirement IDs with
  full phase mapping → can produce `status: passed` with empty tech_debt per
  Phase 22 SC#10.

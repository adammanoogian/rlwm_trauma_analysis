---
phase: 22-milestone-v4-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-collins-k-refit-gpu-lba-batching/14-VERIFICATION.md
  - .planning/phases/15-m3-hierarchical-poc-level2/15-VERIFICATION.md
  - .planning/phases/15-m3-hierarchical-poc-level2/15-03-SUMMARY.md
  - .planning/phases/21-principled-bayesian-model-selection-pipeline/21-VERIFICATION.md
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Three new VERIFICATION.md files exist (14, 15, 21) each with YAML frontmatter status + score and Observable Truths table"
    - "Phase 14 VERIFICATION.md documents every cluster-pending item with deferred_to_execution: bash cluster/21_submit_pipeline.sh OR deferred_to_execution: bash cluster/12_submit_all_gpu.sh (whichever is the canonical cold-start entry for that deliverable)"
    - "Phase 15 absorption resolved: either 15-03-SUMMARY.md exists documenting which deliverables migrated to 16/18/21, or 15-VERIFICATION.md carries the absorption note and 15-03-PLAN.md is preserved as historical record"
    - "Burrows_J_GDPA_Thesis.* is gitignored; git check-ignore Burrows_J_GDPA_Thesis.docx returns exit 0"
    - "Every verification statement in the three new VERIFICATION.md files cites either a pytest test, a grep invariant checked into the repo, or a closure-state script — no 'I confirmed by reading the file' phrasing"
  artifacts:
    - path: ".planning/phases/14-collins-k-refit-gpu-lba-batching/14-VERIFICATION.md"
      provides: "Phase 14 goal-backward audit with cluster-pending framing"
      contains: "deferred_to_execution"
    - path: ".planning/phases/15-m3-hierarchical-poc-level2/15-VERIFICATION.md"
      provides: "Phase 15 goal-backward audit with absorption note for 15-03"
      contains: "absorbed_into"
    - path: ".planning/phases/15-m3-hierarchical-poc-level2/15-03-SUMMARY.md"
      provides: "Phase 15 plan 03 absorption summary (or explicit deletion note)"
      contains: "absorbed into"
    - path: ".planning/phases/21-principled-bayesian-model-selection-pipeline/21-VERIFICATION.md"
      provides: "Phase 21 goal-backward audit (7 success criteria + 11 plans)"
      contains: "master orchestrator"
    - path: ".gitignore"
      provides: "Thesis doc ignore rule (already present on disk, verify + preserve)"
      contains: "Burrows_J_GDPA_Thesis.*"
  key_links:
    - from: ".planning/phases/14-*/14-VERIFICATION.md"
      to: "cluster/21_submit_pipeline.sh OR cluster/12_submit_all_gpu.sh"
      via: "deferred_to_execution YAML field on cluster-pending Observable Truths rows"
      pattern: "deferred_to_execution:.*cluster/"
    - from: ".planning/phases/15-*/15-VERIFICATION.md"
      to: "16-VERIFICATION.md / 18-VERIFICATION.md / 21-VERIFICATION.md"
      via: "Absorbed-into table mapping each 15-03 deliverable to the downstream verified phase"
      pattern: "absorbed_into:"
    - from: ".planning/phases/21-*/21-VERIFICATION.md"
      to: "cluster/21_submit_pipeline.sh"
      via: "ROADMAP SC#1 (cold-start reproducibility) citation and master orchestrator path reference"
      pattern: "cluster/21_submit_pipeline.sh"
---

<objective>
Backfill the three missing VERIFICATION.md files (Phases 14, 15, 21) so every
shipped v4.0 phase has a goal-backward audit on disk. Resolve the Phase 15 plan-03
absorption question (either write 15-03-SUMMARY.md or carry the absorption note
in 15-VERIFICATION.md). Verify the thesis-doc gitignore guard is in place.

Purpose: Closes `tech_debt.unverified_phases` from the v4.0 milestone audit and
satisfies Phase 22 success criteria #4, #6, #8, #9. Without these files,
`/gsd:audit-milestone` re-run cannot produce status `passed`.

Output: Three VERIFICATION.md files mirroring the
`.planning/phases/16-*/16-VERIFICATION.md` pattern (YAML frontmatter + Observable
Truths + Required Artifacts + Key Link Verification + Requirements Coverage +
Anti-Patterns + Human Verification Required + Gaps Summary), plus resolution of
the 15-03 plan-absorption question.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v4.0-MILESTONE-AUDIT.md
@.planning/phases/16-choice-only-family-extension-subscale-level-2/16-VERIFICATION.md
@.planning/phases/13-infrastructure-repair-hierarchical-scaffolding/13-VERIFICATION.md
@.planning/phases/19-associative-scan-likelihood/19-VERIFICATION.md
@.planning/phases/14-collins-k-refit-gpu-lba-batching/14-03-SUMMARY.md
@.planning/phases/15-m3-hierarchical-poc-level2/15-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 14-VERIFICATION.md with cluster-pending framing</name>
  <files>.planning/phases/14-collins-k-refit-gpu-lba-batching/14-VERIFICATION.md</files>
  <action>
Model the new file on `.planning/phases/16-choice-only-family-extension-subscale-level-2/16-VERIFICATION.md`
(gold standard, 100 lines, deviation rows documented). Phase 14 covers K-02, K-03,
GPU-01, GPU-02, GPU-03 (5 requirements). All 5 are code-satisfied on disk but
cluster refit is pending.

**First, determine canonical cold-start entry for each cluster-pending item.**
This is Phase 22 user directive #2 (cluster-freshness framing). Check the Phase 21
master orchestrator:

  bash -c 'grep -n "12_mle\|submit_all_gpu" cluster/21_submit_pipeline.sh 2>/dev/null || echo "not wired"'

If Phase 14 cluster items are NOT wired into `cluster/21_submit_pipeline.sh`, they
have their own cold-start entry at `cluster/12_submit_all_gpu.sh` (verify with
`ls cluster/12_submit_all_gpu.sh`). In that case, the VERIFICATION.md must document
BOTH entries and explicitly flag the non-wiring as a v5.0 candidate:

> Phase 14 cluster-pending items (K-02, K-03, GPU-01..03) are NOT currently wired
> into `bash cluster/21_submit_pipeline.sh`. Their cold-start entry is
> `bash cluster/12_submit_all_gpu.sh`. Extending the Phase 21 orchestrator to
> chain K-refit + GPU LBA upstream of 21.3 is a v5.0 candidate (tracked by
> PROJECT.md v5.0 notes).

YAML frontmatter (required fields):

    ---
    phase: 14-collins-k-refit-gpu-lba-batching
    verified: 2026-04-19
    status: partial
    score: 3/5 code_verified; 2/5 deferred_to_execution
    gaps: []
    cluster_execution_pending:
      - truth: "K-02 / K-03 K recovery r >= 0.50 on constrained-K refit"
        deferred_to_execution: "bash cluster/12_submit_all_gpu.sh (M3 main-branch entry; NOT wired into cluster/21_submit_pipeline.sh — v5.0 candidate)"
        expected_artifact: "output/mle/{model}_individual_fits.csv rows with parameterization_version=collins_k_v1"
      - truth: "GPU-01..03 M4 N=154 GPU wall time < 12h on A100"
        deferred_to_execution: "bash cluster/12_submit_all_gpu.sh (M4-specific GPU submission)"
        expected_artifact: "output/mle/wmrl_m4_individual_fits.csv with wall_time metadata < 12h"
    ---

Body sections (match 16-VERIFICATION.md structure):

1. **Observable Truths** (5 rows, one per requirement):
   - K-02: constrained K bounds implementation in mle_utils.py (VERIFIED on disk;
     evidence: `grep -n "collins_k_v1\|parameterization_version" scripts/fitting/mle_utils.py`).
   - K-03: K recovery r >= 0.50 (DEFERRED; cites cluster_execution_pending block).
   - GPU-01: fit_all_gpu_m4 function in fit_mle.py (VERIFIED on disk; evidence
     grep invariant).
   - GPU-02: fit_all_gpu_m4 handles N=154 real data (VERIFIED on disk; evidence
     grep invariant).
   - GPU-03: GPU speedup verified (DEFERRED; cites cluster_execution_pending block).
2. **Required Artifacts** table: `scripts/fitting/mle_utils.py`, `scripts/fitting/fit_mle.py`,
   `cluster/12_mle_gpu.slurm`, `docs/K_PARAMETERIZATION.md` — all already on disk,
   cite line numbers via grep. DO NOT write "I confirmed by reading the file";
   every row's Evidence column must be a grep command or line reference that any
   reader can run.
3. **Key Link Verification**: EXPECTED_PARAMETERIZATION dict in config.py ↔
   `load_fits_with_validation` validator; `cluster/12_mle_gpu.slurm` ↔ `fit_mle.py`
   via `--compute-diagnostics` flag — both cite the 14-03-SUMMARY.md read-only
   verification findings with line references (grep command), not paraphrase.
4. **Requirements Coverage** table: 5 rows; K-02, GPU-01, GPU-02 marked SATISFIED
   (code on disk); K-03, GPU-03 marked DEFERRED with blocking issue
   "cluster execution pending — see cluster_execution_pending block".
5. **Anti-Patterns Found**: check for any TODO/FIXME in mle_utils.py additions
   via `grep -n "TODO\|FIXME\|XXX" scripts/fitting/mle_utils.py`. If empty, say so.
6. **Human Verification Required**: the two deferred rows (K-03, GPU-03) require
   cluster execution; frame as `cluster/12_submit_all_gpu.sh` cold-start; DO NOT
   say "run sbatch cluster/12_mle_gpu.slurm" as a piecemeal instruction.
7. **Gaps Summary**: no code gaps; two deferred items documented.

**Forbidden phrases** (per Phase 22 user directive #3):
- "I confirmed by reading the file"
- "manual inspection showed"
- "Claude verified" (without citing an automated check)
- "checked via grep once" (must be a reproducible grep invariant any reader can
  run, with the exact command)

Every Evidence cell must be one of:
- A grep command wrapped in backticks (e.g., `grep -n "collins_k_v1" scripts/fitting/mle_utils.py`
  returns lines X,Y).
- A pytest invocation (e.g., `pytest scripts/fitting/tests/test_numpyro_helpers.py::test_foo`).
- A SUMMARY.md path + section reference (e.g., `14-03-SUMMARY.md §Accomplishments`).
- A closure-state script assertion name (e.g., `validation/check_v4_closure.py::assert_phase14_code_complete`).
  </action>
  <verify>
`ls .planning/phases/14-collins-k-refit-gpu-lba-batching/14-VERIFICATION.md` shows
the file exists.

`python -c "import re; t=open('.planning/phases/14-collins-k-refit-gpu-lba-batching/14-VERIFICATION.md',encoding='utf8').read();
assert 'deferred_to_execution' in t, 'missing cluster-freshness framing';
banned=['I confirmed by reading','manual inspection showed','Claude verified']; bad=[p for p in banned if p in t];
assert not bad, f'banned phrases present: {bad}'; print('OK')"` prints OK.

`grep -c "^| " .planning/phases/14-collins-k-refit-gpu-lba-batching/14-VERIFICATION.md`
returns ≥ 20 (table rows across Truths + Artifacts + Key Links + Requirements).

YAML frontmatter parseable by PyYAML (closure-state script in plan 22-04 relies
on this): `python -c "import yaml,re; t=open('.planning/phases/14-collins-k-refit-gpu-lba-batching/14-VERIFICATION.md',encoding='utf8').read();
fm=re.match(r'---\n(.+?)\n---', t, re.S).group(1); d=yaml.safe_load(fm); assert d['phase']=='14-collins-k-refit-gpu-lba-batching';
assert d['status'] in ('passed','partial','gaps_found'); assert 'cluster_execution_pending' in d; print('OK')"` prints OK.
  </verify>
  <done>
14-VERIFICATION.md exists with valid YAML frontmatter (phase, verified, status,
score, gaps, cluster_execution_pending), full 7-section body, zero banned
phrases, every Evidence cell citing a reproducible check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 15-VERIFICATION.md and resolve 15-03 absorption</name>
  <files>
.planning/phases/15-m3-hierarchical-poc-level2/15-VERIFICATION.md
.planning/phases/15-m3-hierarchical-poc-level2/15-03-SUMMARY.md
  </files>
  <action>
**Decision: write both 15-VERIFICATION.md AND 15-03-SUMMARY.md** (user directive
allows either, but writing both preserves the audit trail AND satisfies the
Phase 22 success criterion #4's "OR" branch cleanly). Do NOT delete 15-03-PLAN.md
— it stays as the historical planning record.

**15-03-SUMMARY.md content** — short absorption summary (~70 lines max):

  ---
  phase: 15-m3-hierarchical-poc-level2
  plan: 03
  subsystem: bayesian-pipeline
  requires: ["15-01", "15-02"]
  provides: ["Absorbed into Phases 16, 18, 21 — deliverable-by-deliverable mapping below"]
  affects: ["16-hierarchical-family", "18-manuscript", "21-bayesian-selection"]
  key-files:
    created: []
    modified: []
  key-decisions:
    - "Plan 15-03 deliverables absorbed rather than shipped as a standalone plan — downstream phases obviated the need"
  duration: "absorbed (no standalone execution)"
  completed: "2026-04-19 (closure audit resolution — actual absorption occurred incrementally over Phases 16/18/21)"
  status: "absorbed"
  ---

  # Phase 15 Plan 03: Absorption Summary

  Plan 15-03 was drafted to deliver PPC stratified by trauma group (HIER-09),
  an M3 cluster SLURM script, and end-to-end pipeline validation. Its deliverables
  were absorbed into three downstream phases rather than shipped as a standalone
  plan:

  | 15-03 Deliverable | Absorbed Into | Evidence (grep invariant) |
  |---|---|---|
  | HIER-09: PPC infrastructure stratified by trauma group | Phase 18 plan 18-02 (Bayesian regression pipeline) | `grep -n "run_posterior_predictive_check" scripts/fitting/bayesian_diagnostics.py` returns line X |
  | M3 hierarchical SLURM script | Phase 16 `cluster/13_bayesian_m3.slurm` and Phase 21 `cluster/21_3_fit_baseline.slurm` | `ls cluster/13_bayesian_m3.slurm cluster/21_3_fit_baseline.slurm` |
  | End-to-end pipeline validation | Phase 21 master orchestrator `cluster/21_submit_pipeline.sh` | `grep -c "afterok" cluster/21_submit_pipeline.sh` returns ≥ 19 |
  | Level-2 beta_lec_kappa credible-interval HDI | Phase 21 plan 21-07 winner L2 refit `scripts/21_fit_with_l2.py` | `grep -n "beta_lec_kappa" scripts/fitting/numpyro_models.py` returns multi-line result |

  No code was committed under plan 15-03 itself; the plan is preserved at
  `15-03-PLAN.md` as the historical record of what was originally scoped.

**15-VERIFICATION.md content** — full 7-section goal-backward audit mirroring
16-VERIFICATION.md:

YAML frontmatter:

  ---
  phase: 15-m3-hierarchical-poc-level2
  verified: 2026-04-19
  status: passed_with_absorption
  score: 6/6 requirements satisfied (3 directly, 3 via absorption)
  gaps: []
  absorbed_into:
    - requirement: HIER-09
      from_plan: 15-03
      to_phase: 18
      to_artifact: scripts/fitting/bayesian_diagnostics.py::run_posterior_predictive_check
    - requirement: L2-01
      from_plan: 15-03
      to_phase: 21
      to_artifact: scripts/21_fit_with_l2.py (2-cov path for M3)
  ---

Observable Truths table (6 rows, one per Phase 15 requirement per REQUIREMENTS.md):

- HIER-01 M3 hierarchical NumPyro model with vmap + non-centered — VERIFIED
  (code exists at `scripts/fitting/numpyro_models.py`; grep invariant).
- HIER-07 convergence gate (R-hat ≤ 1.01, ESS_bulk ≥ 400, auto-bump 0.8→0.95→0.99)
  — VERIFIED (evidence: grep `auto_bump` or `target_accept_prob` in fit_bayesian.py).
- HIER-08 shrinkage diagnostic report — VERIFIED via absorption into Phase 16/18
  (cite 16-VERIFICATION.md row for shrinkage infrastructure).
- HIER-09 PPC stratified by trauma group — ABSORBED into Phase 18 plan 18-02
  (cite 18-VERIFICATION.md; evidence grep `run_posterior_predictive_check`).
- HIER-10 parametric dispatch smoke test — VERIFIED (evidence: smoke test in
  `scripts/fitting/tests/`; grep invariant for smoke-test decorator).
- L2-01 LEC → kappa proof-of-concept — ABSORBED into Phase 21 plan 21-07
  (cite 21-07-SUMMARY.md 2-cov path).

Required Artifacts, Key Link Verification, Requirements Coverage, Anti-Patterns,
Human Verification Required, Gaps Summary — mirror 16-VERIFICATION.md structure.
Every Evidence cell is a grep invariant, pytest reference, or SUMMARY.md path.

DO NOT rewrite 15-03-PLAN.md; leave it as historical record.
  </action>
  <verify>
`ls .planning/phases/15-m3-hierarchical-poc-level2/15-VERIFICATION.md` and
`ls .planning/phases/15-m3-hierarchical-poc-level2/15-03-SUMMARY.md` both succeed.

`ls .planning/phases/15-m3-hierarchical-poc-level2/15-03-PLAN.md` still succeeds
(file preserved).

`python -c "import yaml,re;
t1=open('.planning/phases/15-m3-hierarchical-poc-level2/15-VERIFICATION.md',encoding='utf8').read();
d1=yaml.safe_load(re.match(r'---\n(.+?)\n---', t1, re.S).group(1));
assert d1['status']=='passed_with_absorption';
assert 'absorbed_into' in d1 and len(d1['absorbed_into'])>=2;
t2=open('.planning/phases/15-m3-hierarchical-poc-level2/15-03-SUMMARY.md',encoding='utf8').read();
d2=yaml.safe_load(re.match(r'---\n(.+?)\n---', t2, re.S).group(1));
assert d2['status']=='absorbed'; print('OK')"` prints OK.

`grep -c "absorbed_into\|absorbed into" .planning/phases/15-m3-hierarchical-poc-level2/15-VERIFICATION.md`
returns ≥ 2.

Banned phrase check (same as Task 1): zero hits for "I confirmed by reading",
"manual inspection showed", "Claude verified" in either file.
  </verify>
  <done>
15-VERIFICATION.md and 15-03-SUMMARY.md both exist with valid YAML frontmatter.
The absorption mapping is concrete: HIER-09 → Phase 18, L2-01 → Phase 21, with
grep-invariant evidence cells. No banned phrases. 15-03-PLAN.md preserved as
historical record.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 21-VERIFICATION.md covering 7 ROADMAP success criteria + 11 plans</name>
  <files>.planning/phases/21-principled-bayesian-model-selection-pipeline/21-VERIFICATION.md</files>
  <action>
Phase 21 has 11 plans (21-01..21-11) all with SUMMARY.md files on disk. ROADMAP
defines 7 success criteria. No REQ-IDs currently map to Phase 21 (plan 22-03 will
fix this by adding BMS-01..BMS-10); for this file, build the Observable Truths
table directly from the 7 success criteria.

Model the file on 16-VERIFICATION.md (100 lines, passed status) and 19-VERIFICATION.md
(shows `human_verification` YAML key for cluster-pending items).

YAML frontmatter:

  ---
  phase: 21-principled-bayesian-model-selection-pipeline
  verified: 2026-04-19
  status: passed
  score: 7/7 success criteria code_verified; 1 deferred_to_execution for full cold-start pipeline
  gaps: []
  cluster_execution_pending:
    - truth: "ROADMAP SC#1: Pipeline reproducible from cold start via bash cluster/21_submit_pipeline.sh"
      deferred_to_execution: "bash cluster/21_submit_pipeline.sh (canonical cold-start entry)"
      expected_artifact: "output/bayesian/manuscript_tables/table1_loo_stacking.csv + table2_rfx_bms.csv + table3_winner_betas.csv after end-to-end run"
  ---

Observable Truths table (7 rows, one per ROADMAP SC):

1. **SC#1 Pipeline reproducible from cold start** — VERIFIED at code level
   (master orchestrator exists + pre-flight pytest gate + 9-step afterok chain);
   DEFERRED for actual cluster execution. Evidence:
   `grep -c "afterok" cluster/21_submit_pipeline.sh` returns 19 (≥ 6);
   `grep "afterany" cluster/21_submit_pipeline.sh` returns empty;
   `test -x cluster/21_submit_pipeline.sh && echo executable` returns "executable";
   `grep -n "test_numpyro_models_2cov" cluster/21_submit_pipeline.sh` returns the
   pre-flight gate line.
2. **SC#2 Convergence gate refuses past 21.4** — VERIFIED. Evidence:
   `grep -n "exit 1" scripts/21_baseline_audit.py` and
   `grep -n "n_passing < 2" scripts/21_baseline_audit.py`.
3. **SC#3 PSIS-LOO ranks + stacking weights sum to 1.0 + ∆elpd SE** — VERIFIED.
   Evidence: `grep -n "method=['\"]stacking" scripts/21_compute_loo_stacking.py`;
   soft-gate Pareto-k behavior documented per plan 21-06 decision
   (`grep -n "pareto.k.*threshold" scripts/21_compute_loo_stacking.py`).
4. **SC#4 Scale-effect HDIs only within winners** — VERIFIED. Evidence:
   `grep -n "winners.txt" scripts/21_fit_with_l2.py` shows bidirectional
   winner validation; `grep -n "_verify_two_covariate_sites" scripts/21_fit_with_l2.py`
   confirms post-fit beta-site guard.
5. **SC#5 Recovery Pearson r ≥ 0.8 for identifiable params** — VERIFIED
   (infrastructure). Evidence:
   `grep -n "pearson\|r >= 0.80\|kappa" scripts/21_run_bayesian_recovery.py`
   returns the kappa-family filter; DEFERRED for full N=50 synthetic datasets per
   model (part of cold-start pipeline).
6. **SC#6 Manuscript methods cites Baribault & Collins 2023 + Hess 2025** — VERIFIED.
   Evidence: `grep -n "10.1037/met0000554\|10.5334/cpsy.116" paper.qmd` returns
   both DOIs; additionally
   `grep -n "10.1214/17-BA1091" paper.qmd` for Yao et al. 2018 (anchor paper #3).
7. **SC#7 /gsd:audit-milestone passes** — TESTED in audit 2026-04-19;
   status `tech_debt`; closure work in Phase 22 tracks the remaining items.

**Required Artifacts** table — list the 11 plan SUMMARYs + master orchestrator +
dispatcher SLURM wrapper + dispatcher .sh + bms.py module (from 21-02) + paper.qmd
anchor-paper citations. Every Status cell cites a grep invariant or `ls` command.

**Key Link Verification** table — trace the 9-step pipeline:
- 21.1 → 21.2 via prior-predictive gate pass
- 21.2 → 21.3 via recovery test (pytest-gated)
- 21.3 → 21.4 via baseline convergence check
- 21.4 → 21.5 via exit code 0 on ≥ 2 passing models
- 21.5 → 21.6 via winners.txt handoff
- 21.6 → 21.7 via 21_l2/ posterior NetCDFs
- 21.7 → 21.8 via YAML pipeline_action header (PROCEED_TO_AVERAGING)
- 21.8 → 21.9 via averaged_scale_effects.csv handoff
- 21.9 → paper.qmd via inline Quarto `{python} winner_display`

Each row's Status cell cites a grep invariant proving the wire-up exists in code.

**Requirements Coverage** table: Since no BMS-* IDs exist yet, this table lists
the 11 plan SUMMARYs as rows with `SATISFIED` status and the SUMMARY.md path as
evidence. Add a header note: "BMS-01..BMS-10 requirement IDs pending plan 22-03
REQUIREMENTS.md extension; this table lists plans directly until then."

**Anti-Patterns Found**: `grep -rn "TODO\|FIXME\|XXX\|placeholder" scripts/21_*.py`
across all 21_*.py scripts. Report count.

**Human Verification Required**: the cold-start pipeline full-run (SC#1 execution)
framed as `bash cluster/21_submit_pipeline.sh`. DO NOT say "run sbatch X.slurm"
as a piecemeal instruction; the whole point of the master orchestrator is that
the user runs ONE command.

**Gaps Summary**: no code gaps at verification time; Phase 22 plan 22-03 will
back-fill BMS-* requirement IDs; Phase 22 plan 22-04 will add the closure-state
reproducibility guard that asserts the master orchestrator wiring.

**Same banned-phrase check** as Tasks 1 and 2.
  </action>
  <verify>
`ls .planning/phases/21-principled-bayesian-model-selection-pipeline/21-VERIFICATION.md`
succeeds.

`python -c "import yaml,re; t=open('.planning/phases/21-principled-bayesian-model-selection-pipeline/21-VERIFICATION.md',encoding='utf8').read();
d=yaml.safe_load(re.match(r'---\n(.+?)\n---', t, re.S).group(1));
assert d['status']=='passed'; assert 'cluster/21_submit_pipeline.sh' in t;
banned=['I confirmed by reading','manual inspection showed','Claude verified'];
bad=[p for p in banned if p in t]; assert not bad, f'banned: {bad}'; print('OK')"` prints OK.

`grep -c "cluster/21_submit_pipeline.sh" .planning/phases/21-principled-bayesian-model-selection-pipeline/21-VERIFICATION.md`
returns ≥ 3 (SC#1, cluster_execution_pending, Human Verification Required).

`grep -c "^| " .planning/phases/21-principled-bayesian-model-selection-pipeline/21-VERIFICATION.md`
returns ≥ 30 (table rows across Truths + Artifacts + Key Links + Requirements).
  </verify>
  <done>
21-VERIFICATION.md exists, covers all 7 ROADMAP SCs as Observable Truths with
grep-invariant evidence cells, traces the 9-step pipeline wire-up, and references
the master orchestrator as the canonical cold-start entry.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify thesis gitignore guard and bookkeeping</name>
  <files>.gitignore</files>
  <action>
The orchestrator already added `Burrows_J_GDPA_Thesis.*` to `.gitignore` per the
Phase 22 ROADMAP motivation. This task is a minimal verification step, NOT a
rewrite.

1. Confirm the pattern is already present:
   `grep -n "Burrows_J_GDPA_Thesis" .gitignore` should return a line match.
   If present, no edit is needed — proceed to step 2.
   If missing (regression or shell-crash side-effect), add the line
   `Burrows_J_GDPA_Thesis.*` at the bottom of `.gitignore` under a comment
   `# Thesis manuscript (external collaboration; not part of repo)`.

2. Verify the guard works:
   `git check-ignore Burrows_J_GDPA_Thesis.docx` should exit 0 and print the
   filename. `git check-ignore Burrows_J_GDPA_Thesis.md` should exit 0 and
   print the filename. If either exits non-zero, the pattern does not cover
   the extension — widen to `Burrows_J_GDPA_Thesis.*` (which it already is).

3. Confirm the files no longer show as untracked in regular status:
   `git status --untracked-files=normal 2>&1 | grep -c "Burrows_J_GDPA_Thesis"`
   should return 0. (They may still appear under
   `git status --untracked-files=all`, which is expected for ignored files.)

4. Commit only if .gitignore was actually modified. If no edit was needed,
   record the verification outcome in the SUMMARY.md but do not create an
   empty commit.
  </action>
  <verify>
`git check-ignore Burrows_J_GDPA_Thesis.docx` exits 0.

`git check-ignore Burrows_J_GDPA_Thesis.md` exits 0 (if the file exists; if not,
the check is vacuous but the pattern coverage is still valid).

`git status --untracked-files=normal 2>&1 | grep -c "Burrows_J_GDPA_Thesis"`
returns 0.

`grep -n "Burrows_J_GDPA_Thesis" .gitignore` returns exactly one line.
  </verify>
  <done>
.gitignore contains `Burrows_J_GDPA_Thesis.*`; git check-ignore exits 0 on
both thesis files; neither file appears in regular `git status` output. No
redundant commit if the pattern was already present.
  </done>
</task>

</tasks>

<verification>
Wave-level checks (run after all four tasks land):

1. **Three VERIFICATION.md files exist**:
   `ls .planning/phases/14-*/14-VERIFICATION.md .planning/phases/15-*/15-VERIFICATION.md .planning/phases/21-*/21-VERIFICATION.md`
   returns three paths with no errors.

2. **Zero banned phrases across all three files**:
   `grep -rn "I confirmed by reading\|manual inspection showed\|Claude verified\|checked via grep once"
   .planning/phases/14-*/14-VERIFICATION.md .planning/phases/15-*/15-VERIFICATION.md
   .planning/phases/21-*/21-VERIFICATION.md` returns zero matches.

3. **Cluster-freshness framing present on every pending item**:
   `grep -c "bash cluster/21_submit_pipeline.sh\|bash cluster/12_submit_all_gpu.sh"
   .planning/phases/14-*/14-VERIFICATION.md` returns ≥ 2.
   `grep -c "bash cluster/21_submit_pipeline.sh"
   .planning/phases/21-*/21-VERIFICATION.md` returns ≥ 3.

4. **15-03 absorption resolved**:
   `ls .planning/phases/15-*/15-03-SUMMARY.md` succeeds AND
   `grep -c "absorbed_into" .planning/phases/15-*/15-VERIFICATION.md` returns ≥ 2.

5. **Thesis gitignore guard active**:
   `git check-ignore Burrows_J_GDPA_Thesis.docx` exit 0.
</verification>

<success_criteria>
- 14-VERIFICATION.md, 15-VERIFICATION.md, 21-VERIFICATION.md all exist with
  7-section structure (frontmatter + Observable Truths + Required Artifacts +
  Key Link Verification + Requirements Coverage + Anti-Patterns + Human
  Verification Required + Gaps Summary).
- Every cluster-pending deliverable frames to `bash cluster/21_submit_pipeline.sh`
  OR `bash cluster/12_submit_all_gpu.sh` (documented as v5.0 candidate for wiring
  if applicable) as the cold-start entry. No piecemeal sbatch instructions.
- Every Evidence cell cites a grep invariant, pytest test, SUMMARY.md reference,
  or closure-state script assertion. No banned phrases.
- Phase 15 absorption is concrete: 15-03-SUMMARY.md exists with deliverable-by-
  deliverable mapping to Phases 16/18/21; 15-VERIFICATION.md YAML includes
  `absorbed_into:` list.
- Thesis docs are gitignored and do not appear in `git status --untracked-files=normal`.
- Phase 22 closure-state script (plan 22-04) can parse all three VERIFICATION.md
  YAML frontmatters with PyYAML.
</success_criteria>

<output>
After completion, create `.planning/phases/22-milestone-v4-closure/22-02-SUMMARY.md`.
</output>

## Downstream Consumers

- **Plan 22-03:** Reads the three new VERIFICATION.md files to back-fill DEER-01..04
  and BMS-01..BMS-10 satisfaction-evidence columns in the REQUIREMENTS.md
  traceability table. 21-VERIFICATION.md is the source of truth for BMS-*
  satisfaction evidence.
- **Plan 22-04:** Closure-state script asserts:
  - All three VERIFICATION.md files exist and have valid YAML frontmatter.
  - None contain banned phrases.
  - Every `cluster_execution_pending` YAML block references the canonical
    cold-start entry (`cluster/21_submit_pipeline.sh` or
    `cluster/12_submit_all_gpu.sh`).
  - `git check-ignore Burrows_J_GDPA_Thesis.docx` exits 0.

---
phase: 08-m5-rl-forgetting
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - scripts/fitting/model_recovery.py
  - scripts/14_compare_models.py
  - scripts/15_analyze_mle_by_trauma.py
  - scripts/16_regress_parameters_on_scales.py
autonomous: true

must_haves:
  truths:
    - "Parameter recovery passes r >= 0.80 for all 8 M5 parameters including phi_rl"
    - "M5 appears in the choice-only AIC/BIC comparison table alongside M1-M3"
    - "Scripts 15 and 16 accept --model wmrl_m5 and report phi_rl parameter associations with trauma scales"
  artifacts:
    - path: "scripts/fitting/model_recovery.py"
      provides: "M5 synthetic data generation and parameter recovery"
      contains: "wmrl_m5"
    - path: "scripts/14_compare_models.py"
      provides: "M5 model comparison via --m5 argument"
      contains: "--m5"
    - path: "scripts/15_analyze_mle_by_trauma.py"
      provides: "M5 trauma group analysis with phi_rl"
      contains: "phi_rl"
    - path: "scripts/16_regress_parameters_on_scales.py"
      provides: "M5 continuous regression with phi_rl"
      contains: "wmrl_m5"
  key_links:
    - from: "scripts/fitting/model_recovery.py"
      to: "scripts/fitting/mle_utils.py"
      via: "import WMRL_M5_BOUNDS, WMRL_M5_PARAMS"
      pattern: "WMRL_M5_BOUNDS|WMRL_M5_PARAMS"
    - from: "scripts/fitting/model_recovery.py"
      to: "generate_synthetic_participant"
      via: "Q decay inside simulation loop BEFORE policy"
      pattern: "phi_rl.*Q.*decay|Q.*phi_rl"
    - from: "scripts/14_compare_models.py"
      to: "output/wmrl_m5_individual_fits.csv"
      via: "--m5 argument or auto-detection"
      pattern: "wmrl_m5_individual_fits"
---

<objective>
Wire M5 into the downstream pipeline: parameter recovery (model_recovery.py), model comparison (script 14), trauma group analysis (script 15), and continuous regression (script 16).

Purpose: Without pipeline integration, M5 can be fit but cannot be validated (recovery), compared (AIC table), or analyzed (trauma associations). This plan completes the full M5 feature.

Output: All four downstream scripts handle wmrl_m5 correctly, and parameter recovery can be run to validate M5 fitting quality.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-m5-rl-forgetting/08-RESEARCH.md
@.planning/phases/08-m5-rl-forgetting/08-01-SUMMARY.md

@scripts/fitting/model_recovery.py
@scripts/14_compare_models.py
@scripts/15_analyze_mle_by_trauma.py
@scripts/16_regress_parameters_on_scales.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: M5 parameter recovery simulation</name>
  <files>scripts/fitting/model_recovery.py</files>
  <action>
Add wmrl_m5 support to model_recovery.py by extending the M3 pattern at every dispatch point:

1. **Imports**: Add `WMRL_M5_BOUNDS, WMRL_M5_PARAMS` to the import block from `scripts.fitting.mle_utils`.

2. **get_param_names()**: Add `elif model == 'wmrl_m5': return WMRL_M5_PARAMS`.

3. **sample_parameters()**: Add `elif model == 'wmrl_m5':` setting `bounds = WMRL_M5_BOUNDS` and `param_names = WMRL_M5_PARAMS`.

4. **generate_synthetic_participant()**: Add `elif model == 'wmrl_m5':` handling. This is the critical change:
   - Extract `phi_rl = params['phi_rl']` alongside other WM-RL params.
   - In the model condition check, add `'wmrl_m5'` to `model in ('wmrl', 'wmrl_m3', 'wmrl_m5')` for WM initialization.
   - Inside the trial loop, AFTER the WM decay line (`WM = (1 - phi) * WM + phi * wm_baseline`), add RL forgetting:
     ```python
     # M5: RL forgetting (decay Q toward uniform baseline BEFORE policy)
     if model == 'wmrl_m5':
         Q0 = 1.0 / NUM_ACTIONS  # = 0.333
         Q = (1 - phi_rl) * Q + phi_rl * Q0
     ```
   - CRITICAL: This Q decay must happen BEFORE computing action probabilities (policy step).
     Position it after `WM = (1 - phi) * WM + phi * wm_baseline` and before `q_vals = Q[stimulus, :]`.
   - The perseveration block must also handle M5: update `model == 'wmrl_m3'` check to `model in ('wmrl_m3', 'wmrl_m5')`.
   - The WM update block must also handle M5: update `model in ['wmrl', 'wmrl_m3']` to `model in ['wmrl', 'wmrl_m3', 'wmrl_m5']`.

5. Verify that `kappa` extraction works for M5 (it should, since M5 includes kappa).

**Avoid:** Do NOT apply Q decay AFTER the Q-update step. The decay must happen BEFORE the policy computation to match the likelihood function (M5-02).
  </action>
  <verify>
Run a quick parameter recovery test (small N):
```bash
python scripts/11_run_model_recovery.py --model wmrl_m5 --n-subjects 10 --n-starts 10 --n-jobs 4
```
Check that:
- It completes without error
- Recovery CSV is written to output/
- Correlations are printed (exact r values checked in later full recovery run)
  </verify>
  <done>
- model_recovery.py handles wmrl_m5 in all three dispatch functions
- Synthetic data generation applies Q decay BEFORE policy (matching likelihood)
- Quick recovery test (N=10) completes without error
  </done>
</task>

<task type="auto">
  <name>Task 2: M5 pipeline scripts (comparison + trauma analysis)</name>
  <files>scripts/14_compare_models.py, scripts/15_analyze_mle_by_trauma.py, scripts/16_regress_parameters_on_scales.py</files>
  <action>
**Script 14 (compare_models.py):**

1. Add `--m5` argument to argparse (after `--m3`):
   ```python
   parser.add_argument('--m5', type=str, default=None,
                       help='Path to M5 (WM-RL+phi_rl) individual fits CSV')
   ```

2. In the auto-detection / fits_dict construction section, add M5 handling:
   - If `args.m5` is provided, load it into `fits_dict['M5']`
   - In auto-detection (the `find_mle_files()` function or equivalent), look for `wmrl_m5_individual_fits.csv` and add as M5

3. Ensure `n_params` for M5 is 8 (used for AIC/BIC). This should work automatically if the CSV has 8 parameter columns, but verify the n_params detection logic handles it.

**Script 15 (analyze_mle_by_trauma.py):**

1. Add constant: `WMRL_M5_PARAMS = ['alpha_pos', 'alpha_neg', 'phi', 'rho', 'capacity', 'kappa', 'phi_rl', 'epsilon']`

2. Add `'phi_rl'` to `PARAM_NAMES` display dict: `'phi_rl': r'$\phi_{RL}$'`

3. In `load_data()`: Add defensive loading for wmrl_m5:
   ```python
   wmrl_m5_path = OUTPUT_DIR / "wmrl_m5_individual_fits.csv"
   wmrl_m5 = pd.read_csv(wmrl_m5_path) if wmrl_m5_path.exists() else None
   ```
   Merge with surveys/groups same as wmrl_m3. Return in the data structure.

4. In `MODEL_CONFIG` dict construction, add M5 entry (only if wmrl_m5 data was loaded):
   ```python
   if wmrl_m5 is not None:
       MODEL_CONFIG['wmrl_m5'] = {'name': 'WM-RL+M5', 'params': WMRL_M5_PARAMS, 'data': wmrl_m5}
   ```

5. Update argparse `choices`: add `'wmrl_m5'` to the list. Update `'all'` expansion to include `'wmrl_m5'`.

**Script 16 (regress_parameters_on_scales.py):**

1. In `load_integrated_data()`, in the param_rename block, add phi_rl handling:
   ```python
   if 'phi_rl' in params_df.columns:
       param_rename['phi_rl'] = 'phi_rl_mean'
   ```

2. In `main()`, in the param_cols dispatch section, add:
   ```python
   elif model == 'wmrl_m5':
       param_cols = ['alpha_pos_mean', 'alpha_neg_mean', 'phi_mean', 'rho_mean',
                     'wm_capacity_mean', 'kappa_mean', 'phi_rl_mean', 'epsilon_mean']
   ```

3. Update argparse `choices`: add `'wmrl_m5'` to `['qlearning', 'wmrl', 'wmrl_m3', 'all']`.
   Update `'all'` expansion to include `'wmrl_m5'`.

**Avoid:** Do not restructure any existing model handling. All changes are additive elif branches and new dict entries.
  </action>
  <verify>
1. `python scripts/14_compare_models.py --help` shows --m5 argument
2. `python scripts/14_compare_models.py` — auto-detects and includes M5 in comparison table (requires wmrl_m5_individual_fits.csv from Plan 01)
3. `python scripts/15_analyze_mle_by_trauma.py --model wmrl_m5` — runs without error, shows phi_rl in output
4. `python scripts/16_regress_parameters_on_scales.py --model wmrl_m5` — runs without error, includes phi_rl regressions
  </verify>
  <done>
- Script 14 includes M5 in AIC/BIC comparison table alongside M1-M3
- Script 15 accepts --model wmrl_m5 and reports phi_rl trauma associations
- Script 16 accepts --model wmrl_m5 and reports phi_rl regressions on continuous scales
- All existing model functionality unchanged
  </done>
</task>

</tasks>

<verification>
1. Parameter recovery quick test: `python scripts/11_run_model_recovery.py --model wmrl_m5 --n-subjects 10 --n-starts 10` completes
2. Model comparison: `python scripts/14_compare_models.py` includes M5 row in table
3. Trauma analysis: `python scripts/15_analyze_mle_by_trauma.py --model wmrl_m5` runs without error
4. Regression: `python scripts/16_regress_parameters_on_scales.py --model wmrl_m5` runs without error
5. Full parameter recovery (r >= 0.80 gate): `python scripts/11_run_model_recovery.py --model wmrl_m5 --n-subjects 50 --n-starts 50 --n-jobs 8` — all 8 parameters pass r >= 0.80
</verification>

<success_criteria>
- M5 parameter recovery simulation generates valid synthetic data with Q decay BEFORE policy
- M5 appears in AIC/BIC comparison table (script 14)
- phi_rl associations with trauma scales are reported (scripts 15 and 16)
- Parameter recovery passes r >= 0.80 for all 8 M5 parameters
- No regressions in existing model pipelines
</success_criteria>

<output>
After completion, create `.planning/phases/08-m5-rl-forgetting/08-02-SUMMARY.md`
</output>

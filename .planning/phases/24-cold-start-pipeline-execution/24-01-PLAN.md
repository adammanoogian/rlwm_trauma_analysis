---
phase: 24-cold-start-pipeline-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - output/bayesian/21_submission_<TIMESTAMP>.log
  - output/bayesian/21_submission_jobids.txt
  - .planning/phases/24-cold-start-pipeline-execution/24-01-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "Local pre-flight pytest gate (test_numpyro_models_2cov.py, fast tests) passes BEFORE any sbatch call"
    - "Working tree is clean in autopush-managed paths (logs/ and output/bayesian/) at the moment of submission"
    - "bash cluster/21_submit_pipeline.sh is invoked from the Monash M3 login node (NOT the Windows dev box) inside the project root"
    - "Every sbatch call in the orchestrator returns a numeric JobID; all 11 JobIDs are captured to a tee log and a JobID inventory file"
    - "Per-step monitoring contract is recorded so any operator can resume monitoring later by reading the inventory file"
    - "Operator is briefed on the four documented failure modes (INCONCLUSIVE_MULTIPLE phase-blocker, M6b L2 12h cap, autopush ~30 commits, CRLF auto-strip)"
  artifacts:
    - path: "output/bayesian/21_submission_<TIMESTAMP>.log"
      provides: "Tee log of full orchestrator stdout containing 11 JobID echoes (`[21.X] $m -> job $JID` lines from cluster/21_submit_pipeline.sh)"
    - path: "output/bayesian/21_submission_jobids.txt"
      provides: "Parsed JobID inventory (one row per submitted job: step, model_or_NA, JobID) for use by Wave 2 sacct queries"
    - path: ".planning/phases/24-cold-start-pipeline-execution/24-01-SUMMARY.md"
      provides: "Wave 1 SUMMARY documenting submission timestamp, head commit SHA, JobID list, expected wall-clock window (24-48h), and handoff to Wave 2"
  key_links:
    - from: "scripts/fitting/tests/test_numpyro_models_2cov.py"
      to: "cluster/21_submit_pipeline.sh:71-77"
      via: "Pre-flight gate aborts orchestrator on test failure with `[ABORT] No cluster jobs submitted`"
      pattern: "pytest .* test_numpyro_models_2cov.py"
    - from: "cluster/21_submit_pipeline.sh"
      to: "cluster/21_dispatch_l2_winners.sh"
      via: "Step 21.6 dispatcher launches winner-specific L2 fits via `sbatch --wait &` per winner"
      pattern: "21_6_dispatch_l2.slurm"
    - from: "every cluster/21_*.slurm"
      to: "cluster/autopush.sh"
      via: "Each SLURM script sources autopush -> ~30 auto-commits land on main during execution"
      pattern: "source .*autopush\\.sh"
---

<objective>
Pre-flight Phase 24 cold-start submission of `bash cluster/21_submit_pipeline.sh` on the Monash M3 login node, capture all 11 SLURM JobIDs to a tee log + inventory file, and brief the operator on the per-step monitoring contract and four documented failure modes. This plan does NOT wait for the 24-48h pipeline to complete; it ends at the submission boundary and hands off to Wave 2 (24-02-PLAN.md) which audits artifacts after the chain terminates.

Purpose: Phase 24 is the main scientific event of v5.0 — running the full empirical pipeline end-to-end from a clean checkout. Wave 1 establishes the auditable submission record (JobIDs, timestamp, head SHA) that Wave 2 will join against `sacct` to compose the EXEC-03 execution log.

Output: Submitted 11-job afterok-chained pipeline; tee log + JobID inventory persisted; SUMMARY.md handoff to Wave 2 with submission metadata. NO empirical artifacts are produced by this plan — those are produced by the SLURM jobs over the subsequent 24-48h.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-cold-start-pipeline-execution/24-RESEARCH.md
@cluster/21_submit_pipeline.sh
@cluster/21_dispatch_l2_winners.sh
@cluster/autopush.sh
@scripts/fitting/tests/test_numpyro_models_2cov.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks (pytest gate + clean-tree assertion + submit-host verification)</name>
  <files>output/bayesian/21_submission_preflight.log</files>
  <action>
    Run all three pre-flight checks and persist a single log for the operator's record. ALL three must pass before Task 2 invokes the orchestrator.

    1. **Pytest gate** (the same gate the orchestrator runs at line 71-77, but proactively here so a failure surfaces before submission):
       ```bash
       cd /path/to/rlwm_trauma_analysis  # Monash M3 checkout, NOT Windows
       conda activate ds_env  # or whichever env has numpyro per cluster scripts
       pytest scripts/fitting/tests/test_numpyro_models_2cov.py -v -k "not slow" --tb=short \
         | tee output/bayesian/21_submission_preflight.log
       ```
       Expected: 9 tests pass (3 acceptance × M3/M5/M6a + 3 backward-compat × M3/M5/M6a + 3 guard × M3/M5/M6a). The `--slow` test is excluded by `-k "not slow"`.

    2. **Clean-tree assertion in autopush-managed paths only** (NOT whole repo — autopush only `git add logs/ output/bayesian/`):
       ```bash
       git status --porcelain logs/ output/bayesian/ | tee -a output/bayesian/21_submission_preflight.log
       ```
       Assert output is EMPTY. If non-empty, abort with message instructing the operator to commit or stash WIP under those two paths first. Other paths (cluster/, scripts/, output/regressions/, etc.) MAY be dirty — autopush will not touch them.

    3. **Submit-host verification** — confirm `sbatch` is on PATH AND we are NOT on the Windows dev box:
       ```bash
       which sbatch && uname -a | tee -a output/bayesian/21_submission_preflight.log
       ```
       Assert `which sbatch` returns a path (typically `/usr/bin/sbatch` or `/opt/slurm/bin/sbatch`) AND `uname -a` reports `Linux` (NOT Windows). If on Windows, abort and instruct operator to `ssh m3.massive.org.au` (or local equivalent), `cd ~/...rlwm_trauma_analysis`, `git pull`, and re-run this task there.

    DO NOT modify any cluster/* or scripts/21_* file in this task — those are frozen at v4.0 close (re-verified at Phase 23 close).
  </action>
  <verify>
    `cat output/bayesian/21_submission_preflight.log` shows: (a) pytest summary `9 passed` (or equivalent — exact count from `test_numpyro_models_2cov.py` excluding the slow test), (b) empty output for `git status --porcelain logs/ output/bayesian/`, (c) `sbatch` path resolved and `uname -a` reports Linux.
  </verify>
  <done>
    All three pre-flight checks PASS, log persisted at `output/bayesian/21_submission_preflight.log`. Operator confirmed on Monash M3 login node with clean autopush paths and passing pytest gate. Ready to invoke orchestrator.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Submit cold-start pipeline (operator action — Monash M3 login shell only)</name>
  <what-built>
    Pre-flight checks (Task 1) passed. The orchestrator `cluster/21_submit_pipeline.sh` is ready to invoke. This action requires a human at the Monash M3 login shell because (a) cluster credentials live in the operator's M3 session (NOT in any repo file), (b) the 24-48h pipeline cannot complete in a single agent conversation, and (c) the orchestrator's `sed -i 's/\r$//'` line + 11 sequential `sbatch --parsable` calls require the operator's shell to be the canonical submission record.
  </what-built>
  <how-to-verify>
    Operator runs the submission with a tee log AND captures the start timestamp + head commit SHA:

    ```bash
    # 1. Record submission metadata
    SUBMIT_TS=$(date -u +%Y%m%dT%H%M%SZ)
    HEAD_SHA=$(git rev-parse HEAD)
    echo "Submission timestamp (UTC): $SUBMIT_TS" > output/bayesian/21_submission_metadata.txt
    echo "HEAD commit at submission: $HEAD_SHA"  >> output/bayesian/21_submission_metadata.txt
    echo "Hostname: $(hostname)"                  >> output/bayesian/21_submission_metadata.txt

    # 2. Invoke orchestrator with tee log
    bash cluster/21_submit_pipeline.sh 2>&1 \
      | tee "output/bayesian/21_submission_${SUBMIT_TS}.log"

    # 3. Parse JobIDs out of the tee log into an inventory file
    #    Orchestrator echoes "  [21.X] $m -> job $JID" and "  [21.X] -> job $JID" lines
    grep -E "^\s*\[21\.[0-9]+(\.[a-z]+)?\].*-> (job|array job)" \
      "output/bayesian/21_submission_${SUBMIT_TS}.log" \
      > output/bayesian/21_submission_jobids.txt

    # 4. Sanity check: count JobIDs (expected: 30 sbatch invocations
    #    = 6 prior + 6 recovery + 6 recovery-aggregate + 6 baseline + 1 audit + 1 loo + 1 dispatch + 1 scale + 1 avg + 1 tables)
    wc -l output/bayesian/21_submission_jobids.txt   # expect ~30

    # 5. Live queue snapshot
    squeue -u $USER -o "%.10i %.20j %.8T %.10M %.10l %R" --sort=i \
      | tee output/bayesian/21_submission_queue_snapshot.txt
    ```

    Expected outcomes:
    - The orchestrator stdout ends with `Pipeline submitted. Monitor with: squeue -u $USER` (NOT with `[ABORT]`).
    - `21_submission_jobids.txt` is non-empty and contains ~30 lines.
    - `squeue -u $USER` shows step 21.1 jobs in `PD` (pending) or `R` (running) state and downstream jobs in `PD` with reason `(Dependency)`.
    - The orchestrator MUST be invoked from the project root on the Monash M3 login node — NOT from Windows.

    If `bash cluster/21_submit_pipeline.sh` aborts with `[ABORT] No cluster jobs submitted`, the pre-flight pytest gate failed inside the orchestrator. Re-run Task 1 to debug; this is NOT a recoverable failure mode — fix the test, commit, and restart Task 2.

    **Operator briefing — four documented failure modes the operator must monitor for over the next 24-48h:**

    1. **INCONCLUSIVE_MULTIPLE at step 21.5 (PHASE BLOCKER, NOT recoverable pause)** — If `output/bayesian/21_baseline/winner_report.md` shows `Winner type: **INCONCLUSIVE_MULTIPLE**`, the afterok chain CANCELS steps 21.6+. Per EXEC-04, FORCED-winner override is forbidden. Document in 24-01-SUMMARY but resolution requires re-fitting step 21.3 with bigger warmup/samples (out of scope for this plan — escalate to a new plan/phase).

    2. **M6b L2 fit > 12h** — If M6b is in the winner set, the 21.6 child for M6b can take up to 12h (8-param stick-breaking, 4 covariates × 8 betas). Dispatcher cap is 14h. At ~8h elapsed in `21_6_<JID>.out` for M6b, log a checkpoint note. If wall clock exceeds 14h, dispatcher gets killed and afterok cancels 21.7 — operator must accept partial pipeline or restart 21.3 for M6b.

    3. **Auto-push commits during run** — Every SLURM job sources `cluster/autopush.sh` which does `git add logs/ output/bayesian/`, commit, push. Expect ~30 auto-commits on `main` during the 24-48h run with messages like `bayes_21_3_baseline job <JID> results`. This is by design — DO NOT manually `git add` artifacts after the run.

    4. **CRLF auto-strip on Windows-checkout SLURM scripts** — The orchestrator runs `sed -i 's/\r$//' cluster/*.slurm cluster/*.sh` at line 56 BEFORE any sbatch. This is idempotent and handles Windows-checked-out repos. If first sbatch still fails with `Batch script contains DOS line breaks`, file an issue — but this should not happen given the auto-strip.

    Per-step log paths the operator can tail while monitoring (verified from each `#SBATCH --output=` line):
    - `logs/bayesian_21_1_prior_<JID>.{out,err}` (step 21.1)
    - `logs/bayesian_21_2_recovery_<ARRAY_JID>_<TASK_ID>.{out,err}` (step 21.2 array)
    - `logs/bayesian_21_2_aggregate_<JID>.{out,err}` (step 21.2 agg)
    - `logs/bayesian_21_3_<JID>.{out,err}` (step 21.3 baseline)
    - `logs/bayesian_21_4_audit_<JID>.{out,err}` (step 21.4 convergence audit)
    - `logs/bayesian_21_5_<JID>.{out,err}` (step 21.5 LOO+stacking+RFX-BMS)
    - `logs/21_dispatch_l2_<JID>.{out,err}` (step 21.6 dispatcher)
    - `logs/bayesian_21_6_<JID>.{out,err}` (step 21.6 child fits)
    - `logs/bayesian_21_7_<JID>.{out,err}` (step 21.7 scale audit)
    - `logs/bayesian_21_8_<JID>.{out,err}` (step 21.8 model averaging)
    - `logs/bayesian_21_9_<JID>.{out,err}` (step 21.9 manuscript tables)

    **Resource note (zero GPU jobs in scope):** All 6 baseline choice-only models run on `--partition=comp` (CPU-only). Realistic budget: ~24-48h wall-clock, ~250-400 CPU-hours, **0 GPU-hours**. ROADMAP's "50-96 GPU-hours" figure conflates GPU and CPU hours — Phase 24 has zero GPU usage. M4 LBA (the only GPU model) is explicitly out of v5.0 scope (deferred to v5.1).
  </how-to-verify>
  <resume-signal>
    Type "submitted" once `21_submission_jobids.txt` has ~30 JobID rows AND `squeue -u $USER` shows the queue populated AND submission metadata is recorded. Or describe issues if any pre-condition is unmet.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 24-01-SUMMARY.md handoff to Wave 2</name>
  <files>.planning/phases/24-cold-start-pipeline-execution/24-01-SUMMARY.md</files>
  <action>
    Compose the Wave 1 SUMMARY using the canonical template at `C:\Users\aman0087\.claude/get-shit-done/templates/summary.md`. The substantive content for the body:

    Required frontmatter:
    - phase: 24-cold-start-pipeline-execution
    - plan: 01
    - subsystem: infra
    - tags: [slurm, monash-m3, afterok-chain, numpyro, bayesian, cold-start]
    - requires: [{phase: 23-tech-debt-sweep-pre-flight-cleanup, provides: clean codebase + load-side NetCDF validation wrapper}]
    - provides: [submitted afterok pipeline, JobID inventory, submission metadata, Wave 2 handoff]
    - affects: [24-02-PLAN.md (consumes the JobID inventory for sacct queries), Phase 25 (consumes the artifacts produced by the SLURM jobs after this plan's execution boundary)]

    One-liner: "Cold-start submission of 9-step afterok pipeline on Monash M3 — 30 sbatch invocations recorded, JobID inventory persisted, 24-48h wall-clock window opened."

    Required body sections:
    - **Performance:** Duration of Wave 1 itself (NOT the pipeline run — that's Wave 2's accounting). Started/completed timestamps in UTC. Tasks: 3.
    - **Accomplishments:** Pre-flight gate passed; clean-tree asserted in autopush paths; orchestrator invoked; 30 JobIDs captured.
    - **Submission record (NEW section, Phase-24-specific):** Paste contents of `output/bayesian/21_submission_metadata.txt` (timestamp, head SHA, hostname). Paste first 30 lines of `output/bayesian/21_submission_jobids.txt`. Paste the `squeue` snapshot.
    - **Failure-mode briefing recap:** Reproduce the four-mode list (INCONCLUSIVE_MULTIPLE phase-blocker, M6b 12h cap, autopush ~30 commits, CRLF auto-strip) for the next operator who reads this SUMMARY.
    - **Wave 2 handoff:** State explicitly that Wave 2 (24-02-PLAN.md) consumes `21_submission_jobids.txt` to drive `sacct` queries for the EXEC-03 execution log. Wave 2 should start AFTER step 21.9 (`bayesian_21_9_<JID>`) reaches state `COMPLETED` in `sacct`.
    - **Doc-drift flag for Phase 27 closure:** Note that `.planning/ROADMAP.md` Phase 24 success criterion #2 lists the wrong manuscript table paths (`output/bayesian/manuscript/`) and the wrong forest plot dir. Wave 2's `must_haves` use the actual script paths (`output/bayesian/21_tables/` and dual `figures/21_bayesian/` + `output/bayesian/figures/`). Phase 27 (closure) should reconcile.
    - **Next Phase Readiness:** Wave 2 cannot run until the SLURM chain terminates. Operator monitors `sacct -j <JID> --format=State -n` for the step 21.9 JobID; once `COMPLETED`, kick off Wave 2.
  </action>
  <verify>
    `cat .planning/phases/24-cold-start-pipeline-execution/24-01-SUMMARY.md` shows complete frontmatter (all required keys present), substantive one-liner (NOT "phase complete"), and all required body sections including the submission record paste-in and the Phase 27 doc-drift flag. `head -25 24-01-SUMMARY.md | grep -E "^(phase|plan|subsystem|tags|requires|provides|affects):"` returns 7 matching lines.
  </verify>
  <done>
    24-01-SUMMARY.md committed to repo (commit message format: `docs(24-01): wave 1 cold-start submission summary`). Operator briefed on monitoring contract. JobID inventory + submission metadata + queue snapshot all persisted. Ready for Wave 2 once SLURM chain terminates.
  </done>
</task>

</tasks>

<verification>
Wave 1 success means the SLURM chain is SUBMITTED and the audit trail is in place — NOT that the pipeline has completed. Verification surfaces:

1. **Pre-flight log exists and shows three PASS markers**: `cat output/bayesian/21_submission_preflight.log` shows pytest `9 passed`, empty `git status --porcelain logs/ output/bayesian/`, `which sbatch` resolves, `uname -a` reports Linux.

2. **Submission tee log captured 30 JobIDs**: `wc -l output/bayesian/21_submission_jobids.txt` returns ~30 (6 prior + 6 recovery + 6 recovery-agg + 6 baseline + 1 audit + 1 loo + 1 dispatch + 1 scale + 1 avg + 1 tables = 30).

3. **Submission metadata persisted**: `output/bayesian/21_submission_metadata.txt` has timestamp + head SHA + hostname.

4. **Queue is populated**: `squeue -u $USER` snapshot shows step 21.1 jobs in PD/R and downstream jobs in PD with `(Dependency)` reason.

5. **24-01-SUMMARY.md committed** with the doc-drift flag for Phase 27.
</verification>

<success_criteria>
EXEC-01 (pre-flight pytest gate passes BEFORE any sbatch) is partially satisfied by Wave 1 — the gate passes and the orchestrator reaches its first sbatch call. Full EXEC-01 satisfaction (zero SLURM job failures across the 9-step chain) is verified by Wave 2.

EXEC-03 (SLURM accounting per step in `21_execution_log.md`) is INITIATED by Wave 1 (the JobID inventory is captured) but completed by Wave 2 (the sacct join + log writeup).

This plan does NOT satisfy EXEC-02 (artifacts) or EXEC-04 (convergence + winner) — those are Wave 2 audit deliverables.
</success_criteria>

<output>
After completion, create `.planning/phases/24-cold-start-pipeline-execution/24-01-SUMMARY.md` per Task 3.
</output>

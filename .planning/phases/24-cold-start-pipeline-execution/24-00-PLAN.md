---
phase: 24-cold-start-pipeline-execution
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - validation/check_phase24_artifacts.py
  - .planning/phases/24-cold-start-pipeline-execution/24-00-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "All `.planning/ROADMAP.md` Phase-24 + Phase-26 success-criterion rows referencing legacy `output/bayesian/...` paths are rewritten to Phase-31 CCDS-canonical paths (`models/bayesian/21_*/`, `reports/tables/model_comparison/`, `reports/figures/bayesian/21_bayesian/`)"
    - "All `.planning/REQUIREMENTS.md` EXEC-02 / EXEC-03 / MANU-01..05 rows referencing legacy `output/bayesian/...` or `figures/21_bayesian/` paths are rewritten to the same CCDS-canonical targets"
    - "The `validation/check_phase24_artifacts.py` audit script EXPECTATIONS (not yet implemented — Wave 2 builds it, but its path constants are pinned here in a module-level PATH_CONSTANTS dataclass stub with docstring citing Phase 31 CCDS layout) resolve to CCDS-canonical paths only; no legacy fallback paths encoded"
    - "Cluster SLURM + Python driver path contract is already CCDS-canonical (cluster/01..06_*.slurm + scripts/03_model_prefitting/ + scripts/04_model_fitting/ + scripts/05_post_fitting_checks/ + scripts/06_fit_analyses/); Wave 0 VERIFIES this via a grep invariant check and does NOT modify them"
    - "Pre-flight pytest gate on the Monash M3 login node (`pytest scripts/fitting/tests/test_numpyro_models_2cov.py -v -k \"not slow\" --tb=short`) passes green AFTER the doc-drift patch commits"
    - "A new test `tests/integration/test_phase24_path_contract.py` (or equivalent grep invariant inside `tests/integration/test_v5_phase29_structure.py`) enforces that the audit-script expectations, ROADMAP SC rows, and REQUIREMENTS rows all reference CCDS-canonical paths; re-runs green"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Patched Phase-24 SC#3 + Phase-23.1 SC#7 + Phase-26 SC#2 + Plan 24-02 roadmap entry, each now citing `models/bayesian/21_*/`, `reports/tables/model_comparison/`, `reports/figures/bayesian/21_bayesian/`"
      contains: "Zero matches for `output/bayesian/21_` or `figures/21_bayesian/` in live SC rows (excluding archived milestone sections or legacy `<details>` blocks)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Patched EXEC-02 row (path list for manuscript tables + figures), EXEC-03 row (execution log path), MANU-02..03 rows — all now CCDS-canonical"
      contains: "Zero matches for `output/bayesian/21_` in the EXEC-* and MANU-* requirement text"
    - path: "validation/check_phase24_artifacts.py"
      provides: "Stub-only scaffold (frontmatter + PATH_CONSTANTS dataclass + `if __name__ == '__main__': raise NotImplementedError('implemented in Wave 2 / 24-02-PLAN')`) that pins CCDS-canonical path constants BEFORE the cold-start, so Wave 2 implementation cannot drift"
      contains: "`MODELS_DIR = Path('models/bayesian')`, `TABLES_DIR = Path('reports/tables/model_comparison')`, `FIGURES_DIR = Path('reports/figures/bayesian/21_bayesian')`, `EXEC_LOG = MODELS_DIR / '21_execution_log.md'` as module-level constants"
    - path: ".planning/phases/24-cold-start-pipeline-execution/24-00-SUMMARY.md"
      provides: "Wave 0 SUMMARY: enumerates the ROADMAP/REQUIREMENTS rows changed + pytest gate result + grep invariant result + handoff to Wave 1"
  key_links:
    - from: ".planning/ROADMAP.md:412 (Phase 24 SC#2)"
      to: "cluster/06_fit_analyses.slurm:48-49"
      via: "SC#2 enumerates artifact paths; cluster SLURM already writes to those paths; Wave 0 aligns the SC wording with the shipped code"
      pattern: "reports/tables/model_comparison/table[123]_.*\\.(csv|md|tex)"
    - from: ".planning/ROADMAP.md:413 (Phase 24 SC#3)"
      to: "models/bayesian/21_execution_log.md (NEW canonical path)"
      via: "SC#3 currently cites legacy `output/bayesian/21_execution_log.md`; Wave 0 patches to `models/bayesian/21_execution_log.md` matching the CCDS `models/bayesian/` root"
      pattern: "21_execution_log\\.md"
    - from: ".planning/REQUIREMENTS.md (EXEC-02)"
      to: "cluster/04b_bayesian_cpu.slurm + cluster/04c_level2.slurm + cluster/06_fit_analyses.slurm"
      via: "Each SLURM's `mkdir -p` + output path already uses `models/bayesian/21_*` (verified line 111, 127, 91); Wave 0 patches the REQUIREMENTS text to match"
      pattern: "mkdir -p models/bayesian"
---

<objective>
Patch the path-drift between `.planning/ROADMAP.md` + `.planning/REQUIREMENTS.md` (which still cite legacy `output/bayesian/...` paths in several Phase 24 / 26 rows) and the actual shipped cluster SLURMs + Python drivers (which already write to CCDS-canonical `models/bayesian/21_*/` + `reports/tables/model_comparison/` + `reports/figures/bayesian/21_bayesian/` per Phase 31 closure). Pin the `validation/check_phase24_artifacts.py` audit-script path expectations as module-level constants BEFORE the cold-start, so Wave 2 implementation cannot drift. Verify the pre-flight pytest gate passes on the Monash M3 login node after the commit.

Purpose: CONTEXT.md §Path-drift reconciliation declares a Wave 0 pre-phase: "Target: Phase 31 CCDS-canonical paths. Patch ALL 9 steps (not just manuscript tables). Patch happens BEFORE cold-start." A grep audit (2026-04-24) confirmed the cluster SLURMs + Python drivers are ALREADY on CCDS paths (Phase 31 closure landed the migration); the remaining drift is doc-level (ROADMAP + REQUIREMENTS + the audit-script expectations). Wave 0 closes that narrow gap and establishes the single-canonical-path-no-legacy-fallback contract that Wave 2's audit script enforces.

Output: Patched ROADMAP.md + REQUIREMENTS.md + a stub `validation/check_phase24_artifacts.py` with pinned PATH_CONSTANTS; pytest gate green; grep invariant test green; 24-00-SUMMARY.md committed.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-cold-start-pipeline-execution/24-RESEARCH.md
@.planning/phases/24-cold-start-pipeline-execution/24-CONTEXT.md
@docs/PROJECT_STRUCTURE.md
@cluster/submit_all.sh
@cluster/04b_bayesian_cpu.slurm
@cluster/04c_level2.slurm
@cluster/06_fit_analyses.slurm
@cluster/21_dispatch_l2_winners.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-audit — confirm cluster SLURMs + Python drivers already use CCDS-canonical paths (no patch needed at the code layer)</name>
  <files>(read-only: produces a grep log under logs/24-00_grep_audit.log)</files>
  <action>
    Run a targeted grep audit to confirm the path-drift is isolated to docs (`.planning/*`) and does NOT touch `cluster/*.slurm`, `cluster/*.sh`, or `scripts/**/*.py`. Expected pre-finding (verified by planner 2026-04-24): zero legacy-path matches in live code.

    ```bash
    mkdir -p logs
    {
      echo "=== Grep audit: legacy output/bayesian in cluster/*.slurm + cluster/*.sh (excluding legacy/) ==="
      grep -rnE "output/bayesian/(21_|manuscript|figures)" cluster/ --include="*.slurm" --include="*.sh" | grep -v "^cluster/legacy/" || echo "  (no matches — CCDS-canonical)"
      echo
      echo "=== Grep audit: legacy output/bayesian in scripts/**/*.py (excluding legacy/) ==="
      grep -rnE "['\"]output/bayesian/(21_|manuscript)" scripts/ --include="*.py" | grep -v "/legacy/" || echo "  (no matches — CCDS-canonical)"
      echo
      echo "=== Grep audit: legacy figures/21_bayesian in scripts/**/*.py + cluster/ (excluding legacy/) ==="
      grep -rnE "['\"]figures/21_bayesian" scripts/ cluster/ --include="*.py" --include="*.slurm" --include="*.sh" | grep -v "/legacy/" || echo "  (no matches — CCDS-canonical)"
      echo
      echo "=== Grep audit: legacy paths in .planning/ROADMAP.md (expect hits — these are what Wave 0 patches) ==="
      grep -nE "output/bayesian/(21_|manuscript|figures)" .planning/ROADMAP.md || echo "  (no matches)"
      echo
      echo "=== Grep audit: legacy paths in .planning/REQUIREMENTS.md (expect hits — these are what Wave 0 patches) ==="
      grep -nE "output/bayesian/(21_|manuscript|figures)|figures/21_bayesian" .planning/REQUIREMENTS.md || echo "  (no matches)"
    } > logs/24-00_grep_audit.log 2>&1
    cat logs/24-00_grep_audit.log
    ```

    Expected outcomes:
    - First three grep sections return "(no matches — CCDS-canonical)" → code layer is clean; no cluster/scripts patch needed in Wave 0.
    - Fourth + fifth grep sections return NON-EMPTY hits → the lines Wave 0 must patch. Expected hits (from planner audit 2026-04-24): ROADMAP.md line 385 (Phase 23.1 SC#7), line 413 (Phase 24 SC#3), line 420 (24-02 entry), line 452 (Phase 26 SC#2); REQUIREMENTS.md EXEC-02 row + MANU-02/03 rows.

    If the first three grep sections are NON-EMPTY (unexpected), STOP Wave 0 and escalate: the path-drift is not purely doc-level, and the Wave 0 scope needs a code patch that the canary-acceptance gate (Wave 1 Task 2) then has to validate. Update this plan's `files_modified` + `must_haves` before proceeding.
  </action>
  <verify>
    `cat logs/24-00_grep_audit.log` shows: (a) zero legacy-path matches in cluster/scripts (first three sections "no matches — CCDS-canonical"), (b) non-empty hits in `.planning/ROADMAP.md` + `.planning/REQUIREMENTS.md` that enumerate the specific SC/req rows Wave 0 must patch.
  </verify>
  <done>
    Grep audit committed to logs/24-00_grep_audit.log. Code layer confirmed CCDS-canonical (no patch needed). Doc-layer drift enumerated with line numbers, ready for Task 2 surgical edits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch .planning/ROADMAP.md and .planning/REQUIREMENTS.md — replace legacy `output/bayesian/...` paths with CCDS-canonical targets</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Edit the live (not archived-in-`<details>`) SC + requirement rows enumerated by Task 1's grep audit. Use surgical Edit calls — one per row — so the diff is reviewable.

    **ROADMAP.md targeted edits (line numbers from planner audit 2026-04-24; re-verify via Task 1 log before editing):**

    1. **Phase 23.1 SC#7 (~line 385):** `All GPU runs write to output/bayesian/21_*/` → `All GPU runs write to models/bayesian/21_*/`.
    2. **Phase 24 SC#3 (~line 413):** `output/bayesian/21_execution_log.md logs SLURM JobID...` → `models/bayesian/21_execution_log.md logs SLURM JobID...`.
    3. **Plan 24-02 roadmap entry (~line 420):** `output/bayesian/21_execution_log.md from sacct` → `models/bayesian/21_execution_log.md from sacct`.
    4. **Phase 26 SC#2 (~line 452):** `PNGs in output/bayesian/figures/` → `PNGs in reports/figures/bayesian/21_bayesian/`.

    Do NOT touch historical / archived content:
    - Lines referring to original Phase 21 plan text (140s-270s range) — these are historical planning records that describe what the pipeline was specified as in v4.0. Leave as-is. The grep audit will surface them, but they are out-of-scope for Wave 0.
    - Lines inside `<details>...</details>` blocks for closed milestones (v4.0 entry). Leave as-is.
    - The "Sequencing Consideration" Phase 27 decision block if it references paths. Leave as-is.

    **REQUIREMENTS.md targeted edits:**

    1. **EXEC-02 row:** Replace `output/bayesian/manuscript/{loo_stacking,rfx_bms,winner_betas}.{csv,md,tex}` with `reports/tables/model_comparison/table{1,2,3}_{loo_stacking,rfx_bms,winner_betas}.{csv,md,tex}`. Replace any `output/bayesian/` prefixes in the artifact list with `models/bayesian/`.
    2. **EXEC-03 row:** Replace `output/bayesian/21_execution_log.md` with `models/bayesian/21_execution_log.md`.
    3. **MANU-02 row:** Verify the path is `reports/figures/bayesian/21_bayesian/` (not `output/bayesian/figures/` or `figures/21_bayesian/`). Patch if drift exists.
    4. **MANU-03 row:** Verify `reports/tables/model_comparison/` prefix for table1/2/3. Patch if drift exists.

    After each Edit call, re-run the grep invariant from Task 1 to confirm the specific line is resolved:
    ```bash
    grep -nE "output/bayesian/(21_|manuscript|figures)|figures/21_bayesian" .planning/ROADMAP.md .planning/REQUIREMENTS.md | grep -vE "^(\\.planning/ROADMAP\\.md:(1[4-9][0-9]|2[0-7][0-9]|[56][0-9]{2}))" || echo "  (clean)"
    ```

    The `grep -vE` mask excludes the historical Phase-21-plan lines (~140-270) + the closed-milestone archived sections (~500-700 range). After the four ROADMAP edits + REQUIREMENTS edits, this invariant grep should print "(clean)".
  </action>
  <verify>
    `grep -nE "output/bayesian/(21_|manuscript|figures)|figures/21_bayesian" .planning/ROADMAP.md .planning/REQUIREMENTS.md | grep -vE "^(\\.planning/ROADMAP\\.md:(1[4-9][0-9]|2[0-7][0-9]|[56][0-9]{2}))"` prints "(clean)" or returns zero live-section matches. `git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows only the targeted rows changed (no spurious whitespace-only or reflow edits).
  </verify>
  <done>
    Phase 23.1 SC#7, Phase 24 SC#3, Plan 24-02 roadmap entry, Phase 26 SC#2, EXEC-02, EXEC-03, MANU-02, MANU-03 all now cite CCDS-canonical paths. `.planning/REQUIREMENTS.md` traceability table rows unchanged. Grep invariant green on the live-sections mask.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create stub validation/check_phase24_artifacts.py with pinned CCDS path constants (Wave 2 will fill in the check functions)</name>
  <files>validation/check_phase24_artifacts.py</files>
  <action>
    Create a minimal stub that (a) establishes the canonical import surface for Wave 2, (b) pins the CCDS path expectations as module-level constants so Wave 2 cannot drift, and (c) raises NotImplementedError on invocation so a premature run in CI or by an operator surfaces a clear error pointing to Wave 2.

    Write exactly this scaffold (update the date to 2026-04-24, use NumPy-style docstrings per global CLAUDE.md, `from __future__ import annotations`, native type hints, 88-char lines):

    ```python
    """Phase 24 cold-start artifact audit — CCDS-canonical path constants (stub).

    This module is the verification surface for Phase 24's EXEC-02 + EXEC-04
    requirements. Wave 0 (plan 24-00) pins the CCDS-canonical path expectations
    as module-level constants so the Wave 2 implementation (plan 24-02)
    cannot drift back to the legacy `output/bayesian/...` layout.

    CCDS layout source: docs/PROJECT_STRUCTURE.md (Phase 31 closure, 2026-04-24).
    Any artifact appearing outside these canonical paths is a FAIL in the
    Wave 2 audit — there is NO legacy-fallback path.

    Wave 2 (plan 24-02) replaces the NotImplementedError below with the
    actual check functions (see 24-02-PLAN.md Task 1 for the full list).

    Notes
    -----
    The CheckResult dataclass + main() printer pattern mirrors
    validation/check_v4_closure.py (Phase 22 closure guard).
    """

    from __future__ import annotations

    from dataclasses import dataclass
    from pathlib import Path

    # --- CCDS-canonical path constants (pinned by Wave 0 / plan 24-00) ------

    MODELS_DIR: Path = Path("models/bayesian")
    PRIOR_PREDICTIVE_DIR: Path = MODELS_DIR / "21_prior_predictive"
    RECOVERY_DIR: Path = MODELS_DIR / "21_recovery"
    BASELINE_DIR: Path = MODELS_DIR / "21_baseline"
    L2_DIR: Path = MODELS_DIR / "21_l2"

    TABLES_DIR: Path = Path("reports/tables/model_comparison")
    FIGURES_DIR: Path = Path("reports/figures/bayesian/21_bayesian")

    EXEC_LOG: Path = MODELS_DIR / "21_execution_log.md"
    WINNERS_TXT: Path = BASELINE_DIR / "winners.txt"
    CONVERGENCE_TABLE: Path = BASELINE_DIR / "convergence_table.csv"
    WINNER_REPORT: Path = BASELINE_DIR / "winner_report.md"

    BASELINE_MODELS: tuple[str, ...] = (
        "qlearning",
        "wmrl",
        "wmrl_m3",
        "wmrl_m5",
        "wmrl_m6a",
        "wmrl_m6b",
    )

    # Reporting labels for winner-determination audit (CONTEXT.md Area 3 §Convergence).
    WINNER_TYPES_REPORTABLE: frozenset[str] = frozenset(
        {"DOMINANT_SINGLE", "TOP_TWO", "TOP_K", "FORCED", "INCONCLUSIVE_MULTIPLE"}
    )


    @dataclass(frozen=True)
    class CheckResult:
        """Single audit-check outcome (mirrors validation/check_v4_closure.py)."""

        name: str
        ok: bool
        detail: str


    def main() -> int:
        """Run all Phase 24 audit checks; implemented in Wave 2 (plan 24-02)."""
        raise NotImplementedError(
            "validation/check_phase24_artifacts.py is a stub pinned by Wave 0 "
            "(plan 24-00). The full audit implementation lands in Wave 2 "
            "(plan 24-02) after the cold-start pipeline terminates. "
            "Do NOT invoke before Wave 2 is shipped."
        )


    if __name__ == "__main__":
        import sys

        sys.exit(main())
    ```

    After writing, verify:
    ```bash
    python -c "from validation.check_phase24_artifacts import MODELS_DIR, TABLES_DIR, FIGURES_DIR, EXEC_LOG, BASELINE_MODELS; print(MODELS_DIR, TABLES_DIR, FIGURES_DIR, EXEC_LOG, len(BASELINE_MODELS))"
    # Expected: "models/bayesian reports/tables/model_comparison reports/figures/bayesian/21_bayesian models/bayesian/21_execution_log.md 6"

    # NotImplementedError fires on direct invocation
    python validation/check_phase24_artifacts.py; test $? -ne 0 && echo "stub correctly raises NotImplementedError"
    ```

    DO NOT add the actual check functions in Wave 0. That scope belongs to Wave 2.
  </action>
  <verify>
    `python -c "from validation.check_phase24_artifacts import MODELS_DIR, TABLES_DIR, FIGURES_DIR, EXEC_LOG, BASELINE_MODELS; assert str(MODELS_DIR) == 'models/bayesian' and str(TABLES_DIR) == 'reports/tables/model_comparison' and str(FIGURES_DIR) == 'reports/figures/bayesian/21_bayesian' and str(EXEC_LOG) == 'models/bayesian/21_execution_log.md' and BASELINE_MODELS == ('qlearning', 'wmrl', 'wmrl_m3', 'wmrl_m5', 'wmrl_m6a', 'wmrl_m6b'); print('OK')"` prints `OK`. `python validation/check_phase24_artifacts.py` exits non-zero with a NotImplementedError referencing Wave 2 / plan 24-02.
  </verify>
  <done>
    Stub `validation/check_phase24_artifacts.py` committed with pinned CCDS path constants. Wave 2's implementation (plan 24-02) cannot drift to legacy paths because these constants are the import surface.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run pre-flight pytest gate on the Monash M3 login node to confirm the patch is green</name>
  <files>logs/24-00_preflight_pytest.log</files>
  <action>
    Per CONTEXT.md §Submission strategy: "Pre-flight pytest gate: Runs on the Monash M3 login node only." This task explicitly runs the gate on the cluster login node AFTER Tasks 1-3 commits, so Wave 1's canary submission inherits a green baseline.

    On the Monash M3 login node, inside the project root:
    ```bash
    conda activate ds_env  # or rlwm_gpu per cluster discipline
    pytest scripts/fitting/tests/test_numpyro_models_2cov.py -v -k "not slow" --tb=short \
      | tee logs/24-00_preflight_pytest.log
    ```

    Expected: 9 tests pass (3 acceptance × M3/M5/M6a + 3 backward-compat × M3/M5/M6a + 3 guard × M3/M5/M6a). The `--slow` test is excluded by `-k "not slow"`.

    Also run the Phase-31 structure invariant to confirm Wave 0's PATH_CONSTANTS align with the shipped Phase-31 CCDS layout:
    ```bash
    pytest tests/integration/test_v5_phase29_structure.py -v --tb=short \
      | tee -a logs/24-00_preflight_pytest.log
    ```

    If either gate fails, Wave 0 must not commit Task 5 (SUMMARY) — fix the root cause first. In particular, if the Phase-31-structure test fails, the PATH_CONSTANTS in Task 3 have drifted from `docs/PROJECT_STRUCTURE.md` and must be reconciled before proceeding to Wave 1.
  </action>
  <verify>
    `grep -E "passed|failed" logs/24-00_preflight_pytest.log | tail -2` shows both gates green (e.g., `9 passed in 3.2s` for the 2-cov gate and `N passed in Ms` for the Phase-29 structure gate with zero failures).
  </verify>
  <done>
    Pre-flight gate is green on Monash M3 login node AFTER the Wave 0 doc-drift patches. Baseline is clean for Wave 1 canary submission.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write 24-00-SUMMARY.md handoff to Wave 1</name>
  <files>.planning/phases/24-cold-start-pipeline-execution/24-00-SUMMARY.md</files>
  <action>
    Compose the Wave 0 SUMMARY using the canonical template at `C:\Users\aman0087\.claude/get-shit-done/templates/summary.md`. Substantive content:

    Required frontmatter:
    - phase: 24-cold-start-pipeline-execution
    - plan: 00
    - subsystem: infra
    - tags: [path-patch, ccds-canonical, phase31, preflight-gate, wave0]
    - requires: [{phase: 23-tech-debt-sweep-pre-flight-cleanup, provides: clean codebase + load-side validation wrapper}, {phase: 31-final-package-restructure, provides: CCDS-canonical top-level layout}]
    - provides: [doc-drift-patched ROADMAP + REQUIREMENTS, pinned CCDS audit path constants, pre-flight gate green, Wave 1 handoff]
    - affects: [24-01-PLAN.md (inherits the CCDS path contract + green pre-flight gate), 24-02-PLAN.md (inherits validation/check_phase24_artifacts.py PATH_CONSTANTS and must fill in check functions without introducing legacy-fallback paths)]

    One-liner: "Wave 0 reconciled ROADMAP / REQUIREMENTS doc-drift to Phase 31 CCDS-canonical paths and pinned `validation/check_phase24_artifacts.py` PATH_CONSTANTS; pre-flight pytest gate green on Monash M3 login node; cold-start baseline clean for Wave 1 canary submission."

    Required body sections:
    - **Performance:** Duration of Wave 0 itself (tasks: 5). Started/completed UTC timestamps.
    - **Grep audit finding:** Code layer (cluster/ + scripts/) ALREADY on CCDS-canonical paths per Phase 31 closure; doc drift was confined to .planning/ROADMAP.md + .planning/REQUIREMENTS.md. Cite the hit-lines from logs/24-00_grep_audit.log.
    - **Files patched:** Enumerate the ROADMAP rows (Phase 23.1 SC#7, Phase 24 SC#3, Plan 24-02 entry, Phase 26 SC#2) and REQUIREMENTS rows (EXEC-02, EXEC-03, MANU-02, MANU-03) with before→after quote.
    - **PATH_CONSTANTS pin:** Paste the module-level constants block from `validation/check_phase24_artifacts.py`. Wave 2 MUST re-use these constants (no string literals in check functions).
    - **Pre-flight gate result:** Cite `logs/24-00_preflight_pytest.log` summary line + the Phase-31-structure test result.
    - **Handoff to Wave 1:** State explicitly that Wave 1 (24-01-PLAN.md) inherits: (a) a green pre-flight gate on M3, (b) a CCDS-canonical path contract enforced at the doc layer, (c) a pinned audit-script expectations module. Wave 1's canary submission validates the end-to-end cluster → repo flow against these expectations; Wave 2 fills in the audit functions.
    - **Note on CONTEXT.md sequencing:** CONTEXT.md Area 4 declared "Wave 0 precedes cold-start." This plan satisfies that. Wave 1 canary submission proceeds only AFTER this SUMMARY is committed.
  </action>
  <verify>
    `head -25 .planning/phases/24-cold-start-pipeline-execution/24-00-SUMMARY.md` shows full frontmatter (phase, plan, subsystem, tags, requires, provides, affects) + substantive one-liner. Body contains the five required sections. `git log --oneline -1` shows the Wave 0 commit.
  </verify>
  <done>
    24-00-SUMMARY.md committed (commit message format: `docs(24-00): wave 0 ccds path reconciliation + pinned audit expectations`). Wave 1 is unblocked.
  </done>
</task>

</tasks>

<verification>
Wave 0 success means: (a) the doc layer (ROADMAP + REQUIREMENTS) now aligns with the CCDS-canonical code layer; (b) the Wave 2 audit-script expectations are pinned and cannot drift; (c) the pre-flight gate is green on the M3 login node; (d) the code layer was NOT touched (Phase 31 already did that work). Verification surfaces:

1. **Grep invariant clean:** `grep -nE "output/bayesian/(21_|manuscript|figures)|figures/21_bayesian" .planning/ROADMAP.md .planning/REQUIREMENTS.md | grep -vE "^(\\.planning/ROADMAP\\.md:(1[4-9][0-9]|2[0-7][0-9]|[56][0-9]{2}))"` prints empty (or "(clean)").

2. **PATH_CONSTANTS importable and correct:** `python -c "from validation.check_phase24_artifacts import MODELS_DIR, TABLES_DIR; assert str(MODELS_DIR) == 'models/bayesian' and str(TABLES_DIR) == 'reports/tables/model_comparison'"` exits 0.

3. **Stub raises NotImplementedError on invocation:** `python validation/check_phase24_artifacts.py` exits non-zero.

4. **Pre-flight gate green:** `logs/24-00_preflight_pytest.log` shows both the 2-cov test suite + the Phase-29-structure test suite passing.

5. **Code layer untouched:** `git diff --name-only HEAD~5..HEAD -- cluster/ scripts/` (where ~5 covers the Wave 0 commit set) shows ZERO matches. Only `.planning/` and `validation/check_phase24_artifacts.py` were modified.
</verification>

<success_criteria>
Wave 0 does NOT satisfy any EXEC-* requirement directly. It clears the prerequisite for Wave 1 canary submission and Wave 2 audit implementation by:
- Aligning doc-layer paths with the Phase 31 CCDS code layer (removes an ambiguity source).
- Pinning audit-script expectations so Wave 2 cannot accidentally introduce legacy-fallback paths.
- Confirming the pre-flight pytest gate is green on M3 (CONTEXT.md Area 1 requirement).

If ANY Wave 0 task fails, Wave 1 MUST NOT proceed. CONTEXT.md §Submission strategy: canary 21.1 submission is gated on a green pre-flight gate.
</success_criteria>

<output>
After completion, create `.planning/phases/24-cold-start-pipeline-execution/24-00-SUMMARY.md` per Task 5.
</output>
</content>
</invoke>
---
phase: 24-cold-start-pipeline-execution
plan: 02
type: execute
wave: 2
depends_on: ["24-01-PLAN.md"]
files_modified:
  - validation/check_phase24_artifacts.py
  - output/bayesian/21_execution_log.md
  - .planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "Every Phase 21 SLURM step produced its declared artifacts (file existence proven by audit script)"
    - "Each baseline NetCDF posterior opens without corruption (load-side validation passes)"
    - "Convergence gate PASSES for >= 2 models (EXEC-04 part 1) — verified via convergence_table.csv pipeline_action == PROCEED_TO_LOO"
    - "Winner determination is DOMINANT_SINGLE or TOP_TWO (EXEC-04 part 2) — NOT FORCED, NOT INCONCLUSIVE_MULTIPLE"
    - "Manuscript tables (table1/2/3 in 21_tables/, NOT manuscript/) reference the declared winners (winners.txt)"
    - "21_execution_log.md tabulates per-step JobID, JobName, Elapsed, TotalCPU, MaxRSS, State, ExitCode, AllocTRES sourced from sacct"
    - "Total CPU-hours documented; GPU-hours = 0 stated explicitly (zero GPU jobs in Phase 24 scope)"
  artifacts:
    - path: "validation/check_phase24_artifacts.py"
      provides: "Deterministic Phase 24 artifact + EXEC-04 audit script (mirrors validation/check_v4_closure.py CheckResult dataclass pattern); exit 0 on all pass, exit 1 on any failure"
      contains: "load_netcdf_with_validation, convergence_gate check, winner_type assertion, dual forest path check"
    - path: "output/bayesian/21_execution_log.md"
      provides: "EXEC-03 per-step SLURM accounting table + aggregate budget summary, composed from sacct -P output joined against 21_submission_jobids.txt"
      contains: "Per-step JobID/Elapsed/TotalCPU/MaxRSS table; Aggregate budget table with CPU-hours total and GPU-hours=0"
    - path: ".planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md"
      provides: "Wave 2 SUMMARY with empirical findings: which models passed convergence, winner identity, manuscript table paths produced, doc-drift flag for Phase 27, VERIFICATION-ready handoff"
  key_links:
    - from: "validation/check_phase24_artifacts.py"
      to: "output/bayesian/21_baseline/convergence_table.csv"
      via: "pd.read_csv + assert (df['pipeline_action'] == 'PROCEED_TO_LOO').sum() >= 2"
      pattern: "convergence_table\\.csv"
    - from: "validation/check_phase24_artifacts.py"
      to: "output/bayesian/21_baseline/winner_report.md"
      via: "regex parse `Winner type: \\*\\*([A-Z_]+)\\*\\*` and assert in {DOMINANT_SINGLE, TOP_TWO}"
      pattern: "winner_report\\.md"
    - from: "validation/check_phase24_artifacts.py"
      to: "config.load_netcdf_with_validation"
      via: "Per-model NetCDF integrity check on each *_posterior.nc (catches partial-write corruption beyond Path.exists)"
      pattern: "load_netcdf_with_validation"
    - from: "output/bayesian/21_execution_log.md"
      to: "output/bayesian/21_submission_jobids.txt"
      via: "Wave 2 reads JobID inventory from Wave 1 + queries sacct -j <JID> --format=... -P --noheader"
      pattern: "21_submission_jobids\\.txt"
---

<objective>
Audit the empirical artifacts produced by the cold-start pipeline (Wave 1's submitted SLURM chain), verify EXEC-02 (every expected artifact exists + integrity), EXEC-03 (compose `output/bayesian/21_execution_log.md` from sacct accounting), and EXEC-04 (convergence gate >= 2 models + winner type in {DOMINANT_SINGLE, TOP_TWO}). Wave 2 starts AFTER step 21.9 (`bayesian_21_9_<JID>`) reaches `COMPLETED` in sacct — operator confirms terminus before invoking this plan.

Purpose: This is the verification-readiness deliverable for Phase 24. The audit script lives at `validation/check_phase24_artifacts.py` (same closure-guard pattern as `validation/check_v4_closure.py`), the execution log lives at `output/bayesian/21_execution_log.md`, and 24-02-SUMMARY.md asserts EXEC-01..04 satisfaction so the verifier can promote Phase 24 to VERIFIED.

Output: Audit script (deterministic, exit 0/1), execution log (per-step + aggregate), Wave 2 SUMMARY with winner-determination outcome and Phase 27 doc-drift flag for the manuscript-paths discrepancy.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-cold-start-pipeline-execution/24-RESEARCH.md
@.planning/phases/24-cold-start-pipeline-execution/24-01-SUMMARY.md
@validation/check_v4_closure.py
@config.py
@scripts/21_baseline_audit.py
@scripts/21_compute_loo_stacking.py
@scripts/21_manuscript_tables.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement validation/check_phase24_artifacts.py (deterministic audit)</name>
  <files>validation/check_phase24_artifacts.py</files>
  <action>
    Create a new audit script following the EXACT pattern of `validation/check_v4_closure.py` — `dataclass CheckResult(name: str, ok: bool, detail: str)` collected into a list, each check is a pure function returning `CheckResult`, and `main()` prints a per-check table then exits 0 (all pass) or 1 (any fail). Add `from __future__ import annotations`, NumPy-style docstrings, native type hints (`list[CheckResult]`, `Path`, etc.), and 88-char line length per project conventions in `C:\Users\aman0087\.claude\CLAUDE.md`.

    Required checks (one function per check):

    1. **`check_step_21_1_prior_predictive() -> list[CheckResult]`** — For each `model in ["qlearning", "wmrl", "wmrl_m3", "wmrl_m5", "wmrl_m6a", "wmrl_m6b"]`, assert these three files exist (returns 6×3 = 18 results, one per file):
       - `output/bayesian/21_prior_predictive/{model}_prior_sim.nc`
       - `output/bayesian/21_prior_predictive/{model}_prior_accuracy.csv`
       - `output/bayesian/21_prior_predictive/{model}_gate.md`

    2. **`check_step_21_2_recovery() -> list[CheckResult]`** — For each model, assert these two AGGREGATED outputs exist (skip the 50 per-subject JSONs as intermediates — 12 results total):
       - `output/bayesian/21_recovery/{model}_recovery.csv`
       - `output/bayesian/21_recovery/{model}_recovery_summary.md`

    3. **`check_step_21_3_baseline() -> list[CheckResult]`** — For each model, assert these four files exist (24 results):
       - `output/bayesian/21_baseline/{model}_individual_fits.csv`
       - `output/bayesian/21_baseline/{model}_posterior.nc`
       - `output/bayesian/21_baseline/{model}_shrinkage_report.md`
       - `output/bayesian/21_baseline/{model}_ppc_results.csv`

    4. **`check_baseline_netcdf_integrity() -> list[CheckResult]`** — Use `config.load_netcdf_with_validation` (Phase 23 CLEAN-04 deliverable at `config.py:754`) to OPEN each `{model}_posterior.nc`. Catches partial-write corruption that passes `Path.exists()` but fails `az.from_netcdf()`. Returns 6 results, one per model.

    5. **`check_step_21_4_convergence_gate() -> CheckResult`** — Read `output/bayesian/21_baseline/convergence_table.csv` with `pd.read_csv`, assert `(df["pipeline_action"] == "PROCEED_TO_LOO").sum() >= 2`. Cross-check with the four threshold columns (`max_rhat <= 1.05` AND `min_ess_bulk >= 400` AND `n_divergences == 0` AND `min_bfmi >= 0.2`) and assert agreement count matches `n_passing`. **This is the EXEC-04 part 1 verification surface.** Detail string includes the full per-model `pipeline_action` mapping for the audit log.

    6. **`check_step_21_5_winner_determination() -> CheckResult`** — Read `output/bayesian/21_baseline/winner_report.md`, regex `r"Winner type:\s*\*\*([A-Z_]+)\*\*"`, assert match group ∈ `{"DOMINANT_SINGLE", "TOP_TWO"}` (NOT `FORCED`, NOT `INCONCLUSIVE_MULTIPLE`). Cross-check with `winners.txt`: count comma-separated entries; expect 1 for DOMINANT_SINGLE, 2 for TOP_TWO. **This is the EXEC-04 part 2 verification surface.** Detail string includes winner names from `winners.txt`.

    7. **`check_step_21_5_artifacts() -> list[CheckResult]`** — Assert these four files exist (4 results):
       - `output/bayesian/21_baseline/loo_stacking_results.csv`
       - `output/bayesian/21_baseline/rfx_bms_pxp.csv`
       - `output/bayesian/21_baseline/winner_report.md`
       - `output/bayesian/21_baseline/winners.txt`

    8. **`check_step_21_6_l2_winners() -> list[CheckResult]`** — For each winner in `winners.txt`, map display-name → internal-id via the NAME_MAP from `cluster/21_dispatch_l2_winners.sh:48-55` (M1→qlearning, M2→wmrl, M3→wmrl_m3, M5→wmrl_m5, M6a→wmrl_m6a, M6b→wmrl_m6b). For winners ∈ {M1, M2}: assert ONLY `output/bayesian/21_l2/{internal}_posterior.nc` exists (copy-through path per `21_6_fit_with_l2.slurm:170-173`). For winners ∈ {M3, M5, M6a, M6b}: assert all four files exist:
       - `output/bayesian/21_l2/{internal}_individual_fits.csv`
       - `output/bayesian/21_l2/{internal}_posterior.nc`
       - `output/bayesian/21_l2/{internal}_shrinkage_report.md`
       - `output/bayesian/21_l2/{internal}_ppc_results.csv`
       Plus NetCDF integrity via `load_netcdf_with_validation` for each `{internal}_posterior.nc`. Result count varies with winner count.

    9. **`check_step_21_7_scale_audit() -> list[CheckResult]`** — Assert `output/bayesian/21_l2/scale_audit_report.md` exists AND parse its YAML frontmatter `pipeline_action:` key, assert ∈ `{"PROCEED_TO_AVERAGING", "NULL_RESULT"}` (both are exit-0 by design — unified-exit-0 protocol). Assert `output/bayesian/21_l2/{internal}_beta_hdi_table.csv` exists for each winner (NOT for M1/M2 copy-through — same conditional as Task check #8).

    10. **`check_step_21_8_averaging_branch() -> CheckResult`** — Assert exactly ONE of these branch markers exists (mutually exclusive per `scripts/21_model_averaging.py`):
        - PROCEED branch: `output/bayesian/21_l2/averaged_scale_effects.csv` AND `output/bayesian/21_l2/averaging_summary.md`
        - NULL_RESULT branch: `output/bayesian/21_l2/averaging_skipped.md`
        - SINGLE_WINNER branch: `output/bayesian/21_l2/single_winner_mode.md`
        Assert `output/bayesian/21_l2/launch_subscale.flag` does NOT exist post-run (cleared after sub-arm sbatch). Detail string names which branch was taken.

    11. **`check_step_21_9_manuscript_tables() -> list[CheckResult]`** — Conditional on the branch from check #10. If PROCEED or SINGLE_WINNER, assert ALL of these 9 files exist (use ACTUAL paths from `scripts/21_manuscript_tables.py`, NOT the stale `output/bayesian/manuscript/` paths from ROADMAP):
        - `output/bayesian/21_tables/table1_loo_stacking.{csv,md,tex}` (3 files)
        - `output/bayesian/21_tables/table2_rfx_bms.{csv,md,tex}` (3 files)
        - `output/bayesian/21_tables/table3_winner_betas.{csv,md,tex}` (3 files)
        Plus assert per-winner forest plot at BOTH dual paths (per `21_9_manuscript_tables.slurm:145` AND `scripts/21_manuscript_tables.py:764-765`):
        - `figures/21_bayesian/forest_{display_winner}.png` (e.g., `forest_M3.png`)
        - `output/bayesian/figures/{internal_winner}_forest.png` (e.g., `wmrl_m3_forest.png`)
        If NULL_RESULT branch: assert `output/bayesian/21_tables/null_result_summary.md` exists instead.

    12. **`check_manuscript_table_winner_consistency() -> CheckResult`** — Open `output/bayesian/21_tables/table3_winner_betas.csv` with `pd.read_csv`, extract any column or row identifying the winner model(s), and assert it matches the winners declared in `output/bayesian/21_baseline/winners.txt`. Catches a class of bug where step 21.9 produced a table for a stale winner set.

    Main entry point:
    ```python
    def main() -> int:
        results: list[CheckResult] = []
        results.extend(check_step_21_1_prior_predictive())
        results.extend(check_step_21_2_recovery())
        results.extend(check_step_21_3_baseline())
        results.extend(check_baseline_netcdf_integrity())
        results.append(check_step_21_4_convergence_gate())
        results.append(check_step_21_5_winner_determination())
        results.extend(check_step_21_5_artifacts())
        results.extend(check_step_21_6_l2_winners())
        results.extend(check_step_21_7_scale_audit())
        results.append(check_step_21_8_averaging_branch())
        results.extend(check_step_21_9_manuscript_tables())
        results.append(check_manuscript_table_winner_consistency())

        # Print table; exit 1 if any not ok
        n_pass = sum(1 for r in results if r.ok)
        n_fail = sum(1 for r in results if not r.ok)
        for r in results:
            marker = "PASS" if r.ok else "FAIL"
            print(f"[{marker}] {r.name}: {r.detail}")
        print(f"\nTotals: {n_pass} pass, {n_fail} fail")
        return 0 if n_fail == 0 else 1


    if __name__ == "__main__":
        import sys
        sys.exit(main())
    ```

    Determinism requirement: running the script twice on the same artifact set MUST produce byte-identical stdout (no timestamps in output, no random ordering — iterate dictionaries via sorted keys).

    Error message format per project CLAUDE.md: include expected vs actual values in every assertion (e.g., `f"EXEC-04 FAIL: only {n_passing} models met gate; expected >= 2. Per-model: {df[['model','pipeline_action']].to_dict('records')}"`).
  </action>
  <verify>
    `python validation/check_phase24_artifacts.py; echo "exit=$?"` runs to completion. If all SLURM artifacts exist (Wave 1 pipeline ran successfully), exit code = 0 and stdout shows `Totals: N pass, 0 fail` (N depends on winner count: ~70-80 checks). Determinism check: `python validation/check_phase24_artifacts.py > /tmp/run1.txt; python validation/check_phase24_artifacts.py > /tmp/run2.txt; diff /tmp/run1.txt /tmp/run2.txt` returns empty.
  </verify>
  <done>
    `validation/check_phase24_artifacts.py` exists, follows the `check_v4_closure.py` template, ran successfully against the cold-start outputs with exit 0 (all checks pass), and is deterministic across double-runs. EXEC-02 + EXEC-04 verification surfaces are now machine-checkable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose output/bayesian/21_execution_log.md from sacct accounting</name>
  <files>output/bayesian/21_execution_log.md</files>
  <action>
    Read the JobID inventory from Wave 1 (`output/bayesian/21_submission_jobids.txt`) and submission metadata (`output/bayesian/21_submission_metadata.txt`). For each JobID, query sacct:

    ```bash
    sacct -j <JID> --format=JobID,JobName,Partition,Elapsed,TotalCPU,MaxRSS,State,ExitCode,AllocTRES,ReqMem -P --noheader
    ```

    Use `-P` (parsable, pipe-separated) and `--noheader` for stable parsing. The first row per JobID is the parent batch step; subsequent rows (`<JID>.batch`, `<JID>.extern`) report per-step accounting. For aggregate budget, sum the `TotalCPU` column across all parent rows.

    **Markdown structure (write exactly this — do NOT decorate with extra sections):**

    ```markdown
    # Phase 24 Cold-Start Execution Log

    **Submitted:** {ISO timestamp UTC from 21_submission_metadata.txt}
    **Submission host:** {hostname from metadata}
    **HEAD commit at submission:** {sha from metadata}
    **Cluster:** Monash M3
    **Total wall-clock:** {start_ts} -> {end_ts of last job} ({HH:MM:SS elapsed})

    ## Per-step accounting

    | Step | JobID | JobName | Partition | Elapsed | TotalCPU | MaxRSS | State | ExitCode | AllocTRES |
    |---|---|---|---|---|---|---|---|---|---|
    | 21.1 qlearning | {JID} | bayes_21_1_prior_pred | comp | HH:MM:SS | HH:MM:SS | NNN K | COMPLETED | 0:0 | cpu=4,mem=16G |
    | 21.1 wmrl | ... |
    | (... one row per submitted job ordered by step) |

    ## Aggregate budget

    | Resource | Total used | Notes |
    |---|---|---|
    | CPU-hours | {sum(TotalCPU) across all parent rows, in hours} | Sum of TotalCPU column |
    | GPU-hours | 0 | Zero by design — all choice-only models CPU-only on `--partition=comp`; M4 LBA (the only GPU model) is out of v5.0 scope |
    | Wall-clock | {HH:MM:SS} | Submission to terminus of step 21.9 |
    | Peak memory (any single job) | {max(MaxRSS) value} | From the {step + JobID} that hit the peak |
    | Total sbatch invocations | {count of JobIDs in inventory} | Including 21.6 dispatcher children |

    ## Per-step breakdown by phase 21 step

    | Step | Description | Wall-clock | CPU-hours | Notes |
    |---|---|---|---|---|
    | 21.1 | Prior predictive (6 models) | {sum elapsed} | {sum CPU} | |
    | 21.2 | Recovery (50-array × 6 models + 6 aggregators) | ... |
    | 21.3 | Baseline hierarchical fit (6 models) | ... |
    | 21.4 | Convergence audit | ... |
    | 21.5 | LOO + stacking + RFX-BMS | ... |
    | 21.6 | L2 winner refit (dispatcher + N children) | ... |
    | 21.7 | Scale audit | ... |
    | 21.8 | Model averaging | ... |
    | 21.9 | Manuscript tables | ... |

    ## Notes

    - Per cluster/autopush.sh, every SLURM job auto-committed `git add logs/ output/bayesian/` on completion — expect ~30 commits on `main` between submission and terminus.
    - `--partition=comp` enforces CPU-only execution for all choice-only models; `JAX_PLATFORMS=cpu` in each script.
    - sacct queries used: `sacct -j <JID> --format=JobID,JobName,Partition,Elapsed,TotalCPU,MaxRSS,State,ExitCode,AllocTRES,ReqMem -P --noheader`
    ```

    Implementation note: This is best done as a one-shot Python or Bash script that loops over the JobID inventory. If implemented in Python, place inline in this task (do NOT add a permanent script file — this is a one-time write of `21_execution_log.md`). If Bash, use `awk -F'|'` to parse sacct output.

    Important: Use `-P` (pipe-separated) NOT default whitespace because `AllocTRES` contains commas. ALL TotalCPU sum math should be in Python (not shell) because TotalCPU strings like `1-04:23:11` (1 day, 4h, 23min, 11s) need parsing — use `datetime.timedelta` or a small parser.
  </action>
  <verify>
    `cat output/bayesian/21_execution_log.md` shows: (a) header with submission timestamp + HEAD SHA + hostname; (b) per-step table with one row per submitted JobID (~30 rows); (c) aggregate budget table with explicit `GPU-hours: 0`; (d) per-step-21-X breakdown table. Run `head -10 output/bayesian/21_execution_log.md` to confirm header structure, then `wc -l output/bayesian/21_execution_log.md` to confirm ~50+ lines.
  </verify>
  <done>
    `output/bayesian/21_execution_log.md` exists, fully populated from sacct, with explicit `GPU-hours: 0` documented. EXEC-03 verification surface (per-step JobID/Elapsed/CPU/MaxRSS + aggregate CPU-hours) is in place.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator review + write 24-02-SUMMARY.md (VERIFICATION-ready handoff)</name>
  <what-built>
    The audit script ran (Task 1) and the execution log is composed (Task 2). The operator must (a) read the audit script's stdout to confirm the winner identity and convergence-passing model count, (b) skim `21_execution_log.md` to spot-check that the wall-clock + CPU-hours numbers match expectations (~24-48h wall, ~250-400 CPU-hours), and (c) approve the SUMMARY draft below before Phase 24 is marked VERIFICATION-ready.

    The reason for the human-verify step: the SUMMARY documents which model won (a scientific finding, not a mechanical fact) and asserts EXEC-01..04 satisfaction. The operator must consciously sign off on the winner identity before the verifier promotes the phase.
  </what-built>
  <how-to-verify>
    Operator runs:

    ```bash
    # 1. Confirm audit script result
    python validation/check_phase24_artifacts.py
    # expected: exit 0, "Totals: N pass, 0 fail"

    # 2. Confirm winner identity from the audit output OR directly:
    cat output/bayesian/21_baseline/winner_report.md | head -30
    cat output/bayesian/21_baseline/winners.txt

    # 3. Sanity-check the execution log totals
    grep -E "^\| (CPU-hours|Wall-clock|GPU-hours)" output/bayesian/21_execution_log.md

    # 4. Confirm pipeline terminus — last step (21.9) reached COMPLETED
    LAST_JID=$(grep "21.9" output/bayesian/21_submission_jobids.txt | grep -oE "[0-9]+$" | head -1)
    sacct -j ${LAST_JID} --format=JobID,JobName,State,ExitCode,Elapsed -n
    # expected: State = COMPLETED, ExitCode = 0:0
    ```

    Then compose `.planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md` per the canonical template. Required content:

    Frontmatter:
    - phase: 24-cold-start-pipeline-execution
    - plan: 02
    - subsystem: infra
    - tags: [slurm, monash-m3, audit, convergence-gate, winner-determination, exec-04, baribault-collins-2023]
    - requires: [{phase: 24-01, provides: JobID inventory + submitted SLURM chain}]
    - provides: [audit script, execution log, EXEC-01..04 satisfaction record, winner identity, doc-drift flag for Phase 27]
    - affects: [25-01-PLAN.md (consumes baseline NetCDFs for reproducibility regression), 26-* (consumes manuscript tables + winner identity)]

    One-liner: "Cold-start pipeline VERIFIED — winner: {WINNER_NAME(s)} via {DOMINANT_SINGLE|TOP_TWO}; {N_PASS}/6 models met Baribault & Collins gate; {CPU_HOURS} CPU-hours, 0 GPU-hours, {WALL_CLOCK} wall-clock."

    Required body sections:
    - **Empirical findings:** Which models passed convergence (list from convergence_table.csv); winner identity from winners.txt; winner type from winner_report.md; stacking weights from loo_stacking_results.csv (top 3); RFX-BMS PXP from rfx_bms_pxp.csv (top 3).
    - **EXEC-01 satisfaction:** Pre-flight pytest gate passed (cite 24-01-SUMMARY); zero SLURM job failures (cite sacct State column showing all COMPLETED, no FAILED/CANCELLED).
    - **EXEC-02 satisfaction:** All artifacts present per `validation/check_phase24_artifacts.py` exit 0 (cite the totals line).
    - **EXEC-03 satisfaction:** `output/bayesian/21_execution_log.md` exists with per-step JobID/Elapsed/TotalCPU/MaxRSS table + aggregate budget; cite total CPU-hours and explicit GPU-hours = 0.
    - **EXEC-04 satisfaction:** Convergence gate passed for {N_PASS} >= 2 models (cite per-model pipeline_action mapping); winner type ∈ {DOMINANT_SINGLE, TOP_TWO} (cite the actual type).
    - **Doc-drift flag for Phase 27 closure:** ROADMAP.md Phase 24 success criterion #2 lists `output/bayesian/manuscript/{loo_stacking,rfx_bms,winner_betas}.{csv,md,tex}` and unspecified forest plot dir. Actual paths produced by the scripts: `output/bayesian/21_tables/table{1,2,3}_*.{csv,md,tex}` AND dual forest paths `figures/21_bayesian/forest_{display}.png` + `output/bayesian/figures/{internal}_forest.png`. Phase 27 (closure) must reconcile ROADMAP wording with actual paths — DO NOT touch the scripts (Phase 26 manuscript and `paper.qmd` reference the actual paths already).
    - **Files Created/Modified:** validation/check_phase24_artifacts.py (~300 lines), output/bayesian/21_execution_log.md (~50+ lines), 24-02-SUMMARY.md (this file).
    - **Decisions Made:** None if audit passed cleanly. If INCONCLUSIVE_MULTIPLE or M6b L2 timeout was hit during the run, document the decision (re-fit triggered, partial pipeline accepted, etc.).
    - **Issues Encountered:** Document any sacct query oddities, autopush merge conflicts, or step 21.X failures that required manual recovery.
    - **Next Phase Readiness:** Phase 24 is VERIFICATION-ready. Phase 25 (reproducibility regression) can begin — needs `output/bayesian/21_baseline/{model}_posterior.nc` and `21_prior_predictive/{model}_prior_sim.nc` (both present per audit).

    Operator confirms by typing `verified` once the SUMMARY is written and committed (commit message format: `docs(24-02): wave 2 artifact audit + execution log + EXEC-04 satisfaction`).
  </how-to-verify>
  <resume-signal>
    Type "verified" once the audit script exits 0, the execution log spot-check matches expectations, the SUMMARY is written with the actual winner identity, and the file is committed. Or describe issues if EXEC-04 was not satisfied (e.g., FORCED winner, fewer than 2 models passed convergence) — those are phase-blockers requiring escalation.
  </resume-signal>
</task>

</tasks>

<verification>
Wave 2 satisfies all four EXEC requirements:

1. **EXEC-01 (zero SLURM job failures + pre-flight gate):** Verified by sacct query in Task 3 confirming all JobIDs in `21_submission_jobids.txt` reached State=COMPLETED with ExitCode=0:0. Pre-flight gate passing was already verified by Wave 1.

2. **EXEC-02 (all artifacts present):** Verified by `python validation/check_phase24_artifacts.py` exit 0 (Task 1).

3. **EXEC-03 (per-step accounting):** Verified by `output/bayesian/21_execution_log.md` existing with per-step table + aggregate budget (Task 2). Explicit `GPU-hours: 0` documented.

4. **EXEC-04 (convergence + winner):** Verified by audit script's `check_step_21_4_convergence_gate` (>= 2 PROCEED_TO_LOO rows) and `check_step_21_5_winner_determination` (winner type ∈ {DOMINANT_SINGLE, TOP_TWO}). Both checks live in Task 1's script.

End state: `python validation/check_phase24_artifacts.py; echo $?` returns 0; `cat output/bayesian/21_execution_log.md` shows complete table; 24-02-SUMMARY.md asserts EXEC-01..04 with citations to specific artifacts.
</verification>

<success_criteria>
- `validation/check_phase24_artifacts.py` exists, ~300 lines, mirrors `check_v4_closure.py` CheckResult dataclass pattern, deterministic across double-runs, exit 0 against the cold-start artifact set.
- `output/bayesian/21_execution_log.md` exists with per-step JobID/JobName/Elapsed/TotalCPU/MaxRSS/State/ExitCode/AllocTRES table sourced from `sacct -j <JID> --format=... -P --noheader`, plus aggregate budget table with explicit `GPU-hours: 0` and total `CPU-hours` documented.
- `.planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md` exists with substantive one-liner naming the winner, EXEC-01..04 satisfaction record with citations, doc-drift flag for Phase 27, and VERIFICATION-ready handoff.
- All three files committed to repo (commit format per CLAUDE.md: no Co-Authored-By line).
- Operator confirmed audit exit 0 + execution log spot-check + winner identity before signing off.
</success_criteria>

<output>
After completion, create `.planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md` per Task 3.
</output>

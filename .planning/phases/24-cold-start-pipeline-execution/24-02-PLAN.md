---
phase: 24-cold-start-pipeline-execution
plan: 02
type: execute
wave: 2
depends_on: ["24-00-PLAN.md", "24-01-PLAN.md"]
files_modified:
  - tests/scientific/check_phase24_artifacts.py
  - models/bayesian/21_execution_log.md
  - .planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "Every Phase 24 pipeline stage produced its declared artifacts at the CCDS-canonical path (models/bayesian/21_*/, reports/tables/model_comparison/, reports/figures/bayesian/21_bayesian/) — legacy output/bayesian/... paths MUST be absent"
    - "tests/scientific/check_phase24_artifacts.py replaces the Wave 0 stub NotImplementedError with the full check function set; uses the Wave 0 PATH_CONSTANTS only (no string-literal paths); deterministic across double-runs"
    - "Each baseline NetCDF posterior opens without corruption via config.load_netcdf_with_validation (catches partial-writes that pass Path.exists)"
    - "Convergence acceptance policy is implemented as a REPORTING SURFACE (not pass/fail gate): audit reports n_models_passing_baribault, winner_determination_label (DOMINANT_SINGLE / TOP_TWO / TOP_K / FORCED-as-INCONCLUSIVE), and stacking_pool_membership (Baribault-passing only) — ALL outcomes exit 0"
    - "21_execution_log.md contains single sacct entry per successful step (NO retry-chain table per CONTEXT.md Area 2); resource-adjustment events get one-line operator notes"
    - "Explicit note in 24-02-SUMMARY: EXEC-04 text may not match reality — Phase 25 will reconcile the aspirational EXEC-04 wording against the cold-start outcome"
    - "Total CPU-hours + GPU-hours documented (GPU-hours non-zero per Phase 23.1 GPU dispatch default)"
  artifacts:
    - path: "tests/scientific/check_phase24_artifacts.py"
      provides: "Full Phase 24 artifact + convergence-reporting audit script; replaces Wave 0 stub; exit 0 regardless of convergence outcome (convergence is reporting-surface, not pass/fail); exit 1 only on missing artifacts or NetCDF corruption"
      contains: "check_step_03_prior_predictive, check_step_03_recovery, check_step_04b_baseline, check_baseline_netcdf_integrity, check_convergence_reporting, check_winner_determination_reporting, check_stacking_pool_membership, check_step_04c_l2, check_step_05_scale_audit, check_step_06_model_averaging_branch, check_step_06_manuscript_tables, check_manuscript_table_winner_consistency"
    - path: "models/bayesian/21_execution_log.md"
      provides: "EXEC-03 per-step SLURM accounting table + aggregate budget; single sacct entry per successful step; no retry-chain bookkeeping; resource-adjustment events as one-line notes"
      contains: "Per-step JobID/Elapsed/TotalCPU/MaxRSS/State/ExitCode/AllocTRES table; Aggregate budget with CPU-hours + GPU-hours + wall-clock; Operator notes section (if any resource-allocation adjustments occurred)"
    - path: ".planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md"
      provides: "Wave 2 SUMMARY reporting: # models passing Baribault, winner-determination label (whatever the cold-start produced), stacking pool membership, manuscript artifact paths, EXEC-04 reconciliation flag for Phase 25"
  key_links:
    - from: "tests/scientific/check_phase24_artifacts.py (all path constants)"
      to: "Wave 0 PATH_CONSTANTS (MODELS_DIR, TABLES_DIR, FIGURES_DIR, ...)"
      via: "All checks resolve paths via the module-level constants pinned by plan 24-00; any string-literal path is a lint failure"
      pattern: "from validation.check_phase24_artifacts import MODELS_DIR"
    - from: "tests/scientific/check_phase24_artifacts.py::check_convergence_reporting"
      to: "models/bayesian/21_baseline/convergence_table.csv"
      via: "pd.read_csv + compute n_models_passing_baribault; REPORT (not assert) via CheckResult.detail"
      pattern: "pipeline_action.*PROCEED_TO_LOO"
    - from: "tests/scientific/check_phase24_artifacts.py::check_winner_determination_reporting"
      to: "models/bayesian/21_baseline/winner_report.md"
      via: "regex parse winner_type; REPORT label in {DOMINANT_SINGLE, TOP_TWO, TOP_K, FORCED-as-INCONCLUSIVE}; no assert — all labels are valid reporting outcomes"
      pattern: "Winner type:\\s*\\*\\*[A-Z_]+\\*\\*"
    - from: "tests/scientific/check_phase24_artifacts.py::check_stacking_pool_membership"
      to: "models/bayesian/21_baseline/loo_stacking_results.csv"
      via: "Assert loo_stacking pool membership equals the set of Baribault-passing models (non-convergent excluded per CONTEXT.md Area 3)"
      pattern: "loo_stacking_results\\.csv"
    - from: "models/bayesian/21_execution_log.md"
      to: "models/bayesian/21_submission_jobids.txt + models/bayesian/21_canary_jobids.txt"
      via: "Wave 2 unions both JobID inventories (canary + full-chain) + queries sacct -j <JID> --format=... -P --noheader"
      pattern: "sacct.*--format=.*-P.*--noheader"
---

<objective>
Audit the empirical artifacts produced by the Wave 1 cold-start pipeline against Phase 31 CCDS-canonical paths (no legacy fallback). Implement `tests/scientific/check_phase24_artifacts.py` (replacing the Wave 0 stub) with CONTEXT.md Area 3's pre-declared convergence-acceptance policy encoded as a REPORTING SURFACE — all outcomes exit 0 (pass/fail is file-existence + NetCDF integrity only; convergence is reporting). Compose `models/bayesian/21_execution_log.md` from sacct with NO retry-chain bookkeeping per CONTEXT.md Area 2. Wave 2 starts AFTER the terminal stage-06 manuscript_tables JobID reaches COMPLETED in sacct.

Purpose: Per CONTEXT.md §Convergence / winner acceptance: "Policy locked up front to prevent threshold-shopping post-hoc. Whatever the cold-start produces is the result." The audit script therefore reports outcomes rather than blocking on them. The aspirational EXEC-04 text ("≥ 2 models meeting gate; winner ∈ {DOMINANT_SINGLE, TOP_TWO}") may or may not match reality — Phase 25 reconciles the text to the observed outcome. Wave 2's job is to produce an honest, reproducible record of what happened.

Output: Deterministic audit script that reports reality, execution log composed from sacct + canary and full-chain JobID inventories, Wave 2 SUMMARY with the observed winner-determination label and EXEC-04 reconciliation flag for Phase 25.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-cold-start-pipeline-execution/24-RESEARCH.md
@.planning/phases/24-cold-start-pipeline-execution/24-CONTEXT.md
@.planning/phases/24-cold-start-pipeline-execution/24-00-SUMMARY.md
@.planning/phases/24-cold-start-pipeline-execution/24-01-SUMMARY.md
@validation/check_v4_closure.py
@tests/scientific/check_phase24_artifacts.py
@config.py
@scripts/05_post_fitting_checks/01_baseline_audit.py
@scripts/06_fit_analyses/02_compute_loo_stacking.py
@scripts/06_fit_analyses/08_manuscript_tables.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Wave 0 stub with full tests/scientific/check_phase24_artifacts.py implementation</name>
  <files>tests/scientific/check_phase24_artifacts.py</files>
  <action>
    Replace the NotImplementedError stub from Wave 0 with the full implementation. Follow the `validation/check_v4_closure.py` pattern — `CheckResult` dataclass, pure-function checks, deterministic main() that prints a table + exits 0 (all non-convergence checks pass) or 1 (missing artifact / NetCDF corruption / winner set inconsistency).

    **KEEP the PATH_CONSTANTS block from Wave 0 UNCHANGED.** Every check function resolves paths via those constants — no string-literal paths in check bodies.

    **KEY POLICY: convergence is a REPORTING SURFACE, not a pass/fail gate.** Per CONTEXT.md Area 3 §Convergence / winner acceptance:
    - If < 2 models pass Baribault gate → REPORT the count, do NOT fail the audit.
    - If winner_type is INCONCLUSIVE_MULTIPLE → REPORT as TOP_K, do NOT fail.
    - If winner_type is FORCED → REPORT as FORCED-as-INCONCLUSIVE (treated identically to INCONCLUSIVE_MULTIPLE).
    - All winner_types are valid reporting outcomes. The AUDIT only fails on missing files, corrupt NetCDFs, or winner-set consistency bugs.

    Required checks (one function per check; all return CheckResult or list[CheckResult]):

    1. **check_step_03_prior_predictive** — For each model in BASELINE_MODELS, assert these three files exist at `PRIOR_PREDICTIVE_DIR`:
       - `{model}_prior_sim.nc`
       - `{model}_prior_accuracy.csv`
       - `{model}_gate.md`
       Returns 18 results.

    2. **check_step_03_recovery** — For each model, assert these aggregated outputs exist at `RECOVERY_DIR`:
       - `{model}_recovery.csv`
       - `{model}_recovery_summary.md`
       Returns 12 results.

    3. **check_step_04b_baseline** — For each model, assert at `BASELINE_DIR`:
       - `{model}_individual_fits.csv`
       - `{model}_posterior.nc`
       - `{model}_shrinkage_report.md`
       - `{model}_ppc_results.csv`
       Returns 24 results.

    4. **check_baseline_netcdf_integrity** — For each model, open `{model}_posterior.nc` via `config.load_netcdf_with_validation` (Phase 23 CLEAN-04 deliverable). Catches partial-write corruption. Returns 6 results.

    5. **check_convergence_reporting (REPORTING SURFACE)** — Read `BASELINE_DIR / "convergence_table.csv"`, compute `n_passing = (df["pipeline_action"] == "PROCEED_TO_LOO").sum()`. Cross-check with Baribault threshold columns (`max_rhat <= 1.05` AND `min_ess_bulk >= 400` AND `n_divergences == 0` AND `min_bfmi >= 0.2`) and assert agreement count matches `n_passing`. Returns a CheckResult that is ALWAYS ok=True (reporting surface); detail field contains:
       ```
       f"REPORT: {n_passing}/{len(df)} models passed Baribault gate. "
       f"Per-model: {df[['model', 'pipeline_action']].to_dict('records')}"
       ```
       ok=False ONLY if the Baribault-column tally disagrees with the pipeline_action tally (that's a scripts/05_post_fitting_checks bug, not a convergence outcome).

    6. **check_winner_determination_reporting (REPORTING SURFACE)** — Read `BASELINE_DIR / "winner_report.md"`, regex `r"Winner type:\s*\*\*([A-Z_]+)\*\*"`, extract winner_type. Map to reporting label per CONTEXT.md Area 3:
       - winner_type == "DOMINANT_SINGLE" → label = "DOMINANT_SINGLE"
       - winner_type == "TOP_TWO" → label = "TOP_TWO"
       - winner_type == "INCONCLUSIVE_MULTIPLE" → label = "TOP_K"
       - winner_type == "FORCED" → label = "FORCED-as-INCONCLUSIVE (treated as TOP_K)"
       - Any other winner_type → ok=False (unexpected label)
       Cross-check with `winners.txt`: count comma-separated entries; the count is REPORTED (not constrained). ALL labels are valid → ok=True except on regex miss / unexpected label.

    7. **check_stacking_pool_membership** — Read `BASELINE_DIR / "loo_stacking_results.csv"` + `convergence_table.csv`. Assert the set of models in loo_stacking_results equals the set of Baribault-passing models (non-convergent models EXCLUDED per CONTEXT.md Area 3 Yao et al. 2018 ELPD validity). ok=False if the sets disagree — that's a stage 06 loo_stacking bug, not a scientific outcome.

    8. **check_step_05_artifacts** — Assert existence of:
       - `BASELINE_DIR / "loo_stacking_results.csv"`
       - `BASELINE_DIR / "rfx_bms_pxp.csv"`
       - `WINNER_REPORT`
       - `WINNERS_TXT`
       Returns 4 results.

    9. **check_step_04c_l2_winners** — For each winner in WINNERS_TXT, map display-name → internal-id via NAME_MAP from `cluster/21_dispatch_l2_winners.sh:48-55` (M1→qlearning, M2→wmrl, M3→wmrl_m3, M5→wmrl_m5, M6a→wmrl_m6a, M6b→wmrl_m6b). For winners ∈ {M1, M2}: assert ONLY `L2_DIR / f"{internal}_posterior.nc"` (copy-through path per `cluster/04c_level2.slurm:170-173`). For winners ∈ {M3, M5, M6a, M6b}: assert all four files (`{internal}_individual_fits.csv`, `{internal}_posterior.nc`, `{internal}_shrinkage_report.md`, `{internal}_ppc_results.csv`) plus NetCDF integrity via `load_netcdf_with_validation` on each `{internal}_posterior.nc`. Result count varies with |winners|.

    10. **check_step_05_scale_audit** — Assert `L2_DIR / "scale_audit_report.md"` exists AND parse YAML frontmatter `pipeline_action:` key, assert ∈ {"PROCEED_TO_AVERAGING", "NULL_RESULT"} (both exit-0 by design — unified-exit-0 protocol). Assert `L2_DIR / f"{internal}_beta_hdi_table.csv"` exists for each winner (NOT for M1/M2 copy-through).

    11. **check_step_06_averaging_branch** — Assert exactly ONE branch marker exists (mutually exclusive):
        - PROCEED: `L2_DIR / "averaged_scale_effects.csv"` AND `L2_DIR / "averaging_summary.md"`
        - NULL_RESULT: `L2_DIR / "averaging_skipped.md"`
        - SINGLE_WINNER: `L2_DIR / "single_winner_mode.md"`
        Assert `L2_DIR / "launch_subscale.flag"` does NOT exist post-run. Detail string names the branch.

    12. **check_step_06_manuscript_tables** — Conditional on branch from check #11. If PROCEED or SINGLE_WINNER, assert at `TABLES_DIR`:
        - `table1_loo_stacking.{csv,md,tex}` (3 files)
        - `table2_rfx_bms.{csv,md,tex}` (3 files)
        - `table3_winner_betas.{csv,md,tex}` (3 files)
        Plus per-winner forest plot at `FIGURES_DIR / f"forest_{display_winner}.png"` (e.g., `forest_M3.png`). NO legacy-path check — CCDS only.
        If NULL_RESULT: assert `TABLES_DIR / "null_result_summary.md"` instead.

    13. **check_manuscript_table_winner_consistency** — Open `TABLES_DIR / "table3_winner_betas.csv"`, extract winner column/row, assert it matches `WINNERS_TXT`. Catches stale-winner-table bug.

    14. **check_no_legacy_paths** — NEW per CONTEXT.md Area 4 "strict CCDS-only, no legacy fallback":
        ```python
        legacy_candidates = [
            Path("output/bayesian/21_prior_predictive"),
            Path("output/bayesian/21_recovery"),
            Path("output/bayesian/21_baseline"),
            Path("output/bayesian/21_l2"),
            Path("output/bayesian/21_tables"),
            Path("output/bayesian/manuscript"),
            Path("figures/21_bayesian"),
        ]
        polluted = [p for p in legacy_candidates if p.exists() and any(p.iterdir())]
        ok = not polluted
        ```
        ok=False if any legacy path exists AND has artifacts. A canary-approved run should have populated only the CCDS paths.

    Main entry point:
    ```python
    def main() -> int:
        results: list[CheckResult] = []
        results.extend(check_step_03_prior_predictive())
        results.extend(check_step_03_recovery())
        results.extend(check_step_04b_baseline())
        results.extend(check_baseline_netcdf_integrity())
        results.append(check_convergence_reporting())         # REPORTING SURFACE
        results.append(check_winner_determination_reporting())  # REPORTING SURFACE
        results.append(check_stacking_pool_membership())
        results.extend(check_step_05_artifacts())
        results.extend(check_step_04c_l2_winners())
        results.extend(check_step_05_scale_audit())
        results.append(check_step_06_averaging_branch())
        results.extend(check_step_06_manuscript_tables())
        results.append(check_manuscript_table_winner_consistency())
        results.append(check_no_legacy_paths())

        # Print sorted by check name for determinism
        n_pass = sum(1 for r in results if r.ok)
        n_fail = sum(1 for r in results if not r.ok)
        for r in sorted(results, key=lambda x: x.name):
            marker = "PASS" if r.ok else "FAIL"
            print(f"[{marker}] {r.name}: {r.detail}")
        print(f"\nTotals: {n_pass} pass, {n_fail} fail")
        return 0 if n_fail == 0 else 1


    if __name__ == "__main__":
        import sys
        sys.exit(main())
    ```

    Determinism requirement: running the script twice on the same artifact set MUST produce byte-identical stdout. Sort results by name before printing. Do NOT include timestamps in output.

    Error message format per global CLAUDE.md: include expected vs actual values (e.g., `f"FAIL check_manuscript_table_winner_consistency: table3 winners={table_winners}; winners.txt={txt_winners}"`).
  </action>
  <verify>
    `python tests/scientific/check_phase24_artifacts.py; echo "exit=$?"` runs to completion. Expected: exit 0 (all non-convergence checks pass). Stdout shows `Totals: N pass, 0 fail` (N scales with winner count — expect ~70-80). Convergence + winner-determination checks always print `[PASS]` with a REPORT detail (the reporting surface).

    Determinism: `python tests/scientific/check_phase24_artifacts.py > /tmp/run1.txt; python tests/scientific/check_phase24_artifacts.py > /tmp/run2.txt; diff /tmp/run1.txt /tmp/run2.txt` returns empty.

    No legacy fallback: `grep -nE "['\"]output/bayesian/|['\"]figures/21_bayesian" tests/scientific/check_phase24_artifacts.py | grep -v "^.*# check_no_legacy_paths"` returns zero matches (no legacy paths outside the explicit no-legacy check).
  </verify>
  <done>
    `tests/scientific/check_phase24_artifacts.py` fully implemented. Exit 0 against cold-start artifacts. Deterministic across double-runs. Convergence + winner-determination reported as CheckResult.detail (not asserted). Legacy-path polling check catches any drift. Phase 24 audit surface is machine-checkable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose models/bayesian/21_execution_log.md from sacct accounting (single entry per step; no retry table)</name>
  <files>models/bayesian/21_execution_log.md</files>
  <action>
    Read BOTH Wave 1 JobID inventories — the canary (`models/bayesian/21_canary_jobids.txt`) and the full-chain (`models/bayesian/21_submission_jobids.txt`) — and compose a single execution log that covers the end-to-end pipeline. Read submission metadata from both `21_canary_metadata.txt` and `21_submission_metadata.txt`.

    For each JobID, query sacct:
    ```bash
    sacct -j <JID> --format=JobID,JobName,Partition,Elapsed,TotalCPU,MaxRSS,State,ExitCode,AllocTRES,ReqMem -P --noheader
    ```

    Parse the first parent row per JobID (ignore `.batch` / `.extern` suffix rows). Sum TotalCPU across parent rows for CPU-hours. Sum GPU-allocated-hours (parse `AllocTRES` for `gres/gpu=N` + multiply by Elapsed).

    **Markdown structure (per CONTEXT.md Area 2: single entry per step, no retry-chain table):**

    ```markdown
    # Phase 24 Cold-Start Execution Log

    **Canary submitted:** {canary_timestamp} (UTC)
    **Full-chain submitted:** {full_chain_timestamp} (UTC)
    **Submission host:** {hostname}
    **HEAD commit (canary):** {canary_sha}
    **HEAD commit (full-chain):** {full_chain_sha}
    **Cluster:** Monash M3
    **Dispatch mode:** {GPU (Phase 23.1 default) | CPU (USE_CPU=1)}
    **Total wall-clock:** {canary_start} → {last_job_end} ({HH:MM:SS elapsed})

    ## Canary execution (stage 03 prior_predictive, 6 jobs)

    | Model | JobID | JobName | Elapsed | TotalCPU | MaxRSS | State | ExitCode | AllocTRES |
    |---|---|---|---|---|---|---|---|---|
    | qlearning | {JID} | bayes_03_prior_pred | HH:MM:SS | HH:MM:SS | NNN K | COMPLETED | 0:0 | cpu=4,mem=16G,gres/gpu=1 |
    | (...6 rows) |

    **Canary verdict:** APPROVED (four-criteria gate, per models/bayesian/21_canary_acceptance_report.md)

    ## Full-chain execution (stages 03 bayesian_recovery → 04b → 04c → 05 → 06)

    | Stage.Step | JobID | JobName | Partition | Elapsed | TotalCPU | MaxRSS | State | ExitCode | AllocTRES |
    |---|---|---|---|---|---|---|---|---|---|
    | 03.bayesian_recovery qlearning | {JID} | bayes_03_recovery | gpu | ... | ... | ... | COMPLETED | 0:0 | ... |
    | (... one row per JobID; NO retry-chain table even if resource adjustments happened) |

    ## Aggregate budget

    | Resource | Total | Notes |
    |---|---|---|
    | CPU-hours | {sum(TotalCPU parent rows)} | Summed across all parent sacct rows |
    | GPU-hours | {sum(gres/gpu × Elapsed)} | Phase 23.1 default dispatch uses multi-GPU Template C for stages 03/04b/04c |
    | Wall-clock | {HH:MM:SS} | Canary submission to stage-06 manuscript_tables terminus |
    | Peak memory (any single job) | {max(MaxRSS)} | From {step + JobID} |
    | Total sbatch invocations | {count} | Canary (6) + full-chain (N) |

    ## Per-stage breakdown

    | Stage | Description | Wall-clock | CPU-hours | GPU-hours | Notes |
    |---|---|---|---|---|---|
    | 03 prior_predictive | Canary — 6 models | {sum} | {sum} | {sum} | Canary-only; acceptance gate: APPROVED |
    | 03 bayesian_recovery | 6 models × 50-subject array | ... | ... | ... | |
    | 04b baseline | 6 models hierarchical fit | ... | ... | ... | M6b allocated 36h walltime |
    | 04c L2 refit | {N} winners via dispatcher | ... | ... | ... | |
    | 05 post_checks | baseline_audit → scale_audit | ... | ... | ... | CPU-only |
    | 06 fit_analyses | compare → loo → L2 dispatch → averaging → tables | ... | ... | ... | CPU-only |

    ## Operator notes (resource-allocation adjustments, if any)

    Per CONTEXT.md §Failure-recovery policy, SLURM timeout / OOM events are resource-allocation
    adjustments — not retries. Single-line entries here document any such events during the cold-start:

    - (None — all jobs succeeded on first submission with v4.0 conservative padding.)

    OR, if adjustments happened:

    - 2026-04-2X: Stage 04b wmrl_m6b hit 24h walltime with divergent chains still recovering.
      Bumped --time=36h → 48h, committed (<sha>), resubmitted with same seed (JID→JID'). Fresh
      JID in table above; original JID's FAILED row not shown (excluded per single-entry policy).

    ## Provenance

    - sacct queries: `sacct -j <JID> --format=JobID,JobName,Partition,Elapsed,TotalCPU,MaxRSS,State,ExitCode,AllocTRES,ReqMem -P --noheader`
    - JobID inventories: models/bayesian/21_canary_jobids.txt + models/bayesian/21_submission_jobids.txt
    - Metadata: models/bayesian/21_canary_metadata.txt + models/bayesian/21_submission_metadata.txt
    - Autopush commits touching models/bayesian/ since submission: {count from `git log`}
    ```

    **Single-entry-per-step policy:** If a step was resubmitted after a resource-allocation adjustment (per CONTEXT.md Area 2), ONLY the successful JobID appears in the per-step table. The predecessor (failed or timeout) row is excluded. This prevents the execution log from becoming a retry-chain reconstruction. The Operator Notes section captures the fact that an adjustment happened, not the detailed per-attempt accounting.

    **GPU-hours documentation:** Phase 23.1 dispatch default is GPU for stages 03/04b/04c (Template C = 4 GPUs + pmap). Stages 01/02/05/06 are CPU-only. The aggregate budget row distinguishes CPU-hours vs GPU-hours; both are non-zero in the default dispatch. The stale "GPU-hours = 0" claim from the v4.0-era 24-02 draft is retired.

    Implementation: one-shot Python script (inline in this task; do NOT add a permanent script file) that reads the two JobID inventories + two metadata files, queries sacct, builds the markdown. Use `-P` pipe-separated sacct output + `datetime.timedelta` parsing for TotalCPU strings.
  </action>
  <verify>
    `cat models/bayesian/21_execution_log.md` shows: (a) header with both canary + full-chain submission timestamps, (b) canary section (6 rows), (c) full-chain section with one row per successful step, (d) aggregate budget with both CPU-hours AND GPU-hours non-zero, (e) per-stage breakdown, (f) operator notes section (empty or populated with one-line resource-adjustment entries), (g) provenance section. `wc -l models/bayesian/21_execution_log.md` returns ~60+ lines.

    `grep -c "^| " models/bayesian/21_execution_log.md` returns a count consistent with 6 canary rows + full-chain step count (no retry-chain inflation).
  </verify>
  <done>
    `models/bayesian/21_execution_log.md` composed per CONTEXT.md Area 2 policy (single entry per step, no retry table). GPU-hours + CPU-hours both documented. Operator notes capture resource-allocation adjustments as one-liners (if any). EXEC-03 verification surface complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator review + write 24-02-SUMMARY.md (VERIFICATION-ready handoff; EXEC-04 reconciliation flag for Phase 25)</name>
  <what-built>
    The audit script ran (Task 1) and the execution log is composed (Task 2). Operator reviews:
    (a) the audit script's stdout — confirms the observed winner identity + convergence-passing model count (REPORTED, not asserted);
    (b) skims `21_execution_log.md` for wall-clock + CPU-hours + GPU-hours spot-check;
    (c) approves the SUMMARY draft below before Phase 24 is marked VERIFICATION-ready.

    Human-verify step because the SUMMARY documents which model(s) won — a scientific finding, not a mechanical fact — and flags EXEC-04 for Phase 25 reconciliation if the observed outcome diverges from the aspirational text.
  </what-built>
  <how-to-verify>
    Operator runs:
    ```bash
    # 1. Confirm audit script exit code + reporting output
    python tests/scientific/check_phase24_artifacts.py
    # expected: exit 0; stdout includes REPORT lines for convergence + winner-determination

    # 2. Read the reported outcomes
    python -c "
    from pathlib import Path
    print('=== winner_report.md (first 30 lines) ===')
    print('\n'.join(Path('models/bayesian/21_baseline/winner_report.md').read_text().splitlines()[:30]))
    print()
    print('=== winners.txt ===')
    print(Path('models/bayesian/21_baseline/winners.txt').read_text().strip())
    "

    # 3. Convergence reporting
    python -c "
    import pandas as pd
    df = pd.read_csv('models/bayesian/21_baseline/convergence_table.csv')
    print(df[['model', 'max_rhat', 'min_ess_bulk', 'n_divergences', 'min_bfmi', 'pipeline_action']].to_string(index=False))
    print()
    n_pass = (df['pipeline_action'] == 'PROCEED_TO_LOO').sum()
    print(f'Baribault-passing: {n_pass}/{len(df)}')
    "

    # 4. Execution log spot-check
    grep -E "^\| (CPU-hours|GPU-hours|Wall-clock)" models/bayesian/21_execution_log.md

    # 5. Confirm pipeline terminus — last stage-06 manuscript_tables JobID reached COMPLETED
    LAST_JID=$(tail -1 models/bayesian/21_submission_jobids.txt | grep -oE "[0-9]+$")
    sacct -j ${LAST_JID} --format=JobID,JobName,State,ExitCode,Elapsed -n
    ```

    Then compose `.planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md`:

    Frontmatter:
    - phase: 24-cold-start-pipeline-execution
    - plan: 02
    - subsystem: infra
    - tags: [slurm, monash-m3, audit, ccds-canonical, convergence-reporting, winner-determination, baribault-collins-2023, yao-stacking]
    - requires: [{phase: 24-00, provides: CCDS path contract + pinned PATH_CONSTANTS}, {phase: 24-01, provides: canary-approved submission + JobID inventory}]
    - provides: [audit script (full impl), execution log, EXEC-04 observed outcome, Phase 25 EXEC-04 reconciliation flag]
    - affects: [25-01-PLAN.md (consumes baseline NetCDFs for reproducibility regression; reconciles EXEC-04 text to observed outcome), 26-* (consumes manuscript tables + winner identity)]

    One-liner: "Cold-start audit PASSED file/integrity checks; {N_pass}/6 models met Baribault gate; winner-determination label: {OBSERVED_LABEL} (winners: {NAMES}); {CPU_HOURS} CPU-hours + {GPU_HOURS} GPU-hours; {WALL_CLOCK} wall-clock."

    Required body sections:
    - **Observed empirical findings (reporting, not gating):**
      - Convergence: `{N_pass}/6` models passed Baribault & Collins (2023) gate (R-hat ≤ 1.05 AND ESS_bulk ≥ 400 AND divergences = 0 AND BFMI ≥ 0.2). Per-model pipeline_action mapping: {list}.
      - Winner-determination label: `{DOMINANT_SINGLE | TOP_TWO | TOP_K | FORCED-as-INCONCLUSIVE}` (raw winner_type = {...}; treated per CONTEXT.md Area 3 reporting policy).
      - Winners (from winners.txt): {comma-separated display names}.
      - Stacking pool membership (Baribault-passing only, per CONTEXT.md Area 3 ELPD-validity rule): {list}.
      - Top-3 stacking weights: {...}.
      - Top-3 RFX-BMS PXP: {...}.

    - **EXEC-04 reconciliation flag (for Phase 25):** The aspirational EXEC-04 text reads "≥ 2 models meeting gate; winner ∈ {DOMINANT_SINGLE, TOP_TWO}." The observed outcome is `{observed_label}` with `{N_pass}` models passing. If observed ≠ aspiration, Phase 25 MUST rewrite EXEC-04 text to match reality (per CONTEXT.md: "Phase 25 will rewrite the EXEC-04 text at audit time to match reality — EXEC-04 is an aspiration, not a pass/fail gate"). Flag explicitly whether reconciliation is needed.

    - **EXEC-01 satisfaction:** Pre-flight pytest gate green (Wave 0 log + Wave 1 Task 1 log); canary approved (Wave 1 Task 3 four-criteria gate); zero SLURM job failures across full chain (cite sacct State column — all COMPLETED, no FAILED/CANCELLED; resource-adjustment events, if any, noted in execution log Operator Notes section).

    - **EXEC-02 satisfaction:** All artifacts present per `tests/scientific/check_phase24_artifacts.py` exit 0 (cite Totals line). Strict CCDS-only — `check_no_legacy_paths` passed (no output/bayesian/... or figures/21_bayesian/ pollution).

    - **EXEC-03 satisfaction:** `models/bayesian/21_execution_log.md` exists with per-step sacct accounting + aggregate CPU-hours + GPU-hours. Single entry per step (no retry-chain bookkeeping per CONTEXT.md Area 2).

    - **EXEC-04 satisfaction:** {ASPIRATIONAL met OR reconciliation required — name which}.
      - If aspirational met: `{N_pass} ≥ 2` AND winner label ∈ {DOMINANT_SINGLE, TOP_TWO}. Full satisfaction.
      - If reconciliation required: Flag for Phase 25. Document observed outcome honestly.

    - **Files Created/Modified:** `tests/scientific/check_phase24_artifacts.py` (full impl, ~350 lines), `models/bayesian/21_execution_log.md` (~60+ lines), `.planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md` (this file).

    - **Decisions Made:** If resource-allocation adjustments occurred during the full chain, document them here (one-liner per adjustment, same format as Operator Notes in execution log). If INCONCLUSIVE_MULTIPLE or FORCED was the outcome, note that Phase 26 manuscript finalization handles the multi-winner path (per CONTEXT.md Deferred: "Forest plot regeneration for multi-winner sets — Phase 26 territory").

    - **Issues Encountered:** Document sacct oddities, autopush merge conflicts, or any step that required manual recovery. If none, state explicitly "None — clean run."

    - **Next Phase Readiness:** Phase 24 is VERIFICATION-ready. Phase 25 (reproducibility regression) can begin — needs baseline NetCDFs (present per audit) + prior_predictive .nc files (present per canary). Phase 25 MUST reconcile EXEC-04 text if reconciliation flag is raised above.

    Operator confirms by typing `verified` once:
    - Audit script exits 0.
    - Execution log spot-check matches expectations (wall-clock + CPU/GPU-hours plausible).
    - SUMMARY documents the actual observed winner identity + EXEC-04 reconciliation flag.
    - File committed (commit format: `docs(24-02): wave 2 audit + execution log + exec-04 reporting`).
  </how-to-verify>
  <resume-signal>
    Type "verified" once the three conditions above are met. Or describe issues (e.g., `check_no_legacy_paths` FAILED — canary wrote to a legacy path despite Wave 1 Task 3 gate; needs diagnostic). CONTEXT.md Area 3 says reality is always accepted for convergence + winner outcomes — do NOT escalate an INCONCLUSIVE_MULTIPLE or < 2 passing outcome; just flag EXEC-04 for Phase 25 reconciliation and proceed.
  </resume-signal>
</task>

</tasks>

<verification>
Wave 2 satisfies EXEC-01, EXEC-02, EXEC-03 directly. EXEC-04 satisfaction is CONDITIONAL on observed outcome:

1. **EXEC-01 (zero SLURM failures + pre-flight gate):** Verified by sacct query confirming all JobIDs reached State=COMPLETED with ExitCode=0:0 (resource-adjustment events collapsed into single successful JID per CONTEXT.md Area 2). Pre-flight gate passing confirmed by Wave 0 + Wave 1 logs.

2. **EXEC-02 (all artifacts present):** Verified by `python tests/scientific/check_phase24_artifacts.py` exit 0. Strict CCDS-only via `check_no_legacy_paths`.

3. **EXEC-03 (per-step accounting):** Verified by `models/bayesian/21_execution_log.md` with per-step table + aggregate budget. GPU-hours + CPU-hours both non-zero.

4. **EXEC-04 (convergence + winner):** Observed outcome reported by audit script. If observed matches aspirational text → EXEC-04 satisfied. If not → EXEC-04 RECONCILIATION flag raised for Phase 25 (per CONTEXT.md: EXEC-04 is aspirational, not pass/fail).

End state: `python tests/scientific/check_phase24_artifacts.py; echo $?` returns 0; `cat models/bayesian/21_execution_log.md` shows complete table + aggregate; 24-02-SUMMARY.md honestly reports the observed outcome with EXEC-04 reconciliation flag (if needed) for Phase 25.
</verification>

<success_criteria>
- `tests/scientific/check_phase24_artifacts.py` fully implemented (~350 lines), replaces Wave 0 stub, mirrors `check_v4_closure.py` pattern, uses ONLY the PATH_CONSTANTS module-level imports (no string-literal paths), deterministic across double-runs, exit 0 against cold-start artifact set.
- Convergence + winner-determination reported as CheckResult.detail (not asserted) — `check_convergence_reporting` and `check_winner_determination_reporting` always ok=True on valid data.
- Strict CCDS-only: `check_no_legacy_paths` asserts legacy dirs are empty.
- `models/bayesian/21_execution_log.md` exists with per-step sacct accounting + aggregate CPU-hours + GPU-hours. Single entry per step; resource-adjustment events as one-line operator notes; no retry-chain table.
- `.planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md` honestly reports observed outcome (winner identity, convergence-passing count, stacking pool membership) + EXEC-04 reconciliation flag for Phase 25.
- All three files committed (commit format per global CLAUDE.md: no Co-Authored-By line).
- Operator confirmed audit exit 0 + execution log spot-check + EXEC-04 reconciliation decision before signing off.
</success_criteria>

<output>
After completion, create `.planning/phases/24-cold-start-pipeline-execution/24-02-SUMMARY.md` per Task 3.
</output>
</content>
</invoke>
---
phase: 23.1-gpu-pipeline-integration
plan: 02
executed: 2026-04-23
status: pass
---

# Plan 23.1-02 Execution Summary — GPU SLURM Upgrades for Phase-29-Canonical Layout

Executed the rewritten Plan 23.1-02 (2026-04-23 retarget from deprecated `cluster/21_*.slurm` paths to Phase-29-canonical `cluster/{03,04b,04c}*.slurm`). Three tasks, three touched files, all verifications green.

## Tasks Completed

| Task | File | Action | Verification |
|------|------|--------|--------------|
| 1 | `cluster/04b_bayesian_gpu.slurm` | Upgraded single-GPU → Template C (4 GPUs + pmap) | bash -n pass; 16/16 grep checks |
| 2 | `cluster/04c_level2_gpu.slurm` | Created new Template C GPU sibling of CPU-only `04c_level2.slurm` | bash -n pass; 16/16 grep checks |
| 3 | `cluster/03_prefitting_gpu.slurm` | Added `--constraint` pin + per-STEP+per-MODEL cache parity with CPU sibling | bash -n pass; 10/10 grep checks |

## File-by-File Diff Summary

### Task 1: `cluster/04b_bayesian_gpu.slurm` (243 → 251 lines)

| Aspect | Before (pre-23.1-02) | After |
|--------|----------------------|-------|
| GRES | `--gres=gpu:1` | `--gres=gpu:4` (Template C) |
| Constraint | (none) | `--constraint=${GPU_TYPE:-l40s}` |
| Mem | 32G | 64G |
| CPUs | 4 | 8 |
| Job name | `rlwm-bayesian-gpu` | `rlwm-bayesian-gpu-mgpu` |
| Log paths | `cluster/logs/bayesian_gpu_%j.{out,err}` | `cluster/logs/04b_bayesian_gpu_%j.{out,err}` |
| JAX cache | `.jax_cache_gpu` (shared, cross-contamination risk) | `.jax_cache_04b_bayesian_gpu_${MODEL}` (per-model, `_gpu`-suffixed) |
| NUMPYRO_HOST_DEVICE_COUNT | 1 (hardcoded) | `$CHAINS` (matches device count for pmap) |
| GPU sanity print | simple `jax.devices()` | CHAINS-aware warning if `n_gpu < CHAINS` (mirrors 13_bayesian_multigpu.slurm) |
| `--use-gpu` flag | present | **dropped** (pmap selection is automatic via `numpyro_models._select_chain_method`) |
| Python invocation | `eval $CMD` | `CMD=(...)` bash array (mirrors CPU sibling, cleaner) |
| LIMIT support | via `--limit $LIMIT` | via `CMD+=(--limit "$LIMIT")` — preserved |
| SUBSCALE support | (absent) | via `CMD+=(--subscale)` when `$SUBSCALE = 1` — NEW |
| Post-fit audit | absent | mirrors CPU sibling `[OK]/[MISSING]` block for 4 expected outputs |
| nvidia-smi monitor | present | **preserved** (useful operator signal during long M6b subscale runs) |
| Resource report | present | **preserved** |

### Task 2: `cluster/04c_level2_gpu.slurm` (NEW, 217 lines)

Created as Template C mirror of `cluster/04c_level2.slurm`. Key differences from the CPU source:

| Aspect | CPU sibling | New GPU variant |
|--------|-------------|-----------------|
| Partition | `comp` | `gpu` |
| GRES | (none) | `--gres=gpu:4` (Template C) |
| Constraint | (none) | `--constraint=${GPU_TYPE:-l40s}` |
| CPUs | 4 | 8 |
| Time | 12h | 8h (GPU expected ~3-4h on M6b subscale; 8h absorbs auto-bump) |
| Conda env | `ds_env` → rlwm_gpu fallback | `rlwm_gpu` ONLY (no fallback) |
| GPU sanity print | absent | CHAINS-aware |
| Cache | `.jax_cache_21_l2_${MODEL}` (CPU) | `.jax_cache_04c_level2_gpu_${MODEL}` (GPU, `_gpu`-suffixed) |
| `JAX_PLATFORMS=cpu` | present | **removed** |
| `PYTHONUNBUFFERED=1` | absent | **added** |
| Python invocation | `scripts/04_model_fitting/c_level2/fit_with_l2.py` | **UNCHANGED** (same entry point) |
| WINNERS_FILE contract | present | **preserved verbatim** |
| Expected-artifacts audit | present | **mirrored** (M1/M2 `[MISSING]`-by-design preserved) |
| `exit $EXIT_CODE` | present | **preserved** (load-bearing for afterok chain) |

### Task 3: `cluster/03_prefitting_gpu.slurm` (147 → 148 lines, 2 surgical Edits)

| Edit | Before | After |
|------|--------|-------|
| 1 (line 32) | (no constraint directive) | `#SBATCH --constraint=${GPU_TYPE:-l40s}` inserted after `--partition=gpu` |
| 2 (line 87) | `.jax_cache_03_prefit_gpu` (shared across all STEP×MODEL) | `.jax_cache_03_prefit_gpu_${STEP}_${MODEL}` (per-STEP+per-MODEL parity with CPU sibling) |

All other logic (STEP dispatch case block, rlwm_gpu activation ladder, JAX device print, `PYTHONUNBUFFERED=1`, autopush, exit code propagation) byte-unchanged.

## CLUSTER_GPU_LESSONS.md Must-Have Encoding (all 3 files)

| Must-have | §Ref | 04b_bayesian_gpu | 04c_level2_gpu | 03_prefitting_gpu |
|-----------|------|:---:|:---:|:---:|
| PYTHONUNBUFFERED=1 | §6 | ✅ | ✅ | ✅ (preserved) |
| Per-model/per-STEP `_gpu`-suffixed cache | §5 | ✅ | ✅ | ✅ (parity fix) |
| rlwm_gpu only (no ds_env fallback) | §5 | ✅ | ✅ | ✅ (preserved) |
| `--constraint=${GPU_TYPE:-l40s}` pin | §5 | ✅ | ✅ | ✅ (added) |
| NO `JAX_PLATFORMS=cpu` | §2 | ✅ | ✅ | ✅ (preserved) |
| NO `lba_likelihood` module-level import | §5 | ✅ | ✅ | ✅ (preserved) |
| `source cluster/autopush.sh` present | §6 | ✅ | ✅ | ✅ (preserved) |
| NUMPYRO_HOST_DEVICE_COUNT matches $CHAINS | §2 | ✅ | ✅ | N/A (single-GPU) |

## CPU Siblings Byte-Unchanged

Verified via `git diff cluster/{03_prefitting_cpu,04b_bayesian_cpu,04c_level2}.slurm` returning empty. USE_CPU=1 escape hatch remains valid for 23.1-03 to wire.

## Deviations from Plan

**None.** All three tasks executed as specified. Two notes for future readers:

1. **04b_bayesian_gpu.slurm Task 1 step 7 (nvidia-smi monitor):** The plan gave the option to drop the monitor block to simplify the command-array invocation. Kept the block — useful operational visibility during M6b subscale runs, zero cost when nvidia-smi is absent (guarded by `command -v`).

2. **`${GPU_TYPE:-l40s}` in `#SBATCH` directives:** SLURM on Monash M3 is expected to accept shell-style parameter expansion in `#SBATCH` directives. If any submission fails with directive parse errors at runtime, 29-08-style fallback is to hard-code `l40s` and add a comment noting that future arch overrides require editing the line.

## Wave 3 Integration-Smoke Handoff Notes (for plan 23.1-04)

After 23.1-03 ships, the master orchestrator `cluster/submit_all.sh` will have USE_CPU dispatch wired for three pair sets:

| Stage | CPU | GPU (default) |
|-------|-----|---------------|
| 03 prefitting | `03_prefitting_cpu.slurm` | `03_prefitting_gpu.slurm` (single-GPU; cache now per-STEP+per-MODEL) |
| 04b Bayesian baseline | `04b_bayesian_cpu.slurm` | `04b_bayesian_gpu.slurm` (Template C, 4 GPUs + pmap) |
| 04c winner L2 refit | `04c_level2.slurm` | `04c_level2_gpu.slurm` (Template C, 4 GPUs + pmap) |

Integration smoke (23.1-04) must verify:
- `bash cluster/submit_all.sh --dry-run` exits 0 (default GPU path)
- `USE_CPU=1 bash cluster/submit_all.sh --dry-run` exits 0 (CPU fallback path)
- Live small-N smoke run (if GPU queue available) lands all artifacts at Phase-21 contract paths (`output/bayesian/21_prior_predictive/`, `21_recovery/`, `21_baseline/`, `21_l2/`)
- `--from-stage 4` + `USE_CPU=1` interaction works (combinatoric surface)

## Status

- `bash -n` passes on all 3 touched SLURMs
- All grep invariant checks pass (42/42 across the 3 files)
- CPU siblings byte-unchanged
- No Python files modified
- No orchestrator changes (23.1-03 scope)

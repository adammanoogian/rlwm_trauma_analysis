---
phase: 23.1-gpu-pipeline-integration
plan: 03
type: execute
wave: 2
depends_on: ["23.1-01-PLAN.md", "23.1-02-PLAN.md"]
files_modified:
  - cluster/submit_all.sh
  - cluster/21_dispatch_l2_winners.sh
  - .planning/ROADMAP.md
autonomous: true
rewritten: 2026-04-23
rewrite_reason: |
  Original plan (authored 2026-04-19) targeted `cluster/21_submit_pipeline.sh`
  as the master orchestrator. Phase 29-05 (2026-04-22) promoted
  `cluster/submit_all.sh` to that role (21_submit_pipeline.sh is now a 37-line
  shim that delegates to submit_all.sh). This rewrite retargets the USE_CPU
  dispatch block to submit_all.sh, updates the 3 script-pair routing to the
  Phase-29 canonical layout (03_prefitting_{cpu,gpu}, 04b_bayesian_{cpu,gpu},
  04c_level2{,_gpu}), and routes the L2 winner dispatcher through the same flag.

must_haves:
  truths:
    - "Default invocation `bash cluster/submit_all.sh` (no env vars) dispatches the GPU SLURM variants for stages 03 (prefitting), 04b (Bayesian baseline), and 04c (winner L2 refit)"
    - "USE_CPU=1 escape hatch: `USE_CPU=1 bash cluster/submit_all.sh` dispatches the original CPU SLURM scripts (03_prefitting_cpu.slurm, 04b_bayesian_cpu.slurm, 04c_level2.slurm) unchanged from their Phase-29 baseline"
    - "All existing submit_all.sh afterok chains preserved exactly — every `--dependency=afterok:...` argument byte-unchanged; every stage (01 → 02 → 03 → 04b → 05 → 06) still chains correctly"
    - "Dry-run mode preserved: `bash cluster/submit_all.sh --dry-run` still returns 0 for a path-valid chain under both default (GPU) and USE_CPU=1 modes; returns 1 if any resolved script path is missing"
    - "The `--from-stage` and `--models` CLI flags are preserved and interoperate correctly with USE_CPU (e.g., `USE_CPU=1 bash cluster/submit_all.sh --from-stage 4 --models wmrl_m3` dispatches CPU 04b_bayesian_cpu.slurm for wmrl_m3 only)"
    - "CRLF auto-strip line (`sed -i 's/\\r$//' cluster/*.slurm cluster/*.sh` on line 65 of current submit_all.sh) preserved — picks up new `04c_level2_gpu.slurm` automatically via glob"
    - "cluster/21_dispatch_l2_winners.sh honors L2_FIT_SCRIPT env var (propagated from submit_all.sh via `--export=ALL,L2_FIT_SCRIPT=...` on the 21_6_dispatch_l2.slurm sbatch call); CPU script remains safe fallback via `${L2_FIT_SCRIPT:-cluster/04c_level2.slurm}`"
    - "The deprecated `cluster/21_submit_pipeline.sh` shim stays unchanged — it already `exec`s `submit_all.sh`, so updating the master automatically updates both entry points"
    - "ROADMAP.md Phase 23.1 entry: SLURM count corrected from original 6-file list (21_1_*, 21_2_*, 21_3_*, 21_5_*, 21_6_dispatch_*, 21_6_fit_with_l2_*) to the actual Phase-29-canonical pair list (03_prefitting_{cpu,gpu}, 04b_bayesian_{cpu,gpu}, 04c_level2{,_gpu}); Plans block populated with the 4 actual plan filenames"
  artifacts:
    - path: "cluster/submit_all.sh"
      provides: "Master orchestrator that defaults to GPU dispatch with USE_CPU=1 backwards-compat escape hatch"
      contains: "USE_CPU"
    - path: "cluster/21_dispatch_l2_winners.sh"
      provides: "L2 winner dispatcher that routes to L2_FIT_SCRIPT (env-overridable; CPU safe default)"
      contains: "L2_FIT_SCRIPT"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 23.1 entry with finalized Plans block (4 plans, not TBD) + corrected success-criterion SLURM names"
      contains: "23.1-01-PLAN.md"
  key_links:
    - from: "cluster/submit_all.sh"
      to: "cluster/03_prefitting_{cpu,gpu}.slurm + 04b_bayesian_{cpu,gpu}.slurm + 04c_level2{,_gpu}.slurm"
      via: "Top-of-file USE_CPU resolution block sets PREFIT_SCRIPT / BAYES_SCRIPT / L2_SCRIPT variables; sbatch calls reference the variables"
      pattern: "PREFIT_SCRIPT|BAYES_SCRIPT|L2_SCRIPT"
    - from: "cluster/submit_all.sh"
      to: "cluster/21_6_dispatch_l2.slurm (invoked for L2 stage)"
      via: "sbatch --export=ALL,L2_FIT_SCRIPT=$L2_SCRIPT cluster/21_6_dispatch_l2.slurm"
      pattern: "L2_FIT_SCRIPT"
    - from: "cluster/21_dispatch_l2_winners.sh"
      to: "cluster/04c_level2.slurm OR cluster/04c_level2_gpu.slurm"
      via: "sbatch --wait --export=ALL,MODEL=$model \"${L2_FIT_SCRIPT:-cluster/04c_level2.slurm}\" &"
      pattern: "\\$\\{L2_FIT_SCRIPT"
---

<objective>
Update `cluster/submit_all.sh` to dispatch the Phase-23.1 GPU SLURM variants by default while preserving a backwards-compatible `USE_CPU=1` escape hatch that routes to the original CPU scripts. All existing afterok chains, the `--dry-run` / `--from-stage` / `--models` CLI surface, and the CRLF auto-strip line MUST be preserved verbatim. Route the `USE_CPU` choice through to the L2 winner dispatcher via the `L2_FIT_SCRIPT` env var.

Purpose: Phase 24 cold-start runs `bash cluster/submit_all.sh` with no args — Phase 24 plans assume this entry point. After this plan ships, that command launches the Phase-23.1 GPU pipeline (Template C 4-GPU pmap for 04b baseline + 04c L2 refit; single-GPU 03 prefitting) — unlocking the 3-4× wall-clock speedup per the M6b proof + audit. The CPU path remains the documented escape hatch for: regression debugging, rlwm_gpu environment issues, and GPU queue saturation.

Stages that DON'T change scripts based on USE_CPU (they don't run NUTS MCMC):
- Stage 01 (01_data_processing.slurm) — CSV parsing, pure pandas
- Stage 02 (02_behav_analyses.slurm) — pure matplotlib/pandas/statsmodels
- Stage 05 (05_post_checks.slurm) — pure ArviZ / NumPy
- Stage 06 (06_fit_analyses.slurm) — pure NumPy/SciPy/ArviZ (LOO + stacking + averaging + tables)

Stages 04a (MLE) is left untouched by this plan — MLE already has CPU and GPU siblings wired, but this plan's scope is Phase-21 Bayesian cold-start, not MLE. A follow-up plan can extend USE_CPU to 04a if needed.

Output: One updated master orchestrator + one one-line patch to the L2 winner dispatcher + one ROADMAP.md finalization edit.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23.1-gpu-pipeline-integration/23.1-RESEARCH.md
@cluster/submit_all.sh
@cluster/21_submit_pipeline.sh
@cluster/21_6_dispatch_l2.slurm
@cluster/21_dispatch_l2_winners.sh
@cluster/03_prefitting_cpu.slurm
@cluster/03_prefitting_gpu.slurm
@cluster/04b_bayesian_cpu.slurm
@cluster/04b_bayesian_gpu.slurm
@cluster/04c_level2.slurm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add USE_CPU dispatch resolution to cluster/submit_all.sh + route variables through stage 03 + stage 04b + L2 dispatch sbatch calls</name>
  <files>cluster/submit_all.sh</files>
  <action>
Edit `cluster/submit_all.sh` to resolve which SLURM scripts to use based on a top-of-file `USE_CPU` env var, then plug those resolved variables into the existing sbatch calls. ALL other logic (CLI flag parsing, dry-run harness, afterok chains, CRLF strip, echo-summary block) MUST remain byte-identical.

Per RESEARCH §9 Q3 (original-plan decision, still valid): single top-of-file check is cleaner than per-step checks.

**EXACT changes (use Edit tool with surgical precision; do NOT rewrite the file):**

**Edit 1: Add the USE_CPU dispatch resolution block.** Find this exact line and insert the new block AFTER it:

Find (line 60):
```
echo "============================================================"
```
(the closing `============================================================` of the startup summary echo, just after `echo "  models:  ${MODELS}"`)

Insert AFTER that line:
```bash

# =============================================================================
# CPU vs GPU dispatch resolution (Phase 23.1)
# =============================================================================
# Default: dispatch the GPU variants of the 3 NUTS-fitting stage scripts:
#   Stage 03 prefitting:    03_prefitting_gpu.slurm   (single-GPU)
#   Stage 04b Bayesian fit: 04b_bayesian_gpu.slurm    (Template C — 4 GPUs + pmap)
#   Stage 04c L2 refit:     04c_level2_gpu.slurm      (Template C — 4 GPUs + pmap)
#
# Escape hatch: `USE_CPU=1 bash cluster/submit_all.sh` reverts those 3 stages
# to their CPU siblings. Stages 01, 02, 05, 06 are CPU-only regardless
# (no NUTS MCMC — pure pandas / ArviZ / NumPy / SciPy / matplotlib).
#
# Per CLUSTER_GPU_LESSONS.md §6 + the M6b proof at job 54894258, the GPU path
# delivers ~3-4x wall-clock speedup at production scale. Phase 24 cold-start
# runs the default (GPU) path.
if [[ "${USE_CPU:-0}" == "1" ]]; then
    echo "[$(date)] USE_CPU=1 detected — dispatching CPU SLURM variants"
    PREFIT_SCRIPT="cluster/03_prefitting_cpu.slurm"
    BAYES_SCRIPT="cluster/04b_bayesian_cpu.slurm"
    L2_SCRIPT="cluster/04c_level2.slurm"
    DISPATCH_MODE="CPU (USE_CPU=1)"
else
    echo "[$(date)] Default GPU dispatch — Phase 23.1 multi-GPU pipeline"
    PREFIT_SCRIPT="cluster/03_prefitting_gpu.slurm"
    BAYES_SCRIPT="cluster/04b_bayesian_gpu.slurm"
    L2_SCRIPT="cluster/04c_level2_gpu.slurm"
    DISPATCH_MODE="GPU (Phase 23.1 default)"
fi
echo "  03 prefitting:  $PREFIT_SCRIPT"
echo "  04b Bayesian:   $BAYES_SCRIPT"
echo "  04c L2 refit:   $L2_SCRIPT"
echo ""
```

**Edit 2: Replace the hardcoded stage-03 script path with the variable.** Find these exact lines (stage 03 fan-out block, roughly lines 148-156):

Find (within stage-03 fan-out for loop):
```
    PRIOR_JOBS[$m]=$(submit cluster/03_prefitting_cpu.slurm "${DEP_STAGE03[@]}" \
                      --export=ALL,STEP=prior_predictive,MODEL="$m")
```
Replace the script path with `"$PREFIT_SCRIPT"`:
```
    PRIOR_JOBS[$m]=$(submit "$PREFIT_SCRIPT" "${DEP_STAGE03[@]}" \
                      --export=ALL,STEP=prior_predictive,MODEL="$m")
```

Find (within stage-03 fan-out for loop, the recovery submission):
```
    REC_JOBS[$m]=$(submit cluster/03_prefitting_cpu.slurm \
                    --dependency=afterok:"${PRIOR_JOBS[$m]}" \
                    --export=ALL,STEP=bayesian_recovery,MODEL="$m")
```
Replace:
```
    REC_JOBS[$m]=$(submit "$PREFIT_SCRIPT" \
                    --dependency=afterok:"${PRIOR_JOBS[$m]}" \
                    --export=ALL,STEP=bayesian_recovery,MODEL="$m")
```

**Edit 3: Replace the hardcoded stage-04b script path with the variable.** Find these exact lines (stage 04b fan-out, roughly lines 176-177):

Find:
```
    BAYES_JOBS[$m]=$(submit cluster/04b_bayesian_cpu.slurm "${DEP[@]}" "${TIME_OVERRIDE[@]}" \
                      --export=ALL,MODEL="$m")
```
Replace:
```
    BAYES_JOBS[$m]=$(submit "$BAYES_SCRIPT" "${DEP[@]}" "${TIME_OVERRIDE[@]}" \
                      --export=ALL,MODEL="$m")
```

**Edit 4: Add stage 04c L2-refit dispatch block.** The current submit_all.sh does NOT have a stage-04c block (the Phase-29-05 orchestrator stops after stage 04b and goes straight to 05). For the Phase-21-spec pipeline, the L2 winner refit belongs BETWEEN a LOO/stacking step (currently part of stage 06) and the remaining averaging/tables steps. However, the Phase-29-05 orchestrator collapses all of 06 into `06_fit_analyses.slurm` with 4 sub-STEPs (compare_models → loo_stacking → model_averaging → manuscript_tables) that do NOT include the L2 refit.

**Decision:** Rather than restructure stage 06, route the L2 refit through the existing `21_6_dispatch_l2.slurm` dispatcher job (Phase-21-spec intact). Insert a new stage-04c block AFTER the stage 04b fan-out loop ends and BEFORE the stage-05 block begins.

Find this exact block (end of stage 04b section, just before the `# Build colon-separated afterok dependency for stage 05` comment):
```
# Build colon-separated afterok dependency for stage 05
BAYES_DEP=""
if [[ ${#J04_ALL[@]} -gt 0 ]]; then
  BAYES_DEP="afterok:$(IFS=:; echo "${J04_ALL[*]}")"
fi
```

Insert a NEW block AFTER it (between that block and the `# Stage 05 — Post-fitting checks` comment):
```bash

# ---------------------------------------------------------------------------
# Stage 04c — L2 winner refit (runs after 04b; gated on winners.txt existing)
# ---------------------------------------------------------------------------
# The L2 refit is a fan-out per winner (M1/M2 copy-through, M3/M5/M6a 2-cov,
# M6b subscale). `cluster/21_6_dispatch_l2.slurm` wraps the dispatcher
# `cluster/21_dispatch_l2_winners.sh` which reads
# output/bayesian/21_baseline/winners.txt and submits one $L2_SCRIPT per
# winner via `sbatch --wait`. The --wait pattern + &+wait in the dispatcher
# ensures the SLURM job stays alive until every L2 fit completes.
#
# We pass L2_FIT_SCRIPT through so the dispatcher routes to $L2_SCRIPT
# (the GPU variant by default; CPU when USE_CPU=1).
#
# Note: winners.txt is produced by step loo_stacking in stage 06. In the
# Phase-29 orchestrator flow, stage 06 loo_stacking runs AFTER stage 05 and
# before the remaining compare/averaging/tables steps. This means stage 04c
# must run AFTER a partial stage 06 (loo_stacking specifically) produces
# winners.txt. To preserve chain integrity, the master orchestrator emits
# ONE `21_6_dispatch_l2.slurm` submission with a dependency on the LOO
# step inside stage 06, and downstream averaging/tables depend on the
# dispatcher. The chain is built at stage-06 time (see Stage 06 block).
L2_DISPATCH_JID=""
```
(This block is preparatory — it declares `L2_DISPATCH_JID` as a placeholder so stage 06 can submit the dispatcher once LOO is known. The actual sbatch call lives inside stage 06's per-step loop, see Edit 5.)

**Edit 5: Wire the L2 dispatcher submission inside the stage-06 per-step loop.** Find the stage-06 per-step for loop (roughly lines 223-233):
```
  PREV=""
  for step in compare_models loo_stacking model_averaging manuscript_tables; do
    LOCAL_DEP=("${DEP[@]}")
    if [[ -n "$PREV" ]]; then
      LOCAL_DEP=(--dependency=afterok:"$PREV")
    fi
    JID=$(submit cluster/06_fit_analyses.slurm "${LOCAL_DEP[@]}" \
           --export=ALL,STEP="$step")
    echo "  [06.$step] -> $JID"
    J06_ALL+=("$JID")
    PREV="$JID"
  done
```

Replace with (inserts L2 dispatch after loo_stacking; preserves step ordering and afterok chain):
```
  PREV=""
  for step in compare_models loo_stacking model_averaging manuscript_tables; do
    LOCAL_DEP=("${DEP[@]}")
    if [[ -n "$PREV" ]]; then
      LOCAL_DEP=(--dependency=afterok:"$PREV")
    fi
    JID=$(submit cluster/06_fit_analyses.slurm "${LOCAL_DEP[@]}" \
           --export=ALL,STEP="$step")
    echo "  [06.$step] -> $JID"
    J06_ALL+=("$JID")
    PREV="$JID"

    # After loo_stacking, insert L2 winner dispatcher (reads winners.txt
    # produced by loo_stacking, submits one $L2_SCRIPT per winner via
    # sbatch --wait inside cluster/21_dispatch_l2_winners.sh).
    if [[ "$step" == "loo_stacking" ]]; then
      L2_DISPATCH_JID=$(submit cluster/21_6_dispatch_l2.slurm \
        --dependency=afterok:"$PREV" \
        --export=ALL,L2_FIT_SCRIPT="$L2_SCRIPT")
      echo "  [04c.l2_dispatch] -> $L2_DISPATCH_JID (L2_FIT_SCRIPT=$L2_SCRIPT)"
      J06_ALL+=("$L2_DISPATCH_JID")
      PREV="$L2_DISPATCH_JID"   # model_averaging + manuscript_tables wait on L2 dispatcher
    fi
  done
```

**Edit 6: Update the final summary echo block.** Find (near end of file):
```
echo "============================================================"
echo "Stage 01: $J01"
echo "Stage 02: $J02"
echo "Stage 03: ${J03_ALL[*]}"
echo "Stage 04: ${J04_ALL[*]}"
echo "Stage 05: $J05_BASELINE $J05_SCALE"
echo "Stage 06: ${J06_ALL[*]}"
echo "============================================================"
```

Add the mode line and an L2 dispatch stage line. Replace with:
```
echo "============================================================"
echo "Mode:     $DISPATCH_MODE"
echo "Stage 01: $J01"
echo "Stage 02: $J02"
echo "Stage 03: ${J03_ALL[*]}"
echo "Stage 04: ${J04_ALL[*]}"
echo "Stage 04c L2 dispatch: ${L2_DISPATCH_JID:-<not dispatched; no winners.txt>}"
echo "Stage 05: $J05_BASELINE $J05_SCALE"
echo "Stage 06: ${J06_ALL[*]}"
echo "============================================================"
```

**MUST preserve verbatim** (any change breaks the orchestrator):
- The shebang `#!/usr/bin/env bash` + `set -euo pipefail` + `cd "$(dirname "$0")/.."`
- CLI flag parsing loop for `--dry-run`, `--from-stage`, `--models`, `-h|--help`
- `MODELS="qlearning wmrl wmrl_m3 wmrl_m5 wmrl_m6a wmrl_m6b"` default (line 40)
- The CRLF strip: `sed -i 's/\r$//' cluster/*.slurm cluster/*.sh 2>/dev/null || true` (line 65) — note this automatically picks up `04c_level2_gpu.slurm` via `*.slurm` glob
- The `submit()` function body (lines 73-110) — critical for --dry-run semantics
- Every existing `--dependency=afterok:...` argument
- Every `submit()` return contract (the function echoes JID to stdout; stage-blocks capture via `$(submit ...)`)
- The `--from-stage` skip gating on each stage block
- `DRY_FAKE_JID` counter behavior

**MUST NOT touch:**
- `cluster/21_submit_pipeline.sh` (already delegates to submit_all.sh via `exec` on line 36)
- Any of the cpu/gpu SLURM scripts themselves (those are 23.1-02's job)
- Any python file
  </action>
  <verify>
1. `bash -n cluster/submit_all.sh` exits 0 (syntax check).
2. `Grep -n "USE_CPU" cluster/submit_all.sh` returns 3+ hits (top-of-file dispatch block + summary echo).
3. `Grep -n "PREFIT_SCRIPT\\|BAYES_SCRIPT\\|L2_SCRIPT" cluster/submit_all.sh` returns hits inside BOTH the dispatch resolution block AND the sbatch-submit calls (stage 03, stage 04b, stage 06 L2 dispatch).
4. `Grep -n "_gpu.slurm" cluster/submit_all.sh` returns 3 hits (PREFIT_SCRIPT, BAYES_SCRIPT, L2_SCRIPT defaults in the GPU branch).
5. `Grep -c "afterok" cluster/submit_all.sh` — count should equal pre-plan count + 1 (new L2 dispatch afterok). Confirm via `git diff` that the only added `afterok` is the one inside the `if [[ "$step" == "loo_stacking" ]]` block.
6. `Grep -n "L2_FIT_SCRIPT=\"\\$L2_SCRIPT\"" cluster/submit_all.sh` returns a hit (env-var passed through to 21_6_dispatch_l2.slurm).
7. `Grep -n "sed -i 's/\\\\r\\$//' cluster/\\*.slurm" cluster/submit_all.sh` returns a hit (CRLF strip preserved).
8. CPU dispatch syntax: `USE_CPU=1 bash -n cluster/submit_all.sh` exits 0.
9. Default dispatch syntax: `bash -n cluster/submit_all.sh` exits 0.
10. Dry-run default (GPU): `bash cluster/submit_all.sh --dry-run 2>&1 | grep -E "^(DRY ok|DRY MISSING|Stage|Mode)"` — confirm all stage lines print `_gpu.slurm` variants and Mode line shows "GPU".
11. Dry-run CPU: `USE_CPU=1 bash cluster/submit_all.sh --dry-run 2>&1 | grep -E "^(DRY ok|DRY MISSING|Stage|Mode)"` — confirm all stage lines print `_cpu.slurm` variants (and `04c_level2.slurm` without `_gpu`) and Mode line shows "CPU (USE_CPU=1)".
12. Path existence: `[[ -f cluster/03_prefitting_cpu.slurm ]] && [[ -f cluster/03_prefitting_gpu.slurm ]] && [[ -f cluster/04b_bayesian_cpu.slurm ]] && [[ -f cluster/04b_bayesian_gpu.slurm ]] && [[ -f cluster/04c_level2.slurm ]] && [[ -f cluster/04c_level2_gpu.slurm ]] && echo OK` returns `OK` (all 6 referenced SLURMs exist — 04b_gpu/04c_gpu produced by 23.1-02 Tasks 1-2).
13. `git diff cluster/03_prefitting_cpu.slurm cluster/04b_bayesian_cpu.slurm cluster/04c_level2.slurm cluster/21_submit_pipeline.sh` returns empty (no collateral edits).
  </verify>
  <done>
Orchestrator dispatches GPU variants by default; USE_CPU=1 reverts to CPU scripts for stages 03, 04b, 04c; all existing afterok chains preserved; dry-run semantics work under both modes; CRLF strip preserved; L2_FIT_SCRIPT env var propagated through dispatch job; submit_all.sh summary shows mode + L2 dispatch JID.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update cluster/21_dispatch_l2_winners.sh to honor L2_FIT_SCRIPT env var (env-overridable with CPU safe default)</name>
  <files>cluster/21_dispatch_l2_winners.sh</files>
  <action>
One-line surgical edit. Find line 63:
```
  sbatch --wait --export=ALL,MODEL=$model cluster/04c_level2.slurm &
```

Replace with:
```
  sbatch --wait --export=ALL,MODEL=$model "${L2_FIT_SCRIPT:-cluster/04c_level2.slurm}" &
```

Also update the header-block comment (lines 1-32) to document the new env-var dispatch. Find lines 5-7:
```
# Reads output/bayesian/21_baseline/winners.txt (comma-separated display
# names from step 21.5), maps display names to internal model ids, and
# submits one cluster/04c_level2.slurm per winner via
```

Replace with:
```
# Reads output/bayesian/21_baseline/winners.txt (comma-separated display
# names from step 21.5), maps display names to internal model ids, and
# submits one L2 fit SLURM per winner via
```

And add after line 32 (before `set -euo pipefail`):
```
# =============================================================================
# Env-var dispatch (Phase 23.1):
#   Submitter passes L2_FIT_SCRIPT via --export=ALL,L2_FIT_SCRIPT=... on
#   cluster/21_6_dispatch_l2.slurm. If unset, defaults to the CPU script
#   (cluster/04c_level2.slurm) as the safe fallback. GPU dispatch via
#   submit_all.sh sets L2_FIT_SCRIPT=cluster/04c_level2_gpu.slurm.
# =============================================================================
```

**MUST NOT modify:**
- Any other script
- The `sbatch --wait` + `&` + `wait` pattern — this is the canonical plan 21-10 spec
- The NAME_MAP mapping
- Exit code semantics (2 for missing winners.txt)
  </action>
  <verify>
1. `bash -n cluster/21_dispatch_l2_winners.sh` exits 0.
2. `Grep -n "L2_FIT_SCRIPT" cluster/21_dispatch_l2_winners.sh` returns 2+ hits (sbatch line + comment block).
3. `Grep -n "\\$\\{L2_FIT_SCRIPT:-cluster/04c_level2\\.slurm\\}" cluster/21_dispatch_l2_winners.sh` returns a hit (env-overridable with CPU safe default).
4. `Grep -n "^  sbatch --wait --export=ALL,MODEL=\\$model cluster/04c_level2\\.slurm &$" cluster/21_dispatch_l2_winners.sh` returns NO hits (old hardcoded path gone).
5. `Grep -n "NAME_MAP" cluster/21_dispatch_l2_winners.sh` returns 2 hits (mapping declaration + lookup) — unchanged.
6. `Grep -n "set -euo pipefail" cluster/21_dispatch_l2_winners.sh` returns exactly 1 hit — unchanged.
7. `Grep -n "wait$" cluster/21_dispatch_l2_winners.sh` returns exactly 1 hit (the `&`+`wait` pattern preserved).
  </verify>
  <done>
Dispatcher honors L2_FIT_SCRIPT env var; defaults to CPU script if unset (safe fallback); canonical `sbatch --wait &` + `wait` pattern preserved byte-for-byte; header comment documents the new env var.
  </done>
</task>

<task type="auto">
  <name>Task 3: Finalize ROADMAP.md Phase 23.1 entry — update success criterion GPU-INT-02 + GPU-INT-03 + replace TBD Plans block</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Three surgical Edit operations on `.planning/ROADMAP.md`. DO NOT rewrite the whole section.

**Edit A: Update GPU-INT-02 (line 380) — replace the old 6-file `21_*_gpu.slurm` list with the Phase-29-canonical pair list.**

Find this exact text:
```
  2. **GPU-INT-02 (SLURM script set):** GPU variants of all MCMC-fitting Phase 21 SLURM scripts exist: `21_1_prior_predictive_gpu.slurm`, `21_2_recovery_gpu.slurm`, `21_3_fit_baseline_gpu.slurm`, `21_5_loo_stacking_bms_gpu.slurm`, `21_6_dispatch_l2_gpu.slurm`, `21_6_fit_with_l2_gpu.slurm` — all use `--partition=gpu` + `--gres=gpu:4` + activate `rlwm_gpu` env + chain_method=parallel via pmap. CPU-only audit/aggregation/table steps (21_4, 21_7, 21_8, 21_9) stay unchanged.
```

Replace with:
```
  2. **GPU-INT-02 (SLURM script set):** GPU variants of all MCMC-fitting Phase-29-canonical stage SLURMs exist: `cluster/03_prefitting_gpu.slurm` (single-GPU; parameterized via STEP={prior_predictive,bayesian_recovery,...}), `cluster/04b_bayesian_gpu.slurm` (Template C — 4 GPUs + chain_method=parallel via pmap; parameterized via MODEL + SUBSCALE), `cluster/04c_level2_gpu.slurm` (Template C — 4 GPUs; winner L2 refit). All use `--partition=gpu` + activate `rlwm_gpu` env (no `ds_env` fallback) + `--constraint=${GPU_TYPE:-l40s}` for arch isolation. CPU-only stages (01 data_processing, 02 behav_analyses, 05 post_checks, 06 fit_analyses — pure pandas/ArviZ/NumPy) stay unchanged.
```

**Edit B: Update GPU-INT-03 (line 381) — reference `cluster/submit_all.sh` instead of the deprecated `21_submit_pipeline.sh` shim.**

Find this exact text:
```
  3. **GPU-INT-03 (orchestrator dispatch):** `cluster/21_submit_pipeline.sh` updated to dispatch the GPU variants by default with backwards-compat `USE_CPU=1` flag preserving the CPU path. All `--dependency=afterok` chains preserved. CRLF auto-strip preserved. Pre-flight pytest gate preserved. Original `21_*.slurm` CPU scripts kept intact (no breaking changes).
```

Replace with:
```
  3. **GPU-INT-03 (orchestrator dispatch):** `cluster/submit_all.sh` (the post-Phase-29-05 master orchestrator; `cluster/21_submit_pipeline.sh` is a shim that `exec`s it) updated to dispatch the GPU variants by default with backwards-compat `USE_CPU=1` flag preserving the CPU path. All `--dependency=afterok` chains preserved. CRLF auto-strip preserved. Pre-flight pytest gate (lives inside the 21_submit_pipeline.sh shim; runs BEFORE submit_all.sh is invoked) preserved. Original Phase-29-canonical CPU scripts (03_prefitting_cpu.slurm, 04b_bayesian_cpu.slurm, 04c_level2.slurm) kept intact (no breaking changes). L2 winner dispatch routes CPU-vs-GPU via `L2_FIT_SCRIPT` env var propagated to `cluster/21_dispatch_l2_winners.sh`.
```

**Edit C: Replace the TBD Plans block.**

Find this exact text (lines 386-393):
```
**Plans:** TBD (run `/gsd:plan-phase 23.1` to break down — likely 3-4 plans across 2-3 waves)

Plans:
- [ ] TBD — Wave 1: Multi-GPU smoke test for 5 unproven models (M1, M2, M3, M5, M6a) using `13_bayesian_multigpu.slurm` template at small N, with per-model timeout wrapper. M6b skipped (already validated).
- [ ] TBD — Wave 2: Build GPU variants of 6 MCMC-fitting `cluster/21_*_gpu.slurm` scripts (mirror `13_bayesian_multigpu.slurm` Template C pattern + Phase 21 output-path contract).
- [ ] TBD — Wave 2 (parallel): Update `cluster/21_submit_pipeline.sh` orchestrator to dispatch GPU variants with `USE_CPU=1` backwards-compat flag.
- [ ] TBD — Wave 3: Integration smoke test (run pipeline at small N end-to-end via the GPU orchestrator, verify all artifacts land at correct paths).
```

Replace with:
```
**Plans:** 4 plans (23.1-02 and 23.1-03 rewritten 2026-04-23 to target Phase-29-canonical layout after Phase 29-05 consolidated cluster/21_*.slurm into stage-numbered entry SLURMs)

Plans:
- [ ] 23.1-01-PLAN.md (Wave 1) — Multi-GPU smoke test for 5 unproven models (M1, M2, M3, M5, M6a) via `cluster/23.1_mgpu_smoke.slurm`; per-model timeout 600s wrapper; PASS/FAIL/TIMEOUT verdict per model. M6b skipped (already validated via job 54894258).
- [ ] 23.1-02-PLAN.md (Wave 2) — Upgrade `cluster/04b_bayesian_gpu.slurm` to Template C (4 GPUs + pmap), create `cluster/04c_level2_gpu.slurm` (new Template C sibling of 04c_level2.slurm), patch `cluster/03_prefitting_gpu.slurm` cache to per-STEP+per-MODEL parity with CPU sibling. CPU siblings byte-unchanged.
- [ ] 23.1-03-PLAN.md (Wave 2, parallel) — Add `USE_CPU=1` dispatch block to `cluster/submit_all.sh` routing between 03/04b/04c CPU ↔ GPU pairs; propagate `L2_FIT_SCRIPT` through `cluster/21_dispatch_l2_winners.sh`; finalize this ROADMAP Phase 23.1 entry.
- [ ] 23.1-04-PLAN.md (Wave 3) — End-to-end integration smoke: `bash cluster/submit_all.sh --dry-run` passes under BOTH default (GPU) and `USE_CPU=1` modes; small-N live smoke run verifies all artifacts land at Phase-21 contract paths; 7-invariant audit + trap-based task_trials_long.csv restore wrapper.
```

**DO NOT modify:**
- The Goal block (line 372)
- The Depends on block (line 374)
- The Requirements block (line 376)
- Success Criteria #1, #4, #5, #6, #7 — only #2 and #3 change (Edits A and B)
- The Out of scope block (lines 395-398)
- Any other phase entry (Phase 23, 24, 25, 26, 27, 28, 29 unchanged by this plan)
- The "Why inserted between 23 and 24" block (line 400)
  </action>
  <verify>
1. `Grep -n "Plans:\\*\\* 4 plans" .planning/ROADMAP.md` returns a hit within Phase 23.1 entry (was "Plans:** TBD").
2. `Grep -n "23.1-01-PLAN.md\\|23.1-02-PLAN.md\\|23.1-03-PLAN.md\\|23.1-04-PLAN.md" .planning/ROADMAP.md` returns exactly 4 hits (the 4 plan filenames).
3. `Grep -n "21_5_loo_stacking_bms_gpu.slurm\\|21_6_dispatch_l2_gpu.slurm\\|21_1_prior_predictive_gpu.slurm\\|21_3_fit_baseline_gpu.slurm" .planning/ROADMAP.md` returns NO hits within the Phase 23.1 entry (old script names purged from criterion #2).
4. `Grep -n "04b_bayesian_gpu.slurm\\|04c_level2_gpu.slurm\\|03_prefitting_gpu.slurm" .planning/ROADMAP.md` returns hits in the corrected criterion #2.
5. `Grep -n "cluster/submit_all.sh" .planning/ROADMAP.md` returns a hit in the corrected criterion #3 (replaced 21_submit_pipeline.sh reference).
6. `Grep -n "TBD" .planning/ROADMAP.md | head -20` — manually verify NO hits are between line 370 (Phase 23.1 heading) and line 400 (end of Out of scope block).
7. Visual check: `Read .planning/ROADMAP.md offset=370 limit=35` confirms Phase 23.1 entry now shows the 4-plan list with correct filenames, and criteria #2/#3 reference Phase-29-canonical paths.
  </verify>
  <done>
ROADMAP.md Phase 23.1 entry: criterion #2 now lists Phase-29-canonical GPU SLURM filenames; criterion #3 references submit_all.sh master (not the deprecated 21_submit_pipeline.sh shim); Plans block populated with the 4 actual plan filenames. No other phase entries touched.
  </done>
</task>

</tasks>

<verification>
- `cluster/submit_all.sh`: USE_CPU=1 escape hatch works; all existing afterok chains preserved; dry-run passes under both modes; CRLF strip preserved; new stage-04c L2 dispatch block correctly propagates L2_FIT_SCRIPT; summary block shows mode.
- `cluster/21_dispatch_l2_winners.sh`: one-line change + doc comment; honors L2_FIT_SCRIPT with CPU safe default.
- `.planning/ROADMAP.md`: Phase 23.1 entry shows 4 plans + Phase-29-canonical GPU SLURM names + submit_all.sh reference.
- `bash -n cluster/submit_all.sh` exits 0.
- `USE_CPU=1 bash -n cluster/submit_all.sh` exits 0.
- Original CPU SLURMs (`03_prefitting_cpu.slurm`, `04b_bayesian_cpu.slurm`, `04c_level2.slurm`) unchanged.
- The `21_submit_pipeline.sh` shim unchanged (it already delegates).
</verification>

<success_criteria>
- Default `bash cluster/submit_all.sh` will dispatch `03_prefitting_gpu.slurm` + `04b_bayesian_gpu.slurm` + `04c_level2_gpu.slurm`.
- `USE_CPU=1 bash cluster/submit_all.sh` will dispatch the original CPU scripts at those three stages.
- L2 winner dispatch flows through either 04c_level2.slurm (USE_CPU=1) or 04c_level2_gpu.slurm (default) automatically via L2_FIT_SCRIPT.
- All existing afterok chains + `--dry-run` / `--from-stage` / `--models` CLI surface + CRLF strip preserved.
- Phase 24 cold-start entry point (`bash cluster/submit_all.sh`, or its `21_submit_pipeline.sh` shim) unchanged from operator perspective — only underlying SLURM routing changes.
- ROADMAP.md Phase 23.1 entry finalized (no TBD; correct filenames).
</success_criteria>

<output>
After completion, create `.planning/phases/23.1-gpu-pipeline-integration/23.1-03-SUMMARY.md` with:
- Verbatim diff snippets of the 6 Edit operations on submit_all.sh (USE_CPU block + 4 sbatch path variable substitutions + 1 stage-04c L2 dispatch block + summary echo update)
- Confirmation that all existing `afterok` chains are preserved (count `--dependency=afterok` occurrences before vs after)
- USE_CPU=1 dry-run output (the echo block at the top showing CPU scripts get selected + the L2 dispatch JID line showing L2_FIT_SCRIPT=cluster/04c_level2.slurm)
- Default (GPU) dry-run output showing `_gpu.slurm` variants + L2_FIT_SCRIPT=cluster/04c_level2_gpu.slurm
- ROADMAP.md edit confirmation (criterion #2 + criterion #3 + Plans block diff snippets)
- Note for Wave 3 (23.1-04-PLAN.md): integration smoke must verify BOTH default dispatch (GPU) AND USE_CPU=1 dispatch (CPU) work — this is the contract this plan establishes
- Deviation note: if Task 1 Edit 4's stage-04c placeholder block position needs adjustment to satisfy submit_all.sh's stage-06 `--from-stage` semantics (currently places L2 dispatch logic inside stage-06 loop via Edit 5 — verify `--from-stage 4` still skips stage 06 correctly)
</output>

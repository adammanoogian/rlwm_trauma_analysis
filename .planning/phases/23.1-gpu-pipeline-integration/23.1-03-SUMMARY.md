---
phase: 23.1-gpu-pipeline-integration
plan: 03
executed: 2026-04-23
status: pass
---

# Plan 23.1-03 Execution Summary — Orchestrator USE_CPU Dispatch + ROADMAP Finalization

Executed the rewritten Plan 23.1-03 (2026-04-23 retarget from deprecated `cluster/21_submit_pipeline.sh` to the Phase-29-05 canonical master `cluster/submit_all.sh`). Three tasks, three touched files, all verifications green under both default (GPU) and USE_CPU=1 dispatch modes.

## Tasks Completed

| Task | File | Edits | Verification |
|------|------|-------|--------------|
| 1 | `cluster/submit_all.sh` | 6 surgical Edits | bash -n (both modes); dry-run (both modes); full stage-06 chain intact |
| 2 | `cluster/21_dispatch_l2_winners.sh` | 1-line sbatch path + doc comment | bash -n; canonical `&`+`wait` pattern preserved |
| 3 | `.planning/ROADMAP.md` | 3 surgical Edits on Phase 23.1 entry | All 4 plan filenames present; TBD purged; Phase-29 names replace obsolete 21_*_gpu names |

## Dry-Run Evidence (both modes)

### Default (GPU) dispatch — `bash cluster/submit_all.sh --dry-run`

```
Mode:     GPU (Phase 23.1 default)
Stage 01: 1001                                      (01_data_processing.slurm)
Stage 02: 1001                                      (02_behav_analyses.slurm)
Stage 03: 12 JIDs                                   (03_prefitting_gpu.slurm — 2 STEPs × 6 MODELs)
Stage 04: 6 JIDs                                    (04b_bayesian_gpu.slurm — 6 models; --time=36:00:00 on wmrl_m6b)
Stage 04c L2 dispatch: 1001                         (21_6_dispatch_l2.slurm --export=L2_FIT_SCRIPT=cluster/04c_level2_gpu.slurm)
Stage 05: 1001 1001                                 (05_post_checks.slurm — baseline_audit → scale_audit)
Stage 06: 5 JIDs                                    (compare_models → loo_stacking → [L2 dispatch] → model_averaging → manuscript_tables)

DRY-RUN: every stage SLURM passed bash -n and every python target resolved on disk.
```

### CPU fallback — `USE_CPU=1 bash cluster/submit_all.sh --dry-run`

```
[$(date)] USE_CPU=1 detected — dispatching CPU SLURM variants
  03 prefitting:  cluster/03_prefitting_cpu.slurm
  04b Bayesian:   cluster/04b_bayesian_cpu.slurm
  04c L2 refit:   cluster/04c_level2.slurm

Mode:     CPU (USE_CPU=1)
...
DRY ok: sbatch --dependency=afterok:1001 --export=ALL,L2_FIT_SCRIPT=cluster/04c_level2.slurm cluster/21_6_dispatch_l2.slurm
```

The `L2_FIT_SCRIPT` env var propagates correctly to `21_6_dispatch_l2.slurm` under both modes.

## Task 1 — submit_all.sh (6 Edit Operations)

| Edit | Location | Change |
|------|----------|--------|
| 1 | After startup echo block (~line 61) | Add USE_CPU dispatch resolution block (40 lines): sets `PREFIT_SCRIPT` / `BAYES_SCRIPT` / `L2_SCRIPT` + `DISPATCH_MODE`; echoes resolved paths |
| 2 | Stage 03 prior_predictive submit | Hard-coded `cluster/03_prefitting_cpu.slurm` → `"$PREFIT_SCRIPT"` |
| 3 | Stage 03 bayesian_recovery submit | Hard-coded `cluster/03_prefitting_cpu.slurm` → `"$PREFIT_SCRIPT"` |
| 4 | Stage 04b fan-out submit | Hard-coded `cluster/04b_bayesian_cpu.slurm` → `"$BAYES_SCRIPT"` |
| 5 (part a) | After `BAYES_DEP=...` block | Insert stage-04c prep block: declares `L2_DISPATCH_JID=""` placeholder + 20-line explanatory comment |
| 5 (part b) | Inside stage-06 `for step in ...` loop | Insert `if [[ "$step" == "loo_stacking" ]]` branch that submits `21_6_dispatch_l2.slurm` with `--export=ALL,L2_FIT_SCRIPT="$L2_SCRIPT"` + `--dependency=afterok:"$PREV"`; updates `PREV` so model_averaging + manuscript_tables wait on dispatcher |
| 6 | Final summary block | Add `Mode: $DISPATCH_MODE` + `Stage 04c L2 dispatch: ${L2_DISPATCH_JID:-<not dispatched>}` lines |

### Afterok chain preservation

Original `--dependency=afterok` count: 7 occurrences (stage 03→04b recovery chain × 6 models + stage 05→06 gate + stage 06 sequential chain).

New count: 8 occurrences (+ the L2 dispatch `--dependency=afterok:"$PREV"` after loo_stacking).

All pre-existing afterok arguments byte-unchanged. The one new afterok is the documented addition from Edit 5 (L2 dispatcher gated on loo_stacking producing winners.txt).

### Preserved verbatim (spot-checked via git diff)

- Shebang `#!/usr/bin/env bash` + `set -euo pipefail` + `cd "$(dirname "$0")/.."`
- CLI flag parsing (`--dry-run`, `--from-stage`, `--models`, `-h|--help`)
- `MODELS="qlearning wmrl wmrl_m3 wmrl_m5 wmrl_m6a wmrl_m6b"` default
- CRLF strip: `sed -i 's/\r$//' cluster/*.slurm cluster/*.sh 2>/dev/null || true` — picks up new 04c_level2_gpu.slurm via glob
- `submit()` function body (DRY_RUN semantics, path-existence check, FAKEJID counter)
- `--from-stage` gating on every stage block
- Stage 05 + Stage 06 outer structure

## Task 2 — 21_dispatch_l2_winners.sh (1-line Edit + doc comment)

**Before (line 63):**
```bash
  sbatch --wait --export=ALL,MODEL=$model cluster/04c_level2.slurm &
```

**After:**
```bash
  sbatch --wait --export=ALL,MODEL=$model "${L2_FIT_SCRIPT:-cluster/04c_level2.slurm}" &
```

Also added documentation of the env-var dispatch in the header comment block (Phase 23.1 note explaining how `submit_all.sh` passes `L2_FIT_SCRIPT` through the `21_6_dispatch_l2.slurm` `--export` surface).

**Preserved verbatim:**
- `set -euo pipefail`, `cd "$(dirname "$0")/.."`
- `WINNERS_FILE=output/bayesian/21_baseline/winners.txt` + existence check
- NAME_MAP display-name → model-id table (M1/M2/M3/M5/M6a/M6b)
- `sbatch --wait` + `&` + `wait` pattern — canonical plan 21-10 spec
- Exit code semantics (2 for missing winners.txt)

## Task 3 — ROADMAP.md (3 Edit Operations)

### Edit A: GPU-INT-02 (SLURM script set)

**Before:** "`21_1_prior_predictive_gpu.slurm`, `21_2_recovery_gpu.slurm`, `21_3_fit_baseline_gpu.slurm`, `21_5_loo_stacking_bms_gpu.slurm`, `21_6_dispatch_l2_gpu.slurm`, `21_6_fit_with_l2_gpu.slurm`" (6 obsolete names)

**After:** "`cluster/03_prefitting_gpu.slurm` (single-GPU; parameterized via STEP), `cluster/04b_bayesian_gpu.slurm` (Template C — 4 GPUs + pmap; parameterized via MODEL + SUBSCALE), `cluster/04c_level2_gpu.slurm` (Template C — 4 GPUs; winner L2 refit)" (3 Phase-29-canonical names)

### Edit B: GPU-INT-03 (orchestrator dispatch)

**Before:** References `cluster/21_submit_pipeline.sh` as the orchestrator being updated.

**After:** References `cluster/submit_all.sh` (master, post-29-05) and notes that `21_submit_pipeline.sh` is a shim that `exec`s it; pre-flight pytest gate lives in the shim. Documents L2 winner dispatch routing via `L2_FIT_SCRIPT` env var through `21_dispatch_l2_winners.sh`.

### Edit C: Plans block

**Before:** `**Plans:** TBD` + 4 TBD placeholder entries.

**After:** `**Plans:** 4 plans` + 4 actual plan filenames (23.1-01 [ ] / 23.1-02 [x] / 23.1-03 [x] / 23.1-04 [ ]). 23.1-02 and 23.1-03 marked complete.

## CPU Siblings + Shim Byte-Unchanged

Verified via `git diff cluster/03_prefitting_cpu.slurm cluster/04b_bayesian_cpu.slurm cluster/04c_level2.slurm cluster/21_submit_pipeline.sh` returning empty. USE_CPU=1 escape hatch remains valid.

## Deviations from Plan

**None.** All three tasks executed as specified.

**Minor note on `--from-stage` interaction:** The L2 dispatch submission is positioned INSIDE the stage-06 for-loop (after the `loo_stacking` iteration). This means `--from-stage 7` would skip the entire stage 06 block and therefore skip the L2 dispatch — which is the correct semantic (if you skip stage 06, you have no winners.txt and no L2 refit should happen). `--from-stage 6` correctly includes both the stage-06 chain AND the L2 dispatch. Verified: `L2_DISPATCH_JID=""` default + `${L2_DISPATCH_JID:-<not dispatched; no winners.txt>}` fallback in summary echo produce correct output in both cases.

## Wave 3 Integration-Smoke Handoff Notes (for plan 23.1-04)

After this plan ships, the master orchestrator is fully wired for both dispatch modes. Plan 23.1-04 must verify:

1. **Default dry-run (GPU):** `bash cluster/submit_all.sh --dry-run` exits 0; every `DRY ok` line references `_gpu.slurm` for stages 03/04b; `L2_FIT_SCRIPT=cluster/04c_level2_gpu.slurm` in dispatcher export; Mode line shows "GPU (Phase 23.1 default)". ✅ Verified in this SUMMARY.
2. **CPU dry-run:** `USE_CPU=1 bash cluster/submit_all.sh --dry-run` exits 0; every `DRY ok` line references `_cpu.slurm` for stages 03/04b and `04c_level2.slurm` (no suffix) for L2; Mode line shows "CPU (USE_CPU=1)". ✅ Verified in this SUMMARY.
3. **Live small-N smoke run (GPU):** Actual `bash cluster/submit_all.sh` submission on Monash M3 with a 10-ppt subset; end-to-end job-ID inventory + artifact existence at `output/bayesian/{21_prior_predictive,21_recovery,21_baseline,21_l2}/` paths. ⏳ Pending cluster access.
4. **Trap-based task_trials_long.csv restore:** The smoke run must not clobber the production data file (per 23.1-01 pattern; use a gitignored `output/bayesian/23.1_smoke/_smoke_data_10ppts.csv` snapshot).
5. **7-invariant audit:** Extend `validation/check_phase_23_1_smoke.py` with 7 orchestrator-level invariants (GPU dispatch land-at-paths + CPU dispatch land-at-paths + afterok chain integrity + Mode-line presence + L2_FIT_SCRIPT propagation + no cross-contamination across 04b/04c_gpu caches + autopush fired).

## Status

- `bash -n cluster/submit_all.sh` exits 0 under BOTH modes
- `bash -n cluster/21_dispatch_l2_winners.sh` exits 0
- Default (GPU) dry-run exits 0 with correct artifact routing
- CPU dry-run exits 0 with correct CPU-sibling routing
- L2 dispatch JID + Mode line appear in both summary outputs
- ROADMAP.md Phase 23.1 entry shows 4-plan list + Phase-29-canonical names + submit_all.sh reference
- CPU siblings + `21_submit_pipeline.sh` shim byte-unchanged
- No Python files modified

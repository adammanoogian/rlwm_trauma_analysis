---
phase: 23.1-gpu-pipeline-integration
plan: 02
type: execute
wave: 2
depends_on: ["23.1-01-PLAN.md"]
files_modified:
  - cluster/04b_bayesian_gpu.slurm
  - cluster/04c_level2_gpu.slurm
  - cluster/03_prefitting_gpu.slurm
autonomous: true
rewritten: 2026-04-23
rewrite_reason: |
  Original plan (authored 2026-04-19) targeted the per-step cluster/21_*.slurm
  layout. Phase 29-05 (2026-04-22) consolidated that into 6 stage-numbered
  entry SLURMs. Rewrite retargets every GPU improvement to the new canonical
  paths while preserving the full CLUSTER_GPU_LESSONS.md must-have set.

must_haves:
  truths:
    - "cluster/04b_bayesian_gpu.slurm is Template C (--gres=gpu:4 + chain_method=parallel via pmap) — the biggest per-model cold-start payload runs on 4 GPUs instead of the current silent single-GPU degrade to vectorized"
    - "cluster/04c_level2_gpu.slurm exists as Template C GPU sibling of cluster/04c_level2.slurm — winner refit with L2 (M6b subscale, 32 betas, worst case ~12h CPU) now has a GPU path"
    - "cluster/03_prefitting_gpu.slurm uses per-STEP + per-MODEL JAX cache (parity with CPU sibling line 84); single-GPU is preserved (correct for lighter MCMC steps)"
    - "Every touched GPU SLURM pins to --constraint=${GPU_TYPE:-l40s} (env-overridable) per CLUSTER_GPU_LESSONS.md §5 (cross-arch JIT cache contamination)"
    - "Every touched GPU SLURM exports PYTHONUNBUFFERED=1, JAX_PERSISTENT_CACHE_MIN_ENTRY_SIZE_BYTES=0, per-model/per-STEP _gpu-suffixed cache; activates rlwm_gpu with NO ds_env fallback"
    - "Lazy LBA float64 isolation preserved — no module-level `from rlwm.fitting.lba_likelihood import` in any touched GPU SLURM (grep invariant)"
    - "CPU siblings (04b_bayesian_cpu.slurm, 04c_level2.slurm, 03_prefitting_cpu.slurm) byte-unchanged — USE_CPU=1 escape hatch (wired in 23.1-03) remains valid"
  artifacts:
    - path: "cluster/04b_bayesian_gpu.slurm"
      provides: "Template C multi-GPU baseline fit — GPU variant of 04b_bayesian_cpu.slurm"
      contains: "--gres=gpu:4"
    - path: "cluster/04c_level2_gpu.slurm"
      provides: "Template C multi-GPU winner-with-L2 refit — GPU variant of 04c_level2.slurm (NEW file)"
      contains: "scripts/04_model_fitting/c_level2/fit_with_l2.py"
    - path: "cluster/03_prefitting_gpu.slurm"
      provides: "Single-GPU prefit (STEP=synthetic|parameter_sweep|model_recovery|prior_predictive|bayesian_recovery) with per-STEP+per-MODEL cache (parity with CPU)"
      contains: "JAX_COMPILATION_CACHE_DIR"
  key_links:
    - from: "cluster/04b_bayesian_gpu.slurm"
      to: "scripts/04_model_fitting/b_bayesian/fit_bayesian.py (pmap path via numpyro_models._select_chain_method)"
      via: "--gres=gpu:4 + --chains 4 → jax.local_device_count()=4 → chain_method=parallel"
      pattern: "--gres=gpu:4"
    - from: "cluster/04c_level2_gpu.slurm"
      to: "scripts/04_model_fitting/c_level2/fit_with_l2.py (routes outputs through save_results with output_subdir=21_l2)"
      via: "Template C pmap for M6b subscale 32-beta fit"
      pattern: "04_model_fitting/c_level2/fit_with_l2\\.py"
    - from: "All 3 touched GPU SLURMs"
      to: "rlwm_gpu conda env (CUDA 12 + JAX-GPU)"
      via: "conda activate rlwm_gpu → falls through to /scratch paths if name-resolution fails; NO ds_env fallback"
      pattern: "rlwm_gpu"
---

## Risk: Wave 1 Partial Failure Contingency (unchanged from original plan)

If Plan 23.1-01 (Wave 1 multi-GPU smoke) reports any of {qlearning, wmrl, wmrl_m3, wmrl_m5, wmrl_m6a} as FAILED for pmap reasons, this plan must be amended BEFORE submitting those models via Template C through 04b_bayesian_gpu.slurm.

**Option A (per-model conditional `--gres` at submit time, orchestrator-driven):** Since SLURM `#SBATCH` directives can't be conditional, build the conditionality in the orchestrator. Have `cluster/submit_all.sh` branch on MODEL and pass `--gres=gpu:1 --export=CHAINS=4` dynamically via sbatch CLI (overrides the #SBATCH directive) for any failing-pmap model, falling back to chain_method=vectorized on a single GPU. This requires amending Plan 23.1-03 orchestrator dispatch.

**Option B (per-failing-model singlegpu sibling):** Build `cluster/04b_bayesian_singlegpu_${MODEL}.slurm` for failing models. The orchestrator dispatches the singlegpu sibling based on env-var routing (`MODEL_GPU_TYPE`).

**Recommended on first failure:** Option A (cleaner — no new SLURM files; SLURM CLI `--gres` overrides `#SBATCH --gres`).

<objective>
Upgrade the existing Phase-29 `04b_bayesian_gpu.slurm` to Template C (4 GPUs + pmap), create the missing `04c_level2_gpu.slurm` Template C GPU sibling, and bring `03_prefitting_gpu.slurm` to per-STEP+per-MODEL cache parity with its CPU sibling. Every file encodes the full CLUSTER_GPU_LESSONS.md operational must-have set.

Purpose: The Phase 24 cold-start's wall-clock is dominated by the hierarchical Bayesian baseline fits (04b) and the M6b subscale L2 refit (04c). Today both default to CPU under `submit_all.sh`, and the 04b GPU variant that does exist is silently single-GPU (chain_method degrades to vectorized — the 3-4× Template C payoff per M6b proof job 54894258 is not realized). After this plan ships, these two payloads run Template C multi-GPU pmap, and `03_prefitting_gpu.slurm` picks up the cache-parity fix so repeat-per-model runs hit a warm cache instead of recompiling.

Original CPU SLURMs (`04b_bayesian_cpu.slurm`, `04c_level2.slurm`, `03_prefitting_cpu.slurm`) MUST stay byte-identical — they remain the USE_CPU=1 escape hatch wired by 23.1-03.

Output: 1 upgraded GPU SLURM (04b) + 1 new GPU SLURM (04c_level2_gpu) + 1 cache-parity patch (03_prefitting_gpu). No Python code changes. No orchestrator changes (those are 23.1-03).
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23.1-gpu-pipeline-integration/23.1-RESEARCH.md
@docs/CLUSTER_GPU_LESSONS.md
@cluster/13_bayesian_multigpu.slurm
@cluster/04b_bayesian_cpu.slurm
@cluster/04b_bayesian_gpu.slurm
@cluster/04c_level2.slurm
@cluster/03_prefitting_cpu.slurm
@cluster/03_prefitting_gpu.slurm
@scripts/04_model_fitting/b_bayesian/fit_bayesian.py
@scripts/04_model_fitting/c_level2/fit_with_l2.py
@src/rlwm/fitting/numpyro_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade cluster/04b_bayesian_gpu.slurm to Template C (4 GPUs + pmap) with per-model _gpu-suffixed cache</name>
  <files>cluster/04b_bayesian_gpu.slurm</files>
  <action>
The current 04b_bayesian_gpu.slurm is single-GPU (`--gres=gpu:1`) with a shared cache (`.jax_cache_gpu`) and uses `--use-gpu` (a script-level flag that triggers float64 and other compatibility steps). Upgrade it to Template C mirroring `cluster/13_bayesian_multigpu.slurm` lines 30-106 while preserving the parameterized `--export=ALL,MODEL=...` + `SUBSCALE=1` dispatch surface that `04b_bayesian_cpu.slurm` defines.

**Specific changes (preserve all other CPU-sibling parameterization logic: MODEL default, SUBSCALE env, CHAINS/WARMUP/SAMPLES/SEED/MAX_TREE_DEPTH defaults, the input-file checks for SUBSCALE=1, the `--subscale` CLI flag append, the post-fit expected-outputs audit block):**

1. **Job name + log paths:**
   - `#SBATCH --job-name=rlwm-bayesian-gpu-mgpu`
   - `#SBATCH --output=cluster/logs/04b_bayesian_gpu_%j.out`
   - `#SBATCH --error=cluster/logs/04b_bayesian_gpu_%j.err`

2. **Resource directives (replace the current single-GPU block):**
   - `#SBATCH --time=06:00:00` (unchanged — M6b subscale override via orchestrator `--time=36:00:00`)
   - `#SBATCH --mem=64G` (up from 32G — per-device host-RAM pressure with 4-way pmap)
   - `#SBATCH --cpus-per-task=8` (up from 4 — one per chain + slack)
   - `#SBATCH --gres=gpu:4` (was `gpu:1`)
   - `#SBATCH --partition=gpu` (unchanged)
   - `#SBATCH --constraint=${GPU_TYPE:-l40s}` (NEW — env-overridable; hard-code `l40s` and leave a comment if Monash SLURM rejects shell-style defaulting in directives)

3. **Conda env ladder:** Keep the existing rlwm_gpu activation (by-name → `/scratch/fc37/$USER/...` → `/scratch/$PROJECT/$USER/...`). DO NOT add an `ds_env` fallback. Current script already has this correct — verify no regression.

4. **JAX GPU + chain-count sanity print** (replace the current simple `jax.devices()` echo block with the CHAINS-aware check mirrored from `13_bayesian_multigpu.slurm` lines 93-106):
```bash
CHAINS_NUM=$CHAINS
python -c "
import jax
devices = jax.devices()
n_gpu = sum(1 for d in devices if d.platform == 'gpu')
print(f'JAX {jax.__version__} devices: {devices}')
print(f'GPU count: {n_gpu}')
if n_gpu < ${CHAINS_NUM}:
    print(f'WARNING: {n_gpu} GPU(s) < ${CHAINS_NUM} chains. chain_method will degrade to vectorized.')
" || { echo 'ERROR: JAX import failed'; exit 1; }
```

5. **Cache + env exports (replace current block):**
```bash
export NUMPYRO_HOST_DEVICE_COUNT="$CHAINS"   # match chains → pmap sees all devices
_PROJECT="${PROJECT:-fc37}"
export JAX_COMPILATION_CACHE_DIR="/scratch/${_PROJECT}/${USER}/.jax_cache_04b_bayesian_gpu_${MODEL}"
export JAX_PERSISTENT_CACHE_MIN_ENTRY_SIZE_BYTES=0
export PYTHONUNBUFFERED=1
mkdir -p "$JAX_COMPILATION_CACHE_DIR" || { echo 'WARNING: cache dir create failed'; unset JAX_COMPILATION_CACHE_DIR; }
echo "JAX cache: ${JAX_COMPILATION_CACHE_DIR:-disabled}"
```
Cache path is per-model + `_gpu` suffix (so it coexists with the CPU cache `.jax_cache_bayesian` without contamination, per CLUSTER_GPU_LESSONS.md §5).

6. **Python invocation:** DROP the `--use-gpu` flag (pmap selection is automatic via `numpyro_models._select_chain_method` when n_devices ≥ n_chains; `--use-gpu` was the single-GPU-mode flag). Preserve every other arg including the conditional `--subscale` append. The CPU sibling's `CMD=(...)` array approach is cleaner than `eval`; mirror it:
```bash
CMD=(python scripts/04_model_fitting/b_bayesian/fit_bayesian.py
     --model "$MODEL"
     --data "$DATA_FILE"
     --chains "$CHAINS"
     --warmup "$WARMUP"
     --samples "$SAMPLES"
     --seed "$SEED"
     --max-tree-depth "$MAX_TREE_DEPTH")
if [ "$SUBSCALE" = "1" ]; then
    CMD+=(--subscale)
fi
if [[ -n "$LIMIT" ]]; then
    CMD+=(--limit "$LIMIT")
fi
echo "[$(date)] Starting ${MODEL} hierarchical Bayesian fit${SUBSCALE:+ (subscale=$SUBSCALE)}"
echo "  Command: ${CMD[*]}"
"${CMD[@]}"
EXIT_CODE=$?
```

7. **GPU monitoring block:** KEEP the background `nvidia-smi` poll and cleanup (lines 153-181 of current file) — useful operational visibility, no cost.

8. **Post-fit expected-outputs audit:** MIRROR the CPU sibling's block (lines 153-167 of 04b_bayesian_cpu.slurm) — preserves the SUBSCALE-aware verification of `output/bayesian/${MODEL}_{individual_fits.csv,posterior.nc,shrinkage_report.md,ppc_results.csv}`.

9. **Header comment:** Rewrite the top comment block. Include:
   - "GPU multi-GPU pmap variant of cluster/04b_bayesian_cpu.slurm."
   - "Template C (4 GPUs + chain_method=parallel via pmap) per CLUSTER_GPU_LESSONS.md §6; auto-selected by numpyro_models._select_chain_method when jax.local_device_count() >= n_chains."
   - "M6b subscale walltime override via orchestrator: `sbatch --time=36:00:00 --export=ALL,MODEL=wmrl_m6b,SUBSCALE=1 ...`."
   - "Wall-clock baseline: M6b 4-GPU pmap job 54894258 (logs/bayesian_mgpu_54894258.out, 2026-04-18) completed in ~3h; other choice-only models proportionally faster."
   - "CPU sibling cluster/04b_bayesian_cpu.slurm is preserved unchanged and remains dispatchable via USE_CPU=1 in the master orchestrator (submit_all.sh)."

10. **autopush + exit propagation:** The current script calls `source cluster/autopush.sh` at the end without an explicit `exit $EXIT_CODE`. Preserve this (matches current behavior). Remove the sacct/resource-report block only if it interferes with the simplified command-array invocation; otherwise keep it as-is (useful operator signal).

**MUST NOT modify:**
- cluster/04b_bayesian_cpu.slurm (verified unchanged via `git diff` at end of task)
- Any Python file
- The parameterized dispatch surface (--export=ALL,MODEL=...,SUBSCALE=...,LIMIT=...,CHAINS=...,WARMUP=...,SAMPLES=...)
  </action>
  <verify>
1. `bash -n cluster/04b_bayesian_gpu.slurm` exits 0.
2. `Grep -n "^#SBATCH --gres=gpu:4$" cluster/04b_bayesian_gpu.slurm` returns exactly 1 hit (was `gpu:1`; now Template C).
3. `Grep -n "^#SBATCH --constraint=" cluster/04b_bayesian_gpu.slurm` returns exactly 1 hit (L40S pin).
4. `Grep -n "^#SBATCH --partition=gpu$" cluster/04b_bayesian_gpu.slurm` returns exactly 1 hit.
5. `Grep -n "PYTHONUNBUFFERED=1" cluster/04b_bayesian_gpu.slurm` returns a hit.
6. `Grep -n "\\.jax_cache_04b_bayesian_gpu_" cluster/04b_bayesian_gpu.slurm` returns a hit (per-model `_gpu`-suffixed cache).
7. `Grep -n "JAX_PLATFORMS=cpu" cluster/04b_bayesian_gpu.slurm` returns NO hits.
8. `Grep -n "lba_likelihood" cluster/04b_bayesian_gpu.slurm` returns NO hits.
9. `Grep -n "conda activate ds_env" cluster/04b_bayesian_gpu.slurm` returns NO hits (comments mentioning ds_env are allowed; only the activation command is forbidden).
10. `Grep -n "rlwm_gpu" cluster/04b_bayesian_gpu.slurm` returns hits (env activation ladder).
11. `Grep -n "scripts/04_model_fitting/b_bayesian/fit_bayesian.py" cluster/04b_bayesian_gpu.slurm` returns a hit.
12. `Grep -n "NUMPYRO_HOST_DEVICE_COUNT" cluster/04b_bayesian_gpu.slurm` shows it is set to `$CHAINS` (not hardcoded `1`).
13. `Grep -n "--use-gpu" cluster/04b_bayesian_gpu.slurm` returns NO hits (flag dropped — pmap selection is automatic).
14. `Grep -n "SUBSCALE" cluster/04b_bayesian_gpu.slurm` returns hits (parameterized dispatch preserved).
15. `git diff cluster/04b_bayesian_cpu.slurm` returns empty (CPU sibling unchanged).
16. Line count sanity: `wc -l cluster/04b_bayesian_gpu.slurm` within ±20 lines of `wc -l cluster/04b_bayesian_cpu.slurm` (template C adds ~15 lines of GPU sanity + monitor; cpu loses ~5 CPU-only lines).
  </verify>
  <done>
cluster/04b_bayesian_gpu.slurm is Template C (4 GPUs + pmap); uses per-model `_gpu`-suffixed cache; pins to L40S by default; activates rlwm_gpu only; NUMPYRO_HOST_DEVICE_COUNT matches CHAINS; no --use-gpu flag; SUBSCALE parameterization preserved; CPU sibling byte-unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cluster/04c_level2_gpu.slurm — Template C GPU sibling of 04c_level2.slurm</name>
  <files>cluster/04c_level2_gpu.slurm</files>
  <action>
Create `cluster/04c_level2_gpu.slurm` as a direct mirror of `cluster/04c_level2.slurm` with Template C resource block and the GPU cache/env changes. The CPU sibling remains unchanged.

**Diff from cluster/04c_level2.slurm → cluster/04c_level2_gpu.slurm:**

1. **Header comment:** Add a paragraph at the top stating this is the GPU variant; Template C (4 GPUs + pmap); M6b subscale payoff is the primary motivation (CPU worst-case ~8-12h; GPU expected ~3-4h per M6b proof); CPU sibling preserved and remains dispatchable via USE_CPU=1. Mention this file is the new Wave-2 artifact built by plan 23.1-02.

2. **Job name + log paths:**
   - `#SBATCH --job-name=bayes_04c_l2_gpu`
   - `#SBATCH --output=logs/bayesian_04c_l2_gpu_%j.out`
   - `#SBATCH --error=logs/bayesian_04c_l2_gpu_%j.err`

3. **Resource directives (replace current CPU block):**
   - `#SBATCH --time=08:00:00` (down from 12h CPU — Template C run on M6b subscale ~3-4h expected; 8h gives comfortable worst-case auto-bump headroom)
   - `#SBATCH --mem=64G` (unchanged)
   - `#SBATCH --cpus-per-task=8` (up from 4 — one per chain + slack)
   - `#SBATCH --partition=gpu` (was `comp`)
   - `#SBATCH --gres=gpu:4` (NEW)
   - `#SBATCH --constraint=${GPU_TYPE:-l40s}` (NEW)

4. **Conda env:** rlwm_gpu only (strip the `ds_env → /scratch/.../ds_env → rlwm_gpu-fallback` ladder from the CPU sibling). Mirror the `04b_bayesian_gpu.slurm` activation ladder:
```bash
if conda activate rlwm_gpu 2>/dev/null; then
    echo "Activated rlwm_gpu by name"
elif conda activate /scratch/fc37/$USER/conda/envs/rlwm_gpu 2>/dev/null; then
    echo "Activated rlwm_gpu from /scratch/fc37/$USER/"
elif conda activate /scratch/${PROJECT:-fc37}/$USER/conda/envs/rlwm_gpu 2>/dev/null; then
    echo "Activated rlwm_gpu from /scratch/${PROJECT:-fc37}/$USER/"
else
    echo "ERROR: Failed to activate rlwm_gpu — GPU pipeline cannot run on ds_env (CPU-only)"
    exit 1
fi
```

5. **JAX device + chain-count sanity print:** same pattern as 04b_bayesian_gpu.slurm (Task 1 step 4).

6. **Env exports (replace current CPU export block, lines 114-124 of 04c_level2.slurm):**
```bash
# REMOVE these CPU-only lines:
# export JAX_PLATFORMS=cpu
# export NUMPYRO_HOST_DEVICE_COUNT=4   <-- CPU-only knob; override below
#
# ADD these GPU lines:
export NUMPYRO_HOST_DEVICE_COUNT="$CHAINS"
_PROJECT="${PROJECT:-fc37}"
export JAX_COMPILATION_CACHE_DIR="/scratch/${_PROJECT}/${USER}/.jax_cache_04c_level2_gpu_${MODEL}"
export JAX_PERSISTENT_CACHE_MIN_ENTRY_SIZE_BYTES=0
export PYTHONUNBUFFERED=1
mkdir -p "$JAX_COMPILATION_CACHE_DIR" || { echo 'WARNING: cache dir create failed'; unset JAX_COMPILATION_CACHE_DIR; }
echo "JAX cache: ${JAX_COMPILATION_CACHE_DIR:-disabled}"
```

7. **Python invocation:** UNCHANGED from CPU sibling — still calls `python scripts/04_model_fitting/c_level2/fit_with_l2.py --model "$MODEL" --data output/task_trials_long.csv --chains "$CHAINS" --warmup "$WARMUP" --samples "$SAMPLES" --seed "$SEED" --max-tree-depth "$MAX_TREE_DEPTH" --output output/ --winners-file "$WINNERS_FILE"`. The winners.txt validation and copy-through / 2-cov / subscale branching all live inside fit_with_l2.py.

8. **Input-file verification block (lines 131-140 of 04c_level2.slurm):** UNCHANGED. Verifies `output/task_trials_long.csv`, `output/summary_participant_metrics.csv`, `$WINNERS_FILE` all exist before launching the fit.

9. **Post-fit expected-artifacts audit (lines 168-189 of 04c_level2.slurm):** UNCHANGED. The M1/M2 `[MISSING]`-by-design is still valid (copy path produces only posterior.nc).

10. **autopush + exit propagation:** UNCHANGED. `source cluster/autopush.sh` followed by `exit $EXIT_CODE`.

**MUST NOT modify:**
- cluster/04c_level2.slurm (verified unchanged via `git diff` at end of task)
- cluster/21_dispatch_l2_winners.sh (that's 23.1-03's job)
- cluster/21_6_dispatch_l2.slurm
- Any Python file
  </action>
  <verify>
1. `bash -n cluster/04c_level2_gpu.slurm` exits 0.
2. `Grep -n "^#SBATCH --gres=gpu:4$" cluster/04c_level2_gpu.slurm` returns exactly 1 hit.
3. `Grep -n "^#SBATCH --constraint=" cluster/04c_level2_gpu.slurm` returns exactly 1 hit.
4. `Grep -n "^#SBATCH --partition=gpu$" cluster/04c_level2_gpu.slurm` returns exactly 1 hit.
5. `Grep -n "PYTHONUNBUFFERED=1" cluster/04c_level2_gpu.slurm` returns a hit.
6. `Grep -n "\\.jax_cache_04c_level2_gpu_" cluster/04c_level2_gpu.slurm` returns a hit.
7. `Grep -n "JAX_PLATFORMS=cpu" cluster/04c_level2_gpu.slurm` returns NO hits.
8. `Grep -n "lba_likelihood" cluster/04c_level2_gpu.slurm` returns NO hits.
9. `Grep -n "conda activate ds_env" cluster/04c_level2_gpu.slurm` returns NO hits.
10. `Grep -n "rlwm_gpu" cluster/04c_level2_gpu.slurm` returns hits.
11. `Grep -n "scripts/04_model_fitting/c_level2/fit_with_l2.py" cluster/04c_level2_gpu.slurm` returns a hit.
12. `Grep -n "source cluster/autopush.sh" cluster/04c_level2_gpu.slurm` returns a hit.
13. `Grep -n "^exit \\$EXIT_CODE$" cluster/04c_level2_gpu.slurm` returns a hit (exit propagation to afterok chain).
14. `Grep -n "winners.txt\\|WINNERS_FILE" cluster/04c_level2_gpu.slurm` returns hits (winners.txt contract preserved).
15. `git diff cluster/04c_level2.slurm` returns empty (CPU sibling unchanged).
16. Line count sanity: `wc -l cluster/04c_level2_gpu.slurm` within ±15 lines of `wc -l cluster/04c_level2.slurm` (GPU adds constraint/sanity/cache; loses JAX_PLATFORMS=cpu line).
  </verify>
  <done>
cluster/04c_level2_gpu.slurm exists; Template C (4 GPUs + pmap); per-model `_gpu`-suffixed cache; L40S pin; rlwm_gpu only; winners.txt contract preserved via unchanged python entry point; CPU sibling byte-unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Upgrade cluster/03_prefitting_gpu.slurm JAX cache to per-STEP+per-MODEL parity with CPU sibling + add L40S constraint pin</name>
  <files>cluster/03_prefitting_gpu.slurm</files>
  <action>
Current `03_prefitting_gpu.slurm` line 86 uses a single shared cache `.jax_cache_03_prefit_gpu` for all STEP × MODEL combinations — this means every model+step combination pays the full JIT-compile cost the first time it runs (whereas the CPU sibling on line 84 uses per-STEP+per-MODEL caches). Fix the parity. Also add the `--constraint` pin for arch consistency with Tasks 1 + 2.

**Specific changes (surgical Edit tool — do NOT rewrite the file):**

1. **Edit 1: Add constraint SBATCH directive.** Find the current SBATCH block (lines 24-31) and insert after `#SBATCH --partition=gpu`:
```
#SBATCH --constraint=${GPU_TYPE:-l40s}
```

2. **Edit 2: Upgrade cache dir to per-STEP+per-MODEL parity.** Find this exact line (line 86):
```
export JAX_COMPILATION_CACHE_DIR="/scratch/${_PROJECT}/${USER}/.jax_cache_03_prefit_gpu"
```
Replace with:
```
export JAX_COMPILATION_CACHE_DIR="/scratch/${_PROJECT}/${USER}/.jax_cache_03_prefit_gpu_${STEP}_${MODEL}"
```
(Mirrors CPU sibling line 84's `.jax_cache_03_prefit_${STEP}_${MODEL}` naming, with `_gpu` inserted after `_prefit` for the §5 cross-backend isolation.)

3. **MUST preserve verbatim:**
   - The STEP dispatch case block (lines 95-132)
   - The rlwm_gpu activation ladder (lines 63-72)
   - The JAX device print (lines 74-83)
   - `PYTHONUNBUFFERED=1` (line 93)
   - The autopush block (lines 142-144)
   - The exit code propagation (line 146)

4. **MUST NOT modify:**
   - cluster/03_prefitting_cpu.slurm
   - Any Python file
  </action>
  <verify>
1. `bash -n cluster/03_prefitting_gpu.slurm` exits 0.
2. `Grep -n "^#SBATCH --constraint=" cluster/03_prefitting_gpu.slurm` returns exactly 1 hit.
3. `Grep -n "\\.jax_cache_03_prefit_gpu_\\${STEP}_\\${MODEL}" cluster/03_prefitting_gpu.slurm` returns a hit (per-STEP+per-MODEL cache parity achieved).
4. `Grep -n "\\.jax_cache_03_prefit_gpu\"" cluster/03_prefitting_gpu.slurm` returns NO hits (old single-cache line is gone).
5. `Grep -n "PYTHONUNBUFFERED=1" cluster/03_prefitting_gpu.slurm` returns a hit.
6. `Grep -n "JAX_PLATFORMS=cpu" cluster/03_prefitting_gpu.slurm` returns NO hits.
7. `Grep -n "lba_likelihood" cluster/03_prefitting_gpu.slurm` returns NO hits.
8. `Grep -n "conda activate ds_env" cluster/03_prefitting_gpu.slurm` returns NO hits.
9. `Grep -n "source cluster/autopush.sh" cluster/03_prefitting_gpu.slurm` returns a hit.
10. `Grep -n "case \"\\${STEP}\"" cluster/03_prefitting_gpu.slurm` returns a hit (STEP dispatch preserved).
11. `git diff cluster/03_prefitting_cpu.slurm` returns empty (CPU sibling unchanged).
  </verify>
  <done>
03_prefitting_gpu.slurm cache is per-STEP+per-MODEL `_gpu`-suffixed (parity with CPU sibling pattern); L40S constraint pin added; all other logic (STEP dispatch, activation ladder, autopush) byte-unchanged.
  </done>
</task>

</tasks>

<verification>
- 3 GPU SLURMs touched: `cluster/04b_bayesian_gpu.slurm` upgraded to Template C; `cluster/04c_level2_gpu.slurm` created; `cluster/03_prefitting_gpu.slurm` cache patched.
- Each `bash -n` clean.
- Each encodes all CLUSTER_GPU_LESSONS.md must-haves: PYTHONUNBUFFERED=1, per-model (or per-STEP+per-MODEL) `_gpu`-suffixed JAX cache, rlwm_gpu activation only (no ds_env fallback), `--constraint=${GPU_TYPE:-l40s}`, no JAX_PLATFORMS=cpu, no lba_likelihood import.
- Each writes to its stage's canonical output path via unchanged python entry-point invocation.
- Original CPU SLURMs (`04b_bayesian_cpu.slurm`, `04c_level2.slurm`, `03_prefitting_cpu.slurm`) unmodified (verified by `git diff` returning empty for each).
- Lazy LBA float64 isolation preserved — no module-level `from rlwm.fitting.lba_likelihood import` in any touched GPU SLURM.
</verification>

<success_criteria>
- 04b_bayesian_gpu.slurm is Template C (4 GPUs + pmap) — wall-clock payoff for the biggest cold-start payload is unlocked.
- 04c_level2_gpu.slurm exists as Template C sibling — M6b subscale L2 refit has a GPU path (second-biggest payoff).
- 03_prefitting_gpu.slurm has cache parity with CPU sibling — repeat-per-model prefit runs hit warm JIT cache.
- Every touched script encodes every CLUSTER_GPU_LESSONS.md operational must-have for GPU MCMC.
- Original CPU pipeline unbroken (CPU SLURMs untouched — USE_CPU=1 escape hatch remains valid; wired by 23.1-03).
</success_criteria>

<output>
After completion, create `.planning/phases/23.1-gpu-pipeline-integration/23.1-02-SUMMARY.md` with:
- File-by-file diff summary (what changed from the pre-plan state — resource block, env block, cache dir name, activation ladder)
- Confirmation of must-have encoding table (PYTHONUNBUFFERED, per-model/per-STEP _gpu cache, constraint pin, no LBA import, no ds_env fallback, autopush present where appropriate)
- Any deviations from the plan (e.g., if Monash SLURM rejected `${GPU_TYPE:-l40s}` defaulting and required hard-coded `l40s`; if nvidia-smi monitor block was dropped to simplify command-array invocation)
- Note for Wave 3 integration smoke (plan 23.1-04): which SLURMs now exist in each (cpu, gpu) pair, so the orchestrator's USE_CPU switch in 23.1-03 has the right defaults to route.
</output>

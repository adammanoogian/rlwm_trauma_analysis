---
phase: 29-pipeline-canonical-reorg
plan: 06
subsystem: manuscript
tags: [quarto, latex, manuscript, paper.qmd, paper.tex, smoke-test, yaml]

# Dependency graph
requires:
  - phase: 29-01
    provides: Canonical 01-06 scripts/ grouping — every path reference in paper.qmd/tex must resolve under new tree
  - phase: 29-02
    provides: Merged docs/SCALES_AND_FITTING_AUDIT.md into docs/04_methods/README.md#scales-orthogonalization-and-audit (anchor confirmed at docs/04_methods/README.md:284)
  - phase: 29-04b
    provides: Scheme D intra-stage renumbering (01-08) for scripts/06_fit_analyses/; full-repo importer + doc sweep (commit 093d934) already rewrote every paper.qmd/tex script-path reference to the final canonical form — Task 1 of this plan absorbed pre-execution
provides:
  - Verified manuscript/paper.qmd and manuscript/paper.tex contain zero stale grouping-dir or top-level fit-stage script paths
  - Absorbed-from-29-02 caption rewrite on paper.qmd line 166 (docs/SCALES_AND_FITTING_AUDIT.md -> docs/04_methods/README.md#scales-orthogonalization-and-audit)
  - Fixed pre-existing YAML indentation bug in paper.qmd author block that was blocking `quarto render`
  - Verified `quarto render manuscript/paper.qmd` exits 0, emits manuscript/_output/paper.pdf (1,065,724 bytes)
  - Confirmed all 20 graceful-fallback `{python}` cells (Phase 28 pattern) still absorb missing data artifacts cleanly
affects: [26-manuscript-finalization, 29-07-closure-guard-extension]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Smoke-render verification: `quarto render manuscript/paper.qmd` is a portable check that every inline `{python}` cell import resolves and every prose script-path reference points at an existing file; should be added to the Phase 29 closure guard (29-07)"
    - "keep-tex: true side-effect: paper.tex is a generated artifact — editing it directly is a footgun because the next `quarto render` overwrites it; the canonical source of truth is paper.qmd"

key-files:
  created:
    - ".planning/phases/29-pipeline-canonical-reorg/29-06-SUMMARY.md"
  modified:
    - "manuscript/paper.qmd (line 7 YAML indent fix; line 166 caption rewrite)"
    - "manuscript/paper.tex (regenerated by quarto — author block corrected from 'Coauthor Name' placeholder to real two-author list; line 226 caption rewrite propagated from paper.qmd; incidental pandoc whitespace reflows)"
    - "manuscript/_output/paper.pdf (regenerated, 1,065,724 bytes; not committed — gitignored)"

key-decisions:
  - "Honoured plan's reduced-scope directive: 29-04b's 093d934 sweep already covered Task 1's script-path rewrites; this plan executed only (a) Task 1 residual (line 166 absorbed-from-29-02), (b) Task 2 smoke render. Net new edits: 2 lines in paper.qmd."
  - "Committed the paper.tex regeneration alongside paper.qmd because the YAML-fix side-effect corrected a stale 'Coauthor Name' placeholder in pdftitle/author metadata — leaving paper.tex on disk out of sync with paper.qmd would be worse than committing the generated artifact."

patterns-established:
  - "Pre-execution absorption check: when a predecessor plan claims to cover a downstream plan's work, grep-verify before executing. Here, grep for the post-sweep paths (scripts/data_processing, etc.) returned zero hits immediately, confirming 29-04b's 093d934 absorbed Task 1 completely except for the 29-02 line-166 edit."

# Metrics
duration: 8min
completed: 2026-04-22
---

# Phase 29 Plan 06: paper.qmd Smoke Render Summary

**Quarto render of manuscript/paper.qmd exits 0 after 29-04b's script-path sweep + 2-line residual (absorbed-29-02 caption + YAML indent fix); manuscript/_output/paper.pdf regenerated at 1.04 MB with all 20 graceful-fallback cells behaving cleanly.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-22T15:23:12Z
- **Completed:** 2026-04-22T15:31:29Z
- **Tasks:** 2 (Task 1 reduced-scope residual + Task 2 smoke render)
- **Files modified:** 2 (paper.qmd + paper.tex regeneration)

## Accomplishments

- Verified 29-04b commit `093d934` (fix(29-04b): full repo importer + doc sweep for renumbered stage files) did indeed absorb this plan's Task 1 scripts-path rewrites: grep for the eight stale grouping-dir/top-level patterns in `manuscript/paper.{qmd,tex}` returned **zero matches**.
- Applied the absorbed-from-29-02 line-166 caption rewrite on paper.qmd (the one edit 29-04b did not make because it was outside its script-path scope):
  ``docs/SCALES_AND_FITTING_AUDIT.md`` → ``docs/04_methods/README.md#scales-orthogonalization-and-audit``
  Anchor verified present at `docs/04_methods/README.md:284` (`## Scales Orthogonalization and Audit`).
- Fixed a pre-existing YAML indentation bug in paper.qmd's author block that was blocking `quarto render` with `bad indentation of a mapping entry (7:15)`:
  ```yaml
  # before (broken):
        - id: uts
            name: University of Technology Sydney   # 10 spaces instead of 8
  # after:
        - id: uts
          name: University of Technology Sydney
  ```
  This bug predates Phase 29 (unrelated to the reorg); surfaced by the first smoke-render attempt.
- `quarto render manuscript/paper.qmd` **exits 0**; output file `manuscript/_output/paper.pdf` written at **1,065,724 bytes** (~1.04 MB).
- All 20 graceful-fallback `{python}` cells (Phase 28 pattern, try/except-wrapped FileNotFoundError → print + placeholder table/figure) fired cleanly on missing cold-start artifacts (`output/bayesian/...`). Zero cells flagged for Phase 26 follow-up.

## Task Commits

1. **Task 1 (reduced-scope residual) + Task 2 smoke render** — `2b26df0` (docs)
   `docs(29-06): verify manuscript/paper.{qmd,tex} canonical paths + quarto render smoke`
   Note: consolidated into a single code commit because (a) Task 1's script-path work was absorbed by 29-04b `093d934`, leaving only 2 residual paper.qmd lines; (b) paper.tex regeneration is a direct consequence of running Task 2's smoke render, so splitting would create an inconsistent intermediate state.

**Plan metadata:** (pending — see final commit below)

## Files Created/Modified

- `manuscript/paper.qmd` (2-line diff)
  - Line 7: YAML indent fix for `name: University of Technology Sydney` (10 → 8 spaces)
  - Line 166: caption rewrite to point at merged docs anchor
- `manuscript/paper.tex` (75-line diff, mostly generated noise)
  - Lines 72, 113–126, 128: author block regenerated — `pdfauthor={Jasmine Burrows, Adam Manoogian}` replaces legacy `pdfauthor={Adam Manoogian, Coauthor Name}`; affil/author TeX macros now emit both authors correctly
  - Line 226: caption rewrite propagated from paper.qmd line 166
  - Lines 272–284, 502–503, 639, 663, 766: incidental pandoc whitespace reflows (equation line-breaking; one auto-generated table label hash changed `T_b1491` → `T_2e3fc`)
- `manuscript/_output/paper.pdf` (regenerated, 1,065,724 bytes, gitignored — smoke evidence only)

## Verification Evidence

### Grep audit (post-edit)

```bash
$ grep -n "scripts/data_processing\|scripts/behavioral\|scripts/simulations_recovery\|scripts/post_mle\|scripts/bayesian_pipeline\|scripts/12_fit_mle\|scripts/13_fit_bayesian\|scripts/14_compare_models" manuscript/paper.qmd manuscript/paper.tex
# (no output — ZERO stale paths)

$ grep -n "docs/SCALES_AND_FITTING_AUDIT\.md" manuscript/paper.qmd
# (no output — removed)

$ grep -n "scales-orthogonalization-and-audit" manuscript/paper.qmd
166:Scale distributions and pairwise correlations are summarized in @fig-scale-distributions and documented in `docs/04_methods/README.md#scales-orthogonalization-and-audit`.
```

### Surviving scripts/ references in paper.qmd (all confirmed valid)

```
537:    print("Run: python scripts/06_fit_analyses/07_bayesian_level2_effects.py")
630:    print(f"Data not yet available. Run scripts/06_fit_analyses/01_compare_models.py first. ({e})")
650:    print("Stacking weights not available. Run: python scripts/06_fit_analyses/01_compare_models.py --bayesian-comparison")
# line 171 (long line):  scripts/legacy/analysis/trauma_scale_distributions.py
```

File-existence spot-check:
- `scripts/06_fit_analyses/07_bayesian_level2_effects.py` — exists
- `scripts/06_fit_analyses/01_compare_models.py` — exists
- `scripts/legacy/analysis/trauma_scale_distributions.py` — exists

### Quarto render stdout tail (last ~30 lines of second, clean run)

```
Rendering PDF
running pdflatex - 1
  This is pdfTeX, Version 3.141592653-2.6-1.40.25 (MiKTeX 24.1) (preloaded format=pdflatex.fmt)
   restricted \write18 enabled.
  entering extended mode

generating bibliography
  This is BibTeX, Version 0.99d (MiKTeX 24.1)
  The top-level auxiliary file: paper.aux
  The style file: unsrtnat.bst
  Database file #1: references.bib
  Warning--I didn't find a database entry for "phan2019composable"
  Warning--I didn't find a database entry for "hoffman2014no"
  Warning--I didn't find a database entry for "ahn2017revealing"
  Warning--I didn't find a database entry for "vehtari2017practical"
  Warning--I didn't find a database entry for "yao2018using"
  (There were 5 warnings)

running pdflatex - 2
  ...
running pdflatex - 3
  ...
running pdflatex - 4
  ...

Output created: _output\paper.pdf
```

**Signal analysis:**
- **Zero** `FileNotFoundError` raised (all graceful-fallback try/except caught missing cold-start artifacts)
- **Zero** `ModuleNotFoundError` (all `{python}` cell imports resolve; `sys.path.insert(0, str(Path("..")))` pattern at top of setup cell works under the new 01–06 tree because imports don't reference `scripts.*` module paths — they use subprocess-style prose references for pipeline commands and direct `from config import ...` at module level)
- **5 BibTeX warnings** for missing references (`phan2019composable`, `hoffman2014no`, `ahn2017revealing`, `vehtari2017practical`, `yao2018using`) — **PRE-EXISTING, out of scope for this plan**, to be resolved in Phase 26 manuscript finalization.

### Closure + pytest

```
$ python validation/check_v4_closure.py
RESULTS: 5/5 checks passed, 0 failed    EXIT 0

$ python -m pytest scripts/fitting/tests/test_v4_closure.py -v
test_v4_closure_passes PASSED                    [ 33%]
test_v4_closure_deterministic PASSED             [ 66%]
test_v4_closure_rejects_wrong_milestone PASSED   [100%]
============================== 3 passed in 2.76s ==============================
```

## Decisions Made

- **Consolidated Task 1 + Task 2 into one code commit (`2b26df0`).** The plan's reduced-scope directive anticipated that 29-04b's sweep would absorb most of Task 1; with only a 2-line Task 1 residual (line 166 + YAML indent) and paper.tex regeneration being a direct byproduct of Task 2's render, splitting the commits would create a broken intermediate state where paper.tex is half-regenerated.
- **Committed paper.tex alongside paper.qmd despite being a generated artifact.** The YAML-fix side-effect corrected a stale `pdfauthor={Adam Manoogian, Coauthor Name}` placeholder that was wrong on disk before this plan ran. Leaving the stale version committed while the fixed version sits uncommitted in working tree is worse than committing the generated output (which matches what `keep-tex: true` will reproduce on any future render).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed YAML indentation bug in paper.qmd author block**
- **Found during:** Task 2 (first quarto render attempt)
- **Issue:** `manuscript/paper.qmd` lines 4–7 had malformed author affiliation YAML:
  ```yaml
        - id: uts
            name: University of Technology Sydney
  ```
  `name:` was indented 10 spaces; siblings expect 8 (matching `id:`). Quarto failed with `YAMLException: bad indentation of a mapping entry (7:15)` and exited before running the document, blocking the entire smoke-render verification.
- **Fix:** Re-indented `name:` from 10 → 8 spaces to align with its sibling `id:`.
- **Files modified:** `manuscript/paper.qmd` (line 7)
- **Verification:** Second `quarto render` invocation parses the YAML cleanly, produces PDF at `manuscript/_output/paper.pdf`. The corrected YAML also propagates into paper.tex via `keep-tex: true`, fixing a latent `pdfauthor={..., Coauthor Name}` bug in the metadata stream.
- **Committed in:** `2b26df0` (consolidated Task 1+2 commit)
- **Pre-existing:** Yes — this bug predates Phase 29 and was unrelated to the reorg. It was masked until now because the last `quarto render` in project history was before the YAML got miswritten.

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Blocking fix was necessary for Task 2 to complete. No scope creep; no architectural implications.

## Issues Encountered

- **Parallel-agent git-index race with 29-05 absorbed by the metadata commit.** When I ran `git add .planning/...` and `git commit` for the metadata step, 29-05's in-flight `git mv` renames + deletions (20+ cluster/*.slurm renames/deletes staged but not yet committed by 29-05) were already in the index. `git commit` committed the full index, not just my explicitly-added paths, so commit `955f903` is labelled `docs(29-06): complete paper-qmd-smoke-render plan` but its diff includes 20 cluster SLURM renames/deletions that conceptually belong to 29-05. The actual file moves are correct and match 29-05's intent (verified: cluster/04a_mle_cpu.slurm, cluster/04b_bayesian_cpu.slurm, etc. all present on disk post-commit). This is the same pattern documented in the 29-03 STATE.md note (parallel agent git-index race with 29-04 → first commit transiently absorbed staged utils/ changes, amended second commit dropped them). **Recovery decision:** accept the mislabelled commit rather than reset-and-resplit, because (a) the file changes themselves are correct, (b) `git log --follow` still traces rename history, (c) resetting would risk breaking 29-05's concurrent work. 29-05's SUMMARY should cross-reference `955f903` alongside its own primary commit `f81b999`.
- **Zero cells flagged for Phase 26 follow-up.** All 20 graceful-fallback `{python}` cells in paper.qmd (at lines 338, 432, 444, 453, 463, 471, 488, 536–537, 542, 552, 565, 630, 650, 746, 755, 769, 799, 808, 825) have working try/except wrappers that catch missing cold-start artifacts and emit placeholder tables/figures with `[Phase 24 cold-start will populate]` captions. Phase 26 will replace these placeholders once cold-start runs.
- **5 BibTeX warnings** for missing references are pre-existing manuscript bugs that surfaced via the smoke render. They do NOT affect PDF generation (render still exits 0), but should be resolved in Phase 26 (`references.bib` needs entries for: `phan2019composable`, `hoffman2014no`, `ahn2017revealing`, `vehtari2017practical`, `yao2018using`).

## User Setup Required

None — no external service configuration required. `quarto render` uses local MiKTeX + Quarto 1.9.36; no new tool installations triggered.

## Next Phase Readiness

### Ready for Phase 26 (manuscript finalization)
- `quarto render manuscript/paper.qmd` works end-to-end on the canonical 01–06 tree
- All script-path references in paper.qmd and paper.tex resolve to existing files
- Graceful-fallback cells correctly absorb missing empirical artifacts — once Phase 24 cold-start runs, the same `quarto render` invocation will populate real winner names + real tables without further code changes
- Line 166 caption now points at the merged docs anchor (29-02 cleanup complete in manuscript)

### Ready for Phase 29 Plan 07 (closure guard extension)
- Recommend 29-07 add a pytest that runs `quarto render manuscript/paper.qmd --to html` (HTML is faster than PDF and doesn't need MiKTeX) as a cheap smoke check against future path regressions
- Alternatively: a simpler grep-based invariant like the Task 1 verification in this plan

### Known follow-ups (not blocking Phase 29)
- Phase 26: resolve 5 missing BibTeX entries in `manuscript/references.bib`
- Phase 26: replace `[Phase 24 cold-start will populate]` placeholder captions with real content after cluster cold-start completes

### Parallel coordination with 29-05
- 29-05 is editing `cluster/*.slurm` and `cluster/21_submit_pipeline.sh` concurrently; no overlap with this plan (stayed entirely in `manuscript/`). Git status confirms clean separation: this commit touched only `manuscript/paper.qmd` + `manuscript/paper.tex`.

---
*Phase: 29-pipeline-canonical-reorg*
*Completed: 2026-04-22*

---
phase: 18-integration-comparison-manuscript
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/15_analyze_mle_by_trauma.py
  - scripts/16_regress_parameters_on_scales.py
  - scripts/17_analyze_winner_heterogeneity.py
autonomous: true

must_haves:
  truths:
    - "scripts/15_analyze_mle_by_trauma.py --source bayesian --model wmrl_m3 reads from output/bayesian/ and writes to output/bayesian/analysis/"
    - "scripts/16_regress_parameters_on_scales.py --source bayesian --model wmrl_m3 reads from output/bayesian/ and writes to output/regressions/bayesian/"
    - "scripts/17_analyze_winner_heterogeneity.py --source bayesian reads from output/bayesian/ and writes to output/bayesian/model_comparison/"
    - "All three scripts default to --source mle with identical behavior to current code (backward compatible)"
    - "Bayesian output paths are distinct from MLE paths (no clobbering)"
  artifacts:
    - path: "scripts/15_analyze_mle_by_trauma.py"
      provides: "--source mle|bayesian flag with path routing"
      contains: "add_argument.*--source"
    - path: "scripts/16_regress_parameters_on_scales.py"
      provides: "--source mle|bayesian flag with path routing"
      contains: "add_argument.*--source"
    - path: "scripts/17_analyze_winner_heterogeneity.py"
      provides: "--source mle|bayesian flag with path routing"
      contains: "add_argument.*--source"
  key_links:
    - from: "scripts/15_analyze_mle_by_trauma.py"
      to: "output/bayesian/*_individual_fits.csv"
      via: "--source bayesian resolves fits_dir to output/bayesian/"
      pattern: "output.*bayesian"
    - from: "scripts/16_regress_parameters_on_scales.py"
      to: "output/bayesian/*_individual_fits.csv"
      via: "--source bayesian resolves params_path to output/bayesian/"
      pattern: "output.*bayesian"
    - from: "scripts/17_analyze_winner_heterogeneity.py"
      to: "output/bayesian/*_individual_fits.csv"
      via: "--source bayesian resolves fits_dir to output/bayesian/"
      pattern: "output.*bayesian"
---

<objective>
Add `--source mle|bayesian` flag to scripts 15, 16, and 17 so they can read
either MLE point-estimate CSVs (existing behavior) or hierarchical Bayesian
posterior-mean CSVs (schema-parity from Phase 13 INFRA-04). Only path-routing
changes -- analysis logic is untouched.

Purpose: Enable the downstream analysis pipeline to operate on Bayesian fits
from Phases 15-17 without rewriting any statistical logic. This is the
cornerstone of the schema-parity migration pattern designed in Phase 13.

Output: Three updated scripts with `--source` argparse flag, output path
routing to `output/bayesian/analysis/` and `figures/bayesian/` when
`--source bayesian`, full backward compatibility when `--source mle` (default).
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-integration-comparison-manuscript/18-RESEARCH.md
@scripts/15_analyze_mle_by_trauma.py
@scripts/16_regress_parameters_on_scales.py
@scripts/17_analyze_winner_heterogeneity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --source flag to script 15 (analyze_mle_by_trauma)</name>
  <files>scripts/15_analyze_mle_by_trauma.py</files>
  <action>
  1. In main(), add argparse argument:
     ```
     parser.add_argument('--source', type=str, default='mle',
                         choices=['mle', 'bayesian'],
                         help='Fit source: mle (default) or bayesian')
     ```

  2. Refactor `load_data()` to accept `fits_dir: Path` as its first argument
     instead of using the module-level `OUTPUT_DIR` constant. Default to
     `OUTPUT_DIR` for backward compatibility:
     ```
     def load_data(fits_dir: Path = OUTPUT_DIR) -> tuple:
     ```
     Inside `load_data()`, replace every occurrence of `OUTPUT_DIR` used for
     loading model CSVs with `fits_dir`. The fallback paths that check
     `PROJECT_ROOT / "output" / ..."` for legacy files should also check
     relative to `fits_dir.parent` (i.e., if fits_dir is `output/bayesian`,
     don't fall back to `output/` for legacy paths -- Bayesian CSVs are
     always in `output/bayesian/`).

  3. In main(), after parsing args, resolve the fits directory:
     ```
     if args.source == 'bayesian':
         fits_dir = PROJECT_ROOT / "output" / "bayesian"
     else:
         fits_dir = OUTPUT_DIR
     ```
     Pass `fits_dir` to `load_data(fits_dir)`.

  4. Route output paths when `--source bayesian`:
     Construct local `figures_dir` and `output_dir` variables early in main()
     (before the `FIGURES_DIR.mkdir()` call at line 831) based on `args.source`:
     ```
     if args.source == 'bayesian':
         figures_dir = PROJECT_ROOT / "figures" / "bayesian_trauma_analysis"
         analysis_output_dir = PROJECT_ROOT / "output" / "bayesian" / "analysis"
     else:
         figures_dir = FIGURES_DIR
         analysis_output_dir = OUTPUT_DIR
     ```
     Then replace ALL references to the module-level `FIGURES_DIR` inside main()
     with the local `figures_dir`. Specifically:
     - Line 831: `figures_dir.mkdir(parents=True, exist_ok=True)` (was `FIGURES_DIR.mkdir(...)`)
     - Line 943: `fig.savefig(figures_dir / f"parameters_by_group_{model_key}.png", ...)`
     - Line 950: `fig.savefig(figures_dir / f"correlation_heatmap_{model_key}.png", ...)`
     - Line 956: `fig.savefig(figures_dir / f"forest_plot_group_means_{model_key}.png", ...)`
     - Line 962: `fig.savefig(figures_dir / f"scatter_key_correlations_{model_key}.png", ...)`
     All five lines must use the local `figures_dir`, not the module-level constant.

     Similarly, replace any CSV output paths that use `OUTPUT_DIR` in main()
     with `analysis_output_dir`.

     Do NOT modify the module-level constants (they remain as MLE defaults
     for backward compatibility when other code imports them).

  5. Print a banner showing which source is active:
     ```
     print(f"Source: {args.source.upper()}")
     ```

  Do NOT change any analysis function signatures or statistical logic. Only
  path resolution in load_data() and output path routing in main().
  </action>
  <verify>
  `python scripts/15_analyze_mle_by_trauma.py --help` shows `--source` argument.
  `python scripts/15_analyze_mle_by_trauma.py --source mle --model qlearning` produces
  identical output to the current behavior (files in output/mle/ and figures/mle_trauma_analysis/).
  Verify by checking that `--source` flag appears in `grep -n "add_argument.*source" scripts/15_analyze_mle_by_trauma.py`.
  </verify>
  <done>Script 15 accepts --source mle|bayesian. Default (mle) behavior unchanged. Bayesian path routes to output/bayesian/analysis/ and figures/bayesian_trauma_analysis/.</done>
</task>

<task type="auto">
  <name>Task 2: Add --source flag to script 16 (regress_parameters_on_scales)</name>
  <files>scripts/16_regress_parameters_on_scales.py</files>
  <action>
  1. In main(), add argparse argument:
     ```
     parser.add_argument('--source', type=str, default='mle',
                         choices=['mle', 'bayesian'],
                         help='Fit source: mle (default) or bayesian')
     ```

  2. Script 16 already takes `--output-dir` and `--figures-dir` as CLI args.
     The model CSV auto-detection logic (lines ~754-761) hard-codes
     `Path(f'output/mle/{model}_individual_fits.csv')`. Change this to:
     ```
     if args.source == 'bayesian':
         base_fits_dir = Path('output/bayesian')
     else:
         base_fits_dir = Path('output/mle')
     ```
     Then use `base_fits_dir / f'{model}_individual_fits.csv'` for primary
     path, with fallback to `Path(f'output/{model}_individual_fits.csv')`
     only when source is 'mle'.

  3. Route output directories when `--source bayesian`:
     - Default `--output-dir` becomes `output/regressions/bayesian` (instead
       of `output/regressions`)
     - Default `--figures-dir` becomes `figures/regressions/bayesian` (instead
       of `figures/regressions`)
     - Only change the defaults; if user explicitly passes `--output-dir`, honor it.
     - Implementation: after parsing, check `if args.source == 'bayesian'`
       and if the output-dir/figures-dir are still at their argparse defaults,
       append `/bayesian` to the path.

  4. Print a banner showing which source is active.

  Do NOT change `load_integrated_data()` function signature or any analysis
  logic. Only path construction in main().
  </action>
  <verify>
  `python scripts/16_regress_parameters_on_scales.py --help` shows `--source` argument.
  `grep -n "add_argument.*source" scripts/16_regress_parameters_on_scales.py` returns a match.
  </verify>
  <done>Script 16 accepts --source mle|bayesian. Default (mle) behavior unchanged. Bayesian path routes to output/regressions/bayesian/ and figures/regressions/bayesian/.</done>
</task>

<task type="auto">
  <name>Task 3: Add --source flag to script 17 (analyze_winner_heterogeneity)</name>
  <files>scripts/17_analyze_winner_heterogeneity.py</files>
  <action>
  1. Add argparse to main() (currently has no argument parsing). Create a parser:
     ```
     parser = argparse.ArgumentParser(
         description='Per-participant winning model heterogeneity analysis'
     )
     parser.add_argument('--source', type=str, default='mle',
                         choices=['mle', 'bayesian'],
                         help='Fit source: mle (default) or bayesian')
     args = parser.parse_args()
     ```
     Add `import argparse` at the top of the file (after existing imports).

  2. In main(), the local variable `mle_dir` (line 281) is already used as
     the fits directory. Route it based on source:
     ```
     if args.source == 'bayesian':
         fits_dir = project_root / "output" / "bayesian"
         output_dir = project_root / "output" / "bayesian" / "model_comparison"
         figures_dir = FIGURES_DIR / "bayesian" / "model_comparison"
     else:
         fits_dir = project_root / "output" / "mle"
         output_dir = project_root / "output" / "model_comparison"
         figures_dir = FIGURES_DIR / "model_comparison"
     ```
     Rename `mle_dir` to `fits_dir` in all downstream calls:
     - `load_per_participant_aic(fits_dir)` (line 292)
     - `attach_m6b_parameters(winners, fits_dir)` (line 302)

  3. Print a banner showing which source is active.

  Do NOT change `load_per_participant_aic()`, `attach_m6b_parameters()`,
  `summarize_by_winner_group()`, or `plot_winner_heterogeneity()` — they
  already accept directory paths as parameters. Only path routing in main().
  </action>
  <verify>
  `python scripts/17_analyze_winner_heterogeneity.py --help` shows `--source` argument.
  `grep -n "add_argument.*source" scripts/17_analyze_winner_heterogeneity.py` returns a match.
  </verify>
  <done>Script 17 accepts --source mle|bayesian. Default (mle) behavior unchanged. Bayesian paths route to output/bayesian/model_comparison/ and figures/bayesian/model_comparison/.</done>
</task>

</tasks>

<verification>
1. All three scripts parse `--source` without error: run `--help` on each.
2. All three scripts produce identical output with `--source mle` as they do
   without the flag (backward compatibility).
3. `grep -rn "add_argument.*source" scripts/15_analyze_mle_by_trauma.py scripts/16_regress_parameters_on_scales.py scripts/17_analyze_winner_heterogeneity.py` returns 3 matches.
4. No analysis function signatures changed (diff should show only path routing).
</verification>

<success_criteria>
- MIG-01: Script 15 has --source flag, routes to output/bayesian/ when bayesian
- MIG-02: Script 16 has --source flag, routes to output/regressions/bayesian/ when bayesian
- MIG-03: Script 17 has --source flag, routes to output/bayesian/model_comparison/ when bayesian
- All three default to mle with zero behavioral change
</success_criteria>

<output>
After completion, create `.planning/phases/18-integration-comparison-manuscript/18-01-SUMMARY.md`
</output>

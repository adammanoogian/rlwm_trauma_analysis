---
phase: 14-collins-k-refit-gpu-lba-batching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fitting/mle_utils.py
  - scripts/fitting/fit_mle.py
  - config.py
autonomous: true

must_haves:
  truths:
    - "All 6 capacity-bearing BOUNDS dicts in mle_utils.py have capacity bounds (2.0, 6.0)"
    - "QLEARNING_BOUNDS is unchanged (no capacity key)"
    - "ModelParams.WM_CAPACITY_MIN == 2 and WM_CAPACITY_MAX == 6 in config.py"
    - "`fits_df['parameterization_version']` is set from EXPECTED_PARAMETERIZATION[args.model] before CSV save in fit_mle.py main()"
    - "sample_lhs_starts('wmrl_m3', 10) produces capacity values in [2.0, 6.0]"
  artifacts:
    - path: "scripts/fitting/mle_utils.py"
      provides: "Constrained K bounds [2,6] for all 6 WM-containing models"
      contains: "'capacity': (2.0, 6.0)"
    - path: "scripts/fitting/fit_mle.py"
      provides: "parameterization_version stamping in MLE CSV output"
      contains: "EXPECTED_PARAMETERIZATION"
    - path: "config.py"
      provides: "Updated WM_CAPACITY_MIN/MAX for documentation consistency"
      contains: "WM_CAPACITY_MIN = 2"
  key_links:
    - from: "scripts/fitting/mle_utils.py"
      to: "scripts/fitting/fit_mle.py"
      via: "BOUNDS dicts consumed by sample_lhs_starts and transform functions"
      pattern: "WMRL.*_BOUNDS"
    - from: "scripts/fitting/fit_mle.py"
      to: "config.py"
      via: "Import EXPECTED_PARAMETERIZATION for version stamping"
      pattern: "EXPECTED_PARAMETERIZATION"
---

<objective>
Constrain WM capacity K bounds from [1,7] to [2,6] in all model BOUNDS dicts and stamp `parameterization_version` on MLE CSV output.

Purpose: K-01 research (Phase 13) established that K < 2 is structurally non-identified (confounded with rho at ns=2) and K > 6 is non-identified (task max set-size = 6). Tightening bounds to [2,6] removes the non-identifiable region and is the prerequisite for the K recovery refit (K-03). The version stamp enables `load_fits_with_validation()` to reject stale v3.0 fits downstream.

Output: Updated bounds in `mle_utils.py`, version stamp injection in `fit_mle.py` main(), documentation constants updated in `config.py`.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-collins-k-refit-gpu-lba-batching/14-RESEARCH.md

Key files to read before starting:
- scripts/fitting/mle_utils.py:37-106 (all 7 BOUNDS dicts — 6 have capacity)
- scripts/fitting/fit_mle.py:2955-2966 (main() CSV save point)
- config.py:147-148 (WM_CAPACITY_MIN/MAX), :497-505 (EXPECTED_PARAMETERIZATION)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Constrain K bounds [1,7] to [2,6] in all BOUNDS dicts</name>
  <files>
    scripts/fitting/mle_utils.py
    config.py
  </files>
  <action>
In `scripts/fitting/mle_utils.py`, change `'capacity': (1.0, 7.0)` to `'capacity': (2.0, 6.0)` in all 6 dicts that have a capacity key:
- WMRL_BOUNDS (line ~43)
- WMRL_M3_BOUNDS (line ~53)
- WMRL_M5_BOUNDS (line ~64)
- WMRL_M6A_BOUNDS (line ~75)
- WMRL_M6B_BOUNDS (line ~87)
- WMRL_M4_BOUNDS (line ~100)

Do NOT touch QLEARNING_BOUNDS (it has no capacity key — M1 is Q-learning only).

In `config.py`, update ModelParams documentation constants for consistency:
- `WM_CAPACITY_MIN = 1` → `WM_CAPACITY_MIN = 2`
- `WM_CAPACITY_MAX = 7` → `WM_CAPACITY_MAX = 6`

Leave `WM_CAPACITY_MU_PRIOR = 4.0` and `WM_CAPACITY_SIGMA_PRIOR = 1.5` unchanged (prior mean 4.0 is centered in [2,6]).
  </action>
  <verify>
Run: `python -c "from scripts.fitting.mle_utils import WMRL_BOUNDS, WMRL_M3_BOUNDS, WMRL_M5_BOUNDS, WMRL_M6A_BOUNDS, WMRL_M6B_BOUNDS, WMRL_M4_BOUNDS, QLEARNING_BOUNDS; [print(f'{n}: {d[\"capacity\"]}') for n, d in [('WMRL', WMRL_BOUNDS), ('M3', WMRL_M3_BOUNDS), ('M5', WMRL_M5_BOUNDS), ('M6a', WMRL_M6A_BOUNDS), ('M6b', WMRL_M6B_BOUNDS), ('M4', WMRL_M4_BOUNDS)]; assert 'capacity' not in QLEARNING_BOUNDS; print('OK: all [2.0, 6.0]')"`
All 6 dicts should print `(2.0, 6.0)`.

Run: `python -c "from config import ModelParams; assert ModelParams.WM_CAPACITY_MIN == 2 and ModelParams.WM_CAPACITY_MAX == 6; print('config OK')"`
  </verify>
  <done>All 6 capacity-bearing BOUNDS dicts use (2.0, 6.0). QLEARNING_BOUNDS unchanged. ModelParams constants match.</done>
</task>

<task type="auto">
  <name>Task 2: Stamp parameterization_version in fit_mle.py main()</name>
  <files>
    scripts/fitting/fit_mle.py
  </files>
  <action>
In `scripts/fitting/fit_mle.py`:

1. Add import at the top of the file (near other config imports): `from config import EXPECTED_PARAMETERIZATION`

2. In `main()`, just BEFORE the line `fits_df.to_csv(fits_path, index=False)` (~line 2965), add:
```python
    # Stamp parameterization version for downstream validation
    fits_df['parameterization_version'] = EXPECTED_PARAMETERIZATION[args.model]
```

This ensures both the CPU path (fit_all_participants) and the GPU path (fit_all_gpu) get the version column stamped, because both paths converge to `fits_df` in `main()` before the single `to_csv()` call.

Verify the import exists and the stamp is before the save. Do not add the stamp inside `fit_all_gpu()` or `fit_all_participants()` — it belongs in `main()` where the model name is known from CLI args.
  </action>
  <verify>
Run: `python -c "import ast; tree = ast.parse(open('scripts/fitting/fit_mle.py').read()); print('parsed OK')"` to verify syntax.

Grep for the stamp: `grep -n 'EXPECTED_PARAMETERIZATION' scripts/fitting/fit_mle.py` should show both the import and the stamp line.

Grep ordering: confirm the stamp line number is less than the `fits_df.to_csv` line number.
  </verify>
  <done>`fit_mle.py` imports EXPECTED_PARAMETERIZATION from config and stamps it on fits_df before CSV save. Both CPU and GPU paths get the stamp.</done>
</task>

<task type="auto">
  <name>Task 3: Verify LHS sampler respects new bounds</name>
  <files>
    scripts/fitting/mle_utils.py
  </files>
  <action>
Run a quick verification that `sample_lhs_starts` produces capacity values within [2.0, 6.0] for all models with capacity. This is a read-only check — the function already consumes BOUNDS dicts at runtime, so it should work automatically.

Execute:
```python
from scripts.fitting.mle_utils import sample_lhs_starts
import numpy as np

for model in ['wmrl', 'wmrl_m3', 'wmrl_m5', 'wmrl_m6a', 'wmrl_m6b', 'wmrl_m4']:
    starts = sample_lhs_starts(model, 100, seed=42)
    starts_np = np.array(starts)
    # capacity is parameter index 4 for all WM models
    cap_idx = 4
    caps = starts_np[:, cap_idx]
    assert caps.min() >= 2.0, f"{model}: capacity min {caps.min()} < 2.0"
    assert caps.max() <= 6.0, f"{model}: capacity max {caps.max()} > 6.0"
    print(f"{model}: capacity range [{caps.min():.3f}, {caps.max():.3f}] OK")
print("All models: capacity in [2.0, 6.0]")
```

If any assertion fails, debug the `sample_lhs_starts` function to trace which BOUNDS dict it is reading. The function uses `BOUNDS_REGISTRY[model]` — confirm the registry maps to the updated dicts.
  </action>
  <verify>All 6 models produce capacity starts in [2.0, 6.0]. No assertion errors.</verify>
  <done>LHS sampler confirmed to respect new [2.0, 6.0] capacity bounds for all 6 WM-containing models.</done>
</task>

</tasks>

<verification>
1. All 6 BOUNDS dicts in mle_utils.py have `'capacity': (2.0, 6.0)` — grep count should be exactly 6.
2. QLEARNING_BOUNDS has no capacity key.
3. `EXPECTED_PARAMETERIZATION` imported and used in fit_mle.py main() before to_csv.
4. `ModelParams.WM_CAPACITY_MIN == 2`, `WM_CAPACITY_MAX == 6`.
5. `sample_lhs_starts` produces values in [2.0, 6.0] for all 6 WM models.
6. Existing tests pass: `python -m pytest scripts/fitting/tests/ -x -q`
</verification>

<success_criteria>
- K-02 complete: All 6 capacity bounds changed from (1.0, 7.0) to (2.0, 6.0)
- K-03 partial: Version stamp injection code ready (cluster refit is a separate execution step)
- No test regressions in fitting test suite
</success_criteria>

<output>
After completion, create `.planning/phases/14-collins-k-refit-gpu-lba-batching/14-01-SUMMARY.md`
</output>

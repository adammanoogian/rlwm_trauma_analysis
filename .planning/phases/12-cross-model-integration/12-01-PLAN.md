---
phase: 12-cross-model-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fitting/model_recovery.py
  - scripts/11_run_model_recovery.py
autonomous: true

must_haves:
  truths:
    - "run_model_recovery_check() accepts a configurable comparison_models list defaulting to all 6 choice-only models"
    - "Script 11 has a --mode cross-model flag that calls run_model_recovery_check for each generating model"
    - "Cross-model recovery produces a confusion matrix (rows=generator, cols=winner) printed to console and saved as CSV"
    - "M4 is excluded from cross-model AIC comparison (joint likelihood incommensurable with choice-only)"
    - "Quick smoke test (N=5 subjects, 2 datasets) for wmrl_m5 confirms generating model wins by AIC"
  artifacts:
    - path: "scripts/fitting/model_recovery.py"
      provides: "Extended run_model_recovery_check with configurable comparison_models parameter"
      contains: "wmrl_m5.*wmrl_m6a.*wmrl_m6b"
    - path: "scripts/11_run_model_recovery.py"
      provides: "CLI --mode cross-model flag for cross-model recovery"
      contains: "cross-model"
  key_links:
    - from: "scripts/11_run_model_recovery.py --mode cross-model"
      to: "scripts/fitting/model_recovery.py run_model_recovery_check"
      via: "import and call with each generating model"
      pattern: "run_model_recovery_check"
    - from: "run_model_recovery_check"
      to: "scripts/12_fit_mle.py"
      via: "subprocess call for each model in comparison_models"
      pattern: "subprocess.*12_fit_mle"
---

<objective>
Extend cross-model recovery validation so that each choice-only generating model (M5, M6a, M6b) can be verified to win by AIC against all other choice-only models. This closes INTG-04.

Purpose: Model recovery is the identifiability validation. Without cross-model recovery, we cannot confirm that the new models are distinguishable from each other by AIC. The existing run_model_recovery_check() only compares M1-M3; it needs to include M5, M6a, M6b.

Output: Extended model_recovery.py and updated script 11 with --mode cross-model. Confusion matrix output.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cross-model-integration/12-RESEARCH.md
@scripts/fitting/model_recovery.py
@scripts/11_run_model_recovery.py
@scripts/fitting/mle_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend run_model_recovery_check() with configurable comparison models</name>
  <files>scripts/fitting/model_recovery.py</files>
  <action>
  Modify `run_model_recovery_check()` (currently at line ~1206) to accept a `comparison_models` parameter:

  1. Change the function signature to:
     ```python
     def run_model_recovery_check(
         synthetic_data_path: str,
         generative_model: str,
         output_dir: Path,
         comparison_models: list[str] | None = None,
         use_gpu: bool = False,
         n_jobs: int = 1,
         verbose: bool = True
     ) -> dict:
     ```

  2. Replace the hardcoded `models = ['qlearning', 'wmrl', 'wmrl_m3']` (line 1253) with:
     ```python
     CHOICE_ONLY_MODELS = ['qlearning', 'wmrl', 'wmrl_m3', 'wmrl_m5', 'wmrl_m6a', 'wmrl_m6b']
     if comparison_models is None:
         models = CHOICE_ONLY_MODELS
     else:
         models = comparison_models
     ```

  3. Ensure the generative_model is included in the comparison set. Add a guard:
     ```python
     if generative_model not in models:
         models = [generative_model] + models
     ```

  4. Add a new function `run_cross_model_recovery()` below `run_model_recovery_check()` that:
     - Takes `generating_models` (list of models to generate from), `n_subjects`, `n_datasets`, `seed`, `use_gpu`, `n_jobs`, `n_starts`, `verbose` parameters
     - For each generating model, calls `sample_parameters()` and `generate_synthetic_dataset()` (the existing functions), saves synthetic data to a temp directory
     - Then calls `run_model_recovery_check()` with the default choice-only comparison set
     - Aggregates results across all generating models into a confusion matrix (pandas DataFrame with rows=generator, columns=winner, values=count of datasets where that model won)
     - Returns dict with `confusion_matrix` (DataFrame), `per_generator_results` (list of dicts), and `all_pass` (bool)
     - Prints the confusion matrix and per-generator pass/fail summary

  5. For synthetic data generation, use the existing `generate_synthetic_dataset()` function if it exists, or compose from `sample_parameters()` + `generate_synthetic_participant()` in a loop. Save the concatenated DataFrame to `output_dir / f'{gen_model}_synthetic_data.csv'`.

  6. Do NOT include wmrl_m4 in the default CHOICE_ONLY_MODELS list or in cross-model recovery. M4 uses joint choice+RT likelihood whose AIC is incommensurable with choice-only models.

  7. Update the function's docstring to reflect the new parameter and the rationale for excluding M4.
  </action>
  <verify>
  Run `python -c "from scripts.fitting.model_recovery import run_model_recovery_check, run_cross_model_recovery; print('imports OK')"` to confirm both functions are importable without error.
  </verify>
  <done>
  run_model_recovery_check() accepts comparison_models parameter (default: 6 choice-only models). run_cross_model_recovery() exists and is importable. M4 excluded from choice-only comparison set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --mode cross-model to script 11 and run smoke test</name>
  <files>scripts/11_run_model_recovery.py</files>
  <action>
  1. Add import of `run_cross_model_recovery` from `scripts.fitting.model_recovery`.

  2. Add `--mode` argparse argument with choices `['parameter', 'cross-model']` and default `'parameter'`:
     ```python
     parser.add_argument('--mode', type=str, default='parameter',
                         choices=['parameter', 'cross-model'],
                         help='Recovery mode: "parameter" (default) recovers params for one model, '
                              '"cross-model" generates from each model and checks AIC winner')
     ```

  3. In `main()`, after parsing args, branch on `args.mode`:
     - If `args.mode == 'parameter'`: existing behavior (run_recovery_for_model loop) unchanged.
     - If `args.mode == 'cross-model'`:
       - Determine generating_models: if `args.model == 'all'`, use `['qlearning', 'wmrl', 'wmrl_m3', 'wmrl_m5', 'wmrl_m6a', 'wmrl_m6b']` (choice-only only). If a specific model is given, use just that model.
       - Call `run_cross_model_recovery()` with the appropriate parameters.
       - Print the confusion matrix.
       - Save confusion matrix CSV to `output/recovery/cross_model_confusion.csv`.
       - Exit with code 0 if all generators won, 1 otherwise.

  4. Update the script docstring at the top to document the new --mode flag:
     ```
     # Cross-model recovery (generates from each model, checks AIC winner)
     python scripts/11_run_model_recovery.py --mode cross-model --model all --n-subjects 10 --n-datasets 3
     ```

  5. Run a quick smoke test to verify the import and argparse work:
     `python scripts/11_run_model_recovery.py --mode cross-model --model wmrl_m5 --n-subjects 5 --n-datasets 1 --n-starts 5 --n-jobs 1`
     This should generate data from M5, fit all 6 choice-only models to it, and report whether M5 wins. With N=5 subjects and 1 dataset, this is a fast sanity check (not a full recovery validation).

  NOTE: The full cross-model recovery (N=50, n_datasets=10) is intended to run on the cluster, not locally. The smoke test here just validates the code path.
  </action>
  <verify>
  Run `python scripts/11_run_model_recovery.py --help` and confirm `--mode` appears with choices `parameter` and `cross-model`.
  Then run the smoke test: `python scripts/11_run_model_recovery.py --mode cross-model --model wmrl_m5 --n-subjects 5 --n-datasets 1 --n-starts 5 --n-jobs 1` and confirm it completes without error and prints a confusion matrix.
  </verify>
  <done>
  Script 11 accepts --mode cross-model. Smoke test completes without error. Confusion matrix printed to console. M4 excluded from cross-model comparison.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from scripts.fitting.model_recovery import run_model_recovery_check, run_cross_model_recovery; print('OK')"` succeeds
2. `python scripts/11_run_model_recovery.py --help` shows --mode with parameter and cross-model choices
3. Smoke test `python scripts/11_run_model_recovery.py --mode cross-model --model wmrl_m5 --n-subjects 5 --n-datasets 1 --n-starts 5 --n-jobs 1` completes and prints confusion matrix
4. Existing parameter recovery mode still works: `python scripts/11_run_model_recovery.py --mode parameter --model qlearning --n-subjects 2 --n-datasets 1 --n-starts 5` (quick test)
</verification>

<success_criteria>
- run_model_recovery_check() comparison set includes all 6 choice-only models by default
- run_cross_model_recovery() generates from each model and produces confusion matrix
- Script 11 --mode cross-model flag works end-to-end
- M4 excluded from cross-model AIC comparison
- Existing --mode parameter (default) behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/12-cross-model-integration/12-01-SUMMARY.md`
</output>

---
phase: 23-tech-debt-sweep-pre-flight-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/__pycache__/16b_bayesian_regression.cpython-313.pyc
  - docs/03_methods_reference/MODEL_REFERENCE.md
  - scripts/fitting/tests/test_no_16b_references.py
autonomous: true

must_haves:
  truths:
    - "`find . -path ./.git -prune -o -name '16b*' -print` returns zero source files (only possibly `.pyc` cache files, which are regenerable)."
    - "The ROADMAP SC#3 raw command `find . -name '16b*'` is to be interpreted in practice as `find . -path ./.planning -prune -o -name '16b*' -print` (or equivalent) — the `.planning/` archival tree is deliberately excluded per v4.0 closure protocol (see discovery_notes below), matching the guard test's skip set."
    - "No live Python source under scripts/ or cluster/ references `16b_bayesian_regression`."
    - "docs/03_methods_reference/MODEL_REFERENCE.md contains no live cross-reference to `scripts/16b_bayesian_regression.py`."
    - "pytest scripts/fitting/tests/ -v passes after removal."
    - "python validation/check_v4_closure.py --milestone v4.0 exits 0 after removal."
  artifacts:
    - path: "scripts/fitting/tests/test_no_16b_references.py"
      provides: "Grep-invariant guard asserting no live code or docs reference 16b_bayesian_regression; also asserts no `16b*` file exists outside .planning/"
      min_lines: 20
  key_links:
    - from: "scripts/fitting/tests/test_no_16b_references.py"
      to: "scripts/ + cluster/ + docs/03_methods_reference/"
      via: "recursive grep for '16b_bayesian_regression' / 'scripts/16b'"
      pattern: "16b_bayesian_regression|scripts/16b"
---

<objective>
Close CLEAN-03 by (a) deleting the orphaned `__pycache__/16b_bayesian_regression.cpython-313.pyc` cache file, (b) scrubbing the remaining `MODEL_REFERENCE.md` cross-reference to `16b_bayesian_regression.py`, and (c) installing a pytest guard that fails CI if any future commit reintroduces a `16b_bayesian_regression` reference in live code, cluster SLURM files, or user-facing docs.

Purpose: The Python source file `scripts/16b_bayesian_regression.py` was already deleted in a prior cleanup (grep during planning found only the stale `.pyc` in __pycache__ and one active docs cross-reference). This plan finishes the job so Phase 24 cold-start runs never need to debug a "why is 16b referenced?" question.

Output: Pycache file removed; MODEL_REFERENCE.md cross-reference updated; new invariant guard test; one or two atomic commits; pytest + v4.0 closure guard green.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@docs/03_methods_reference/MODEL_REFERENCE.md
</context>

<discovery_notes>
Pre-flight grep performed during planning (2026-04-19):

- `find scripts -name "16b*"` → only `scripts/__pycache__/16b_bayesian_regression.cpython-313.pyc` (bytecode cache, no .py source exists).
- `grep -rn "16b_bayesian\|scripts/16b" cluster/` → **zero matches**. (Already clean — no cluster SLURM files reference 16b. SC#3's "cluster SLURM references removed" is already satisfied.)
- `grep -rn "16b_bayesian\|scripts/16b" docs/` → text matches: the only live match is `docs/03_methods_reference/MODEL_REFERENCE.md` (the binary match against the Senta PDF is coincidental and OUT OF SCOPE — the grep hit "16b" in the paper filename). The MODEL_REFERENCE.md line will need to be located and updated in Task 2.
- `.planning/` contains historical references (v4.0-ROADMAP.md, Phase 13 plans, Phase 18 plans) to `16b_bayesian_regression.py`. These are archival / historical documents — per v4.0 closure protocol and CLAUDE.md "no duplicate information / no `/legacy` marking", these should NOT be edited. The invariant guard test should SKIP the `.planning/` tree.
- Git history contains many references — out of scope; commit messages are immutable.

**SC#3 find-command scope clarification (plan-checker WARNING #4 resolution):**

ROADMAP.md Phase 23 SC#3 lists the raw command `find . -name "16b*"` without explicit prune flags. In this plan, that success criterion is to be interpreted as:

```bash
find . -path ./.planning -prune -o -name "16b*" -print
```

(or any equivalent form that excludes the `.planning/` archival tree — e.g. piping through `grep -v '^./.planning/'`). The `.planning/` exclusion is an **explicit policy decision**, not an oversight:

- The v4.0 closure protocol and CLAUDE.md "no `/legacy` marking" rule treat `.planning/` as an immutable archival record of completed work.
- Historical phase plans (Phase 13, Phase 18) and the archived `.planning/milestones/v4.0-*.md` bundle legitimately reference the old `scripts/16b_bayesian_regression.py` by name as part of their record.
- Rewriting those archival mentions would corrupt project history.

The guard test deliberately skips `.planning/` to match this policy. To keep the SC#3 raw command and the guard test enforcement aligned, the guard test also asserts that no file named `16b*` exists **outside** `.planning/` — same behaviour as the pruned `find` command, stronger than the raw form.

Archive-policy allowance for `.planning/milestones/v4.0-*.md` references to 16b is explicit.
</discovery_notes>

<tasks>

<task type="auto">
  <name>Task 1: Write grep-invariant guard for 16b references</name>
  <files>scripts/fitting/tests/test_no_16b_references.py</files>
  <action>
Create `scripts/fitting/tests/test_no_16b_references.py` with pytest functions that:

1. `test_no_16b_text_references_in_live_source()`:
   - Walks these directory trees (resolve from repo root via `Path(__file__).resolve().parents[3]`):
     - `scripts/` (excluding `__pycache__/` and `tests/fixtures/`)
     - `cluster/`
     - `docs/03_methods_reference/`
   - For each `.py`, `.slurm`, `.sh`, `.md` file, reads source as text and asserts neither pattern occurs:
     - `16b_bayesian_regression`
     - `scripts/16b` (path reference)
   - EXPLICITLY SKIPS these trees (they are historical / archival and OUT OF SCOPE):
     - `.planning/` (archival phase plans + milestone docs)
     - `.git/`
     - `**/__pycache__/**`
     - Binary files (guard the read with `errors='ignore'` or skip extensions other than .py/.slurm/.sh/.md)
   - On failure, prints file path + line number + offending line per CLAUDE.md error-message convention.

2. `test_no_16b_files_outside_planning_tree()` (NEW — bridges the ROADMAP SC#3 wording gap):
   - Walks the entire repo root.
   - Asserts no file (at any depth) whose basename matches the glob `16b*` exists **outside** the `.planning/` tree and outside `.git/`.
   - This is the file-existence analog of `find . -path ./.planning -prune -o -name '16b*' -print | grep -v '^$'` returning empty.
   - Rationale: SC#3's raw `find . -name "16b*"` command must be interpreted with the archival-policy `.planning/` exclusion (see discovery_notes). This assertion gives CI a deterministic check that matches that interpretation.
   - Explicitly asserts `scripts/16b_bayesian_regression.py` does NOT exist at the top level (the source file is already gone; this locks it in).
   - Pycache files (`*.pyc`) under `__pycache__/` outside `.planning/` also count as violations — the test will fail if `scripts/__pycache__/16b_bayesian_regression.cpython-*.pyc` reappears.

Follow project conventions:
- `from __future__ import annotations` at top.
- NumPy-style docstring on each function.
- Python 3.10+ type hints.
- Absolute imports only.
- 88-char line limit.

Module docstring: "CLEAN-03 enforcement: no live source reference to 16b_bayesian_regression in scripts/, cluster/, or docs/03_methods_reference/; and no `16b*` file anywhere outside `.planning/` (archival tree deliberately excluded per v4.0 closure protocol). The source file was deleted in a prior cleanup; this guard prevents silent reintroduction via `git revert` or copy-paste."

**Commit after this task:** stage ONLY the new test file. Commit message:
```
chore(tech-debt): add guard test for 16b_bayesian_regression references

CLEAN-03 enforcement: pytest guards that fail if any live source file
(scripts/, cluster/, docs/03_methods_reference/) reintroduces a
16b_bayesian_regression reference, or if any 16b* file reappears
outside the .planning/ archival tree. Archival .planning/ docs are
intentionally skipped — they are historical records of the superseded
pipeline, locked by v4.0 closure protocol.
```

Note: these guards will likely FAIL immediately because MODEL_REFERENCE.md still has a live reference and/or the stale pycache still exists. That's expected — Task 2 fixes both.
  </action>
  <verify>
`python -m pytest scripts/fitting/tests/test_no_16b_references.py -v` FAILS because `docs/03_methods_reference/MODEL_REFERENCE.md` still references 16b_bayesian_regression and/or the `.pyc` cache file still exists outside `.planning/`. The failure messages must correctly identify the file + line (for the text guard) or file path (for the file-existence guard).
  </verify>
  <done>
- `scripts/fitting/tests/test_no_16b_references.py` exists, follows conventions.
- Both guard functions are defined.
- Tests fail with the expected MODEL_REFERENCE.md text violation and/or pycache file-existence violation (not a syntax/import error).
- One `chore(tech-debt): ...` commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove 16b pycache, update MODEL_REFERENCE.md, verify closure</name>
  <files>
  - scripts/__pycache__/16b_bayesian_regression.cpython-313.pyc
  - docs/03_methods_reference/MODEL_REFERENCE.md
  </files>
  <action>
1. **Delete the pycache file:**
   ```bash
   rm -f scripts/__pycache__/16b_bayesian_regression.cpython-313.pyc
   ```
   (If other Python versions have cached variants like `.cpython-310.pyc`, delete those too: `find scripts/__pycache__ -name "16b_bayesian_regression*" -delete`.)

2. **Update `docs/03_methods_reference/MODEL_REFERENCE.md`:**
   - Grep the file for `16b_bayesian` / `scripts/16b` to locate the exact line(s):
     ```bash
     grep -n "16b_bayesian\|scripts/16b" docs/03_methods_reference/MODEL_REFERENCE.md
     ```
   - For each match, either:
     (a) Delete the line/paragraph entirely if it's describing a superseded workflow, OR
     (b) Replace the `scripts/16b_bayesian_regression.py` path with `scripts/18_bayesian_level2_effects.py` (the Phase 18 schema-parity replacement) + a one-line note: "Formerly `scripts/16b_bayesian_regression.py` (deleted in v5.0 Phase 23 — superseded by the Phase 18 + Phase 21 hierarchical pipeline)."
   - Choose (a) or (b) per paragraph based on whether surrounding text still makes sense without the old reference. If unsure, prefer (a) (delete) — per CLAUDE.md "Avoid clutter: remove outdated sections rather than marking them deprecated".

3. **Scrub cluster/ as a sanity check (expected to already be clean):**
   ```bash
   grep -rn "16b_bayesian\|scripts/16b" cluster/
   ```
   Must return zero lines. If any appear (unexpected given pre-flight), delete or update them.

4. **Re-run the guard test from Task 1:**
   ```bash
   python -m pytest scripts/fitting/tests/test_no_16b_references.py -v
   ```
   MUST now pass — both the text-reference guard and the file-existence guard.

5. **Run the full fitting test suite + v4.0 closure guard:**
   ```bash
   python -m pytest scripts/fitting/tests/ -v
   python validation/check_v4_closure.py --milestone v4.0
   ```
   Both must be green.

6. **Confirm the SC#3-interpreted find command returns empty:**
   ```bash
   find . -path ./.planning -prune -o -path ./.git -prune -o -name "16b*" -print
   ```
   Must return zero output lines. (This is the practical interpretation of ROADMAP SC#3's raw `find . -name "16b*"` — see discovery_notes for the `.planning/` exclusion rationale.)

**Commit after this task:** stage the pycache deletion + the MODEL_REFERENCE.md edit. Commit message:
```
chore(tech-debt): remove residual 16b_bayesian_regression traces

CLEAN-03 resolution:
- Delete stale __pycache__/16b_bayesian_regression.cpython-313.pyc
  (Python source was deleted in a prior cleanup; pycache was orphaned).
- Scrub live cross-reference in docs/03_methods_reference/MODEL_REFERENCE.md
  (16b superseded by Phase 18 schema-parity pipeline + Phase 21
  hierarchical refits).
- Guard test `test_no_16b_references.py` (added in previous commit)
  now passes — both the text-reference check and the file-existence
  check.

Cluster SLURM already clean (pre-flight grep confirmed zero matches).

Closure guard: python validation/check_v4_closure.py --milestone v4.0
exits 0 — no v4.0 invariants broken.
```

Use `git add` with the specific file paths (not `git add .`) to avoid accidentally staging unrelated unstaged files from the repo.
  </action>
  <verify>
1. `find scripts -name "16b_bayesian_regression*"` → zero lines.
2. `find . -path ./.planning -prune -o -path ./.git -prune -o -name "16b*" -print` → zero lines.
3. `grep -rn "16b_bayesian\|scripts/16b" scripts/ cluster/ docs/03_methods_reference/` → zero lines.
4. `python -m pytest scripts/fitting/tests/ -v` → all green (both guard functions now pass).
5. `python validation/check_v4_closure.py --milestone v4.0` → exit 0.
6. `git log --oneline -2` shows the two new `chore(tech-debt)` commits from this plan.
  </verify>
  <done>
- Pycache file gone; MODEL_REFERENCE.md scrubbed.
- Both guard functions pass; full fitting suite green; v4.0 closure guard green.
- The pruned-find command (the practical interpretation of SC#3) returns empty.
- Two atomic commits landed, each with `chore(tech-debt): ...` prefix and NO Co-Authored-By line.
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
# CLEAN-03 success criteria from ROADMAP Phase 23 SC#3
# NOTE: SC#3 raw command `find . -name "16b*"` is interpreted in practice with
# .planning/ pruned — the archival tree is deliberately preserved per v4.0
# closure protocol. See discovery_notes for rationale.
find . -path ./.git -prune -o -path ./.planning -prune -o -name "16b*" -print   # zero source files
grep -rn "16b_bayesian_regression" scripts/ cluster/ docs/03_methods_reference/  # zero matches
python -m pytest scripts/fitting/tests/test_no_16b_references.py -v              # pass (both guards)
python -m pytest scripts/fitting/tests/ -v                                        # full green
python validation/check_v4_closure.py --milestone v4.0                            # exit 0
```
</verification>

<success_criteria>
- CLEAN-03 closed: 16b pycache removed, MODEL_REFERENCE.md scrubbed, guard test in place (with both text-reference and file-existence checks), archival .planning/ docs intentionally preserved.
- SC#3 find-command scope gap bridged: the pruned `find . -path ./.planning -prune -o -name "16b*" -print` returns empty AND the guard test asserts the same via a dedicated file-existence function.
- Two atomic commits on `main`, each `chore(tech-debt): ...`, no Co-Authored-By.
- Cluster SLURM was already clean — verified in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/23-tech-debt-sweep-pre-flight-cleanup/23-03-SUMMARY.md` summarising:
- Requirements closed: CLEAN-03
- Files deleted: `scripts/__pycache__/16b_bayesian_regression.cpython-313.pyc` (+ any other Python-version variants)
- Files added: `scripts/fitting/tests/test_no_16b_references.py` (two guard functions: text-reference + file-existence)
- Files edited: `docs/03_methods_reference/MODEL_REFERENCE.md` (deleted or rewrote paragraphs referencing 16b)
- Note: the Python source file `scripts/16b_bayesian_regression.py` was already deleted pre-Phase-23 — this plan removed the residual pycache and doc reference
- SC#3 find-command scope: documented the practical interpretation (`.planning/` pruned) and locked it in via the file-existence guard
- Two commit SHAs
</output>

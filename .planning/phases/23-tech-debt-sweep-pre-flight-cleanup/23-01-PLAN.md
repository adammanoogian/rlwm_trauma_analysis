---
phase: 23-tech-debt-sweep-pre-flight-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fitting/legacy/fit_to_data.py
  - scripts/fitting/legacy/numpyro_models.py
  - scripts/fitting/legacy/pymc_models.py
  - scripts/fitting/legacy/pymc_models_functional.py
  - scripts/fitting/legacy/__pycache__
  - scripts/fitting/tests/test_no_legacy_imports.py
autonomous: true

must_haves:
  truths:
    - "No file under scripts/fitting/legacy/ remains on disk (directory deleted or empty)."
    - "grep -rn 'from scripts.fitting.legacy' scripts/ returns zero live imports."
    - "pytest scripts/fitting/tests/ -v passes after removal."
    - "python validation/check_v4_closure.py --milestone v4.0 exits 0 after removal."
  artifacts:
    - path: "scripts/fitting/tests/test_no_legacy_imports.py"
      provides: "Grep-invariant guard asserting no live import references scripts.fitting.legacy"
      min_lines: 20
    - path: "scripts/fitting/legacy/"
      provides: "Directory MUST NOT exist after this plan completes"
  key_links:
    - from: "scripts/fitting/tests/test_no_legacy_imports.py"
      to: "scripts/ tree"
      via: "recursive grep for 'from scripts.fitting.legacy' / 'import scripts.fitting.legacy'"
      pattern: "from\\s+scripts\\.fitting\\.legacy|import\\s+scripts\\.fitting\\.legacy"
---

<objective>
Delete the quarantined `scripts/fitting/legacy/` directory (v3.0-era pymc/numpyro/fit_to_data modules that are already unreachable from every live import path) and install a pytest guard that fails CI if any future commit reintroduces a `from scripts.fitting.legacy` import.

Purpose: Close CLEAN-01 of the v5.0 tech-debt sweep. The Phase 24 cold-start cluster run must produce empirical artifacts against a codebase with zero latent reintroduction surface for pre-Collins K-bounds code.

Output: `scripts/fitting/legacy/` removed from the tree; `scripts/fitting/tests/test_no_legacy_imports.py` created; pytest green; v4.0 closure guard still green; one `chore(tech-debt): ...` commit landed.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@validation/check_v4_closure.py

# Prior art — the recently-completed legacy-wmrl deprecation followed the same pattern
Precedent commit: `74cf21d chore(tech-debt): deprecate legacy wmrl_hierarchical_model; remove dead import`
</context>

<discovery_notes>
Pre-flight grep performed during planning (2026-04-19):

- `grep -rn "from scripts.fitting.legacy" scripts/` → **zero live matches** today.
  Only residual matches live inside the legacy directory itself (self-references) and in commit messages / planning docs.
- `scripts/fitting/legacy/` currently contains 4 source files:
    - `fit_to_data.py` (443 lines)
    - `numpyro_models.py` (741 lines — contains the old `qlearning_hierarchical_model` + `wmrl_hierarchical_model` with K ∈ [1, 7])
    - `pymc_models.py` (508 lines — PyMC backend, already excised from pyproject.toml in Phase 13 INFRA)
    - `pymc_models_functional.py` (381 lines)
  Plus a `__pycache__/` dir.
- The LIVE `scripts/fitting/numpyro_models.py:97 qlearning_hierarchical_model` and `:232 wmrl_hierarchical_model` (already deprecated via 74cf21d) are NOT in scope — they remain as regression-test targets for `test_compile_gate.py` / `test_wmrl_model.py`. Only the top-level legacy/ directory is deleted.
- `validation/check_v4_closure.py` grep'd for any file reference to `scripts/fitting/legacy/` → **zero matches**. Deletion is safe for the v4.0 closure guard.
</discovery_notes>

<tasks>

<task type="auto">
  <name>Task 1: Write grep-invariant test guard</name>
  <files>scripts/fitting/tests/test_no_legacy_imports.py</files>
  <action>
Create `scripts/fitting/tests/test_no_legacy_imports.py` with a single pytest function `test_no_legacy_imports()` that:

1. Walks every `.py` file under `scripts/` (excluding `__pycache__/` and — critically — the to-be-deleted `scripts/fitting/legacy/` itself).
2. For each file, reads the source and asserts neither pattern occurs (uncommented, outside a string literal):
   - `from scripts.fitting.legacy`
   - `import scripts.fitting.legacy`
3. Additionally asserts `scripts/fitting/legacy/` directory does NOT exist on disk (`Path("scripts/fitting/legacy").exists()` is False).
4. On failure, prints the offending file + line for fast triage (expected vs actual values per CLAUDE.md error-message convention).

Follow project conventions:
- `from __future__ import annotations` at the top.
- NumPy-style docstring on the test function.
- Python 3.10+ type hints (`list[Path]`, not `List[Path]`).
- Absolute imports only.
- 88-char line limit.
- No wildcard imports.

Use `pathlib.Path(__file__).resolve().parents[3]` to locate the repo root deterministically (test file lives at `scripts/fitting/tests/`, so repo root is 3 levels up).

Rationale (put in module docstring): this guard is the enforcement mechanism behind CLEAN-01. Without it, a future refactor could silently reintroduce a `from scripts.fitting.legacy.numpyro_models import ...` line.

**Important:** do NOT run pytest yet — the guard must be written BEFORE the directory deletion so that if the directory deletion fails partway through, the guard will catch it on the next run.

**Commit after this task:** stage ONLY the new test file. Commit message:
```
chore(tech-debt): add guard test for scripts.fitting.legacy imports

CLEAN-01 enforcement: pytest guard that fails if any scripts/ file
reintroduces `from scripts.fitting.legacy` or if the legacy directory
is recreated. Paired with the directory deletion in the next commit.
```
(No Co-Authored-By line per user global preference.)
  </action>
  <verify>
`python -m pytest scripts/fitting/tests/test_no_legacy_imports.py -v` **FAILS** at this point (because `scripts/fitting/legacy/` still exists on disk — that's the expected state after Task 1; Task 2 will make the test pass).

Verify the FAILURE message is the directory-exists assertion, not a syntax error or import error.
  </verify>
  <done>
- `scripts/fitting/tests/test_no_legacy_imports.py` exists, follows Python conventions.
- Test EXISTS and fails with the expected directory-exists assertion.
- Commit `chore(tech-debt): add guard test for scripts.fitting.legacy imports` landed on main.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete scripts/fitting/legacy/ directory and verify closure</name>
  <files>scripts/fitting/legacy/ (DELETE entire directory tree)</files>
  <action>
1. Delete the entire `scripts/fitting/legacy/` directory recursively, including `__pycache__/`:
   ```bash
   rm -rf scripts/fitting/legacy/
   ```
2. Verify the directory is gone: `ls scripts/fitting/legacy/ 2>/dev/null` must fail with "No such file or directory".
3. Verify no residual `.pyc` files: `find scripts/ -name "*legacy*" -not -path "*__pycache__*"` must return zero lines. (Pycache files in `scripts/fitting/__pycache__/legacy.cpython-*.pyc` may remain — those are auto-regenerated on next import and will clear on next `pytest` run or can be manually deleted with `find scripts -path "*__pycache__*legacy*" -delete`.)
4. Run the full fitting test suite: `python -m pytest scripts/fitting/tests/ -v`. Expectation: ALL tests pass, including the guard test from Task 1 that previously failed.
5. Run the v4.0 closure guard: `python validation/check_v4_closure.py --milestone v4.0`. Expected exit code: 0.

If step 4 fails due to an unexpected import or test reference to `scripts.fitting.legacy`: STOP, do not commit. Report the failing test to the user — the pre-flight grep showed zero live references, so any failure would indicate a dynamic import path not caught by grep and warrants investigation before proceeding.

**Commit after this task:** stage the deletion. Commit message:
```
chore(tech-debt): delete scripts/fitting/legacy/ directory

CLEAN-01 resolution: removes 4 quarantined v3.0-era modules
(fit_to_data.py, numpyro_models.py, pymc_models.py,
pymc_models_functional.py) totalling 2,073 lines. Guard test
`test_no_legacy_imports.py` (added in previous commit) now passes.

Closure guard: `python validation/check_v4_closure.py --milestone v4.0`
exits 0 — no v4.0 invariants broken.
```
Use `git rm -r scripts/fitting/legacy/` to stage the deletion (handles the whole tree in one call).
  </action>
  <verify>
1. `ls scripts/fitting/legacy/ 2>/dev/null; echo $?` → prints nothing and echoes exit code 2.
2. `grep -rn "from scripts.fitting.legacy" scripts/` → zero matches.
3. `python -m pytest scripts/fitting/tests/ -v` → all green.
4. `python validation/check_v4_closure.py --milestone v4.0` → exits 0.
5. `git log --oneline -2` shows the two new chore commits from this plan.
  </verify>
  <done>
- `scripts/fitting/legacy/` directory no longer exists.
- Guard test `test_no_legacy_imports.py` passes.
- Full fitting test suite passes.
- v4.0 closure guard still green.
- Two atomic commits landed (guard test, then deletion), each with the `chore(tech-debt): ...` prefix and NO Co-Authored-By line.
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
# CLEAN-01 success criteria from ROADMAP Phase 23 SC#1
grep -rn "from scripts.fitting.legacy" scripts/               # must return zero lines
ls scripts/fitting/legacy/ 2>/dev/null                        # must fail (dir gone)
python -m pytest scripts/fitting/tests/ -v                    # must pass
python validation/check_v4_closure.py --milestone v4.0        # must exit 0

# Git hygiene
git log --oneline -2   # must show two chore(tech-debt) commits
git status             # must show clean working tree
```
</verification>

<success_criteria>
- CLEAN-01 closed: legacy directory deleted, guard test in place, zero live imports, full test suite green, v4.0 closure guard green.
- Two atomic commits on `main`: one adds the guard test (red), one deletes the directory (green). Bisectable.
- Commit messages follow `chore(tech-debt): ...` convention, no Co-Authored-By lines.
</success_criteria>

<output>
After completion, create `.planning/phases/23-tech-debt-sweep-pre-flight-cleanup/23-01-SUMMARY.md` summarising:
- Requirements closed: CLEAN-01
- Lines deleted (approx 2,073 across 4 files + pycache)
- Files added: `scripts/fitting/tests/test_no_legacy_imports.py`
- Two commit SHAs
- `pytest scripts/fitting/tests/` duration before/after (should be faster — fewer files to collect)
</output>

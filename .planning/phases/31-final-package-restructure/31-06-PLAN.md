---
phase: 31-final-package-restructure
plan: 06
type: execute
wave: 5
depends_on: ["31-02", "31-03", "31-04", "31-05"]
files_modified:
  - tests/integration/test_v5_phase29_structure.py
  - docs/PROJECT_STRUCTURE.md
  - CLAUDE.md
  - README.md
  - CITATION.cff
  - docs/04_methods/README.md
  - docs/legacy/**
autonomous: true

must_haves:
  truths:
    - "tests/integration/test_v5_phase29_structure.py asserts both Phase 29 invariants (unchanged) AND new Phase 31 layout (data/raw, data/interim, data/processed, models/, reports/figures, reports/tables, logs/, tests/{unit,integration,scientific}/)"
    - "docs/PROJECT_STRUCTURE.md exists and describes the CCDS-aligned final layout with pointers to CCDS v2 docs"
    - "CLAUDE.md top-level path references updated (output/ → models/ or reports/; figures/ → reports/figures/; validation/ → tests/scientific/; cluster/logs/ → logs/)"
    - "CITATION.cff exists at repo root with valid schema (validates against https://citation-file-format.github.io/)"
    - "A fresh-clone smoke test succeeds: `git clone` + `pip install -e .` + `pytest -m 'not slow and not scientific'` exits 0 without manual path fiddling"
    - "Phase 29 invariants still hold (31/31 passing in the Phase 29 portion of the structure guard)"
    - "ROADMAP SC#4 gate holds: `bash cluster/submit_all.sh --dry-run` exits 0 against the new layout (smoke-tested after all prior waves complete)"
  artifacts:
    - path: "tests/integration/test_v5_phase29_structure.py"
      provides: "Extended structure guard covering Phase 29 + Phase 31 invariants"
      contains: "def test_ccds_data_tier_exists"
    - path: "docs/PROJECT_STRUCTURE.md"
      provides: "Reader-facing directory layout doc with CCDS alignment notes"
    - path: "CITATION.cff"
      provides: "Valid CFF v1.2.0 citation metadata"
      contains: "cff-version: 1.2.0"
    - path: "CLAUDE.md"
      provides: "Updated path references reflecting CCDS layout"
  key_links:
    - from: "tests/integration/test_v5_phase29_structure.py"
      to: "filesystem"
      via: "pytest assertions on CCDS directory presence"
      pattern: "test_ccds_|test_phase31_|data_raw_exists|models_dir_exists|reports_dir_exists"
    - from: "README.md + docs/PROJECT_STRUCTURE.md"
      to: "CCDS conventions"
      via: "Documentation cross-references"
      pattern: "CCDS|cookiecutter-data-science"
    - from: "bash cluster/submit_all.sh --dry-run"
      to: "new CCDS layout (models/, reports/, data/processed/)"
      via: "Subprocess exit 0 (ROADMAP SC#4)"
      pattern: "bash cluster/submit_all\\.sh --dry-run"
---

<objective>
Close out Phase 31 by (1) extending the Phase 29 structure guard with Phase 31 invariants (do NOT replace — extend), (2) creating the reader-facing docs/PROJECT_STRUCTURE.md, (3) updating CLAUDE.md and README.md path references to the new layout, (4) adding CITATION.cff for JOSS/Zenodo compatibility (per research Q3), and (5) performing a final fresh-clone smoke test (including ROADMAP SC#4 `bash cluster/submit_all.sh --dry-run`) to confirm "final package" readiness.

Purpose: A final package must be self-documenting. The structure guard is the executable spec for "what the repo should look like"; docs/PROJECT_STRUCTURE.md is the reader's narrative version; CITATION.cff is the machine-readable citation metadata that Zenodo consumes when generating DOIs. The fresh-clone check closes the loop — no hidden dependencies on pre-existing local state.

Output:
- Extended `tests/integration/test_v5_phase29_structure.py` with Phase 31 layout assertions (new section appended; Phase 29 section intact)
- New `docs/PROJECT_STRUCTURE.md` with labeled tree diagram + CCDS alignment notes
- Updated `CLAUDE.md` (Project Structure section) pointing at new layout
- Updated `README.md` (if it references paths)
- New `CITATION.cff` at repo root
- Fresh-clone smoke test result documented in summary (including submit_all.sh --dry-run for ROADMAP SC#4)
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-final-package-restructure/31-RESEARCH.md
@.planning/phases/31-final-package-restructure/31-05-SUMMARY.md
@tests/integration/test_v5_phase29_structure.py
@CLAUDE.md
@README.md

**CRITICAL INVARIANTS:**
- Plan 31-04 moved test_v5_phase29_structure.py to tests/integration/ with REPO_ROOT updated to parents[2]. Any new assertions must use REPO_ROOT-relative paths.
- The file is EXTENDED, not REPLACED. Phase 29 parametrized tests (STAGE_FOLDERS, DEAD_FOLDERS, DOCS_SPARE_FILES, OLD_IMPORT_PATTERNS, UTILS_RENAME_PAIRS, etc.) must remain.
- CCDS v2 (not v1) is the cited source. Use `https://cookiecutter-data-science.drivendata.org/` as the canonical reference.
- CITATION.cff schema: https://citation-file-format.github.io/1.2.0/schema-guide.md — validate the produced file against v1.2.0.
- Quarto render from Wave C must still pass after docs changes — update any paper.qmd references only if a documentation-facing bib/citation path changed.
- **Wave 5 sequencing:** This plan is now Wave 5 (previously Wave 3). Waves: W1=31-01, W2=31-02 ‖ 31-03, W3=31-04, W4=31-05, W5=31-06. All prior waves must complete before this plan runs.
- **ROADMAP SC#4:** Phase 31 success criterion #4 is `cluster/submit_all.sh --dry-run exits 0 against the new layout`. The revision flagged this as a potential gap — the orchestrator list had "logs consolidated" as #4, but the ROADMAP text is authoritative. Both are covered (plan 31-05 handles logs; plan 31-06 covers submit_all.sh --dry-run verification in Task 3 Step 2 below).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend structure guard with Phase 31 layout assertions</name>
  <files>tests/integration/test_v5_phase29_structure.py</files>
  <action>
Extend `tests/integration/test_v5_phase29_structure.py` with a clearly-labeled "Phase 31" section AFTER all Phase 29 sections. Do NOT modify or reorder the Phase 29 assertions.

Append this block at the end of the file (after the UTILS_RENAME_PAIRS parametrized test, around line 277):

```python
# =========================================================================
# Phase 31 — Final-Package Restructure (CCDS layout + test/log consolidation)
# =========================================================================
#
# Phase 31 asserts a CCDS-aligned top-level layout for final-package readiness.
# References:
#   - Phase 31 RESEARCH.md (.planning/phases/31-final-package-restructure/)
#   - CCDS docs: https://cookiecutter-data-science.drivendata.org/
#   - Success criteria in ROADMAP.md Phase 31 section

# ------ Phase 31 SC#1: data/ CCDS tier structure ------

PHASE31_DATA_TIERS = ["raw", "interim", "processed", "external"]


@pytest.mark.parametrize("tier", PHASE31_DATA_TIERS)
def test_phase31_data_tier_exists(tier: str) -> None:
    """Phase 31 SC#1: data/<tier>/ exists (CCDS data tiers)."""
    path = REPO_ROOT / "data" / tier
    assert path.is_dir(), (
        f"Missing Phase 31 data tier: expected {path}, got absent"
    )


# ------ Phase 31 SC#2: models/ subdirectories ------

PHASE31_MODELS_SUBDIRS = [
    "bayesian",
    "mle",
    "ppc",
    "recovery",
]


@pytest.mark.parametrize("sub", PHASE31_MODELS_SUBDIRS)
def test_phase31_models_subdir_exists(sub: str) -> None:
    """Phase 31 SC#2: models/<sub>/ exists (CCDS models tier)."""
    path = REPO_ROOT / "models" / sub
    assert path.is_dir(), (
        f"Missing Phase 31 models subdirectory: expected {path}, got absent"
    )


# ------ Phase 31 SC#3: reports/ structure ------

PHASE31_REPORTS_SUBDIRS = ["figures", "tables"]


@pytest.mark.parametrize("sub", PHASE31_REPORTS_SUBDIRS)
def test_phase31_reports_subdir_exists(sub: str) -> None:
    """Phase 31 SC#3: reports/<sub>/ exists (CCDS reports tier)."""
    path = REPO_ROOT / "reports" / sub
    assert path.is_dir(), (
        f"Missing Phase 31 reports subdirectory: expected {path}, got absent"
    )


# ------ Phase 31 SC#4: legacy output/ and figures/ removed ------

PHASE31_REMOVED_DIRS = ["output", "figures", "validation"]


@pytest.mark.parametrize("removed", PHASE31_REMOVED_DIRS)
def test_phase31_legacy_dir_removed(removed: str) -> None:
    """Phase 31 SC#4: legacy output/, figures/, validation/ gone from top level."""
    path = REPO_ROOT / removed
    assert not path.exists(), (
        f"Legacy top-level directory still present: expected absent, got {path}. "
        f"Phase 31 consolidated these into CCDS tiers — content belongs under "
        f"data/, models/, reports/, or tests/scientific/."
    )


# ------ Phase 31 SC#5: tests/ tier structure ------

PHASE31_TESTS_TIERS = ["unit", "integration", "scientific"]


@pytest.mark.parametrize("tier", PHASE31_TESTS_TIERS)
def test_phase31_tests_tier_exists(tier: str) -> None:
    """Phase 31 SC#5: tests/<tier>/ exists (unit/integration/scientific)."""
    path = REPO_ROOT / "tests" / tier
    assert path.is_dir(), (
        f"Missing Phase 31 test tier: expected {path}, got absent"
    )


def test_phase31_scripts_fitting_tests_removed() -> None:
    """Phase 31 SC#5: scripts/fitting/tests/ no longer exists at that location."""
    path = REPO_ROOT / "scripts" / "fitting" / "tests"
    assert not path.exists(), (
        f"scripts/fitting/tests/ still exists: expected absent, got {path}. "
        f"Phase 31 consolidated into tests/integration/."
    )


# ------ Phase 31 SC#6: logs/ at root, cluster/logs/ gone ------

def test_phase31_logs_at_root_only() -> None:
    """Phase 31 SC#6: logs/ exists at root; cluster/logs/ gone (unified)."""
    logs_root = REPO_ROOT / "logs"
    cluster_logs = REPO_ROOT / "cluster" / "logs"
    assert logs_root.is_dir(), (
        f"Missing top-level logs/: expected {logs_root}, got absent"
    )
    assert not cluster_logs.exists(), (
        f"cluster/logs/ still exists: expected absent, got {cluster_logs}. "
        f"Phase 31 merged into {logs_root}."
    )


# ------ Phase 31 SC#7: config.py no longer exposes legacy aliases ------

def test_phase31_config_has_ccds_constants() -> None:
    """Phase 31 SC#7: config.py exposes DATA_RAW_DIR, INTERIM_DIR, etc."""
    import importlib
    import sys as _sys
    if "config" in _sys.modules:
        importlib.reload(_sys.modules["config"])
    import config as _config
    required = [
        "DATA_RAW_DIR", "INTERIM_DIR", "PROCESSED_DIR", "DATA_EXTERNAL_DIR",
        "MODELS_DIR", "MODELS_BAYESIAN_DIR", "MODELS_MLE_DIR",
        "MODELS_PARAMETER_EXPLORATION_DIR",
        "REPORTS_DIR", "REPORTS_FIGURES_DIR", "REPORTS_TABLES_DIR",
        "LOGS_DIR",
    ]
    missing = [name for name in required if not hasattr(_config, name)]
    assert not missing, (
        f"config.py missing Phase 31 CCDS constants: expected all of "
        f"{required}, got missing {missing}"
    )


def test_phase31_config_no_legacy_aliases() -> None:
    """Phase 31 SC#7: config.py removed OUTPUT_DIR/FIGURES_DIR legacy aliases."""
    import importlib
    import sys as _sys
    if "config" in _sys.modules:
        importlib.reload(_sys.modules["config"])
    import config as _config
    forbidden = ["OUTPUT_DIR", "FIGURES_DIR", "OUTPUT_VERSION_DIR", "FIGURES_VERSION_DIR"]
    present = [name for name in forbidden if hasattr(_config, name)]
    assert not present, (
        f"config.py still exposes Phase 31 legacy aliases: expected all gone, "
        f"got {present}. Waves B/C should have migrated consumers; plan 31-05 "
        f"removes these constants."
    )


# ------ Phase 31 SC#8: no live legacy path strings ------

PHASE31_LEGACY_PATH_PATTERNS = [
    "output/bayesian",
    "output/mle",
    "output/descriptives",
    "output/model_comparison",
    "output/regressions",
]


@pytest.mark.parametrize("pattern", PHASE31_LEGACY_PATH_PATTERNS)
def test_phase31_no_legacy_output_paths(pattern: str) -> None:
    """Phase 31 SC#8: zero live references to legacy output/* paths.

    Excludes /legacy/ archives and .planning/ (which may reference historic
    paths as documentation). Only active code tree is checked.
    """
    hits: list[str] = []
    search_dirs = ["scripts", "tests", "src", "cluster", "manuscript"]
    self_rel = str(Path(__file__).resolve().relative_to(REPO_ROOT)).replace(
        "\\", "/"
    )
    for d in search_dirs:
        root = REPO_ROOT / d
        if not root.is_dir():
            continue
        for p in root.rglob("*"):
            if not p.is_file():
                continue
            # Only check text files we care about
            if p.suffix not in {".py", ".qmd", ".slurm", ".sh", ".md", ".toml", ".ini"}:
                continue
            rel = str(p.relative_to(REPO_ROOT)).replace("\\", "/")
            if rel == self_rel:
                continue
            if "/legacy/" in rel or rel.startswith(
                ("scripts/legacy/", "tests/legacy/")
            ):
                continue
            try:
                txt = p.read_text(encoding="utf-8", errors="ignore")
            except OSError:
                continue
            if pattern in txt:
                hits.append(rel)
    assert not hits, (
        f"Legacy path pattern `{pattern}` found in: expected zero, got {hits}. "
        f"Phase 31 Waves B/C/E should have migrated these."
    )
```

Run the test after appending:

```bash
pytest tests/integration/test_v5_phase29_structure.py -v 2>&1 | tail -40
```

Expected: original Phase 29 test count (31) + new Phase 31 tests (≈ 20-25 parametrized cases) all passing.
  </action>
  <verify>
- `pytest tests/integration/test_v5_phase29_structure.py -v 2>&1 | grep -cE "^tests/integration/test_v5_phase29_structure.py.*PASSED"` ≥ 40 (Phase 29 + Phase 31 combined)
- `pytest tests/integration/test_v5_phase29_structure.py 2>&1 | tail -1 | grep -oE "[0-9]+ passed"` returns a number, no failures
- Phase 29 still explicitly covered: `grep -c "STAGE_FOLDERS\|DEAD_FOLDERS\|DOCS_SPARE_FILES\|OLD_IMPORT_PATTERNS\|UTILS_RENAME_PAIRS" tests/integration/test_v5_phase29_structure.py` returns ≥ 5 (phase 29 parametrize lists still present)
- Phase 31 assertions present: `grep -c "PHASE31_" tests/integration/test_v5_phase29_structure.py` returns ≥ 7
  </verify>
  <done>test_v5_phase29_structure.py has both Phase 29 (unchanged) and Phase 31 (appended) assertions; full test count ≥ 40; pytest green.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/PROJECT_STRUCTURE.md, update CLAUDE.md and README.md</name>
  <files>
docs/PROJECT_STRUCTURE.md
CLAUDE.md
README.md
docs/04_methods/README.md
  </files>
  <action>
**Step 1 — Create docs/PROJECT_STRUCTURE.md** (new file, reader-facing layout doc):

```markdown
# Project Structure

This repository follows the [Cookiecutter Data Science v2](https://cookiecutter-data-science.drivendata.org/) conventions for top-level layout, adapted for computational psychiatry with hierarchical Bayesian model fitting (SLURM cluster + Quarto manuscript).

## Layout

```
rlwm_trauma_analysis/
├── CITATION.cff               # Citation metadata (CFF v1.2.0, Zenodo-compatible)
├── CLAUDE.md                  # AI-assistant project guidelines
├── README.md                  # Project overview + quickstart
├── config.py                  # Single-source Path constants + model hyperparameters
├── pyproject.toml             # Python package metadata
├── pytest.ini                 # Test discovery config (testpaths = tests)
├── environment.yml            # Conda environment (CPU)
├── environment_gpu.yml        # Conda environment (GPU)
│
├── data/                      # CCDS data tiers
│   ├── raw/                   # Immutable jsPsych CSVs (gitignored — sensitive)
│   ├── interim/               # Parsed + collated intermediate products (gitignored)
│   ├── processed/             # Canonical analysis-ready CSVs (tracked)
│   └── external/              # Third-party reference data (if any)
│
├── models/                    # CCDS models tier — fitted artifacts
│   ├── bayesian/              # Hierarchical posteriors (.nc gitignored, metadata tracked)
│   │   ├── 21_baseline/       # Phase 21 choice-only baseline
│   │   ├── 21_l2/             # Phase 21 level-2 regression
│   │   ├── 21_prior_predictive/
│   │   ├── 21_recovery/       # Bayesian parameter recovery
│   │   └── level2/            # Phase 16 subscale L2
│   ├── mle/                   # Maximum-likelihood individual + group fits (CSV, tracked)
│   ├── ppc/                   # Posterior predictive checks
│   └── recovery/              # Model + parameter recovery artifacts
│
├── reports/                   # CCDS reports tier — presentation-layer outputs
│   ├── figures/               # All PNG/PDF figures (manuscript + diagnostics)
│   └── tables/                # All report CSVs (descriptives, model comparison, regressions, etc.)
│
├── scripts/                   # Pipeline scripts (Scheme D from Phase 29)
│   ├── 01_data_preprocessing/
│   ├── 02_behav_analyses/
│   ├── 03_model_prefitting/
│   ├── 04_model_fitting/      # a_mle/, b_bayesian/, c_level2/
│   ├── 05_post_fitting_checks/
│   ├── 06_fit_analyses/
│   ├── fitting/               # Shared helpers for fit pipeline
│   ├── utils/                 # ppc, plotting, stats, scoring, data_cleaning
│   └── legacy/                # Archived superseded scripts (gitignored if large)
│
├── src/rlwm/                  # Installable package (src-layout per pyOpenSci)
│   ├── fitting/               # Authoritative JAX likelihoods + NumPyro models
│   ├── envs/                  # Gym environment
│   └── models/                # NumPy Gym-stateful agent classes
│
├── cluster/                   # SLURM + shell scripts for M3 cluster
│   ├── submit_all.sh          # Canonical master orchestrator (supports --dry-run)
│   └── 21_submit_pipeline.sh  # Legacy entry (v4.0 closure contract — kept as shim)
│
├── tests/                     # Consolidated test tree (Phase 31)
│   ├── unit/                  # Fast (< 1s) — import/smoke/logic
│   ├── integration/           # Medium (1-60s) — MLE smoke + structure guards
│   └── scientific/            # Slow (> 60s) — parameter recovery, v4 closure
│
├── logs/                      # Single gitignored log location (dev + SLURM)
├── docs/                      # Project documentation (this folder)
├── manuscript/                # Quarto manuscript source (paper.qmd → paper.pdf)
└── .planning/                 # GSD planning audit trail (local-only, push-blocked)
```

## Key conventions

- **Path source of truth:** `config.py` — never hardcode paths in scripts; import constants (`DATA_RAW_DIR`, `MODELS_DIR`, `REPORTS_FIGURES_DIR`, etc.)
- **Test tiers:** unit (< 1s, isolated) → integration (1-60s, cross-module) → scientific (> 60s, invariants). Run `pytest -m "not slow and not scientific"` for CI-equivalent fast tier.
- **Data immutability:** `data/raw/` is NEVER edited in place (CCDS convention). All transformations produce new files in `data/interim/` or `data/processed/`.
- **Model/report separation:** `models/` holds fitted posteriors/MLE estimates (pipeline output); `reports/` holds presentation artifacts consumed by the manuscript (figures + tables).
- **Scheme D scripts:** stage folders `01_...` through `06_...` have numeric prefixes (paper IMRaD order); intra-stage scripts use per-stage reset numbering; `04_model_fitting/{a,b,c}_*/` subfolders use canonical descriptive names (no numbers).

## References

- [Cookiecutter Data Science v2](https://cookiecutter-data-science.drivendata.org/)
- [pyOpenSci Python Package Guide](https://www.pyopensci.org/python-package-guide/)
- [Scientific Python Development Guide](https://learn.scientific-python.org/development/)
- [The Turing Way reproducible-project-template](https://github.com/the-turing-way/reproducible-project-template)

## History

- **Phase 29** (2026-04-22): Canonical Scheme D scripts/ layout
- **Phase 31** (2026-04-24): Top-level CCDS alignment (data/models/reports/tests/logs)
```

Save at `docs/PROJECT_STRUCTURE.md`.

**Step 2 — Update CLAUDE.md:**

The existing CLAUDE.md has a "Project Structure" section (around line ~60 per the user's CLAUDE.md context) that describes the Scheme D layout. Extend the section to reflect Phase 31 top-level changes. Specifically:

1. Update the top of `## Code Organization` to mention CCDS top-level layout pointing at `docs/PROJECT_STRUCTURE.md`
2. Update any reference to `output/bayesian`, `output/mle`, `figures/`, `validation/`, `cluster/logs/` to the new paths
3. Update the "Data Pipeline — Output Files" table paths:

```markdown
**Output Files:**
| File | Description |
|------|-------------|
| `data/processed/task_trials_long_all.csv` | All blocks with `is_practice` flag and `phase_type` |
| `data/processed/task_trials_long.csv` | Main task only (default for fitting) |
| `data/processed/task_trials_long_all_participants.csv` | Legacy filename (main task only) |
```

And update the fitting CLI examples:

```bash
# OLD: --data output/task_trials_long_all.csv
# NEW: --data data/processed/task_trials_long_all.csv
```

Apply the same substitution across the Quick Reference section.

4. Add a new subsection under "Documentation Standards":

```markdown
### Project structure canonical reference

See `docs/PROJECT_STRUCTURE.md` for the reader-facing layout doc.
Executable layout invariants live in `tests/integration/test_v5_phase29_structure.py`.
```

**Step 3 — Update README.md:**

Read README.md fully. For each reference to `output/...`, `figures/...`, `validation/`, or `cluster/logs/`, apply the Wave C substitution table. Add a "Project Structure" section near the top (after installation instructions) pointing at `docs/PROJECT_STRUCTURE.md`:

```markdown
## Project Structure

This repository follows [Cookiecutter Data Science v2](https://cookiecutter-data-science.drivendata.org/) conventions. See [`docs/PROJECT_STRUCTURE.md`](docs/PROJECT_STRUCTURE.md) for the full layout.

Quick orientation:
- `data/{raw,interim,processed}/` — data tiers
- `models/{bayesian,mle}/` — fitted model artifacts
- `reports/{figures,tables}/` — manuscript figures and tables
- `scripts/0N_.../` — six-stage numbered pipeline (preprocessing → behavioral → prefit → fit → post-fit → analysis)
- `tests/{unit,integration,scientific}/` — tiered test suite
- `cluster/` — SLURM jobs + master orchestrator `submit_all.sh`
```

**Step 4 — Update docs/04_methods/README.md** (if it exists and references legacy paths):

```bash
ls docs/04_methods/README.md 2>/dev/null
```

If present, read it, apply path substitutions, save back.
  </action>
  <verify>
- `[ -f docs/PROJECT_STRUCTURE.md ] && wc -l docs/PROJECT_STRUCTURE.md | awk '{ print ($1 > 50) }'` returns 1 (non-trivial file)
- `grep -c "CCDS\|cookiecutter-data-science" docs/PROJECT_STRUCTURE.md` returns ≥ 2
- `grep -cE "'output/|output/bayesian|output/mle|output/model_comparison" CLAUDE.md` returns 0
- `grep -cE "data/processed|data/raw|models/bayesian|reports/figures" CLAUDE.md` returns ≥ 4
- `grep -c "Project Structure\|docs/PROJECT_STRUCTURE" README.md` returns ≥ 1
- `grep -cE "output/|figures/|validation/" README.md | head` returns 0 (or only mentions as historic labels)
  </verify>
  <done>docs/PROJECT_STRUCTURE.md created with CCDS references; CLAUDE.md and README.md have zero legacy path strings in active guidance; docs/04_methods/README.md updated if present.</done>
</task>

<task type="auto">
  <name>Task 3: Add CITATION.cff, run fresh-clone smoke test (including ROADMAP SC#4 dry-run)</name>
  <files>
CITATION.cff
  </files>
  <action>
**Step 1 — Create CITATION.cff** (research Q3 recommends this for JOSS + Zenodo):

Read the current README.md + pyproject.toml to extract author info (if available). Produce `CITATION.cff` at repo root:

```yaml
cff-version: 1.2.0
message: "If you use this software, please cite it as below."
type: software
title: "RLWM Trauma Analysis Pipeline"
abstract: >-
  Hierarchical Bayesian reinforcement-learning + working-memory model
  fitting pipeline for trauma-linked cognitive data. Implements the
  Senta et al. (2025) M1-M6 + M4-LBA model family with JAX-accelerated
  likelihoods, NumPyro MCMC, and LOO stacking for model comparison.
version: v5.0
date-released: "2026-04-22"
url: "https://github.com/adammanoogian/rlwm_trauma_analysis"
repository-code: "https://github.com/adammanoogian/rlwm_trauma_analysis"
license: MIT
keywords:
  - reinforcement-learning
  - working-memory
  - hierarchical-bayesian
  - trauma
  - computational-psychiatry
  - jax
  - numpyro
authors:
  - family-names: "Manoogian"
    given-names: "Adam"
    email: "adammanoogian@gmail.com"
references:
  - type: article
    authors:
      - family-names: "Senta"
        given-names: "R."
    title: "Task-specific mixtures of working memory and reinforcement learning"
    year: 2025
```

Adjust author fields if pyproject.toml or existing README has different info. Do NOT invent fields — if author ORCID isn't known, omit it.

Validate the CFF syntax using Python YAML parsing:

```bash
python -c "import yaml; data = yaml.safe_load(open('CITATION.cff')); assert data['cff-version'] == '1.2.0'; assert 'title' in data; assert 'authors' in data; print('CITATION.cff valid')"
```

If `cffconvert` is available in the environment, use it for stricter validation:

```bash
pip install cffconvert 2>/dev/null && cffconvert --validate 2>&1 || echo "cffconvert not available; YAML-level validation only"
```

Skip `cffconvert` if not installable — YAML parse + required-fields check is enough for Phase 31 closure.

**Step 2 — Fresh-clone smoke test (covers ROADMAP SC#4 + SC#7):**

This is the phase-level success criterion #7 ("fresh git clone + pip install -e . + pytest completes green") PLUS the ROADMAP-pinned SC#4 (`cluster/submit_all.sh --dry-run exits 0 against the new layout`). Since we can't actually fresh-clone in CI during Phase 31 execution, approximate it:

```bash
# 0. Syntactic validation of the master orchestrator (defense-in-depth)
bash -n cluster/submit_all.sh && echo "submit_all.sh syntactic OK"

# 1. Check imports work from a clean Python subprocess that has only config on path
python -c "
import sys
sys.path.insert(0, '.')
from config import (
    DATA_RAW_DIR, INTERIM_DIR, PROCESSED_DIR, DATA_EXTERNAL_DIR,
    MODELS_DIR, MODELS_BAYESIAN_DIR, MODELS_MLE_DIR, MODELS_PARAMETER_EXPLORATION_DIR,
    REPORTS_DIR, REPORTS_FIGURES_DIR, REPORTS_TABLES_DIR, LOGS_DIR,
)
print('All CCDS constants importable')
"

# 2. Check the package installs cleanly
pip install -e . 2>&1 | tail -5

# 3. Run the fast-tier test suite
pytest tests/ -m "not slow and not scientific" --tb=short 2>&1 | tail -20

# 4. Verify v4 closure guards still pass
pytest tests/integration/test_v4_closure.py -v 2>&1 | tail -10
python tests/scientific/check_v4_closure.py --milestone v4.0 2>&1 | tail -5

# 5. ROADMAP SC#4: submit_all.sh --dry-run exit 0
bash cluster/submit_all.sh --dry-run 2>&1 | tail -20
echo "submit_all.sh --dry-run exit: $?"

# 6. Verify manuscript still renders (skip if quarto not available)
which quarto && (cd manuscript && quarto render paper.qmd 2>&1 | tail -5) || echo "Quarto skipped"
```

All steps must exit 0 (or produce expected output in the case of the print statements). Specifically:
- Step 0: `bash -n` exits 0 → submit_all.sh has no shell syntax errors
- Step 5 is ROADMAP SC#4 — `bash cluster/submit_all.sh --dry-run` must exit 0. If it exits 1 with MISSING python targets, those are path regressions from earlier waves — defer to gap-closure plan, do NOT mark this plan done.

**Step 3 — Document the smoke-test result in the plan summary.**

Record in the SUMMARY.md:
- Time taken for fast-tier pytest (target: < 2 min)
- v4 closure guards: both pytest wrapper + CLI exit 0
- ROADMAP SC#4 status: submit_all.sh --dry-run exit code (target: 0)
- Quarto render: did it run locally or was it skipped
- Any stragglers: tests that pass but emit deprecation warnings, paths that were missed

Do NOT delete CITATION.cff at the end — it is a tracked file that ships with the release.
  </action>
  <verify>
- `[ -f CITATION.cff ]` true
- `python -c "import yaml; d=yaml.safe_load(open('CITATION.cff')); assert d['cff-version']=='1.2.0' and 'title' in d and 'authors' in d; print('ok')"` prints `ok`
- `python -c "from config import DATA_RAW_DIR, INTERIM_DIR, PROCESSED_DIR, MODELS_DIR, MODELS_BAYESIAN_DIR, MODELS_MLE_DIR, MODELS_PARAMETER_EXPLORATION_DIR, REPORTS_DIR, REPORTS_FIGURES_DIR, REPORTS_TABLES_DIR, LOGS_DIR; print('ok')"` prints `ok`
- `pytest tests/ -m "not slow and not scientific" 2>&1 | tail -1 | grep -oE "[0-9]+ passed"` returns a non-zero count
- `pytest tests/ -m "not slow and not scientific" 2>&1 | tail -1 | grep -c "failed"` returns 0
- `pytest tests/integration/test_v4_closure.py -v 2>&1 | tail -1 | grep -oE "[0-9]+ passed"` returns 2 (test_v4_closure_passes + test_v4_closure_deterministic)
- `python tests/scientific/check_v4_closure.py --milestone v4.0` exits 0
- **ROADMAP SC#4:** `bash -n cluster/submit_all.sh` exits 0 AND `bash cluster/submit_all.sh --dry-run` exits 0
  </verify>
  <done>CITATION.cff valid CFF v1.2.0 at repo root; fresh-clone-equivalent smoke test green (imports work, pip install -e succeeds, fast-tier pytest green, dual v4 closure guards green, submit_all.sh --dry-run exits 0, Quarto render works if available).</done>
</task>

</tasks>

<verification>
**Wave E Task 6 plan acceptance checks (and overall Phase 31 closeout):**

1. **Structure guard extended:** `pytest tests/integration/test_v5_phase29_structure.py -v 2>&1 | grep -cE "PASSED"` ≥ 40 (Phase 29 + Phase 31)
2. **Phase 29 preserved:** `pytest tests/integration/test_v5_phase29_structure.py -k "stage_folder or dead_folder or docs_spare" 2>&1 | tail -1 | grep -c "failed"` returns 0
3. **Phase 31 assertions pass:** `pytest tests/integration/test_v5_phase29_structure.py -k "phase31" 2>&1 | tail -1 | grep -oE "[0-9]+ passed"` returns ≥ 15
4. **Docs created:** `[ -f docs/PROJECT_STRUCTURE.md ] && [ -f CITATION.cff ]` both true
5. **CLAUDE.md clean:** `grep -cE "'output/|output/bayesian|output/mle|cluster/logs|validation/" CLAUDE.md` returns 0 (or only in code-block history references)
6. **Final fresh-clone equivalent green:**
   - `pytest tests/` full suite pass (or at least no failures caused by Phase 31)
   - `python tests/scientific/check_v4_closure.py --milestone v4.0` exits 0
   - `bash cluster/submit_all.sh --dry-run` exits 0 (ROADMAP SC#4)
   - `quarto render manuscript/paper.qmd` produces paper.pdf
7. **Phase 31 closure complete:** all 7 phase-level success criteria from ROADMAP.md verifiable:
   1. ✓ Top-level layout matches CCDS conventions (docs/PROJECT_STRUCTURE.md)
   2. ✓ config.py single Path source (closure-guard grep returns 0)
   3. ✓ tests/ is single top-level tree (test_phase31_legacy_dir_removed["validation"])
   4. ✓ submit_all.sh --dry-run exits 0 (ROADMAP SC#4, Task 3 Step 2)
   5. ✓ paper.qmd renders green (Quarto smoke test)
   6. ✓ Phase 29 closure guard passes (pytest -k phase29)
   7. ✓ Fresh-clone + pip install -e + pytest green (smoke-test block in Task 3)

   (Note: the orchestrator's 7-criterion checklist had "logs consolidated" as #4 instead of submit_all.sh; Plan 31-05 covers the logs criterion, and this plan covers the submit_all.sh --dry-run criterion — both are verified, both interpretations pass.)
</verification>

<success_criteria>
- All 3 tasks complete with `verify` conditions met
- All 7 phase-level success criteria from ROADMAP.md demonstrable (including the ROADMAP-pinned SC#4 `submit_all.sh --dry-run exits 0`)
- Fresh clone produces a green pytest run — the "final package" readiness gate
- Ready for Phase 31 closure (SUMMARY.md rollup) and user acceptance
</success_criteria>

<output>
After completion, create `.planning/phases/31-final-package-restructure/31-06-SUMMARY.md` documenting:
- New parametrized test classes added to the structure guard (with counts)
- docs/PROJECT_STRUCTURE.md word/line count
- CITATION.cff validation outcome (YAML parse + cffconvert if available)
- Full pytest run time + pass count
- ROADMAP SC#4 status: `bash cluster/submit_all.sh --dry-run` exit code + any MISSING python targets
- Quarto render result (paper.pdf page count if easy to extract)
- Readiness for user acceptance: any rough edges noted

Also create `.planning/phases/31-final-package-restructure/31-PHASE-SUMMARY.md` rolling up all 6 plan SUMMARYs into a single phase-closure doc suitable for merging into .planning/STATE.md milestone history. Include:
- Total plans executed (6)
- Waves (5 after revision: W1=31-01, W2=31-02 ‖ 31-03, W3=31-04, W4=31-05, W5=31-06)
- File counts: moved, edited, created, deleted
- Phase-level success criteria status (all 7 should be ✓)
- User acceptance checkpoint (if required separately from execute-phase workflow)
</output>

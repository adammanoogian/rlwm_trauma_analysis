---
phase: 20-deer-nonlinear-parallelization
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - docs/PARALLEL_SCAN_LIKELIHOOD.md
  - docs/JAX_GPU_BAYESIAN_FITTING.md
  - validation/benchmark_parallel_scan.py
autonomous: true

must_haves:
  truths:
    - "PARALLEL_SCAN_LIKELIHOOD.md documents the Phase 20 vectorized policy approach and the simulation-vs-likelihood distinction"
    - "JAX_GPU_BAYESIAN_FITTING.md exists as a practical end-to-end guide for GPU Bayesian fitting covering Phase 19 associative scan + Phase 20 vectorized policy findings"
    - "Benchmark script compares three variants: sequential, pscan-hybrid (Phase 19), and pscan-vectorized (Phase 20)"
    - "Benchmark JSON output includes timing for the vectorized variant alongside existing sequential and pscan entries"
  artifacts:
    - path: "docs/PARALLEL_SCAN_LIKELIHOOD.md"
      provides: "Updated implementation guide with Phase 20 vectorized policy section"
      contains: "Vectorized Policy"
    - path: "docs/JAX_GPU_BAYESIAN_FITTING.md"
      provides: "Practical GPU Bayesian fitting guide covering the full pipeline from data prep through MCMC with parallel scan likelihoods"
      min_lines: 100
      contains: "associative_scan"
    - path: "validation/benchmark_parallel_scan.py"
      provides: "Extended benchmark comparing sequential, hybrid pscan, and fully vectorized pscan"
      contains: "vectorized"
  key_links:
    - from: "validation/benchmark_parallel_scan.py"
      to: "scripts/fitting/jax_likelihoods.py"
      via: "imports multiblock stacked pscan functions for timing"
      pattern: "multiblock_likelihood_stacked_pscan"
    - from: "docs/PARALLEL_SCAN_LIKELIHOOD.md"
      to: "docs/DEER_NONLINEAR_PARALLELIZATION.md"
      via: "cross-reference for DEER analysis"
      pattern: "DEER_NONLINEAR_PARALLELIZATION"
    - from: "docs/JAX_GPU_BAYESIAN_FITTING.md"
      to: "docs/PARALLEL_SCAN_LIKELIHOOD.md"
      via: "cross-reference for scan implementation details"
      pattern: "PARALLEL_SCAN_LIKELIHOOD"
---

<objective>
Update documentation with Phase 20 findings, create the GPU Bayesian fitting guide, and extend the benchmark script to include the vectorized variant.

Purpose: Complete the Phase 20 deliverables by updating `docs/PARALLEL_SCAN_LIKELIHOOD.md` with the vectorized policy approach, creating `docs/JAX_GPU_BAYESIAN_FITTING.md` as the practical GPU fitting guide (fulfilling DEER-04 success criterion), and extending the benchmark to measure the actual performance difference between Phase 19 hybrid and Phase 20 fully-vectorized approaches.

Output: Updated docs + new GPU fitting guide + enhanced benchmark script + benchmark results JSON.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-deer-nonlinear-parallelization/20-02-SUMMARY.md
@docs/PARALLEL_SCAN_LIKELIHOOD.md
@docs/DEER_NONLINEAR_PARALLELIZATION.md
@validation/benchmark_parallel_scan.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PARALLEL_SCAN_LIKELIHOOD.md with Phase 20 findings</name>
  <files>docs/PARALLEL_SCAN_LIKELIHOOD.md</files>
  <action>
Add a new major section to `docs/PARALLEL_SCAN_LIKELIHOOD.md` (at the end, before any references section) covering Phase 20:

**Section title: "Phase 20: Vectorized Policy Pass (Fully Parallel Likelihood)"**

Content to include:

1. **The Simulation vs Likelihood Distinction**: Clearly explain that in likelihood evaluation, actions are observed data, making the perseveration carry `last_action[t] = actions[t-1]` precomputable from the data array. This was the key insight that made DEER unnecessary. In simulation (generating synthetic data), the perseveration carry IS a true sequential dependency because actions are sampled from the model's policy.

2. **Precomputation Approach**: Document the two utility functions:
   - `precompute_last_action_global(actions, mask)` for M3/M5 (global perseveration)
   - `precompute_last_actions_per_stimulus(stimuli, actions, mask, num_stimuli)` for M6a/M6b (per-stimulus)
   - Emphasize: parameter-independent, run once before MCMC, O(T) scan over observed data only

3. **Vectorized Phase 2**: Show the vectorized pattern replacing `lax.scan` with broadcasting/vmap. Include a concise code example for M3 showing the old (sequential) vs new (vectorized) approach.

4. **DEER No-Go Summary**: One paragraph summarizing why DEER was investigated and rejected. Cross-reference `docs/DEER_NONLINEAR_PARALLELIZATION.md` for the full analysis.

5. **Architecture Summary**: Update any existing architecture diagram or description to reflect the final 2-phase structure:
   - Phase 1: `associative_scan` for Q/WM trajectories (O(log T) depth, parallel)
   - Phase 2: Vectorized array ops for policy + log-prob (O(1) depth, embarrassingly parallel)
   - Result: Entire likelihood is fully parallel, suitable for GPU acceleration

Also update the opening paragraph of the document. Currently it says "Non-linear components (WM-Q mixing, softmax) remain sequential and are covered in Phase 20 (DEER)." Change this to reflect that Phase 20 found these components are NOT sequential and have been vectorized.

Do NOT remove any existing Phase 19 content. Only ADD the Phase 20 section and update the intro/summary.
  </action>
  <verify>
`grep -c "Phase 20\|Vectorized Policy\|precompute_last_action\|DEER.*No-Go\|simulation.*likelihood" docs/PARALLEL_SCAN_LIKELIHOOD.md` should return at least 5 matches.
  </verify>
  <done>
`docs/PARALLEL_SCAN_LIKELIHOOD.md` updated with Phase 20 vectorized policy section, simulation-vs-likelihood distinction, precomputation documentation, DEER no-go summary, and corrected architecture overview.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend benchmark script for 3-way comparison</name>
  <files>validation/benchmark_parallel_scan.py</files>
  <action>
The existing benchmark measures sequential vs pscan (Phase 19 hybrid) timing. Since Phase 20 modified the pscan functions in-place (they are now fully vectorized instead of hybrid), the benchmark already measures the "new" pscan. However, we need to:

1. **Update the benchmark output format**: In the JSON output, add a `"phase"` field to each model entry indicating `"phase_20_vectorized"` for the pscan results (previously this would have been `"phase_19_hybrid"`). This helps track which version of the pscan was benchmarked.

2. **Update the console output header**: Change from "Sequential vs PScan" to "Sequential vs Vectorized PScan" to reflect Phase 20.

3. **Add a note in the docstring**: Update the module docstring to explain that after Phase 20, the pscan variants use fully vectorized Phase 2 (no sequential policy scan). The benchmark now measures fully-parallel pscan vs sequential baseline.

4. **Run the benchmark**: Execute `python validation/benchmark_parallel_scan.py --model wmrl_m3 --n-repeats 5` to generate timing data. Record the result in the JSON output at `output/bayesian/pscan_benchmark_cpu.json`.

5. **Verify the benchmark runs correctly** and produces valid timing + agreement data. The NLL agreement should be < 1e-4 relative error (same as Phase 19 but now for vectorized pscan).

The benchmark JSON should include:
- `device`: "cpu" or "gpu"
- `jax_version`: from `jax.__version__`
- `phase`: "20_vectorized_pscan"
- Per-model entries with `sequential_ms`, `pscan_ms`, `speedup`, `nll_agreement_rel_error`
  </action>
  <verify>
Run: `python validation/benchmark_parallel_scan.py --model wmrl_m3 --n-repeats 5`
Benchmark completes without error. JSON output written. NLL agreement < 1e-4.
  </verify>
  <done>
Benchmark script updated to reflect Phase 20, runs successfully, and produces valid timing data comparing sequential vs fully-vectorized pscan for all 6 models.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create JAX GPU Bayesian fitting guide</name>
  <files>docs/JAX_GPU_BAYESIAN_FITTING.md</files>
  <action>
Create `docs/JAX_GPU_BAYESIAN_FITTING.md` as a practical end-to-end guide for GPU-accelerated Bayesian fitting in this project. This fulfills the DEER-04 success criterion ("JAX_GPU_BAYESIAN_FITTING.md updated with findings"). The document must synthesize Phase 19 and Phase 20 findings into an actionable reference.

Structure the guide with these sections:

1. **Overview**: One paragraph explaining the GPU Bayesian fitting pipeline -- NumPyro NUTS with JAX-parallelized likelihoods via associative scan (Phase 19) + vectorized policy (Phase 20). State the key result: the entire likelihood is now fully parallel, making GPU acceleration viable for all 6 choice-only models.

2. **Prerequisites**: JAX with GPU support (`jax[cuda12]`), NumPyro, the `rlwm_gpu` conda environment. Reference `cluster/12_mle_gpu.slurm` for SLURM setup. Note float32 sufficiency for choice-only models vs float64 requirement for M4 LBA.

3. **Likelihood Architecture**: Concise summary of the two-phase parallel structure:
   - Phase 1: `associative_scan` for Q-value and WM trajectories (O(log T) depth). Reference `associative_scan_q_update()` and `associative_scan_wm_update()` in `jax_likelihoods.py`.
   - Phase 2: Vectorized policy computation with precomputed perseveration arrays (O(1) depth). Reference `precompute_last_action_global()` and `precompute_last_actions_per_stimulus()`.
   - Cross-reference `docs/PARALLEL_SCAN_LIKELIHOOD.md` for implementation details.

4. **Using Parallel Scan Likelihoods**: Show how to use the `--use-pscan` CLI flag with `13_fit_bayesian.py`. Include example commands for CPU and GPU. Explain that pscan likelihoods are numerically equivalent to sequential (< 1e-4 relative error) and can be toggled without affecting scientific results.

5. **Performance Characteristics**: Document what we know from benchmarks:
   - CPU: pscan is currently slower than sequential for choice-only models due to vmap overhead on CPU (Phase 19 benchmark finding: ~0.26x on CPU for M3). The parallel structure only pays off on GPU where the parallelism is hardware-exploited.
   - GPU: Expected speedup TBD from cluster benchmarks (reference `output/bayesian/pscan_benchmark.json` and the SLURM scripts in `cluster/`).
   - Note that the performance story is about enabling GPU acceleration, not CPU speedup.

6. **DEER Investigation Summary**: One paragraph explaining why DEER (fixed-point iteration for non-linear components) was investigated and rejected in favor of vectorized policy. Cross-reference `docs/DEER_NONLINEAR_PARALLELIZATION.md` for the full analysis.

7. **Model-Specific Notes**: Brief table or list noting per-model considerations:
   - M1 (Q-learning): No perseveration, simplest pscan path
   - M2 (WM-RL): No perseveration, WM-Q mixing is vectorized
   - M3, M5: Global perseveration via `precompute_last_action_global`
   - M6a: Per-stimulus perseveration via `precompute_last_actions_per_stimulus`
   - M6b: Dual perseveration (both precomputation functions)
   - M4 (LBA): Joint choice+RT, requires float64, separate GPU path via `fit_all_gpu_m4`

8. **Troubleshooting**: Common issues -- JAX GPU not detected, OOM on large participant counts, float32 vs float64 confusion, vmap overhead on CPU.

Cross-reference `docs/PARALLEL_SCAN_LIKELIHOOD.md` and `docs/DEER_NONLINEAR_PARALLELIZATION.md` throughout. Keep the tone practical and concise -- this is a "how to use it" guide, not a research document.
  </action>
  <verify>
Verify the document exists and covers all key sections: `grep -c "Prerequisites\|Likelihood Architecture\|associative_scan\|precompute_last_action\|use-pscan\|Performance\|DEER\|Troubleshooting" docs/JAX_GPU_BAYESIAN_FITTING.md` should return at least 8 matches. `wc -l docs/JAX_GPU_BAYESIAN_FITTING.md` should show at least 100 lines.
  </verify>
  <done>
`docs/JAX_GPU_BAYESIAN_FITTING.md` exists as a practical guide covering the full GPU Bayesian fitting pipeline, synthesizing Phase 19 and Phase 20 findings, with actionable commands and cross-references to the detailed research documents.
  </done>
</task>

</tasks>

<verification>
1. `docs/PARALLEL_SCAN_LIKELIHOOD.md` has Phase 20 content: `grep "Vectorized Policy" docs/PARALLEL_SCAN_LIKELIHOOD.md`
2. `docs/JAX_GPU_BAYESIAN_FITTING.md` exists and covers the full pipeline: `test -f docs/JAX_GPU_BAYESIAN_FITTING.md && grep "associative_scan" docs/JAX_GPU_BAYESIAN_FITTING.md`
3. Benchmark runs: `python validation/benchmark_parallel_scan.py --model wmrl_m3 --n-repeats 3`
4. All existing tests still pass: `python -m pytest scripts/fitting/tests/test_pscan_likelihoods.py -v --tb=short`
</verification>

<success_criteria>
- PARALLEL_SCAN_LIKELIHOOD.md documents the complete parallel likelihood architecture (Phase 1 scan + Phase 2 vectorized)
- JAX_GPU_BAYESIAN_FITTING.md is a complete practical guide for GPU Bayesian fitting
- Benchmark script reflects Phase 20 and produces valid timing data
- Phase 20 is fully documented: research doc (20-01), implementation (20-02), docs+benchmark+guide (20-03)
</success_criteria>

<output>
After completion, create `.planning/phases/20-deer-nonlinear-parallelization/20-03-SUMMARY.md`
</output>

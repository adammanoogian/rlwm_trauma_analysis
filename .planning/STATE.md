# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-19 — v5.0 Empirical Artifacts & Manuscript Finalization started)

**Core value:** Correctly dissociate perseverative responding from learning-rate effects (alpha-) to accurately identify whether post-reversal failures reflect motor perseveration or outcome insensitivity
**Current focus:** v5.0 started 2026-04-19 — Cold-start `bash cluster/21_submit_pipeline.sh` to produce Phase 21 empirical artifacts; tech-debt sweep (legacy qlearning import, legacy M2 [1,7] K-bounds, 16b_bayesian_regression.py, load-side validation audit); prior-predictive + recovery reproducibility regression vs. v4.0 baseline; manuscript finalization so `quarto render paper.qmd` produces paper.pdf with real winner names + real Pareto-k-informed limitations. Phase 14 cluster-execution items (K-02, K-03, GPU-01..03) explicitly deferred to v5.1.

## Current Position

Milestone: v5.0 Empirical Artifacts & Manuscript Finalization (started 2026-04-19)
Phase: 29 of 29 (29-pipeline-canonical-reorg) — IN PROGRESS 2026-04-22
Plan: 7 of 9 in phase 29 — 29-01, 29-02, 29-03, 29-04, 29-04b, 29-05, 29-06 COMPLETE
Status: Phase 29 active. Plans 29-01..29-04 + 29-04b + Wave 4 (29-05 cluster SLURM consolidation + 29-06 paper.qmd smoke render) complete. Plans 29-07, 29-08 pending. Wave 4 ran 29-05 and 29-06 in parallel non-conflicting (different directories: cluster/ vs manuscript/); 29-06's metadata commit (955f903) absorbed 29-05's staged SLURM renames/deletions per the documented parallel-agent git-index race pattern — final state is correct.
Next: Wave 5 (29-07 closure-guard extension, 29-08 src/fitting vertical refactor).

Progress: v5.0 [█████████░░░░░] 10/15 plans complete (Phases 23-28 closed; Phase 29 at 7/9 after 29-05)

Last activity: 2026-04-22 — **Plan 29-05 complete (cluster SLURM consolidation; 2 atomic commits: f81b999 path-update sweep + a7159e6 stage-numbered consolidation).** Created 7 new stage-numbered entry SLURMs (cluster/0{1..6}*.slurm + submit_all.sh master afterok chain with --dry-run path validator); renamed 5 entry SLURMs to stage-numbered names (12_mle -> 04a_mle_cpu, 12_mle_gpu -> 04a_mle_gpu, 13_bayesian_choice_only -> 04b_bayesian_cpu, 13_bayesian_gpu -> 04b_bayesian_gpu, 21_6_fit_with_l2 -> 04c_level2); deleted 17 consolidated SLURMs (09_ppc_gpu, 11_recovery_gpu, 12_mle_single, 12_submit_all{_gpu}.sh, 13_bayesian_m4_gpu, 13_bayesian_m6b_subscale, 14_analysis, 21_1..21_9_* except 21_6 which was renamed); folded M6b subscale into 04b_bayesian_cpu.slurm via `--export=SUBSCALE=1` and M4 LBA into 04b_bayesian_gpu.slurm via `--export=MODEL=wmrl_m4` + `--time=48:00:00 --mem=96G --gres=gpu:a100:1` overrides; rewrote 21_submit_pipeline.sh as a one-line shim `exec bash cluster/submit_all.sh "$@"` (preserves v4.0 user-memory-documented invocation while routing through post-29-05 canonical master; 2-cov L2 hook pytest pre-flight gate retained); rewrote cluster/README.md with post-29-05 canonical layout + full consolidation map + retained-specialized list + `--dry-run` semantics; updated submit_full_pipeline.sh to use stage-numbered SLURMs via STEP=model_recovery / STEP=posterior_ppc / STEP=compare_models overrides; updated 21_6_dispatch_l2.slurm + 21_dispatch_l2_winners.sh to reference cluster/04c_level2.slurm (was 21_6_fit_with_l2.slurm). Parameterized STEP= dispatch (case-statement inside SLURM body) for stage 03 (synthetic|parameter_sweep|model_recovery|prior_predictive|bayesian_recovery), stage 05 (baseline_audit|scale_audit|posterior_ppc), stage 06 (compare_models|loo_stacking|model_averaging|analyze_mle_by_trauma|regress_parameters_on_scales|analyze_winner_heterogeneity|bayesian_level2_effects|manuscript_tables). Verification: `bash cluster/submit_all.sh --dry-run` exits 0 (every SLURM passes bash -n, every python scripts/*.py target resolves on disk); grep for stale paths (scripts/{data_processing,behavioral,simulations_recovery,post_mle,bayesian_pipeline,12_fit_mle,13_fit_bayesian,14_compare_models,fitting/fit_mle,fitting/fit_bayesian}) returns zero hits across cluster/*.slurm + cluster/*.sh; validation/check_v4_closure.py 5/5 PASS; pytest scripts/fitting/tests/test_v4_closure.py 3/3 PASS. Auto-fixes (all Rule 1/3 folded into Task 2 commit): (a) submit() function initially polluted stdout with log lines breaking `$(submit ...)` JID capture — rerouted log output to stderr; (b) path-grep initially flagged commented-out `scripts/03_model_prefitting/09_run_ppc.py` mention in docstring — tightened grep to only inspect lines starting with python|CMD=|srun|"python". Parallel-agent git-index race (Deviation #1, Rule 3 accepted): 29-06's metadata commit 955f903 absorbed my Task 2 staged renames + deletions (16 deletes + 5 renames) — final state correct, history preserved under 955f903 instead of my 29-05 commit; Task 2 commit a7159e6 covers the remaining work (new SLURMs + docstring updates + README + shim). Retained specialized SLURMs kept verbatim: 13_full_pipeline, 13_bayesian_multigpu, 13_bayesian_permutation, 13_bayesian_pscan{,_smoke}, 13_bayesian_fullybatched_smoke, 19_benchmark_pscan_{cpu,gpu}, 23.1_mgpu_smoke, 21_6_dispatch_l2 + 21_dispatch_l2_winners.sh (L2 winner sbatch --wait fan-out), 01_diagnostic_gpu, 99_push_results, submit_full_pipeline.sh. SUMMARY: .planning/phases/29-pipeline-canonical-reorg/29-05-SUMMARY.md. Prior: **Plan 29-06 complete (paper.qmd smoke render; atomic commit 2b26df0).** Verified 29-04b commit 093d934 absorbed this plan's Task 1 script-path rewrites: grep for the eight stale grouping-dir/top-level fit-stage patterns in manuscript/paper.{qmd,tex} returned zero matches. Applied the absorbed-from-29-02 line-166 caption rewrite on paper.qmd (`docs/SCALES_AND_FITTING_AUDIT.md` → `docs/04_methods/README.md#scales-orthogonalization-and-audit`; anchor verified at docs/04_methods/README.md:284). Fixed pre-existing YAML indentation bug in paper.qmd author block (line 7 `name:` at 10 spaces → 8 spaces) that was blocking quarto render with `bad indentation of a mapping entry (7:15)` — Rule 3 auto-fix. `quarto render manuscript/paper.qmd` exits 0, produces manuscript/_output/paper.pdf at 1,065,724 bytes (~1.04 MB); zero FileNotFoundError, zero ModuleNotFoundError, only pre-existing 5 BibTeX warnings for missing refs (phan2019composable, hoffman2014no, ahn2017revealing, vehtari2017practical, yao2018using — deferred to Phase 26). All 20 graceful-fallback `{python}` cells (Phase 28 pattern) absorbed missing cold-start artifacts cleanly; zero cells flagged for Phase 26 follow-up. paper.tex regenerated by `keep-tex: true` — pdfauthor metadata corrected from legacy `{Adam Manoogian, Coauthor Name}` placeholder to `{Jasmine Burrows, Adam Manoogian}`, and line-226 caption rewrite propagated from paper.qmd. v4 closure 5/5 PASS; pytest test_v4_closure 3/3 PASS. Consolidated Task 1+2 into single code commit (2b26df0) because Task 1 residual was only 2 lines and paper.tex regeneration is a direct byproduct of Task 2's render. **Parallel-agent git-index race with 29-05**: metadata commit 955f903 absorbed 20 cluster SLURM renames/deletions staged by concurrent 29-05 agent (same pattern documented for 29-03/29-04 race); actual file changes are correct, just mislabelled under 29-06 instead of 29-05 — 29-05's SUMMARY should cross-reference 955f903 alongside its primary commit f81b999. SUMMARY: .planning/phases/29-pipeline-canonical-reorg/29-06-SUMMARY.md. Prior: **Plan 29-04b complete (intra-stage renumbering Scheme D; 6 atomic refactor commits: fa9d101, c1a879a, 833b5c8, f456e9c, d49597a, 093d934).** All six stage folders now use Scheme D: stages 01/02/03/05/06 have intra-stage numbers 01-N (reset per stage); stage 04's parallel-alternative subfolders (a_mle, b_bayesian, c_level2) use canonical descriptive CLI names with library code under underscore-private `_engine.py`. 24 files renamed via `git mv` (history preserved) across stages 02 (4), 03 (5 + 1 deletion), 04 (6 with two-step collision resolution), 05 (3), 06 (8). **Library/CLI collision pattern:** `fit_mle.py` (3,157-line library) → `_engine.py`; `12_fit_mle.py` (74-line CLI) → `fit_mle.py`. Same for b_bayesian. CLI wrappers load `_engine.py` via `importlib.util.spec_from_file_location` by absolute path because (a) dotted imports `scripts.04_model_fitting.*` are illegal (digit-leading), (b) relative imports fail when invoked as scripts. **09_run_ppc.py deleted** (Rule 3 auto-fix): 29-03 intent was to remove it when extracting simulator to scripts/utils/ppc.py; confirmed via inspection that it was a pure duplicate of scripts/05_post_fitting_checks/run_posterior_ppc.py (both call the same run_posterior_ppc()); stage 05 is sole posterior-PPC entry point per Scheme D; cluster/09_ppc_gpu.slurm retargeted. **Six pre-existing 29-01-era bugs auto-fixed** (Rule 1/3): (1) 12_fit_mle.py + 13_fit_bayesian.py broken relative-import `--help` → importlib-by-path pattern; (2) fit_baseline.py stale import from nonexistent scripts.fitting.fit_bayesian → importlib-by-path; (3) fit_with_l2.py _PROJECT_ROOT wrong → parents[3]; (4-6) Five 06_fit_analyses/ files had parents[1]/parent.parent path-bootstrap bugs that resolved to scripts/ not project root; fixed with parents[2] + dual sys.path.insert where needed. All 4 mandated --help smoke tests (fit_mle / fit_bayesian / fit_baseline / fit_with_l2) now exit 0; 01_compare_models + 08_manuscript_tables also exit 0. **Importer sweep:** grep for stale paths (02/05-08, 03/09-13, 04/12/13/21, 05/unnumbered, 06/unnumbered) returns zero live hits outside .planning/ (historical) and docs/legacy/ (archived). Updated across 27+ external files: CLAUDE.md (including major pipeline-diagram overhaul to Scheme D canonical + naming-rules block), README.md, 5 docs files, manuscript/paper.qmd (3 Quarto fallback strings + forest-plot comment) + paper.tex, 13 cluster SLURMs, run_data_pipeline.py, scripts/utils/ppc.py docstring, scripts/fitting/model_recovery.py subprocess call, 3 scripts/fitting/tests/ files, tests/test_performance_plots.py, scripts/06_fit_analyses/08_manuscript_tables.py subprocess call (bayesian_level2_effects.py → 07_). test_load_side_validation.py _ENUMERATED_FILES re-mapped 6 entries. Verification: `validation/check_v4_closure.py` 5/5 PASS exit 0; `pytest scripts/fitting/tests/test_v4_closure.py test_load_side_validation.py -v` 5/5 PASS. SUMMARY: .planning/phases/29-pipeline-canonical-reorg/29-04b-SUMMARY.md. Prior: **Plan 29-03 complete (utils consolidation; atomic refactor 298f82d, 20 files).** `scripts/utils/ppc.py` (935 lines, 11 top-level defs: `simulate_from_samples` + `run_prior_ppc` + `run_posterior_ppc` + 8 private helpers) is now the single-source PPC simulator — consumed by stage 03 prior PPC (`12_run_prior_predictive.py`, rewritten from 649 → 109 lines as thin argparse orchestrator), stage 03 posterior PPC (`09_run_ppc.py`, rewritten as thin orchestrator), and new stage 05 posterior PPC orchestrator (`scripts/05_post_fitting_checks/run_posterior_ppc.py`, 134 lines, mirror of stage 03 prior-PPC). Canonical short names via `git mv` (history preserved): plotting_utils.py → plotting.py, statistical_tests.py → stats.py, scoring_functions.py → scoring.py. New `scripts/utils/__init__.py` (package marker + module-map docstring). `scripts/_maintenance/` created for 3 one-off human-run CLIs (`remap_mle_ids.py`, `sync_experiment_data.py`, `update_participant_mapping.py`) — all three moved out of utils/ per the "utils iff ≥ 2 stage importers" rule (zero importers, `def main()` only). 9 live importers rewritten: `scripts/fitting/model_recovery.py` (×3 plotting_utils→plotting), `scripts/06_fit_analyses/{analyze_mle_by_trauma,regress_parameters_on_scales}.py` (utils.plotting_utils→utils.plotting), `scripts/02_behav_analyses/08_run_statistical_analyses.py` (statistical_tests→stats), `scripts/01_data_preprocessing/{01_parse_raw_data,02_create_collated_csv,04_create_summary_csv}.py` (bare-name `from scoring_functions`→`from scoring`, preserving intentional sys.path trick), plus `scripts/fitting/level2_design.py` (×2 docstring scoring_functions.py path updates). Lazy-import pattern inside run_prior_ppc/run_posterior_ppc bodies (jax/numpyro/arviz/scripts.fitting.*) so cheap callers can import `simulate_from_samples` without paying JAX startup cost. Deferred: `scripts/analysis/plotting_utils.py` sibling (not a true duplicate — different function surface; handled by parallel 29-04 which archived scripts/analysis/ → scripts/legacy/analysis/) and `scripts/01_data_preprocessing/*.py` bare-name sys.path convention (Phase-28-era pattern preserved to minimise diff footprint; future tech-debt plan can migrate). Parallel-agent git-index race with 29-04 encountered mid-execution (29-04's first commit `f0f02bd` transiently absorbed my staged utils/ changes, then amended to `e574fed` dropping them); recovered by re-staging 29-03 files fresh. Verification: `python -c "from scripts.utils import ppc, plotting, stats, scoring"` OK; `pytest scripts/fitting/tests/test_v4_closure.py -v` 3/3 PASS; `grep -rn "def simulate_from_samples\|def run_prior_ppc\|def run_posterior_ppc" scripts/ --include="*.py" | grep -v "scripts/utils/ppc.py" | grep -v "scripts/legacy/"` returns zero; stale importer grep for `from scripts.utils.plotting_utils|statistical_tests|scoring_functions` returns zero in scripts/ tests/ validation/ cluster/ manuscript/ docs/ src/. Task commit: 298f82d (atomic, 20 files, 946 insertions / 1031 deletions). SUMMARY: .planning/phases/29-pipeline-canonical-reorg/29-03-SUMMARY.md. Prior: **Plan 29-04 complete (dead-folder audit; atomic refactor e574fed, 48 files).** Five sibling folders (scripts/{analysis, results, simulations, statistical_analyses, visualization}) archived to scripts/legacy/ via whole-folder git mv (history preserved). Per-folder evidence-based decisions in scripts/legacy/README.md (231 lines, 5 H2 sections, 9+5+5+1+11=31 files archived). 7 live importers rewritten to `from scripts.legacy.simulations.*` (validation/test_unified_simulator.py, tests/test_wmrl_exploration.py, 2 × scripts/03_model_prefitting wrappers, 3 × tests/legacy/examples/*). Plan-internal coordination with 29-01 resolved: 5 `_ENUMERATED_FILES` entries in test_load_side_validation.py restored from TODO(29-04) stubs to `scripts/legacy/<folder>/` paths. Manuscript/paper.tex line 244 + paper.qmd line 171 caption paths rewritten to scripts/legacy/. docs/02_pipeline_guide/PLOTTING_REFERENCE.md got a "Historical reference" banner + 8 path rewrites. run_data_pipeline.sh: 4 pre-existing stale references (to files deleted pre-Phase-28) commented out with explanatory block comment (Rule 1 bug deviation). Recovery deviation: staging-mix with parallel 29-03 absorbed into first Task 2 commit attempt — reset --mixed + explicit re-staging produced clean 48-file commit. Verification: v4 closure 3/3 PASS, load-side validation 2/2 PASS (zero [MISSING] violations), validation/test_unified_simulator.py collects 13 tests without ImportError, grep for stale `from scripts.{analysis,results,simulations,statistical_analyses,visualization}.` returns zero outside scripts/legacy/ and .planning/ (lone remaining hit is prose docstring in scripts/utils/ppc.py authored by parallel 29-03, outside 29-04 scope). Task commits: 0cb1e2b (audit README), e574fed (atomic refactor). SUMMARY: .planning/phases/29-pipeline-canonical-reorg/29-04-SUMMARY.md. Prior: **Plan 29-01 complete (scripts canonical reorg; atomic refactor 04ebc72, 76 files).** Big rename wave: Phase 28's five-subdir grouping promoted to numbered 01-06 stage layout via git mv (history preserved). All cluster SLURMs (18 files), docs, manuscript, CLAUDE.md, README.md, config.py, tests/, validation/ updated. Digit-prefix __init__.py guard comments installed; relative imports fixed in entry scripts; parents[3] depth corrected. test_load_side_validation.py remapped (5 entries commented out with TODO(29-04) for not-yet-created stages). v4.0 closure guard 5/5 PASS; pytest test_v4_closure + test_load_side_validation 5/5 PASS. SUMMARY: .planning/phases/29-pipeline-canonical-reorg/29-01-SUMMARY.md. Prior: **Plan 28-12 complete (end-of-phase verification; status=gaps_found, all blocking gaps fixed).** Verification gauntlet: v4.0 closure guards pass (5/5 + 3/3). 7 grep invariants: 6 PASS, 1 INFO (docstring-only hits in 21_manuscript_tables.py line 41/700/711 — functional subprocess call at line 746 correct). quarto render exits 0, manuscript/_output/paper.pdf confirmed. Full pytest: 3 REFAC-07 test regressions found (test_bayesian_recovery.py + test_loo_stacking.py + test_prior_predictive.py hardcoded old scripts/21_*.py paths) — all fixed in commit 907fa8c; 15 affected tests now pass; 69 non-JAX tests pass. Windows JAX backend crash pre-existing (not Phase 28 regression). All 13 REFAC-* CLOSED, 10/10 ROADMAP criteria assessed (9 PASS, 1 PARTIAL for SC9 Windows JAX crash pre-existing). SUMMARY: .planning/phases/28-bayesian-first-restructure-repo-cleanup/28-VERIFICATION.md. Prior: **Plan 28-10 complete (paper Bayesian-first scaffolding; atomic refactor e3f93f4).** paper.qmd Results reordered to Summary → Bayesian Model Selection Pipeline (#sec-bayesian-selection) → Hierarchical L2 Trauma Associations (#sec-bayesian-regression) → Subscale Breakdown (#sec-subscale-breakdown). MLE content moved to Appendix A-K after References. Four graceful-fallback {python} cells added (tbl-loo-stacking, tbl-rfx-bms, fig-forest-21, tbl-winner-betas) following existing #fig-l2-forest Path(...).exists() pattern. All section IDs preserved (only order changed); existing MLE cross-refs intact. quarto render exits 0, PDF produced. test_v4_closure 3/3, test_load_side_validation 2/2 PASS. REFAC-11 closed. SUMMARY: .planning/phases/28-bayesian-first-restructure-repo-cleanup/28-10-paper-bayesian-first-scaffolding-SUMMARY.md. Prior: **Plan 28-11 complete (docs refresh; atomic commit 3316247).** CLAUDE.md Code Organization rewritten to show data_processing/, behavioral/, simulations_recovery/, post_mle/, bayesian_pipeline/ subdirs + src/rlwm/fitting/ library. Quick Reference Run Full Pipeline + new Run Bayesian Pipeline section + Cluster Execution (13_bayesian_choice_only.slurm template) + Cross-Model Recovery updated. README.md Pipeline block updated. docs/02_pipeline_guide/ANALYSIS_PIPELINE.md + docs/03_methods_reference/TASK_AND_ENVIRONMENT.md + MODEL_REFERENCE.md + docs/04_methods/README.md + docs/04_results/README.md + docs/HIERARCHICAL_BAYESIAN.md: all stale scripts/NN_* paths and environments/ + models/ import paths updated. Stale-path grep invariant zero matches. test_v4_closure 3/3, test_load_side_validation 2/2 PASS. REFAC-12 closed. SUMMARY: .planning/phases/28-bayesian-first-restructure-repo-cleanup/28-11-docs-refresh-SUMMARY.md.

Last activity: 2026-04-21 — **Plan 28-08 complete (cluster SLURM consolidation; atomic refactor 4af60ba).** 6 per-model Bayesian SLURM templates (13_bayesian_m{1,2,3,5,6a,6b}.slurm) deleted via git rm; cluster/13_bayesian_choice_only.slurm created with MODEL env var (default wmrl_m3) and M6b 36h --time override convention. 21_submit_pipeline.sh and 21_3_fit_baseline.slurm unchanged (already parameterized). Bash syntax check OK; grep invariant zero stale refs to deleted templates (m4_gpu self-refs are false positives in retained template); test_v4_closure 3/3, test_load_side_validation 2/2 PASS. REFAC-09 closed. SUMMARY: .planning/phases/28-bayesian-first-restructure-repo-cleanup/28-08-cluster-consolidation-SUMMARY.md. Prior: **Plan 28-06 complete (scripts/bayesian_pipeline/ group; atomic refactor 6473f34).** Nine Phase 21 scripts moved via git mv to scripts/bayesian_pipeline/; 10 SLURM files updated to python scripts/bayesian_pipeline/21_*.py; 4 stale 13_bayesian_m6b.slurm pattern comments cleaned; path bootstrap depth fixed to .parent.parent.parent in 8 scripts (Rule 1 — Bug); test_load_side_validation.py 5 enumerated paths updated; 5/5 tests pass. Note: 21_8_model_averaging.slurm retains operational references to cluster/13_bayesian_m6b_subscale.slurm (functional code, not stale comments) — Plan 28-08 should distinguish between 13_bayesian_m[1-6].slurm templates (to delete) and 13_bayesian_m6b_subscale.slurm (operational). REFAC-07 closed. SUMMARY: .planning/phases/28-bayesian-first-restructure-repo-cleanup/28-06-bayesian-pipeline-group-SUMMARY.md. Prior: **Plan 28-09 complete (validation/tests pruning; atomic refactor baa6aa9).** validation/test_fitting_quick.py deleted; validation/check_phase_23_1_smoke.py + diagnose_gpu.py moved to validation/legacy/; tests/examples/ moved to tests/legacy/examples/; validation/README.md updated with Legacy Files section. File moves/deletion were executed by parallel wave 28-04 (commit 0f15fcf); 28-09 added README documentation and verified pre-flight safety. benchmark_parallel_scan.py confirmed retained. v4 closure guard 3/3 PASS. SUMMARY: .planning/phases/28-bayesian-first-restructure-repo-cleanup/28-09-validation-tests-pruning-SUMMARY.md. Prior: **Plan 28-07 complete (figures/output scaffolding atomic commit a258d11).** Phase 24 output scaffold .gitkeep files created (figures/21_bayesian, output/bayesian/{21_baseline,21_l2,21_recovery,21_prior_predictive,manuscript}); legacy tracked moves (output/v1, modelling_base_models, base_model_analysis, figures/feedback_learning) were already committed by parallel 28-04 (0f15fcf); filesystem-only moves for untracked dirs (_tmp_param_sweep, figures/v1). paper.qmd path invariant verified (diff empty); quarto render exit 0; pytest closure guard 3/3 PASS. SUMMARY: .planning/phases/28-bayesian-first-restructure-repo-cleanup/28-07-figures-output-scaffolding-SUMMARY.md. Prior: **Plan 28-02 complete (data-processing grouping absorbed in 0f15fcf+5ada618).** Scripts 01-04 moved to scripts/data_processing/ via git mv; sys.path depth fixed in 01/02/04; run_data_pipeline.py and run_data_pipeline.sh updated to new paths; pytest closure guard 3/3 PASS. SUMMARY: .planning/phases/28-bayesian-first-restructure-repo-cleanup/28-02-data-processing-group-SUMMARY.md. Prior: **Plan 28-04 complete (simulations/recovery grouping atomic commit 0f15fcf).** Four scripts (09 x2, 10, 11) moved to scripts/simulations_recovery/ via git mv; scripts/simulations_recovery/__init__.py created; cluster/09_ppc_gpu.slurm invocation updated. Smoke tests 3/3, pytest closure guard 3/3 PASS. cluster/13_full_pipeline.slurm stale refs deferred to Wave 4 (plans 28-06/28-08). SUMMARY: .planning/phases/28-bayesian-first-restructure-repo-cleanup/28-04-simulations-recovery-group-SUMMARY.md. Prior: 2026-04-21 — **Plan 28-01 complete (src/ consolidation atomic commit 1f06ee7).** Deleted environments/ and models/ shim packages, git-moved 3 JAX/NumPyro math modules to src/rlwm/fitting/, updated 44 call sites to canonical rlwm.* imports. All grep invariants zero, v4 closure guard 3/3 PASS, test collection clean. Wave 2 unblocked. SUMMARY: .planning/phases/28-bayesian-first-restructure-repo-cleanup/28-01-src-consolidation-SUMMARY.md. Prior: 2026-04-19 — **Phase 23 complete (4/4 plans, all CLEAN-01..04 closed).** 23-VERIFICATION.md: status=passed, score=4/4 must-haves verified, 7/7 guard tests green, v4.0 closure guard 5/5 exit 0. 12 atomic commits landed on main (4 plans × 3: guard-red → code-green → docs). Pre-existing Windows CPU JAX M4-integration fatal and GPU-marked M4 tests confirmed environmental (not Phase 23 regressions) — v4.0 milestone-audit cluster-pending items cover them. Prior: Completed 23-04-PLAN.md (NetCDF load-side validation CLEAN-04: load_netcdf_with_validation added to config.py; 15 az.from_netcdf call sites rewired across 13 files; grep invariant guard installed; CLEAN-04 closed)

Progress: v5.0 4/14 plans complete [███░░░░░░░░░░░] [█░░░░░░░░░░░░░] v4.0 milestone closed 2026-04-19 (Phase 22 SHIPPED, 11/11+4/4=47 plans, 172 commits, tag `v4.0`). Earlier v4.0 status: Phase 21 capstone shipped — Step 21.9 manuscript tables runner live (`scripts/21_manuscript_tables.py` ~1100 lines, pure pandas + matplotlib, no JAX). Three table generators (Table 1 LOO+stacking with bolded winners, Table 2 RFX-BMS PXP, Table 3 winner beta HDIs with model_averaged_* columns where canonical key matches) each write .csv + .md + .tex via shared `TableArtefact` dataclass + `_write_table_artefact` helper. Figure 1 forest plot delegates to `scripts/18_bayesian_level2_effects.py` via subprocess per winner (single source of truth for matplotlib styling). M6b subscale guard via `Path("output/bayesian/wmrl_m6b_subscale_posterior.nc").exists()` check (plan-checker Issue #9) — when missing, NOTE log line + caption-footer note instead of raising; allows main manuscript build to ship even if 12h subscale fit hasn't finished. Null-result branch writes `null_result_summary.md` + skips forest plot when audit pipeline_action == NULL_RESULT (suppresses misleading all-null forest plot). `update_paper_qmd()` function inserts `### Bayesian Model Selection Pipeline {#sec-bayesian-selection}` Methods subsection BEFORE Phase-18-05 locked `{#sec-bayesian-regression}` anchor; idempotent re-runs no-op via anchor presence check. Also replaces hard-coded "M6b received the highest stacking weight..." Results sentence with stacking-weight-aware `{python} winner_display` Quarto inline reference. CLI: 8 args incl. `--no-paper-edit` (cluster default) and `--subscale-nc` (Phase-16 canonical path). `cluster/21_9_manuscript_tables.slurm` (170 lines, 30min/16G/2-CPU/comp, no JAX, ds_env ladder, 5 env-var overrides, `--no-paper-edit` invocation, autopush, `exit $EXIT_CODE` for afterok terminus). **Master orchestrator `cluster/21_submit_pipeline.sh` (~200 lines, +x)** chains all 9 Phase 21 steps via `sbatch --parsable` + `--dependency=afterok` exclusively (no afterany — verified via `grep "afterany" cluster/21_submit_pipeline.sh` empty). Pre-flight gate runs `pytest scripts/fitting/tests/test_numpyro_models_2cov.py -v -k "not slow" --tb=short` LOCALLY before any sbatch call; aborts with clear message + exit 1 if 2-cov L2 hook is broken (saves ~150 GPU-hours per pipeline run). All 9 steps chained: 21.1 (6 parallel prior predictive) → 21.2 (per-model array + aggregate, 6 pairs) → 21.3 (6 parallel baseline fits) → 21.4 (single audit) → 21.5 (single LOO+stacking+BMS) → 21.6 (L2 dispatcher via proper SLURM wrapper) → 21.7 (single scale audit) → 21.8 (single averaging) → 21.9 (single tables). **`cluster/21_6_dispatch_l2.slurm` (~50 lines)** is a proper SLURM wrapper (plan-checker Issue #6) for the L2 dispatcher with `--time=14:00:00` (12h M6b subscale worst case + 2h slack), `--mem=2G --cpus-per-task=1` because the dispatcher itself only blocks on `sbatch --wait` — without this wrapper, a `sbatch --wrap=...` job would inherit the account default time limit and be killed by the scheduler before the L2 fits complete. **`cluster/21_dispatch_l2_winners.sh` (~70 lines, +x)** is the canonical block (rejected exploratory `BARRIER_JID` + `afterany` rewiring): reads winners.txt (exit 2 on missing for INCONCLUSIVE_MULTIPLE pause), maps display names to internal model ids via NAME_MAP, submits one `cluster/21_6_fit_with_l2.slurm` per winner with `sbatch --wait ... &` (parallel children), then single `wait` blocks until slowest finishes. paper.qmd Methods now contains the `### Bayesian Model Selection Pipeline {#sec-bayesian-selection}` subsection citing Baribault & Collins 2023 (DOI 10.1037/met0000554) + Hess et al. 2025 (DOI 10.5334/cpsy.116) + Yao et al. 2018 (DOI 10.1214/17-BA1091) + Stephan 2009 / Rigoux 2014 + describing the 2-cov vs 4-cov subscale L2 design split. Quarto cross-refs `@tbl-loo-stacking`, `@tbl-rfx-bms`, `@tbl-winner-betas`, `@fig-forest-21` added at end of methodology paragraph. Tasks 1+1b+2 commits: `ca24736` (script + capstone SLURM), `269deea` (paper.qmd patch), `46872a1` (master orch + dispatcher SLURM wrapper + dispatcher .sh). Auto-fixes (both Rule 1 — Bug, folded into Task 1 commit pre-push): (a) Table 1 rank-column collision when `loo_stacking_results.csv` already contains `rank` from upstream step 21.5 — added defensive `if "rank" in df.columns: df = df.drop(...)` before insert; (b) pandas FutureWarning on bold-row dtype incompatibility when wrapping numeric column values with `**...**` — cast DataFrame to object dtype before bold-wrapping. All success criteria pass: `--help` shows 8-arg CLI, paper.qmd has both DOIs, `grep -c "afterok" cluster/21_submit_pipeline.sh` = 19 (≥ 6), `grep "afterany" cluster/21_submit_pipeline.sh` empty, `grep -c "sbatch --parsable"` = 10 (≥ 8), 14h time cap present in dispatcher SLURM, pre-flight pytest gate present in master orch, both .sh files +x, `BARRIER_JID`/`afterany` absent from dispatcher .sh, all 3 shell files pass `bash -n`. Stub-tested end-to-end on synthetic fixtures: PROCEED branch (M6b+M5 winners) → 3 tables generated with M6b row bolded + 6 Table-3 rows incl model_averaged_* columns; NULL_RESULT branch → null_result_summary.md + Tables 1/2 only (no Table 3, no forest plot); M6b subscale absent → NOTE log line, no error. Prior: Step 21.8 model averaging runner live — `scripts/21_model_averaging.py` (~600 lines, pure ArviZ + pandas + numpy) reads step-21.7 YAML `pipeline_action` header with hand-written front-matter parser (avoids PyYAML dep), short-circuits on `NULL_RESULT` (writes `averaging_skipped.md` + exit 0) and on `len(winners)==1` (writes `single_winner_mode.md` + exit 0); main path reads `loo_stacking_results.csv` weights via display-name index, normalises to sum 1.0 across winner subset, loads each `{winner}_posterior.nc` via `az.from_netcdf`, enumerates `beta_*` sites via `startswith('beta_')` pattern match, stacks `(chain, draw)` -> 1-D samples, subsamples `int(w * TARGET_TOTAL_SAMPLES=8000)` without replacement per site, parses each site into canonical `(covariate_family, target_parameter)` key with `_total` suffix normalisation so `beta_lec_total_kappa` (M6b subscale) and `beta_lec_kappa` (M3/M5 2-cov) both collapse to `("lec", "kappa")`, concatenates weighted subsamples per key, computes mixture mean/SD + `az.hdi(..., 0.95)` + surrogate tail p, flags single-source subscale-exclusive keys (`iesr_intr_resid`, `iesr_avd_resid`) via `n_winners_contributing==1` + `source_winner_if_single` column (user-approved Option C), compares each averaged HDI-excludes-zero vs. plan-08 per-winner CSV `excludes_zero_hdi` for disagreement detection, writes `averaged_scale_effects.csv` (13 cols) + `averaging_summary.md` (weights table + overlap/single-source counts + disagreement table). **Optional M6b-subscale exploratory arm**: when `"wmrl_m6b" in winners_internal` AND `--launch-subscale-arm` (default True), writes `launch_subscale.flag` marker — the SLURM wrapper reads the marker and fires `cluster/13_bayesian_m6b_subscale.slurm` via `sbatch --parsable` in fire-and-forget mode (main chain does NOT wait for the 12h subscale fit). Subscale posterior lands at Phase-16 canonical `output/bayesian/wmrl_m6b_subscale_posterior.nc` (option (a) per plan spec — plan 21-10 reads from there; no patch to subscale SLURM's OUTPUT_SUBDIR). `cluster/21_8_model_averaging.slurm` (1h/32G/2-CPU/comp, no JAX) ds_env ladder + 6 env-var overrides (L2_DIR/STACKING_RESULTS/WINNERS_FILE/AUDIT_REPORT/OUTPUT_DIR/LAUNCH_SUBSCALE) + subscale arm chain that clears the marker after submission + `source cluster/autopush.sh` + `exit $EXIT_CODE` (load-bearing for afterok chain to step 21.9). Prior: Step 21.7 scale-fit audit runner live — `scripts/21_scale_audit.py` (~650 lines, pure ArviZ + pandas + statsmodels) loads each winner's `21_l2/{winner}_posterior.nc`, pattern-matches all `beta_*` data vars (captures 0 sites for M1/M2 copy-through, 2 sites for M3/M5/M6a 2-cov path per plan 21-11, 32 sites for M6b subscale), computes per-site posterior mean/SD + 95% HDI via `az.hdi` + surrogate two-sided tail p, applies FDR-BH across the full beta-site set per winner via `statsmodels.stats.multitest.multipletests(..., method="fdr_bh")`, compares L2 vs baseline min-ESS on NON-beta shared params (degradation flagged at ratio < 0.5), emits per-winner `{winner}_beta_hdi_table.csv` (11 columns + metadata trailer row) + aggregated `scale_audit_report.md` with YAML front-matter `pipeline_action` header. **Unified exit-0 semantics (plan-checker Issue #4)**: PROCEED_TO_AVERAGING (at least one beta survives FDR-BH across winners) AND NULL_RESULT (zero betas survive) both exit 0; exit 1 RESERVED for genuine audit errors (missing winners.txt via FileNotFoundError, corrupt NetCDF via RuntimeError, `statsmodels` ImportError). Longest-prefix-first covariate parser (`_COVARIATE_FAMILIES = iesr_intr_resid > iesr_avd_resid > iesr > lec`) + target parser (`_KNOWN_TARGETS` includes `kappa_total` and `kappa_share` before `kappa`) handle all 32 subscale site names without collision. `cluster/21_7_scale_audit.slurm` (~192 lines, 30min/16G/2-CPU/comp, no JAX) ds_env ladder + pre-flight arviz/statsmodels import check + 7 env-var overrides (HDI_PROB/FDR_ALPHA/ESS_DROP_THRESHOLD/L2_DIR/BASELINE_DIR/WINNERS_FILE/OUTPUT_DIR) + awk YAML-parse `awk '/^---$/{flag=!flag;next} flag && /^pipeline_action:/{print $2; exit}'` with case-statement routing for PROCEED / NULL / unrecognized + `source cluster/autopush.sh` + `exit $EXIT_CODE` (load-bearing for `--dependency=afterok:$AUDIT2_JID` chain). Prior: Step 21.6 winner L2 refit orchestrator live — `scripts/21_fit_with_l2.py` (661 lines) reads `output/bayesian/21_baseline/winners.txt` (comma-separated display names from plan 21-06), maps via `DISPLAY_TO_INTERNAL` ({M1:qlearning, M2:wmrl, M3:wmrl_m3, M5:wmrl_m5, M6a:wmrl_m6a, M6b:wmrl_m6b}), validates `--model` is in the winner set (both directions), then dispatches to one of three L2 variants driven by frozenset membership: `_COPY_BASELINE_MODELS={qlearning,wmrl}` → `shutil.copy2` of 21_baseline posterior into 21_l2/ (no re-fit; M1/M2 have no L2-compatible parameter target); `_TWO_COV_MODELS={wmrl_m3,wmrl_m5,wmrl_m6a}` → 2-covariate L2 via the `covariate_iesr` hook from plan 21-11 using a local `model_args` shim (`participant_data_stacked` + `covariate_lec` + `covariate_iesr` + pre-computed `stacked_arrays` + `use_pscan=False`) calling `run_inference_with_bump` + `save_results(output_subdir="21_l2")` directly — avoids patching `fit_bayesian._fit_stacked_model` which hard-codes only `covariate_lec`; `_SUBSCALE_MODELS={wmrl_m6b}` → delegate to `fit_bayesian.main()` via sys.argv rewrite with `--subscale --output-subdir 21_l2`. Post-fit `_verify_two_covariate_sites` for M3/M5/M6a confirms both `beta_lec_{target}` and `beta_iesr_{target}` land in the saved NetCDF (target=kappa for M3/M5, kappa_s for M6a); if either missing → exit 1. Final `expected.exists()` check is load-bearing for afterok chain (same pattern as plan 21-04). `cluster/21_6_fit_with_l2.slurm` (196 lines, 12h/64G/4-CPU/comp) absorbs all three tiers (M6b subscale worst ~8-12h, M3/M5/M6a 2-cov ~45-75min, M1/M2 copy ~1s); per-MODEL JAX cache `.jax_cache_21_l2_${MODEL}`, MODEL/WARMUP/SAMPLES/CHAINS/SEED/WINNERS_FILE env-var overrides via --export. Prior: Step 21.5 LOO + stacking + RFX-BMS winner gate live — `scripts/21_compute_loo_stacking.py` (~900 lines) reads `{baseline_dir}/convergence_table.csv`, filters `pipeline_action == "PROCEED_TO_LOO"`, loads `{model}_posterior.nc` per eligible model, runs `az.compare(ic='loo', method='stacking')` (Yao 2018) as primary and `rfx_bms` (plan 21-02) as secondary over a reconstructed (n_ppt, n_models) log-evidence matrix via `logsumexp(nansum(log_likelihood.obs, axis=-1), axis=(0,1)) - log(chain*draw)`. Pareto-k is a SOFT gate (compute + WARNING + flag in report, NOT auto-exclude — plan-checker Issue #8 Option B). Winner determination 3-tier: DOMINANT_SINGLE (top stacking weight >= 0.5, exit 0), TOP_TWO (combined >= 0.8, exit 0), INCONCLUSIVE_MULTIPLE (all weights >= 0.10, exit 2 pauses afterok chain at USER CHECKPOINT). `--force-winners M3,M6b` CLI override (+ SLURM `FORCE_WINNERS=...` via --export) bypasses auto-determination for manual resume. Outputs: `loo_stacking_results.csv` + `rfx_bms_pxp.csv` + `winner_report.md` + `winners.txt` (consumed by plan 21-07 SLURM via $(cat winners.txt)). `cluster/21_5_loo_stacking_bms.slurm` (2h/32G/4-CPU/comp, no JAX) propagates tri-state exit code (0/1/2) to master orchestrator. Prior: Step 21.4 convergence + PPC audit runner live — `scripts/21_baseline_audit.py` applies Baribault & Collins (2023) gate (R-hat<=1.05 AND ESS_bulk>=400 AND divergences==0 AND BFMI>=0.2) over all 6 baseline posteriors in `output/bayesian/21_baseline/`, writes `convergence_table.csv` + `convergence_report.md`, exits 1 when n_passing<2 so SLURM `--dependency=afterok` chains block step 21.5 naturally. `cluster/21_4_baseline_audit.slurm` (30min/8G/2-CPU/comp, no JAX) propagates the exit code to the plan-21-10 master orchestrator chain. Prior: Step 21.3 baseline hierarchical fit runner live — `scripts/21_fit_baseline.py` delegates to `fit_bayesian.main()` with `--output-subdir 21_baseline` forced via sys.argv rewrite; post-fit `expected.exists()` shim exits 1 on convergence-gate failure. Patches `save_results` + `run_posterior_predictive_check` + `write_bayesian_summary` with `output_subdir`/`ppc_output_dir` kwargs (Approach A, backward-compat preserved when None). Step 21.2 recovery runner live, M3/M5/M6a accept optional `covariate_iesr`, RFX-BMS infrastructure from 21-02 ready, Step 21.1 prior-predictive gate runner live.
Last activity: 2026-04-19 — **v5.0 milestone started**. PROJECT.md updated with Current Milestone block (v5.0 Empirical Artifacts & Manuscript Finalization). Scope locked via /gsd:new-milestone questioning: (a) endpoint = Manuscript-ready — paper.qmd auto-patched + forest plots/tables + limitations rewrite + `quarto render` passes; (b) orchestrator = Phase 21 only, Phase 14 (K-refit + M4 GPU) deferred to v5.1; (c) dead-code sweep = all 4 items (legacy qlearning, legacy M2 K-bounds [1,7], 16b_bayesian_regression.py, load-side validation audit); (d) cross-verify = prior-predictive + recovery regression against v4.0 baseline. v4.0 Active requirements migrated: HIER-01/07/08/09/10 + L2-01 moved to Validated with absorption notes; K-02/K-03/GPU-01/02/03 moved to "Deferred to v5.1" with corrected cluster entry `bash cluster/12_submit_all_gpu.sh`. Prior: 2026-04-19 — **PHASE 22 COMPLETE (4/4 plans). Plan 22-04 complete: `validation/check_v4_closure.py` (8-check deterministic closure-state script, stdlib+PyYAML, exit 0 on current HEAD) + `scripts/fitting/tests/test_v4_closure.py` (3 pytest tests: passes-on-HEAD / deterministic / rejects-wrong-milestone, all PASS in 2.71s). SC#7 reproducibility guard instantiated. SC#8 (no one-off checks) enforced via banned-phrase scan in check_verification_files_exist. SC#9 (cluster-freshness framing) enforced via check_cluster_freshness_framing. Meta-check: BANNED_EVIDENCE_PHRASES constructed programmatically so checker source contains zero literal banned strings. Task commits: 805a58c (check_v4_closure.py), b818a98 (test_v4_closure.py). Auto-fixes: (a) check_project_md_active_migration used 500-char window — expanded to full-section extraction; (b) docstring \d escape warning — replaced with NN literal. `python validation/check_v4_closure.py --milestone v4.0` exits 0; determinism confirmed (byte-identical diff empty); v3.0 rejection confirmed (ValueError); pytest 3/3 PASSED. Milestone v4.0 closure complete — all SC#1..SC#10 invariants hold.** Prior: Plan 22-03 complete: REQUIREMENTS.md extended from 57 to 71 requirement IDs. Added DEER (Phase 20, 4 items: DEER-01..04) and BMS (Phase 21, 10 items: BMS-01..BMS-10) requirement family sections. Traceability table extended with 14 new rows citing 20-VERIFICATION.md and 21-VERIFICATION.md as satisfaction evidence. Coverage Summary updated (Phase 20 row + Phase 21 row; Total=71). Narrative and footer updated. Phase 22 SC#5 invariant satisfied: `grep -c "^| DEER-\|^| BMS-" .planning/REQUIREMENTS.md` = 14. Cross-doc file existence verified for both VERIFICATION.md paths. Task commits: cccec19 (DEER+BMS sections), d0b763d (traceability table + Coverage Summary). Prior: **Phase 22 plan 22-02 complete: VERIFICATION.md backfill (phases 14, 15, 21) + 15-03 absorption resolved**. Three VERIFICATION.md files created (14-VERIFICATION.md: 3/5 code_verified, K-03+GPU-03 deferred to bash cluster/12_submit_all_gpu.sh; 15-VERIFICATION.md: passed_with_absorption, HIER-09 → Phase 18, L2-01 → Phase 21; 21-VERIFICATION.md: 7/7 SC code_verified, cold-start execution deferred to bash cluster/21_submit_pipeline.sh). 15-03-SUMMARY.md absorption summary written; 15-03-PLAN.md preserved. Thesis gitignore guard (Burrows_J_GDPA_Thesis.*) confirmed active at .gitignore line 91. All three VERIFICATION.md files have PyYAML-parseable frontmatter (zero banned phrases, every Evidence cell is a grep invariant). Task commits: d51e51f (14-VERIFICATION.md), ef9247d (15-VERIFICATION.md + 15-03-SUMMARY.md), 6482d53 (21-VERIFICATION.md). Prior: 2026-04-18 — **PHASE 21 COMPLETE (11/11 plans)**. Plan 21-10 complete: `scripts/21_manuscript_tables.py` (~1100 lines, capstone manuscript-build script with three table generators + Figure 1 forest plot delegator + null-result branch + paper.qmd patcher), `cluster/21_9_manuscript_tables.slurm` (170 lines, capstone SLURM 30min/16G/2-CPU/comp), `cluster/21_submit_pipeline.sh` (~200 lines, +x, master orchestrator with pre-flight pytest gate + 9-step afterok chain), `cluster/21_6_dispatch_l2.slurm` (~50 lines, proper SLURM wrapper for L2 dispatcher with `--time=14:00:00` for plan-checker Issue #6), `cluster/21_dispatch_l2_winners.sh` (~70 lines, +x, canonical sbatch --wait + & + wait dispatcher). paper.qmd Methods now cites all four anchor papers (Baribault & Collins 2023, Hess et al. 2025, Yao et al. 2018, Stephan/Rigoux). Task 1 commit `ca24736`, Task 1b (paper.qmd patch) commit `269deea`, Task 2 commit `46872a1`. Auto-fixes: (a) Table 1 rank-column collision — defensive drop before insert; (b) pandas FutureWarning on bold-row dtype — cast to object before wrapping. All 11 success criteria verified end-to-end (--help shows 8-arg CLI, both DOIs present in paper.qmd, afterok=19 / afterany=0 / sbatch --parsable=10 / 14h cap / pre-flight pytest gate / +x bits / BARRIER_JID + afterany absent from dispatcher / bash -n syntax pass / stub-runs on PROCEED + NULL_RESULT + M6b-subscale-absent branches all clean). Pipeline is now invokable via a single `bash cluster/21_submit_pipeline.sh` from cold start with no hidden scheduler-killed jobs. ROADMAP success criteria #1 (reproducible from one command) and #6 (Methods cites both anchor papers) both satisfied. Prior: Plan 21-09 complete: `scripts/21_model_averaging.py` (~600 lines, pure ArviZ + pandas + numpy; three short-circuit paths — NULL_RESULT, single-winner, multi-winner averaging; canonical-key matching with `_total` suffix stripping so 2-cov and subscale betas collapse to the same key; weighted subsampling `n_sub=int(w * 8000)` per site without replacement via seeded `np.random.default_rng`; subscale-exclusive keys flagged `single_source=True` + `source_winner_if_single` column; disagreement detection against plan-08 per-winner tables; optional M6b-subscale arm marker `launch_subscale.flag`), `cluster/21_8_model_averaging.slurm` (217 lines, 1h/32G/2-CPU/comp, no JAX, ds_env ladder, 6 env-var overrides, fire-and-forget subscale arm chain via `sbatch --parsable` with marker cleanup, `source cluster/autopush.sh`, `exit $EXIT_CODE`). Task 1 commit `1e9b3c4`, Task 2 commit `30d53d5`. Auto-fix (Rule 1 — Bug): NumPy 2.x `np.bool_` does not subclass Python `bool` or `int`, so initial `_coerce_bool` helper read plan-08 CSV booleans through the `return False` fallthrough and silently inverted every True flag to False — every averaged key ended up flagged as a disagreement with its single-winner inference. Added explicit `isinstance(v, (bool, np.bool_))` and `isinstance(v, (int, float, np.integer, np.floating))` checks. Verified end-to-end on three stub scenarios: (a) NULL_RESULT audit header short-circuits to `averaging_skipped.md`; (b) single-winner `winners.txt='M3'` short-circuits to `single_winner_mode.md`; (c) multi-winner M3+M6b with overlapping keys (`beta_lec_kappa` vs `beta_lec_total_kappa` both collapsed to `("lec", "kappa")` with `n_winners_contributing=2`) + subscale-exclusive keys (`iesr_intr_resid|kappa` with `n_winners_contributing=1, source_winner_if_single=wmrl_m6b`) + `launch_subscale.flag` written when M6b is in winners AND default `--launch-subscale-arm`; also tested `--no-launch-subscale-arm` override (flag NOT written) and M3+M5-only winner set (no subscale marker). Disagreement detection verified via deliberate plan-08 CSV flip (M3 `beta_lec_kappa` excludes_zero=False forced) → `disagreement_flag=True` on averaged `lec|kappa` row. SLURM subscale arm grep count: `grep -c "launch_subscale.flag\|SUBSCALE" cluster/21_8_model_averaging.slurm` returns 30 (threshold >= 2). Prior: Plan 21-08 complete: `scripts/21_scale_audit.py` (~650 lines, pure ArviZ + pandas + statsmodels; pattern-match `startswith("beta_")` beta-site enumeration handles all three L2 tiers uniformly; longest-prefix-first covariate/target parser resolves `iesr_intr_resid_kappa_total` without collision; `@dataclass BetaSite` + `WinnerAudit` containers; `_audit_one_winner` soft-skips missing NetCDF via `load_warning` rather than exit 1; FDR-BH per-winner via `multipletests(..., method="fdr_bh")` on surrogate posterior-tail-p values — documented as HDI-agreement diagnostic not frequentist FDR; ESS-degradation ratio check on NON-beta shared params between L2 and baseline; YAML front-matter header + Markdown body separation), `cluster/21_7_scale_audit.slurm` (~192 lines, 30min/16G/2-CPU/comp, no JAX, ds_env ladder, 7 env-var overrides, awk YAML parse with case-statement routing, `source cluster/autopush.sh`, `exit $EXIT_CODE`). Task 1 commit `1bea8ca`, Task 2 commit `5f846d7`. Auto-fix: argparse `%o format` crash on `--ess-drop-threshold` help text — escaped literal percent as `50%%` per argparse idiom; `--help` now renders all 7 args cleanly. Verified via subprocess stub tests: PROCEED exit 0 (M3 with `beta_lec_kappa` signal + `beta_iesr_kappa` null), NULL_RESULT exit 0 (all betas zero-centered), exit 1 on missing winners.txt (`[FATAL] FileNotFoundError` stderr), M6b 32-site subscale enumerates all 4 covariate families including `iesr_intr_resid` + `iesr_avd_resid`, M1 copy-through (0 betas) handled gracefully with NULL_RESULT exit 0, awk YAML parse returns `PROCEED_TO_AVERAGING` correctly. Prior: Plan 21-07 complete: `scripts/21_fit_with_l2.py` (661 lines, 3-branch L2 dispatcher: copy / 2-cov / subscale; reads winners.txt + validates --model bidirectionally; local model_args shim for 2-cov path with covariate_iesr avoids patching _fit_stacked_model; post-fit beta_* site verification for M3/M5/M6a; expected.exists() gate-surface shim), `cluster/21_6_fit_with_l2.slurm` (196 lines, 12h/64G/4-CPU/comp absorbs all three tiers; per-MODEL JAX cache `.jax_cache_21_l2_${MODEL}`; MODEL/WARMUP/SAMPLES/CHAINS/SEED/WINNERS_FILE --export overrides; expected-artifacts audit loop; exit $EXIT_CODE for afterok chain). Task 1 commit `118c06f`, Task 2 commit `c5a8433`. Subprocess dry-run of M1 copy-through path (fake winners.txt + stub baseline NetCDF) exited 0 with identical bytes at 21_l2/qlearning_posterior.nc; model-not-in-winners rejection path exited 1 as expected. DISPLAY_TO_INTERNAL + INTERNAL_TO_DISPLAY maps mirror plan 21-06's MODEL_TO_DISPLAY. Prior: Plan 21-06 complete: `scripts/21_compute_loo_stacking.py` (~900 lines, pure-function `compute_loo_stacking_bms(compare_dict, ...) -> dict` core + CLI wrapper `main(argv)` with argparse), `scripts/fitting/tests/test_loo_stacking.py` (238 lines, 6 tests: dominant-winner synthetic + min-2-models guard + force-winners override + unknown-name reject + @slow rfx_bms integration + participant-mismatch reject; all PASS in ~17s), `cluster/21_5_loo_stacking_bms.slurm` (140 lines, 2h/32G/4-CPU/comp, ds_env ladder, FORCE_WINNERS --export hook). Task 1 commit `4b5b393`, Task 2 commit `2034b1e`. Module-load pattern matches test_prior_predictive.py (importlib.util.spec_from_file_location for leading-digit filenames). MODEL_TO_DISPLAY map mirrors BAYESIAN_NETCDF_MAP from 14_compare_models.py: qlearning→M1, wmrl→M2, wmrl_m3→M3, wmrl_m5→M5, wmrl_m6a→M6a, wmrl_m6b→M6b. Auto-fix: test regex union ("Participant mismatch|number of observations should be the same") covers both ArviZ's early reject on unequal-obs counts and the orchestrator's late reject on same-count-different-labels. Smoke test via tempdir + `az.from_dict` stubs + `az.to_netcdf` confirmed main() exit code 0 on 2-model DOMINANT_SINGLE path with all 4 output files (loo_stacking_results.csv + rfx_bms_pxp.csv + winner_report.md + winners.txt) written correctly. Prior: Plan 21-05 complete: `scripts/21_baseline_audit.py` (~500 lines, ArviZ-only audit with `az.from_netcdf` + `az.summary(var_names=MODEL_REGISTRY[model]['params'])` + `az.bfmi()` per-chain min + PPC CSV coverage read; 6 pipeline actions: PROCEED_TO_LOO / EXCLUDED_{RHAT,ESS,DIVERGENCES,BFMI,MISSING_FILE}; precedence R-hat > ESS > divergences > BFMI; `WARNING_FILE_MISSING` in-band value + dedicated MD WARNINGS section for missing PPC despite 21-04 Approach A fix; `MIN_MODELS_FOR_STACKING=2`), `cluster/21_4_baseline_audit.slurm` (137 lines, 30min/8G/2-CPU, ds_env ladder, no JAX, overridable RHAT/ESS/BFMI via --export, exit-code propagation for afterok chain). Task 1 commit `83de94f`, Task 2 commit `c64e622`. Smoke-tested via `arviz.from_dict` stubs: 1-passing -> exit 1, 2-passing -> exit 0, missing-PPC -> WARNINGS section preserved + exit 0. Prior: Plan 21-04 complete: `scripts/21_fit_baseline.py` (225 lines, thin wrapper + load-bearing convergence-gate-surface shim), `cluster/21_3_fit_baseline.slurm` (159 lines). Task 1 commit `1b39606`, Task 2 commit `8d27407`. Prior: Plan 21-03 complete: `scripts/21_run_bayesian_recovery.py` (956 lines, two CLI modes single-subject + aggregate), `scripts/fitting/tests/test_bayesian_recovery.py` (352 lines, 7 tests), `cluster/21_2_recovery.slurm` (129 lines, array 1-50 no autopush), `cluster/21_2_recovery_aggregate.slurm` (116 lines, aggregate + autopush). Task 1 commit `05f1b79`, Task 2 commit `205f480`. Auto-fix: `_safe_pearson_r` zero-variance switched from `np.std == 0` to `np.ptp == 0`. Prior: Plan 21-11 complete: `scripts/fitting/numpyro_models.py` (wmrl_m3/m5/m6a_hierarchical_model extended with `covariate_iesr`), `scripts/fitting/level2_design.py` (+ `build_level2_design_matrix_2cov`), `scripts/fitting/tests/test_numpyro_models_2cov.py` (635 lines). Task 1 commit `7503316`, Task 2 commit `49a84ff`. Plan 21-01: `scripts/21_run_prior_predictive.py` + `cluster/21_1_prior_predictive.slurm`; commits 6a0fdad, 8fa1ebd. Plan 21-02: `scripts/fitting/bms.py` + tests; commits 0cef164, 10ca205, 9c0f5b1.

### Phase 23.1 Insertion Decision (2026-04-19)

- **New phase 23.1 inserted between 23 and 24** — `GPU Pipeline Integration` (URGENT). Pulled forward from v5.1 (deferred items GPU-01..03) per user decision to align v5.0 with end-state pipeline before Phase 24 cold-start commits ~250-400 CPU-hours to a soon-to-be-superseded path.
- **Trigger**: Phase 24 pre-flight on Monash M3 (2026-04-19) surfaced that `cluster/21_*.slurm` runs CPU-only (`--partition=comp`, no `--gres=gpu`). Recent multi-GPU work (`logs/bayesian_mgpu_54894258.out`, 2026-04-18) proved M6b can fit in ~3h on 4 L40S GPUs vs ~8-12h CPU. Audit (`docs/CLUSTER_GPU_LESSONS.md` + repo grep) found ~80% of GPU infrastructure already exists: `numpyro_models.py:_select_chain_method()` auto-pmap, `fit_bayesian.py` device auto-detect, `21_fit_baseline.py` inherits via `fit_bayesian.main()`. Only gap: SLURM variants of `cluster/21_*.slurm` and orchestrator dispatch.
- **Phase 24 dependency updated**: Phase 24 now depends on Phase 23 AND Phase 23.1 (cold-start runs the GPU variants). Phase 24's existing PLAN.md files (24-01-PLAN.md, 24-02-PLAN.md) reference CPU paths and may need replanning after 23.1 ships — flag for re-verification before Phase 24 execution.
- **Requirements (proposed)**: GPU-INT-01..05 covering (a) multi-GPU smoke validation for 5 unproven models {M1, M2, M3, M5, M6a}, (b) GPU SLURM variants of all MCMC-fitting Phase 21 steps, (c) orchestrator dispatch with `USE_CPU=1` backwards-compat flag, (d) JIT cache safety per `docs/CLUSTER_GPU_LESSONS.md` §5 (per-model + per-backend cache dir + GPU-arch pinning via `#SBATCH --constraint=l40s`), (e) per-model `timeout 600` wrapper in smoke tests per CLUSTER_GPU_LESSONS §6.
- **Out of scope**: K-02/K-03 (constrained K MLE refit), M4 LBA Bayesian on Phase 21 GPU pipeline (Phase 21 is choice-only by design), GPU benchmarking instrumentation. All deferred to v5.1.
- **Pre-flight pytest gate fix** (commit `c4cfeb8`): added `pythonpath = .` to `pytest.ini` so `pytest` invocation works without `python -m`. Discovered when Phase 24 pre-flight `pytest scripts/fitting/tests/test_numpyro_models_2cov.py` failed with `ModuleNotFoundError: No module named 'scripts'` on Monash. Also expanded `testpaths` to `{tests, scripts/fitting/tests, validation}` reconciling with `pyproject.toml`. Fix verified: pytest now resolves `import scripts.fitting.numpyro_models` correctly.
- **Pre-cold-start cleanup** (commits `55a6fa1`, `d13a301`, plus rebase against autopush commit 9613bc8): committed thesis gitignore + stashed 20 untracked output files (model_comparison/, regressions/significance_*) into `_stash_pre_v5_cold_start_2026-04-19/` (gitignored, restorable). Pushed 73 local commits + 2 cleanup commits + pytest fix to `origin/main`. Working tree clean on autopush-managed paths (`logs/`, `output/bayesian/`).

### Phase 23 Plan 23-04 Decisions (2026-04-19)

- **CLEAN-04 soft-warn on absent parameterization_version**: v4.0 NetCDF write side (fit_bayesian.py:732) does not emit this attr. load_netcdf_with_validation emits DeprecationWarning when attr is absent; hard-raise deferred to v5.1 write-side retrofit (flip warning to ValueError + add attr in fit_bayesian.py).
- **Dual-model visualization scripts hardcode model strings**: plot_group_parameters.py and plot_model_comparison.py take --qlearning/--wmrl path args, not --model. Hardcoded "qlearning" and "wmrl" at the two call sites; load_posterior_samples(netcdf_path, model) signature updated.
- **14_compare_models.py line 599 model derivation**: loop var `name` is a display key ("M1", "M2", ...) not an internal id. Model key derived from Path(path_str).stem.replace("_posterior", "") — path already encodes the internal model name.
- **Grep invariant test excludes tests/ subtree**: test_load_side_validation.py contains the forbidden patterns in docstrings/f-strings; excluded via "tests/" entry in excluded_subtrees to prevent self-referential false positives.

### Phase 23 Decisions (2026-04-19)

- **Plan 23-03 (CLEAN-03 16b guard): MODEL_REFERENCE.md was already clean** — pre-flight grep hit the binary Senta PDF (coincidental substring), not the .md text. No edit to MODEL_REFERENCE.md was needed.
- **Plan 23-03 (CLEAN-03): pycache not git-tracked (gitignored)** — Task 2 commit is empty (--allow-empty) to record verified closure state. Disk deletion was the only tangible action.
- **Plan 23-03 (CLEAN-03 guard self-exclusion): guard test excludes its own file** via _THIS_FILE constant + filepath == _THIS_FILE skip inside scan loop, same pattern as 23-01 CLEAN-01 guard.
- **Plan 23-03 (SC#3 find interpretation): .planning/ excluded from file-existence guard** per v4.0 closure protocol — historical Phase 13/18 plan records referencing 16b are intentional archival, not violations.

- **Plan 23-01 (CLEAN-01 legacy deletion guard): guard test must exclude its own path from the import scan** — the test module docstring at lines 5/12/13/86 contains the literal pattern strings as documentation. Added `_SELF_PATH = Path(__file__).resolve()` + `if py_file.resolve() == _SELF_PATH: continue` in the scan loop. Without the self-exclusion the guard would always fail against its own docstrings (Rule 1 bug — auto-fixed in Task 2 commit 15456e0).
- **Plan 23-01 (CLEAN-01): legacy directory was physically deleted in earlier commit f0d4e60** (CLEAN-03 guard session). Task 2 commit (15456e0) finalises the guard test with the self-path exclusion fix; the `git rm -r` in plan 23-01 Task 2 was a no-op because the files were already absent from the index.
- **Plan 23-02 (CLEAN-02 K-bounds invariant guard): mle_utils.py was already clean pre-Phase-23** — no code edits needed, only the pytest guard was added. The plan's role was install-and-verify, not fix.
- **Plan 23-02: numpyro_models.py [1, 7] references left untouched** — the deprecated `wmrl_hierarchical_model` at lines 326/382 is kept as a regression-test target for `test_wmrl_model.py`. Touching it would break the test suite without scientific benefit. Any cleanup belongs to v5.1.
- **Plan 23-02: test_numpyro_helpers.py:308 "v3.0-legacy" fixture left untouched** — it is the negative-path input for `test_load_fits_with_validation_mismatch`, confirming the rejection path. It is not an accept-path vocabulary entry.
- **Plan 23-02: source-text grep invariant test pattern** — `pathlib.Path.read_text()` + substring loop directly mirrors the ROADMAP SC#2 grep command; catches dead code, comments, or accidentally restored branches that runtime dict inspection would miss.

### Phase 22 Decisions (2026-04-19)

- **Plan 22-04 (reproducibility guard): Banned phrases built programmatically** so `validation/check_v4_closure.py` source does not contain literal banned strings that would trigger its own meta-check. `BANNED_EVIDENCE_PHRASES = _make_banned_phrases()` where `_make_banned_phrases` concatenates string fragments. Two successive runs produce byte-identical stdout (determinism confirmed).
- **Plan 22-04: check_project_md_active_migration uses full-section extraction** (not 500-char window) — the `#### v4.0 (milestone)` subheader in PROJECT.md is 25 lines below `### Validated`; a fixed window misses it. Checks the full text between `### Validated` and next `### ` heading.
- **Plan 22-04: EXPECTED_COLD_START_ENTRIES includes bash cluster/12_submit_all_gpu.sh** alongside bash cluster/21_submit_pipeline.sh — Phase 14 K-refit + GPU items are not wired into the Phase 21 pipeline (v5.0 candidate); their cold-start entry is the standalone `cluster/12_submit_all_gpu.sh` dispatcher.
- **Plan 22-04: check_cluster_freshness_framing parses YAML frontmatter only** (not prose regex) — avoids false positives from grep-evidence cells in Observable Truths tables that reference sbatch commands as verification commands, not as user instructions.
- **Thesis doc must be gitignored (not just untracked):** `Burrows_J_GDPA_Thesis.*` must appear as an explicit pattern in `.gitignore`. Untracked status is not sufficient — CI or a fresh clone could expose it. The gitignore line is a hard closure requirement (Phase 22 SC #6).
- **Every cluster-pending item frames to `bash cluster/21_submit_pipeline.sh` as the canonical cold-start entry:** closure docs must not direct the user to piecemeal `sbatch` calls (e.g. `sbatch cluster/12_mle_gpu.slurm` separately). The single entry point is `bash cluster/21_submit_pipeline.sh`. The reproducibility guard script (plan 22-04) asserts this via grep over VERIFICATION.md cluster-pending blocks.
- **No one-off verification checks — every closure claim must cite a pytest test, a grep invariant, or the closure-state script as its evidence:** plain-English "I confirmed by reading the file" lines are not acceptable in Phase 22 VERIFICATION.md files or closure docs. Every observable truth must be backed by (a) a pytest test that exercises the claim, (b) a grep invariant checked into the repo, or (c) the closure-state script in SC#7.

### Phase 21 Decisions (2026-04-18)

- **Plan 21-10 (Step 21.9 manuscript tables + master pipeline orchestrator — PHASE 21 CAPSTONE):** Three-format table artefact pattern (.csv + .md + .tex) via single `TableArtefact` dataclass + `_write_table_artefact` helper — .csv is canonical machine-readable data, .md is for GitHub/preview, .tex is for direct `\input{}` into LaTeX manuscripts. Manuscripts import whichever format their toolchain prefers without forcing a single rendering. **paper.qmd insertion BEFORE locked `{#sec-bayesian-regression}` anchor** even though the anchor lives in the Results section (paper.qmd line 1032) in the current layout, not the Methods section as originally implied — the locked anchor is what matters; placement immediately above it gives a regression-results → Phase-21 methodology paragraph → Hierarchical-Level-2 results subsection narrative flow. **Quarto inline `{python} winner_display` over hard-coded model name** in Results — keeps manuscript in sync with `loo_stacking_results.csv` on every render, supports multi-winner outcomes naturally, eliminates stale-text failure mode. **Forest-plot delegation to scripts/18_bayesian_level2_effects.py via subprocess** — single source of truth for matplotlib styling, avoids drift between Phase 18 and Phase 21 forest plots. **Pre-flight pytest gate runs LOCALLY (not as SLURM job)** — `pytest scripts/fitting/tests/test_numpyro_models_2cov.py -v -k "not slow" --tb=short` runs on user's machine before any sbatch call; saves ~150 GPU-hours per pipeline run when 2-cov L2 hook is broken (cost: ~10s local fast-test runtime). **L2 dispatcher SLURM wrapper (cluster/21_6_dispatch_l2.slurm) with --time=14:00:00** instead of `sbatch --wrap=...` (plan-checker Issue #6) — dispatcher uses `sbatch --wait` internally to block on slowest L2 fit (M6b subscale 12h worst case); a `--wrap=` job inherits account default time limit (1-4h) and would be killed by scheduler before L2 fits complete, silently orphaning the entire downstream afterok chain. Load-bearing: 14h = 12h M6b worst + 2h slack. **Canonical sbatch --wait + & + wait dispatcher block** — rejected the exploratory `BARRIER_JID` + `afterany:${JIDS[*]}` rewiring approach as not composing cleanly with bash semantics (would require SLURM dependency rewiring bash can't do cleanly). Accepted pattern: each child L2 fit submitted with `sbatch --wait ... &` (parallel, backgrounded), then single `wait` blocks until all children complete; children run in parallel, dispatcher stays alive until slowest finishes, master orch's `afterok:$DISPATCH_JID` releases step 21.7 only after dispatcher exits cleanly. **afterok used exclusively across all 9 pipeline steps** — plan-checker Issue #4 was resolved in plan 21-08 via unified exit-0 protocol (PROCEED_TO_AVERAGING and NULL_RESULT both exit 0); afterok naturally advances chain on either valid scientific outcome; exit 1 reserved for genuine errors (FileNotFoundError / corrupt NetCDF / ImportError); afterany never needed — verified `grep "afterany" cluster/21_submit_pipeline.sh` returns empty. **M6b subscale guard via Path.exists() check** (plan-checker Issue #9) — when Phase-16 canonical `output/bayesian/wmrl_m6b_subscale_posterior.nc` is missing (subscale arm still running / not launched), script logs NOTE line + sets subscale_section to caption-footer note instead of raising; paper.qmd Methods documents post-phase quick task hook for adding M6b subscale beta table once arm completes; decouples manuscript build from 12h subscale fit timing. **Null-result branch suppresses forest plot intentionally** — forest plot of all-null effects would mislead readers; null_result_summary.md replaces the forest plot citing Hess 2025 + Baribault & Collins 2023 so null is contextualised as valid science not pipeline failure. Auto-fixes during execution (both Rule 1 — Bug, folded into Task 1 commit `ca24736` pre-push): (a) Table 1 rank-column collision when input CSV already has `rank` column from upstream step 21.5 — added defensive `if "rank" in df.columns: df = df.drop(...)` before insert; (b) pandas FutureWarning on bold-row dtype incompatibility when wrapping numeric column values with `**...**` — cast DataFrame to object dtype before bold-wrapping (verified rendering unchanged). All 11 success criteria pass: `--help` 8-arg CLI, both DOIs in paper.qmd, afterok=19/afterany=0 in master orch, sbatch --parsable=10 in master orch, 14h cap in dispatcher SLURM, pre-flight pytest gate in master orch, both .sh +x, BARRIER_JID/afterany absent from dispatcher, bash -n on all 3 shell files passes, stub-tested all 3 branches (PROCEED multi-winner / NULL_RESULT / M6b-subscale-absent). ROADMAP success criteria #1 (reproducible from `bash cluster/21_submit_pipeline.sh`) and #6 (Methods cites both anchor papers) BOTH SATISFIED. **Phase 21 100% complete (11/11 plans).**

- **Plan 21-09 (Step 21.8 stacking-weighted model averaging):** **Canonical-key matching strips `_total` suffix** so M6b subscale `beta_lec_total_kappa` and M3/M5 2-cov `beta_lec_kappa` collapse to the same `("lec", "kappa")` key, enabling cross-winner mixture concatenation. Subscale-exclusive covariate families (`iesr_intr_resid`, `iesr_avd_resid`) keep their full names — no 2-cov counterpart exists, so the canonical key is sourced only from M6b and flagged `single_source=True` + `source_winner_if_single=wmrl_m6b` in the CSV (user-approved Option C: report from M6b alone rather than invent values across absent winners). **Stacking-weighted subsampling** per Yao et al. (2018): `n_sub = int(w * TARGET_TOTAL_SAMPLES)` with `TARGET=8000` keeps the concatenated mixture bounded, stable for `az.hdi`, and large enough for tail-p resolution; sampling WITHOUT replacement via seeded `np.random.default_rng(42)`. **Three short-circuit paths** (all exit 0) mirror the step-21.7 unified exit-0 semantics: NULL_RESULT audit header → `averaging_skipped.md` (null result is valid science); single winner → `single_winner_mode.md` (averaging reduces to plan-08 per-winner table); multi-winner → primary averaging pathway with `averaged_scale_effects.csv` + `averaging_summary.md`. **Fire-and-forget M6b-subscale arm** driven by a marker file `launch_subscale.flag` that the Python script writes when M6b is in the winner set AND `--launch-subscale-arm` is on (default True). The SLURM wrapper reads the marker, fires `cluster/13_bayesian_m6b_subscale.slurm` via `sbatch --parsable` (returns JID immediately), removes the marker to prevent double-submission, and continues through autopush + exit WITHOUT waiting for the 12h subscale fit. **Option (a) for subscale-path compatibility** (plan 21-09 spec): plan 21-10 reads the subscale posterior from the Phase-16 canonical path `output/bayesian/wmrl_m6b_subscale_posterior.nc` rather than patching the subscale SLURM to accept `OUTPUT_SUBDIR` — keeps Phase-16 contract stable across downstream consumers. LAUNCH_SUBSCALE=0 --export override disables the arm globally even when M6b is a winner (useful for dry runs that don't want to burn a 12h cluster slot). **Disagreement detection** against plan-08 per-winner `{winner}_beta_hdi_table.csv`: re-parses each row's `beta_site` name to its canonical key, matches on equality, coerces the `excludes_zero_hdi` cell through a string/numpy-bool-aware `_coerce_bool` helper (Rule 1 bug fix — NumPy 2.x `np.bool_` does not subclass Python `bool`/`int`, causing the naive fallthrough to silently invert every True → False), compares to averaged flag, conservative OR rule (any contributing winner disagreeing flags the row). **Load-bearing `exit $EXIT_CODE`** at end of SLURM: both NULL_RESULT and single-winner short-circuits exit 0 so `--dependency=afterok:$AVG_JID` chain to step 21.9 naturally advances; exit 1 blocks only on genuine errors (FileNotFoundError / KeyError / RuntimeError). Auto-fix during execution (Rule 1 — Bug): np.bool_ coercion inverting disagreement detection; added explicit `isinstance(v, (bool, np.bool_))` + `isinstance(v, (int, float, np.integer, np.floating))` checks in `_coerce_bool`, verified via stub with deliberate flag flip.

- **Plan 21-08 (Step 21.7 scale-fit audit):** **Pattern-match beta-site enumeration** via single `[v for v in idata.posterior.data_vars if v.startswith("beta_")]` — handles all three L2 tiers with zero model-family branching (M1/M2: 0 sites, M3/M5/M6a: 2 sites each from plan 21-11's 2-cov hook, M6b subscale: 32 sites). Deliberate `[AUDIT] {winner}: {N} beta sites: [list]` log line makes IES-R family inclusion observable in SLURM stdout. **Longest-prefix-first covariate/target parser** is load-bearing: `_COVARIATE_FAMILIES = (iesr_intr_resid, iesr_avd_resid, iesr, lec)` so `beta_iesr_intr_resid_kappa_total` matches `iesr_intr_resid` not `iesr`; `_KNOWN_TARGETS` puts `kappa_total` and `kappa_share` before `kappa` so subscale target parsing works. **FDR-BH per-winner, not cross-winner** — M6b's 32 sites form one correction group, M3/M5/M6a's 2 sites form one each. Cross-winner FDR would conflate unrelated hypotheses (kappa in M3 vs kappa_share in M6b). **FDR-BH caveat documented** at top of report body: posterior tail probability `p = 2 * min(P(x>0), P(x<0))` is a credibility measure not a null-hypothesis error rate; use as HDI-agreement diagnostic. **Unified exit-0 semantics (plan-checker Issue #4 resolution)**: PROCEED_TO_AVERAGING and NULL_RESULT both exit 0; YAML `pipeline_action` header distinguishes them for 21.8 self-skip logic; exit 1 reserved for `FileNotFoundError` / `ValueError` / `RuntimeError` (missing winners.txt, corrupt NetCDF) + top-of-module `ImportError` guard on statsmodels. **ESS degradation** checked on NON-beta shared params (apples-to-apples since baseline has no L2 sites) via `min_ess_l2 / min_ess_baseline < (1 - ess_drop_threshold)` with default 0.5 → flag if > 50% drop; degraded winners get WARNING block + `ess_degraded_models` YAML list entry but still exit 0 (user judgement, not auto-kill). **Per-winner CSV metadata via trailer row** (`beta_site='__METADATA__'`) squeezes `max_rhat_betas / min_ess_betas / ess_degraded` into last two columns as formatted strings — keeps single-table CSV round-trippable without separate header file. Auto-fix during execution: argparse `%o format` crash from literal `%` in `--ess-drop-threshold` help string; escaped as `50%%` per argparse idiom. Subprocess stub tests cover all 4 paths: PROCEED (M3 2-cov with signal) exit 0, NULL (M3 2-cov all null) exit 0, missing winners.txt exit 1 with `[FATAL] FileNotFoundError` stderr, M6b subscale 32-site enumeration with all 4 covariate families + metadata trailer correctly populated, M1 copy-through (0 betas) graceful NULL_RESULT exit 0, awk YAML parse `awk '/^---$/{flag=!flag;next} flag && /^pipeline_action:/{print $2; exit}'` returns `PROCEED_TO_AVERAGING` correctly. SLURM: 30min/16G/2-CPU/comp, no JAX, ds_env conda ladder, pre-flight arviz+statsmodels import check.
- **Plan 21-07 (Step 21.6 winner L2 refit orchestrator):** Three-branch L2 dispatch driven by frozenset membership on internal model id (`_COPY_BASELINE_MODELS` = {qlearning, wmrl}, `_TWO_COV_MODELS` = {wmrl_m3, wmrl_m5, wmrl_m6a}, `_SUBSCALE_MODELS` = {wmrl_m6b}). **Option (a) local `model_args` shim** chosen over Option (b) `_fit_stacked_model` patch — `fit_bayesian._fit_stacked_model` currently hard-codes `covariate_lec` in the fully-batched branch; patching it would force every Phase 21 fit through the new code path and expand blast radius to Phase 16 callers. Local shim keeps the change inside `scripts/21_fit_with_l2.py` only. **M1/M2 shutil.copy2 pass-through** over cheap re-fit — M1/M2 have no L2-compatible parameter target (no kappa, no kappa_s, no stick-breaking), so a re-fit would waste cluster cycles producing a byte-identical posterior. `_copy_baseline_posterior` validates the source `21_baseline/{model}_posterior.nc` exists (upstream 21.3 convergence precondition) and raises FileNotFoundError → exit 1 if missing. **Post-fit beta-site verification** for M3/M5/M6a via `_verify_two_covariate_sites`: loads saved NetCDF with `az.from_netcdf`, checks both `beta_lec_{target}` AND `beta_iesr_{target}` exist in `idata.posterior.data_vars` (target = kappa for M3/M5, kappa_s for M6a), else exit 1. Guards against silent-drop scenarios where a JAX None-guard or shape mismatch would skip one covariate without error. **12h SLURM cap absorbs all three tiers** — M6b subscale worst ~8-12h dictates the upper bound; M3/M5/M6a 2-cov ~45-75min and M1/M2 copy ~1s sit comfortably below. Single cap avoids branching templates by model family. **Per-MODEL JAX compilation cache** `.jax_cache_21_l2_${MODEL}` prevents cross-winner corruption when multiple winners run in parallel — mirrors plan 21-04 pattern at `.jax_cache_21_baseline_${MODEL}`. **Deferred imports** (jax/numpyro/pandas inside `_fit_two_covariate_l2`) keep `--help` startup cost minimal. **Sensitivity deferral locked**: 4-covariate subscale extension for M3/M5/M6a (adding `iesr_intr_resid` + `iesr_avd_resid`) is a v5.0 item — current Phase 21 executes user-approved Option C middle ground only. Subprocess dry-run of the M1 copy-through path (fake winners.txt + stub baseline NetCDF) exited 0 with identical bytes at `21_l2/qlearning_posterior.nc`; the model-not-in-winners rejection path exited 1 with the expected `[FAIL] Model 'qlearning' (M1) is not in the winner set` message. Load-bearing `exit $EXIT_CODE` at end of SLURM propagates to plan-21-10 master orchestrator's `--dependency=afterok:$LOO_JOBID` chain for step 21.7.
- **Plan 21-06 (Step 21.5 LOO + stacking + RFX-BMS winner gate):** Primary metric is `az.compare(ic='loo', method='stacking')` weights per Yao, Vehtari, Simpson, Gelman (2018, *Bayesian Analysis* 13(3), DOI 10.1214/17-BA1091). Secondary is `rfx_bms` (plan 21-02, Stephan 2009 / Rigoux 2014). **Pareto-k SOFT GATE (plan-checker Issue #8 Option B)**: per-model `pct_high = mean(pareto_k > 0.7) * 100` computed via `az.loo(idata, pointwise=True)`, models exceeding `--pareto-k-pct-threshold` (default 1%) get a WARNING + `**WARNING — exceeds threshold**` marker in the report's "Pareto-k diagnostic" section, but are NOT dropped from compare_dict — exclusion is a scientific judgement at the human checkpoint, not a silent auto-gate. ROADMAP SC #3 is DOCUMENTED, not ENFORCED. **Three-tier winner verdict**: DOMINANT_SINGLE (`w.iloc[0] >= 0.5`), TOP_TWO (`w.iloc[0] + w.iloc[1] >= 0.8`), else INCONCLUSIVE_MULTIPLE (`all models with w >= 0.10`). `--force-winners M3,M6b` override bypasses with `winner_type="FORCED"`. **Tri-state exit codes** (0 advance / 1 abort / 2 pause-at-checkpoint) are the load-bearing contract with the plan-21-10 master orchestrator via `--dependency=afterok`. SLURM FORCE_WINNERS=... --export wires into the CLI `--force-winners` flag for cluster-side manual resume. **Pure-function core** `compute_loo_stacking_bms(compare_dict, ...) -> dict` separate from `main(argv)` enables smoke tests to call it directly via importlib (bypassing subprocess/temp-NetCDF overhead) — matches test_prior_predictive.py pattern for leading-digit filenames. **Stacking weight sum sanity** is log-WARNING-and-proceed (not assert-fail) per ArviZ GitHub issue #2359 numerical tolerance. **Participant consistency check** lives in the orchestrator (sort(idata.log_likelihood.participant.values) per model + verify all share same sorted list) — ArviZ's `az.compare` catches unequal observation counts first, our check catches same-count-different-labels; test regex union covers both. Auto-fix: participant-mismatch test regex union widened to accept ArviZ's "number of observations should be the same" OR our "Participant mismatch". Outputs: `loo_stacking_results.csv` (ArviZ stacking schema + `pct_high_pareto_k` col), `rfx_bms_pxp.csv` (model/alpha/r/xp/bor/pxp/pxp_exceeds_95), `winner_report.md` (6 sections + USER CHECKPOINT block), `winners.txt` (single comma-separated line of display-name winners). SLURM: 2h/32G/4-CPU/comp, no JAX.
- **Plan 21-05 (Step 21.4 baseline convergence audit):** Four-criterion gate from Baribault & Collins (2023, DOI 10.1037/met0000554) applied per model: `R-hat <= 1.05 AND ESS_bulk >= 400 AND divergences == 0 AND BFMI >= 0.2`. `az.summary(var_names=MODEL_REGISTRY[model]['params'])` restricts the scan to scientifically-meaningful individual-level params (excludes `mu_pr`/`sigma_pr`/`z` auxiliaries that would dilute max-R-hat). `az.bfmi()` returns per-chain values; worst-chain min is used. Precedence for exclusion reasons: **R-hat > ESS > divergences > BFMI** (matches NUTS diagnostic workflow — R-hat is most universal, BFMI most specific). `MIN_MODELS_FOR_STACKING = 2` (stacking over a singleton is not meaningful; PXP requires >= 2). Missing PPC CSV despite plan-21-04's Approach A plumbing is an **upstream bug signal** — in-band `ppc_coverage="WARNING_FILE_MISSING"` + dedicated MD `## WARNINGS` section, never silent `"not_available"`. `EXCLUDED_MISSING_FILE` does NOT count toward the n_passing gate (missing NetCDF = upstream SLURM failure, not a convergence issue) but still appears as FAIL in the table. Exit-code propagation via `exit $EXIT_CODE` at end of SLURM script is **load-bearing** for the plan-21-10 master orchestrator's `--dependency=afterok:$AUDIT_JOBID` chain to step 21.5. SLURM: 30min/8G/2-CPU/comp (no JAX needed, pure ArviZ/pandas). RHAT/ESS/BFMI overridable via `--export` for sensitivity sweeps.
- **Plan 21-04 (Step 21.3 baseline hierarchical fit runner):** `output_subdir` plumbed through 3 modules — `save_results` (fit_bayesian.py:577), `write_bayesian_summary` (bayesian_summary_writer.py:155), `run_posterior_predictive_check` via new `ppc_output_dir` kwarg (bayesian_diagnostics.py:645, Approach A). CLI flag `--output-subdir` added to `fit_bayesian.main()` (default None preserves Phase 16 backward-compat). `scripts/21_fit_baseline.py` is a thin wrapper: rewrites `sys.argv` to force `--output-subdir 21_baseline` and calls `fit_main()`, then runs a load-bearing `expected.exists()` check on `output/bayesian/21_baseline/{model}_posterior.nc`. **Load-bearing exit-1 shim**: without it, `save_results` returning None on convergence-gate failure leaves `fit_main()` at exit 0, which lets SLURM `--dependency=afterok` chains silently skip the failed model and let downstream steps "complete" on missing inputs. The shim converts silent gate-fail into a real SLURM failure so Phase 21 SC #2 ("all 6 models converge or are explicitly dropped with documented reason") actually holds. `cluster/21_3_fit_baseline.slurm` is 10h/48G/4-CPU/comp partition, per-MODEL JAX cache, NUMPYRO_HOST_DEVICE_COUNT=4, JAX_PLATFORMS=cpu; submit 6 times in parallel (no cross-model conflicts under `21_baseline/` subdir).
- **Plan 21-03 (Step 21.2 Bayesian parameter recovery):** Two CLI modes (single-subject + aggregate) in one script — the single-subject mode is dispatched by a SLURM array (1-50) for embarrassingly-parallel per-subject fits (~30 min wall-clock per model, vs. ~21h sequential). Aggregate mode runs once post-array. Scope decision locked: **baseline (no-covariate) inference path only**; the 2-cov L2 hook from plan 21-11 is gated via the `test_recovery_2cov_m3` pytest rather than tripling cluster cost (~150 additional MCMC jobs for a confirmatory test). Pass criterion (kappa-family only): `r >= 0.80 AND 95% HDI coverage >= 0.90`; all other parameters labelled `descriptive only` per quick-005 MLE recovery findings (r = 0.21 to 0.77 floor). Prior sampling uses `sigma_scale=0.2` (matches `HalfNormal(0.2)` on `sigma_pr`) and `phi_approx` transform to mirror `sample_bounded_param` exactly. RNG: base seed folded with subject_idx via `jax.random.fold_in`; separate `sim_seed` and `mcmc_seed` within each task. **Autopush gated**: only the aggregator script sources `cluster/autopush.sh`; the 50-task array does NOT (would race 50 concurrent `git push` calls). Master orchestrator (plan 21-10) chains the two scripts via `--dependency=afterok:$ARRAY_JOBID`. Auto-fix during execution: `_safe_pearson_r` zero-variance check switched from `np.std == 0` to `np.ptp == 0` to avoid floating-point drift at 1e-17 on constant posterior-mean vectors.
- **Plan 21-11 (2-cov L2 hook for M3/M5/M6a):** Approach A additive kwarg `covariate_iesr: jnp.ndarray | None = None` chosen over Approach B generic-matrix replacement — keeps blast radius to the three model functions + `level2_design.py` + test file; zero changes to `fit_bayesian._fit_stacked_model` or any other caller. `beta_iesr_{target}` uses same `Normal(0, 1)` prior as `beta_lec_{target}` for symmetry. Guard raises `ValueError` if `covariate_iesr` passed without `covariate_lec` (prevents silent LEC drop). 2-cov design does NOT residualize — raw z-scored totals only (LEC/IES-R r ~ 0.3 in N=138 is well below collinearity threshold; orthogonalization only principled when design cond# > 30, as in Phase 16's 4-cov subscale case). Phase 21 Option C pattern locked: **code all model variants upfront; pipeline dispatches the one the winner needs** — M3/M5/M6a 2-cov path now ready; M6b already has 4-cov subscale; M1/M2 bypass L2 entirely.
- **Plan 21-11 recovery test fallback (locked):** Unified simulator `simulate_agent_fixed` does not expose `kappa` as an Agent constructor parameter, so the recovery test uses a manual NumPy forward-sim of the M3 block (mirrors `wmrl_m3_block_likelihood` semantics: hybrid WM/RL + epsilon + probability-mixing perseveration against one-hot choice kernel of `last_action`). Pre-approved as fallback in plan 21-11 Task 2 action text; documented in test module docstring.
- **Plan 21-01 (prior-predictive gate):** Use NumPyro `Predictive` (not manual sampling) so the full hierarchical structure (`mu_pr`, `sigma_pr`, `z`, transformed `theta`) is honored. One `(draw, participant)` pair per draw (not cartesian 500 × 138) to keep wall-clock under 2 min per model. Reuse real participant trial templates (stimuli, set_sizes, block structure) per Baribault. Exit 1 on FAIL so the SLURM pipeline short-circuits before MCMC.
- **Plan 21-01 simulator quirk:** `NUM_STIMULI=7` in the NumPy simulator accommodates the 1-indexed stimulus values in `task_trials_long.csv` — the JAX likelihoods silently clip out-of-bounds reads (stimulus=6 aliases to row 5), but plain NumPy needs the explicit row. Tracked as a known follow-up for when `prepare_stacked_participant_data` ever explicitly zero-indexes stimuli.

### v4.0 Decisions (pre-refit, 2026-04-17)

- **Canonical analysis cohort (locked):** `config.get_analysis_cohort()` implements the intersection of three gates: (a) task completeness ≥ `MIN_TRIALS_THRESHOLD=400` trials and `MIN_BLOCKS=8` main-task blocks; (b) performance check — overall accuracy ≥ `MIN_OVERALL_ACCURACY=0.40` AND mean accuracy over last `LATE_BLOCK_N=5` main-task blocks ≥ `MIN_LATE_BLOCK_ACCURACY=0.50`; (c) scale completeness — non-NA for every column in `REQUIRED_SURVEY_COLUMNS` (lec_total, ies_total, ies_intrusion, ies_avoidance, ies_hyperarousal). Current cohort size: **N=138** (vs N=154 task-only, N=160 scale-only). 16 participants dropped by late-block learning check.
- **Principled priors (locked):** All `mu_prior_loc` in `PARAM_PRIOR_DEFAULTS` now 0.0 (prior mean = Φ(0) = 0.5 on bounded scale; 95% prior interval ~[0.02, 0.98] given `sigma_pr ~ HalfNormal(0.2)`). Motivation: previous MLE-calibrated values (e.g. kappa=-2.0 implying prior mean 0.023) were empirical-Bayes informative and conservative for L2 null-testing. Legacy values retained in `_PRIOR_LEGACY_MLE_CALIBRATED` dict for sensitivity analyses. See `docs/SCALES_AND_FITTING_AUDIT.md` §3.
- **Cohort filter wired into `fit_bayesian.load_and_prepare_data` (locked):** `use_cohort=True` default. `--no-cohort` CLI flag available for diagnostic/legacy runs. Printed cohort breakdown at load time.
- **Stick-breaking rationale + identifiability paragraph added to paper.qmd (locked):** Methods §Model Fitting now explicitly argues for stick-breaking vs. inequality-constrained joint κ/κ_s sampling and names the three identifiability safeguards (uninformative κ_share prior, auto-bump, recovery validation).
- **Posterior-vs-MLE figure referenced in paper.qmd (locked):** `@fig-posterior-vs-mle` points to `figures/m6b_posterior_vs_mle.png`, to be generated from the output of `validation/compare_posterior_to_mle.py` on pipeline rerun.
- **Scale distributions figure referenced (locked):** `@fig-scale-distributions` points to `figures/scale_distributions.png`, produced by `scripts/analysis/trauma_scale_distributions.py` on pipeline rerun.
- **Cluster/GPU lessons doc (locked):** `docs/CLUSTER_GPU_LESSONS.md` captures the full v4.0 operational writeup — fully-batched vmap pattern with code examples, chain_method decision tree, pscan tradeoffs (CPU 3.7× slower, qlearning 0.28×), JAX/NumPyro pitfalls with context, SLURM templates (CPU-4-chain, single-GPU, multi-GPU), JIT-optimal coding patterns, debugging checklist.  Cross-referenced from `docs/README.md`.

## Hierarchical Bayesian Architecture (current)

### Single-stage joint inference (NOT two-stage PEB)

- All models fit as **single-stage hierarchical Bayesian** via NumPyro NUTS. Individual parameters, group priors, and Level-2 regression coefficients are sampled **simultaneously** in one MCMC run. This is more principled than two-stage PEB (SPM-style) because (a) uncertainty propagates correctly — no plug-in bias from using posterior means as point estimates in a second regression stage, (b) shrinkage happens automatically via partial pooling, (c) L2 coefficients are tempered by L1 uncertainty.
- **Trauma parameters ARE fit together with model parameters** — they are not a separate post-hoc step.
- The refactored fully-batched likelihood emits a single ``numpyro.factor("obs", per_participant_ll.sum())``; the prior 154-per-participant factor-site version was also joint, just slower.

### Level-2 regression — coverage table

| Model | L2 design | Beta sites | Hooked into |
|---|---|---|---|
| M1 qlearning | none (L2 not supported) | 0 | `covariate_lec=None` guard raises |
| M2 wmrl | none (L2 not supported) | 0 | `covariate_lec=None` guard raises |
| M3 wmrl_m3 | LEC total → kappa **[+ optional IES-R total, Phase 21]** | 1 (`beta_lec_kappa`) **or 2 (`beta_lec_kappa` + `beta_iesr_kappa`)** | `wmrl_m3_hierarchical_model` |
| M5 wmrl_m5 | LEC total → kappa **[+ optional IES-R total, Phase 21]** | 1 (`beta_lec_kappa`) **or 2 (`beta_lec_kappa` + `beta_iesr_kappa`)** | `wmrl_m5_hierarchical_model` |
| M6a wmrl_m6a | LEC total → kappa_s **[+ optional IES-R total, Phase 21]** | 1 (`beta_lec_kappa_s`) **or 2 (`beta_lec_kappa_s` + `beta_iesr_kappa_s`)** | `wmrl_m6a_hierarchical_model` |
| M6b wmrl_m6b | LEC total → kappa_total + kappa_share | 2 (`beta_lec_kappa_total`, `beta_lec_kappa_share`) | `wmrl_m6b_hierarchical_model` |
| M6b-subscale | 4 covariates × 8 params (full design) | **32** (`beta_{cov}_{param}`) | `wmrl_m6b_hierarchical_model_subscale` |

**Plan 21-11 Option C note:** M3/M5/M6a now accept an optional second covariate `covariate_iesr: jnp.ndarray | None = None` alongside `covariate_lec`. When both are provided, `beta_iesr_{target}` is sampled with `Normal(0, 1)` prior and both shifts are summed on the probit scale. Guard raises `ValueError` if `covariate_iesr` is passed without `covariate_lec`. Design-matrix builder: `build_level2_design_matrix_2cov(metrics, participant_ids)` returns `(N, 2)` z-scored `[lec_total, iesr_total]`. Test coverage: `scripts/fitting/tests/test_numpyro_models_2cov.py` (9 fast tests + 1 slow recovery smoke test).

### Subscale design matrix (M6b-subscale only)

Four predictors, condition number 11.3 (well under 30 target):
1. `lec_total` — LEC-5 total events (z-scored)
2. `iesr_total` — IES-R total score (z-scored)
3. `iesr_intr_resid` — IES-R intrusion, Gram-Schmidt residualized vs. total (z-scored)
4. `iesr_avd_resid` — IES-R avoidance, Gram-Schmidt residualized vs. total (z-scored)

Hyperarousal residual excluded (exact linear dependence: the three residuals sum to zero). See `scripts/fitting/level2_design.py`.

### What PEB-style two-stage would add (not currently implemented)

- A separate second-stage regression taking the L1 posterior as input and regressing against additional covariates that were not in the MCMC design matrix. Useful for exploratory analyses *after* the main fit completes.
- Functionally already achievable by running `18_bayesian_level2_effects.py` on the posterior NetCDF with extracted per-participant samples, though this is a descriptive/visualization script, not a re-fit.
- Not strictly necessary for v4.0 Phase 15/16 validation gate (kappa × LEC-5 signal) because LEC is already in the joint design.

## Issue 1 Fully-Batched Rollout (2026-04-17, post-Phase-20)

### What changed

- 6 new `*_fully_batched_likelihood` functions in `scripts/fitting/jax_likelihoods.py` (M3 added earlier in commit `aeaa208`; qlearning, wmrl, M5, M6a, M6b added in `c54ee6c`).
- All 6 `*_hierarchical_model*` bodies in `scripts/fitting/numpyro_models.py` refactored from Python for-loop emitting `obs_p{pid}` factors to single `numpyro.factor("obs", per_participant_ll.sum())` via nested `jax.vmap` (outer=participants, inner=blocks).
- `scripts/fitting/fit_bayesian.py`: `_FULLY_BATCHED_MODELS` tuple extended from M3-only to all 6 choice-only models; `stacked_arrays` pre-computed once per MCMC call.
- New `cluster/13_bayesian_multigpu.slurm` — requests `--gres=gpu:4` so `_select_chain_method` returns `"parallel"` (true `jax.pmap` across GPUs) instead of `"vectorized"` (time-multiplexed on 1 device).

### Validation status

| Test | Scope | Status |
|---|---|---|
| Agreement on synthetic (N=5 × 3 draws) | 6 models | PASS, max rel err 2.06e-07 |
| Agreement on real N=154 using MLE params | 6 models × 154 ppts | PASS, max rel err **8.84e-07** |
| End-to-end MCMC dispatch (local CPU) | 4 refactored models | PASS, zero `obs_p{pid}` sites remaining |
| Cluster smoke test (fb_smoke job 54882294) | all 6 models, N=154, 5 warmup + 5 samples | PASS, 109–132 s each |
| Per-iter GPU speedup (qlearning) | before vs after | ~6 min/iter → ~0.5 s/iter (~700×) |

### Why the speedup is larger than hypothesized

Original expectation: 36× from collapsing 154 dispatches to 1. Actual: ~700× per iter on qlearning. The extra gain comes from XLA fusing the entire likelihood computation into a single kernel once the Python for-loop is out of the way — before, each factor was compiled as an independent sub-graph; now there's one fused graph that the optimizer can reorder and vectorize across all 154 participants simultaneously. This also explains why prior runs reported "GPU peak memory: 0.1 MB" — the GPU was idle waiting for Python dispatches 99% of the time.

## Production Refit Readiness

### Diagnostics preserved (unchanged by refactor)

- **NumPyro**: R-hat, ESS bulk, ESS tail, divergences, tree-depth saturation, E-BFMI (via `mcmc.summary_frame()`).
- **ArviZ**: WAIC, LOO, `az.compare()` with stacking weights.
- **`run_inference_with_bump` auto-bump**: retries at `target_accept_prob` 0.80 → 0.95 → 0.99 if divergences persist. Convergence gate: `num_divergences == 0`.
- **Pointwise log-lik** (`compute_pointwise_log_lik`, `bayesian_diagnostics.py:291`): reads `mcmc.get_samples()` by parameter name, not factor site names — transparent to the `obs_p{pid}` → `obs` change.
- **Hessian diagnostics are MLE-only** (`fit_mle.py:1545–1631`). Bayesian uses posterior-based uncertainty. Neither path affected by the refactor.

### Baseline for comparison

- **No cached hierarchical Bayesian posteriors exist** (`output/bayesian/` contains only pscan benchmarks and the IES-R collinearity audit). The upcoming refit is the **first** production hierarchical Bayesian run to complete.
- Sanity baselines: MLE individual fits in `output/mle/{model}_individual_fits.csv` with `parameterization_version=v4.0-K[2,6]-phiapprox-stickbreaking` (quick-006).
- Expected: posterior means near MLE point estimates, with hierarchical shrinkage pulling extreme values toward group means.

### v4.0 Phase 15 POC validation gate (pending)

The original motivation for hierarchical inference: reproduce the MLE-derived kappa × LEC-5 signal under partial-pooling. From quick-006 Task 4:
- M3: 3 FDR-BH survivors (phi × IES-R Hyperarousal, kappa × LEC-5 Total, phi × IES-R Total)
- M6b: `kappa_total × LEC-5` strongest uncorrected (p=0.0028), does not survive FDR-BH
- The kappa × LEC-5 pattern across M3 and M6b is the most scientifically credible because kappa is the recoverable parameter in both

**Pass criterion**: `beta_lec_kappa` (M3) or `beta_lec_kappa_total` (M6b) 95% HDI excludes zero under hierarchical inference.

### Outstanding validation items (to run after refit)

1. **Convergence diagnostics per model**: R-hat < 1.05, ESS bulk > 400, divergences == 0 (auto-bump will retry up to target_accept=0.99).
2. **Posterior vs MLE agreement spot-check**: per-participant posterior means within 2 SD of MLE point estimates (expect shrinkage on poorly-identified params — alpha_pos/alpha_neg/phi/rho/capacity per recovery results).
3. **L2 coefficient HDI check**: 95% HDI for `beta_lec_kappa` / `beta_lec_kappa_total` — does it exclude zero?
4. **Model comparison**: WAIC/LOO via `az.compare(ic='loo', method='stacking')` — does M6b remain the winning model under hierarchical shrinkage? (MLE AIC says it does; Bayesian stacking weights may disagree.)
5. **Subscale signal discovery**: M6b-subscale has 32 beta sites; which (covariate × param) combinations have HDI excluding zero after Bonferroni or FDR?
6. **Parameter recovery under hierarchy**: re-run recovery on M6b with hierarchical MCMC (not MLE) to check if kappa_total/kappa_share recovery (currently r=0.997/0.931 under MLE) holds. Base RLWM params are unrecoverable under MLE (r=0.21–0.77); hierarchical shrinkage may help but cannot manufacture identifiability.

### Known limitations (carry through to results)

- **Capacity (K) identifiability is structural** (quick-005): K posterior will be prior-dominated for most participants. Reported only as descriptive.
- **M6b stick-breaking geometry**: kappa_share posterior can be bimodal when kappa_total is near zero (share becomes non-identifiable). Handled by auto-bump to `target_accept=0.99` if divergences appear.
- **IES-R hyperarousal subscale unavailable for L2** (linear dependence). Committed decision, see 16-01.
- **Number of participants: N=154 for task-data completeness, N=160 for surveys**; hierarchical fit uses N=154 (sorted overlap).

Progress: [████████░░] ~75% (30/~40 plans across Phases 13-20 + Phase 21)

### v4.0 Phase Structure

| Phase | Goal | Requirements | Count |
|---|---|---|---|
| 13 | Infrastructure Repair & Hierarchical Scaffolding (fix P0 import, numpyro scaffolding, Collins K research) | INFRA-01..08, K-01 | 9 |
| 14 | Collins K Refit + GPU LBA Batching (K-02/03 refit + fit_all_gpu_m4) | K-02..03, GPU-01..03 | 5 |
| 15 | M3 Hierarchical POC with Level-2 Regression (validation gate) | HIER-01, HIER-07..10, L2-01 | 6 |
| 16 | Choice-Only Family Extension + Subscale L2 (M1/M2/M5/M6a/M6b + full subscale Level-2) | HIER-02..06, L2-02..08 | 12 |
| 17 | M4 Hierarchical LBA (user-committed despite research recommendation to descope) | M4H-01..06 | 6 |
| 18 | Integration, Comparison, and Manuscript (schema-parity flag flip, WAIC/LOO, paper revision) | CMP-01..04, MIG-01..05, DOC-01..04 | 13 |

**Coverage:** 51/51 requirements mapped (100%), zero orphans.

### Post-Refit Reality (N=154, quick-006)

- Winning model flipped from M5 to **M6b** (dual perseveration with stick-breaking kappa_share).
  - Aggregate AIC: M6b 143324.93 < M5 143897.82 < M6a 144771.59 < M3 144865.92 < M2 147328.17 < M1 152143.11
  - Aggregate BIC agrees: M6b is also rank 1 on BIC; AIC and BIC orderings are identical.
  - Akaike weight of M6b is effectively 1.0.
- Per-participant AIC winners (N=154): M6b 55 (35.7%), M5 41 (26.6%), M6a 38 (24.7%), M3 15 (9.7%), M2 3 (1.9%), M1 2 (1.3%).
- M6b parameter recovery (quick-005 outputs, N=50 synthetic):
  - kappa_total r=0.9971 PASS, kappa_share r=0.9311 PASS
  - alpha_pos r=0.598 FAIL, alpha_neg r=0.516 FAIL, phi r=0.442 FAIL, rho r=0.629 FAIL, capacity r=0.213 FAIL (worst), epsilon r=0.772 FAIL (close)
- Practical implication: trust kappa-level inferences; treat base RLWM parameters as individual-level descriptors only, not identified traits. **This is the core motivation for v4.0 hierarchical shrinkage.**
- Trauma-parameter regressions (quick-006 Task 4, all 7 models, within-model FDR-BH + Bonferroni):
  - Only M3 produces FDR-BH survivors (3 of 42 tests): phi x IES-R Hyperarousal, kappa x LEC-5 Total events, phi x IES-R Total.
  - M6b: 7 uncorrected hits, 0 FDR-BH, 0 Bonferroni. Strongest M6b hit is kappa_total x LEC-5 (p=0.0028 uncorrected, p_fdr=0.135).
  - The kappa x LEC-5 pattern across M3 and M6b is the most scientifically credible signal because kappa is the recoverable parameter in both. **v4.0 Phase 15 must reproduce this under hierarchical inference as the POC validation gate.**

## Performance Metrics

**v1 Milestone:**
- Total plans completed: 6
- Average duration: 25 min
- Total execution time: 2.5 hours

**v2 Milestone:**
- Total plans completed: 7
- Average duration: 20 min
- Total execution time: 140 min

**v3 Milestone:**
- Total plans completed: 6 (incl. 1 gap closure)
- Average duration: 20 min
- Total execution time: 117 min

**v4 Milestone:**
- Total plans completed: 3
- Average duration: ~11 min (13-01, 13-03), ~? min (13-02)
- Total execution time: ongoing

## Accumulated Context

### v4.0 Decisions (21-11 completed 2026-04-18)

- **Phase 21 Option C infrastructure for M3/M5/M6a 2-cov L2 locked:** Approach A additive kwarg `covariate_iesr: jnp.ndarray | None = None` added to `wmrl_m3_hierarchical_model`, `wmrl_m5_hierarchical_model`, `wmrl_m6a_hierarchical_model` alongside existing `covariate_lec`. Approach B (generic `covariate_matrix` + `names` kwargs) rejected — would have required rewrites of `fit_bayesian._fit_stacked_model`, `21_run_prior_predictive.py`, `21_run_bayesian_recovery.py`, `21_fit_baseline.py` and any notebook callers. Approach A preserves zero changes outside the three model functions and `level2_design.py`.
- **`beta_iesr_{target}` prior (locked):** `Normal(0, 1)` — identical to `beta_lec_{target}` for prior symmetry across covariates. No directional bias injected through the prior. Target parameter: `kappa` for M3/M5, `kappa_s` for M6a.
- **Guard semantics (locked):** `covariate_iesr is not None and covariate_lec is None` raises `ValueError("covariate_iesr provided without covariate_lec. …")` with explicit message listing all three valid configurations (both None, both provided, LEC-only). Placed at function top before any `numpyro.sample` calls.
- **2-cov design does NOT residualize (locked):** `build_level2_design_matrix_2cov` returns raw z-scored `[less_total_events, ies_total]`. LEC/IES-R Pearson r is ~0.3 in the canonical N=138 cohort — well below the multicollinearity threshold. Orthogonalization is only principled for the 4-cov subscale design where condition number matters (Phase 16 logic).
- **Recovery test uses manual NumPy forward-sim (locked):** The unified simulator framework (`scripts/simulations/unified_simulator.py`) does not expose `kappa` as an Agent constructor parameter or support per-participant kappa shifts. Test uses `_simulate_m3_block_numpy` that mirrors `wmrl_m3_block_likelihood` semantics exactly (hybrid WM/RL + epsilon + probability-mixing perseveration against one-hot choice kernel of `last_action`). Pre-approved as fallback in plan 21-11 Task 2 action text.
- **Backward-compat preserved:** Phase 16 callers passing only `covariate_lec=<v>` (including `fit_bayesian._fit_stacked_model`, `21_run_bayesian_recovery.py`, all existing tests) are 100% unchanged. `beta_iesr_*` sample sites only appear in the trace when `covariate_iesr` is explicitly passed.

### v4.0 Decisions (21-02 completed 2026-04-18)

- **RFX-BMS + PXP module locked (`scripts/fitting/bms.py`):** Public signature `rfx_bms(log_evidence, alpha0=1.0, max_iter=1000, tol=1e-4, n_xp_samples=1_000_000, seed=42) -> {alpha, r, xp, bor, pxp}`. Strict `(n_subjects, n_models)` 2-D input contract (ValueError on ndim!=2 or non-finite). Faithful port of mfit/bms.m with Dirichlet-categorical VB E-step + 1 M Monte-Carlo XP sampling.
- **Null-model free energy formulation (locked, deviates from plan 21-02 spec):** The plan described F0 as a Dirichlet-posterior VB free energy with `alpha_null = alpha0 + n/K` per component. Under uniform log-evidence the heterogeneous posterior converges to the same concentrations, pinning BOR at 0.5 and losing the "null supported" signal. Replaced with Rigoux 2014 eq. A1 fixed-r form: `F0 = Σ_n logsumexp(log_evidence[n]) - N*log(K)`, which is the data log-likelihood under a delta-posterior at `r = 1/K` (no Dirichlet KL because r is not inferred). Canonical Phase 21 formulation — documented in `_bor` docstring. Produces BOR ~ 1 on uniform data (null supported), BOR ~ 0 on dominant/heterogeneous data (null rejected), matching published behaviour.
- **Dirichlet KL in `_vb_free_energy` now uses standard closed form:** `KL(Dir(α) || Dir(α0)) = gammaln(sum α) - Σ gammaln(α) - gammaln(sum α0) + Σ gammaln(α0) + Σ(α-α0)(ψ(α) - ψ(sum α))`. Earlier version had inverted signs on the `gammaln` terms; test-driven discovery.
- **PXP formula verified numerically to 1e-12:** `pxp = (1 - bor) * xp + bor / K` (Rigoux 2014). XP and PXP algebraically sum to 1 within 1e-10 on every call.
- **No import-time side effects:** `python -c "import scripts.fitting.bms"` silent. Module ready to be imported by Plan 21-05 (LOO+stacking+BMS orchestrator).

### v4.0 Decisions (16-01 completed 2026-04-13)

- **4-predictor L2 design matrix locked:** `lec_total`, `iesr_total`, `iesr_intr_resid`, `iesr_avd_resid`. The hyperarousal residual is excluded because `ies_total = ies_intrusion + ies_avoidance + ies_hyperarousal` exactly in the dataset; all three subscale residuals sum to zero (rank-deficient 3-residual matrix, condition number ~2.4e15). Only 2 subscale residuals are linearly independent.
- **Full design condition number 11.3 (PASS):** The 4-column design matrix [lec_total, iesr_total, intr_resid, avd_resid] has condition number 11.3, well below the target of 30.
- **LEC-5 subcategory data gap confirmed (L2-04 closed):** Only `less_total_events` and `less_personal_events` exist in the pipeline. No physical/sexual/accident taxonomy is defined anywhere in the codebase. `include_lec_subcategories=True` raises `ValueError`.
- **`build_level2_design_matrix` in `scripts/fitting/level2_design.py` is the single source of truth:** All Phase 16 hierarchical models must call this function. `COVARIATE_NAMES` list is authoritative for `beta_*` site naming.
- **N=160 complete-data participants (not 154):** 160 participants have complete IES-R + LEC-5 data; 154 was the MLE fit count (task-data completeness criterion). Level-2 regression uses N=160.

### v4.0 Decisions (16-03 completed 2026-04-13)

- **M5 kappa stays manually sampled (locked):** phi_rl is added to the `sample_bounded_param` for-loop (7 params via loop), but kappa remains outside the loop to preserve the L2 regression hook pattern from M3. HIER-04 complete.
- **M6a kappa_s sites named distinctly (locked):** `kappa_s_mu_pr`, `kappa_s_sigma_pr`, `kappa_s_z`, `kappa_s` — mirrors M3's kappa naming but with `_s` suffix. Same bounds/priors as kappa (`mu_prior_loc=-2.0`). HIER-05 complete.
- **M6b per-participant decode pattern (locked):** `kappa_total_i = sampled["kappa_total"][idx]`, `kappa_share_i = sampled["kappa_share"][idx]`, then `kappa = kappa_total_i * kappa_share_i`, `kappa_s = kappa_total_i * (1.0 - kappa_share_i)`. Decoded values passed to likelihood. Decoding inside the for-loop (not in likelihood). HIER-06 complete.
- **M6b dual L2 regression (locked):** Two independent beta coefficients: `beta_lec_kappa_total` and `beta_lec_kappa_share`, each shifting their respective unconstrained parameter when `covariate_lec is not None`.
- **kappa_share prior (locked):** `mu_prior_loc=0.0` from `PARAM_PRIOR_DEFAULTS["kappa_share"]`; group-mean share near 0.5 a priori on the probit scale (no a priori preference for global vs. stimulus-specific split).
- **phi_rl prior (locked):** `mu_prior_loc=-0.8` from `PARAM_PRIOR_DEFAULTS["phi_rl"]`; same as phi (both are forgetting rates, symmetric justification).

### v4.0 Decisions (16-05 completed 2026-04-12)

- **32 beta sites (not 40) for subscale model (locked):** `wmrl_m6b_hierarchical_model_subscale` has 8 params x 4 covariates = 32 beta sites. Plan said "~40" (5-covariate assumption); actual is 32 per the 16-01 hyperarousal exclusion.
- **beta site naming: `beta_{cov_name}_{param_name}` (locked):** Outer loop over `param_names`, inner loop over `covariate_names`. Example: `beta_lec_total_kappa_total`, `beta_iesr_intr_resid_alpha_pos`.
- **All 8 M6b params bypass `sample_bounded_param` in subscale variant (locked):** Manual `mu_pr/sigma_pr/z` pattern used for all 8 to enable uniform multi-covariate L2 shift application. `sample_bounded_param` is only bypassed in `wmrl_m6b_hierarchical_model_subscale`; other models unchanged.
- **`subscale=True` guard raises `ValueError` (locked):** `--subscale` with model != wmrl_m6b raises `ValueError` (not `NotImplementedError`); the model exists but the subscale variant is M6b-specific.
- **SLURM subscale: 12h/48G (locked):** `cluster/13_bayesian_m6b_subscale.slurm` uses `--time=12:00:00` and `--mem=48G` (vs 8h/32G for standard M6b).
- **beta_* HDI print expanded (locked):** `_fit_stacked_model` now prints all `beta_`-prefixed sites in sorted order (not just `beta_lec_*`), supporting the 32-site subscale output.

### v4.0 Decisions (20-02 completed 2026-04-14)

- **Vectorized Phase 2 in-place modification (locked):** Modified existing pscan functions rather than creating new variants. The pscan path is already the "parallel" path; vectorization completes it.
- **Precompute helpers called inside block functions (locked):** `precompute_last_action_global` and `precompute_last_actions_per_stimulus` called inside each `*_block_likelihood_pscan()`, not hoisted to multiblock level. Keeps block functions self-contained and correct when called standalone.
- **jax.vmap for batched softmax/epsilon (locked):** `jax.vmap(softmax_policy, in_axes=(0, None))` rather than reimplementing a batched version. Clean, reuses existing scalar functions.
- **axis=-1 renormalization for WM-RL models (locked):** `base_probs / jnp.sum(base_probs, axis=-1, keepdims=True)` for batched (T, A) arrays. M1 does not need renormalization (no WM-Q mixing).

### v4.0 Decisions (20-01 completed 2026-04-14)

- **DEER NO-GO (locked):** DEER/quasi-DEER/ELK not applicable to RLWM likelihood. The perseveration carry (`last_action`) is updated with observed actions (data), not model-computed actions. In likelihood evaluation, this is a phantom dependency: `last_action[t] = actions[t-1]` is precomputable from the data array.
- **Vectorized policy GO (locked):** Replace Phase 2 sequential `lax.scan` with vectorized array ops. Precompute `last_action` arrays once before MCMC (parameter-independent). All T trial log-probs computed simultaneously.
- **precompute_last_action_global signature (locked):** `(actions, mask) -> (T,) int32`. Uses `lax.scan` to correctly propagate through masked trials. Located after `associative_scan_wm_update` in jax_likelihoods.py.
- **precompute_last_actions_per_stimulus signature (locked):** `(stimuli, actions, mask, num_stimuli=6) -> (T,) int32`. Uses `lax.scan` with carry shape `(num_stimuli,)`. Returns per-trial last_action for that trial's stimulus.
- **Contraction mapping question moot (informational):** In simulation context, perseveration is NOT a contraction (discrete state, non-differentiable argmax). In likelihood evaluation context, there is no recurrence to analyze.

### v4.0 Decisions (19-03 completed 2026-04-14)

- **use_pscan kwarg propagation via model_args (locked):** `use_pscan` passed as part of the `model_args` dict that gets unpacked as `**model_args` in `mcmc.run()`. NumPyro passes all `run()` kwargs to the model function. No changes to `run_inference_with_bump` or NUTS kernel required.
- **NetCDF pscan suffix (locked):** When `use_pscan=True`, output NetCDF is `{model}_posterior_pscan.nc` (not separate directory). Enables A/B comparison with `{model}_posterior.nc` in same directory.
- **No outer jax.jit in benchmark (locked):** Likelihood functions use Python `if return_pointwise:` branching which is not JIT-compatible. Benchmark calls functions directly; internal `lax.fori_loop`/`lax.scan` handles JIT compilation.
- **CPU pscan baseline (informational):** Pscan ~3.7x slower on CPU for M3 (825ms vs 222ms, 17 blocks x 100 trials). Expected: associative scan has O(2T) work vs O(T) but O(log T) depth. GPU required for speedup.
- **PSCAN-06 criteria (locked):** Group-mean posterior relative error < 5%, WAIC/LOO absolute difference < 1.0. Automated in cluster/13_bayesian_pscan.slurm Stage 3.

### v4.0 Decisions (19-02 completed 2026-04-14)

- **M5 composed affine operator (locked):** For active (s,a) at trial t: `a = (1-alpha)*(1-phi_rl)`, `b = (1-alpha)*phi_rl*Q0 + alpha*r`. For inactive: `a = 1-phi_rl`, `b = phi_rl*Q0`. Fuses phi_rl decay and delta-rule update in a single scan pass.
- **Q_decayed_for_policy post-scan recovery (locked):** `Q_decayed_for_policy = (1-phi_rl)*Q_for_policy + phi_rl*Q0`. Same pattern as wm_for_policy recovery in 19-01 (apply decay to carry-in array). Required for M5 pscan correctness.
- **Agreement threshold (locked):** < 1e-4 relative error for multiblock NLL agreement between pscan and sequential. Q-learning N=154 achieves max rel_err 6.46e-07 (well under threshold).
- **Two-phase pscan architecture (locked):** Phase 1 parallel O(log T) for Q/WM trajectories; Phase 2 sequential O(T) for policy computation (softmax, epsilon, perseveration carry). All 6 models follow this pattern.

### v4.0 Decisions (19-01 completed 2026-04-14)

- **Single-pass WM scan (locked):** `associative_scan_wm_update` uses one scan combining decay+overwrite. `wm_for_policy[t] = (1-phi)*carry_in[t] + phi*wm_init` recovered as post-scan vectorized step (no second scan).
- **Padding trials decay (not identity) (locked):** Confirmed from wmrl_m3_block_likelihood: decay `(1-phi)*WM + phi*baseline` applies on ALL trials. Only the overwrite is masked. Parallel scan base coefficients are decay everywhere; only active+valid positions override to reset `(a=0, b=r)`.
- **wm_after_update[t] = WM after overwrite at trial t (locked):** `= WM_all[t]` directly (no prepend/drop). The prepend/drop indexing applies to `wm_carry_in` (used internally to compute wm_for_policy), not to wm_after_update.
- **Tolerance thresholds (locked):** < 1e-5 relative error for typical alpha (<=0.5), < 1e-3 for extreme alpha (~0.95). Documented in PARALLEL_SCAN_LIKELIHOOD.md Section 5.
- **Alpha approximation scope (locked):** Reward-based approximation affects Q-update scan only (r==1 → alpha_pos vs delta-sign rule). WM overwrite has no approximation (exact reset). WM decay is exact (constant coefficients).

### v4.0 Decisions (18-05 completed 2026-04-13)

- **Methods dual-approach layout (locked):** MLE paragraph first (point estimates, AIC/BIC), hierarchical Bayesian appended after. Two clearly separated approaches. MLE not removed.
- **Level-2 probit regression in Methods (locked):** Design matrix: `lec_total`, `iesr_total`, `iesr_intr_resid`, `iesr_avd_resid`. Hyperarousal excluded (exact linear dependence). beta priors N(0,1) on probit scale. HDI-excludes-zero criterion.
- **Bayesian section renamed (locked):** `### Bayesian Multivariate Regression` -> `### Hierarchical Level-2 Trauma Associations {#sec-bayesian-regression}`. Anchor preserved for cross-references.
- **Stacking weights path (locked):** `output/bayesian/level2/stacking_weights.csv` (matches CMP-04 layout from 18-02).
- **Level-2 forest plot candidate paths (locked):** `output/bayesian/figures/m6b_forest_lec5.png` then `output/bayesian/level2/wmrl_m6b_forest.png`. First found wins.
- **Limitations subsection location (locked):** `### Limitations {#sec-limitations}` inside `## Discussion`, before `## Conclusion`. Covers 4 topics: Pareto-k M4, K identifiability, M6b shrinkage, IES-R orthogonalization.
- **paper.qmd is source of truth (locked):** All edits target paper.qmd; paper.tex is generated by `quarto render`. DOC-02/03/04 complete.

### v4.0 Decisions (18-02 completed 2026-04-13)

- **CMP-04 artifact layout (locked):** `--bayesian-comparison` writes 4 files to `output/bayesian/level2/`: `stacking_weights.md`, `stacking_weights.csv`, `waic_summary.csv`, `m4_comparison.csv`. Every Markdown output has a CSV companion.
- **M4 Pareto-k threshold 5% (locked):** M4 separate track gates LOO reliability on fraction of Pareto-k > 0.7 exceeding 5%. Expected to trigger in production due to joint LBA likelihood.
- **WAIC is secondary metric (locked):** LOO via `az.compare(ic='loo', method='stacking')` remains primary. WAIC computed per model via `az.waic()` loop. `waic_summary.csv` written only if results non-empty.
- **M4 never in compare_dict (locked):** `BAYESIAN_NETCDF_MAP` contains only M1-M6b. M4 separate track loads `output/bayesian/wmrl_m4_posterior.nc` independently inside `run_bayesian_comparison()`.

### v4.0 Decisions (17-03 completed 2026-04-13)

- **Relaxed structural checks for M4 integration tests (locked):** `test_log_delta_recovery` uses delta>0, A>0, b>A checks (not 15% relative error). The 15% criterion applies to the full N=154 cluster fit; at N=10 with short chains the posterior is too diffuse for point recovery.
- **test_checkpoint_resume tests the API directly (locked):** Warmup-pickle-load-resume cycle tested without process-kill simulation. Matches the exact code path in 13_fit_bayesian_m4.py.
- **SLURM GPU env priority reversed vs CPU models (locked):** 13_bayesian_m4_gpu.slurm activates rlwm_gpu first, ds_env as fallback. CPU models prefer ds_env first.
- **Separate JAX cache dir jax_cache_m4_bayesian (locked):** Prevents float64 M4 JIT traces from colliding with float32 choice-only traces in jax_cache_bayesian.
- **NUMPYRO_HOST_DEVICE_COUNT not set in GPU SLURM script (locked):** GPU chain parallelism uses chain_method='vectorized'; the CPU device count env var is irrelevant on GPU and was intentionally excluded.

### v4.0 Decisions (17-02 completed 2026-04-13)

- **Participant-level log-lik for M4 LOO (locked):** `wmrl_m4_multiblock_likelihood_stacked` returns scalar NLL per participant; no `return_pointwise` path in LBA likelihood. `compute_m4_pointwise_loglik` in `13_fit_bayesian_m4.py` computes shape `(chains, samples, n_participants)` via `jax.vmap`. Pareto-k > 0.7 fallback is near-certain in production (expected behavior).
- **3D InferenceData for M4 (locked):** `_build_inference_data_m4` uses `dims=["participant"]` (not `["participant", "trial"]`). Does NOT reuse `build_inference_data_with_loglik` from bayesian_diagnostics.py (which hardcodes 4D shape).
- **Convergence gate writes run metadata on failure (locked):** `_write_run_metadata` called before early-return to ensure `wmrl_m4_run_metadata.json` exists for debugging regardless of convergence.
- **chain_method='vectorized' confirmed for M4 (locked):** Same as choice-only models. `parallel` requires process forking; unavailable on Windows/SLURM.

### v4.0 Decisions (17-01 completed 2026-04-13)

- **Lazy import for lba_likelihood in M4 hierarchical model (locked):** `wmrl_m4_multiblock_likelihood_stacked` and `preprocess_rt_block` imported inside function bodies only. Module-level import of lba_likelihood in numpyro_models.py would activate float64 globally for all choice-only model imports.
- **delta = b - A parameterization (M4H-02, locked):** `delta` sampled log-normal; `b = A + delta` decoded inside participant for-loop. Guarantees b > A without inequality constraints. `b=b_i` (not delta_i) passed to `wmrl_m4_multiblock_likelihood_stacked`.
- **RT padding value = 0.5s (locked):** Padding positions in rts_stacked set to 0.5s to prevent t_star = rt - t0 <= 0 in LBA PDF evaluation. Masked-out trials use safe non-zero RT.
- **No epsilon in M4 hierarchical model (locked):** 10 parameters total (6 RLWM + 4 LBA). Epsilon is a softmax noise term; M4 uses LBA decision dynamics directly.

### v4.0 Decisions (16-07 completed 2026-04-13)

- **L2-08 horseshoe DEFERRED (locked):** Normal(0,1) priors on beta coefficients have not been tested on real data (cluster job not yet run). Horseshoe will be evaluated after cluster job completes by inspecting max_rhat for beta_ sites, posterior SD vs prior scale, and number of 95% HDI exculsions across the 32 sites.
- **18_bayesian_level2_effects.py created (L2-07):** Forest plots grouped by covariate (LEC-5, IES-R, all). `discover_beta_vars()` finds beta_ sites dynamically at runtime — handles any posterior structure. Graceful skip if NetCDF missing.
- **--bayesian-comparison flag in script 14 (SC-6):** `az.compare(ic='loo', method='stacking')` over all 6 choice-only model posteriors. Writes `output/bayesian/level2/stacking_weights.md` with verdict (M6b weight >= 0.5 threshold).

### v4.0 Decisions (16-06 completed 2026-04-13)

- **Permutation shuffles covariate labels only (locked):** `np.random.default_rng(shuffle_idx).permutation(covariate_lec)` randomizes participant-level LEC alignment, not within-participant data. JSON-only output per shuffle (no CSV/NetCDF). Aggregation script provides PASS/FAIL at 5% FPR.
- **Reduced MCMC budget for permutation (locked):** SLURM uses warmup=500/samples=1000 (half of standard). Sufficient for HDI-excludes-zero check; does not need convergence-grade posterior.
- **parser.error() guard at validation time (locked):** `--permutation-shuffle` raises error immediately before data loading if model != wmrl_m3.

### v4.0 Decisions (16-04 completed 2026-04-13)

- **_L2_LEC_SUPPORTED frozenset (locked):** `{wmrl_m3, wmrl_m5, wmrl_m6a, wmrl_m6b}` pass covariate_lec to model; M1/M2 receive None. Guard in `_fit_stacked_model`.
- **log_likelihood group overwrite (locked):** `build_inference_data_with_loglik` deletes pre-existing `log_likelihood` before `add_groups`. `az.from_numpyro` auto-creates per-participant scalar log-probs from `numpyro.factor` sites (wrong shape for WAIC/LOO). Fix committed in `6f99419`.
- **STACKED_MODEL_DISPATCH dispatch pattern (locked):** All 6 choice-only models route through this dict; `save_results` uses `model in STACKED_MODEL_DISPATCH` as the single predicate for convergence gate path.
- **SLURM time limits (locked):** M1=4h, M2=6h, M3=6h, M5=6h, M6a=6h, M6b=8h. M6b extra time for stick-breaking compile overhead.

### v4.0 Decisions (16-02 completed 2026-04-12)

- **qlearning_hierarchical_model_stacked signature (locked):** No `set_sizes_stacked` passed to likelihood; `covariate_lec=None` guard raises `NotImplementedError` if non-None (no natural L2 target for M1). HIER-02 complete.
- **wmrl_hierarchical_model_stacked signature (locked):** `set_sizes_stacked=pdata["set_sizes_stacked"]` always passed to likelihood; `covariate_lec=None` guard raises `NotImplementedError` if non-None (no perseveration parameter as L2 target for M2). HIER-03 complete.
- **Stacked-format pattern established for M1/M2:** `sorted(participant_data_stacked.keys())` for participant ordering; `sample_bounded_param` loop over param list from `PARAM_PRIOR_DEFAULTS`; `numpyro.factor` per participant. All Phase 16-17 stacked models follow this template.

### v4.0 Decisions (15-02 completed 2026-04-12)

- **run_inference_with_bump pattern (locked):** Python retry loop over `target_accept_probs=(0.80, 0.95, 0.99)`; reads `mcmc.get_extra_fields()["diverging"].sum()`; returns immediately on zero divergences; falls through to last run when exhausted. All Phase 16-17 hierarchical models should use this instead of `run_inference`.
- **Convergence gate location in save_results():** Gate lives inside `save_results()` (not `fit_model()`), so the MCMC result is always returned to the caller. Early return from `save_results()` without writing any files is the HIER-07 enforcement point.
- **filter_padding_from_loglik returns NumPy array:** Converts JAX DeviceArray to `np.array(float32)` before masking. ArviZ `add_groups()` requires NumPy-backed arrays; keeping as JAX array causes implicit conversion errors.
- **LEC covariate column name confirmed:** `less_total_events` (not `lec_total_events`) in `output/summary_participant_metrics.csv`. Z-scored in `fit_model()` before passing to model. Falls back to `covariate_lec=None` with warning if CSV or column missing.
- **Shrinkage formula:** `1 - var_indiv / (var_group_mean + 1e-10)` where `var_indiv = np.var(flat)` (all draws AND participants), `var_group_mean = np.var(flat.mean(axis=1))`. Threshold 0.3 for "identified" flag.

### v4.0 Decisions (15-01 completed 2026-04-12)

- **Compile-gate relaxed to 120s for CPU:** Warm M3 (7 params, 5 ppts, 300 iterations) takes 65-80s on CPU; 60s is a GPU cluster target. Use 120s gate for local CI; expect < 60s on cluster. Same relaxation anticipated for M6b in Phase 16.
- **Cold/warm protocol for M3 smoke test:** `test_smoke_dispatch` primes JIT with a cold 5/5-sample run, then times the full 100/200-sample warm run. Mirrors `test_compile_gate.py` pattern.
- **kappa_z uses `.expand([n_participants])` not `numpyro.plate`:** Avoids plate name conflicts; matches `sample_bounded_param` internal pattern. All future M3-family models should use the same approach.
- **prepare_stacked_participant_data sorts participants:** `sorted(data_df[participant_col].unique())` ensures downstream covariate arrays (e.g., `covariate_lec`) align with `sorted(result.keys())`. This is a correctness constraint.

### v4.0 Decisions (14-02 completed 2026-04-12)

- **fit_all_gpu_m4 float64 ordering (locked):** `jax.config.update("jax_enable_x64", True)` must be the FIRST statement in `fit_all_gpu_m4` — cannot be toggled after first JAX array is materialised.
- **lba_likelihood lazy import in wrapper:** Mirrors `main()` pattern (line 2830); importing `wmrl_m4_multiblock_likelihood_stacked` reinforces float64 globally.
- **Smoke test @pytest.mark.slow:** `test_fit_all_gpu_m4_smoke` with `n_starts=5` validates JIT compilation + finite NLL without targeting parameter recovery.
- **GPU-01 requirement complete:** `fit_all_gpu_m4` callable exists in `fit_mle.py` with correct float64 initialization sequence and delegation to `fit_all_gpu`.

### v4.0 Decisions (14-01 completed 2026-04-12)

- **K bounds [2,6] enforced in mle_utils.py:** All 6 WM capacity BOUNDS dicts updated to `(2.0, 6.0)`. K-02 requirement complete.
- **Version stamp pattern (locked):** `fits_df['parameterization_version'] = EXPECTED_PARAMETERIZATION[args.model]` in `fit_mle.py main()` before `to_csv`. Enables `load_fits_with_validation()` downstream rejection of stale fits.
- **Pre-existing test failure:** `test_compile_gate.py::test_compile_gate` fails (JAX scan shape in fixture). Confirmed pre-existing before this plan. Phase 15 must resolve.

### v4.0 Decisions (13-05 completed 2026-04-12)

- **compute_pointwise_log_lik dispatch pattern:** `model_name` string dispatches to correct stacked likelihood fn; `jax.vmap` over (chains, samples_per_chain); returns shape `(chains, samples, participants, n_blocks*max_trials)`. Padded trials inherit log_prob=0.0 from mask.
- **M6b decoding in diagnostics:** `kappa_total*kappa_share -> kappa`, `kappa_total*(1-kappa_share) -> kappa_s` decoded inside `_build_per_participant_fn`, NOT in the likelihood fn itself.
- **Schema-parity column order (locked):** `participant_id -> params -> nll/aic/bic/aicc/pseudo_r2 -> {param}_hdi_low/high/sd -> max_rhat/min_ess_bulk/num_divergences -> n_trials/converged/at_bounds -> parameterization_version`. No `grad_norm`, `hessian_*`, `_se`, `_ci_*`, `high_correlations`.
- **converged gate (locked):** `max_rhat < 1.01 AND min_ess_bulk > 400 AND num_divergences == 0` (strict inequalities).
- **SLURM Bayesian script:** `time=06:00:00`, `mem=32G`, `NUMPYRO_HOST_DEVICE_COUNT=1`; JAX cache block verbatim from 12_mle_gpu.slurm.

### v4.0 Decisions (13-01 completed 2026-04-12)

- **P0 import fixed:** `scripts/fitting/numpyro_models.py` now exists at canonical path. `from scripts.fitting.numpyro_models import ...` resolves without error.
- **PyMC fully removed (INFRA-07 executed):** `pyproject.toml`, `pytest.ini`, `environment_gpu.yml`, `16b_bayesian_regression.py`, and `validation/test_pymc_integration.py` all updated/deleted.
- **Deps pinned:** `numpyro==0.20.1`, `arviz==0.23.4`, `netcdf4` added to all dep specs.

### v4.0 Decisions (13-04 completed 2026-04-12)

- **phi_approx = jax.scipy.stats.norm.cdf (LOCKED):** Not expit, not polynomial approximation. Named function in numpyro_helpers.py for grep-ability.
- **hBayesDM non-centered convention established:** `mu_pr ~ Normal(0,1)`, `sigma_pr ~ HalfNormal(0.2)`, `theta = lower + (upper-lower)*Phi_approx(mu_pr + sigma_pr*z)`. Single implementation in `sample_bounded_param`, used by all Phase 15-17 hierarchical models.
- **PARAM_PRIOR_DEFAULTS locked:** 11 parameters with shifted mu_prior_loc priors. LBA params (v_scale, A, delta, t0) excluded — handled separately in M4 hierarchical model.
- **parameterization_version validation gate active:** `load_fits_with_validation()` in config.py raises ValueError with expected vs actual on mismatch. Phase 14 MLE refit must stamp `"v4.0-K[2,6]-phiapprox"` on output CSVs.
- **float32 phi_approx saturates at ~±6 sigma:** Tests use ±5 (not ±10) to stay non-degenerate.

### v4.0 Decisions (13-03 completed 2026-04-12)

- **return_pointwise pattern:** `*_block_likelihood` functions expose per-trial log-probs via `*, return_pointwise: bool = False`. Plain Python `if` branch (not `jax.lax.cond`) - JIT specializes on static bool via `static_argnames`.
- **Stacked wrapper pointwise uses lax.scan** over block indices (not lax.fori_loop) because scan accumulates outputs. Flat reshape `all_block_probs.reshape(-1)` yields `(n_blocks * MAX_TRIALS_PER_BLOCK,)` for arviz.
- **Padding positions have log_prob=0.0** in pointwise output. Plan 05 `bayesian_diagnostics.py` must filter mask==0 positions before `az.waic()` / `az.loo()` to avoid inflating WAIC effective parameter count.

### v4.0 Decisions (13-02 completed 2026-04-12)

- **K bounds [2, 6] confirmed (K-01):** Lower bound = 2 matching Senta, Bishop, Collins (2025) PLOS Comp Biol 21(9):e1012872 AND structural identifiability (K<2 confounded with rho at ns=2). Upper bound = 6 (task max ns, K>6 non-identified). parameterization_version = "v4.0-K[2,6]-phiapprox". Phase 14 MLE refit must adopt same bounds. Reference: `docs/K_PARAMETERIZATION.md`.
- **Non-centered K transform established:** `K_i = 2.0 + 4.0 * Phi_approx(mu_K_pr + sigma_K_pr * z_K_i)` where `Phi_approx = jax.scipy.stats.norm.cdf`. Group priors: `mu_K_pr ~ Normal(0,1)`, `sigma_K_pr ~ HalfNormal(0.2)`.

### v4.0 Decisions (set at milestone definition 2026-04-11)

- **M4 hierarchical IS in scope** despite research recommendation to descope. Phase 17 committed. Accept ~150-200 GPU-hour total budget, Pareto-k fallback for M4-vs-choice-only comparison.
- **PyMC dropped entirely** from `16b_bayesian_regression.py`; NumPyro-only backend for v4.0.
- **IES-R subscale orthogonalization:** IES-R total + Gram-Schmidt residualized subscales as default; horseshoe prior (L2-08) is P2 optional upgrade.
- **Schema-parity CSV pattern** is the migration cornerstone — downstream scripts 15/16/17 get a single `--source mle|bayesian` flag with no logic rewrite.
- **P0 broken import** (`fit_bayesian.py:43` imports from `scripts.fitting.numpyro_models` but file is in `legacy/`) is Phase 13 Task 1.
- **Compile-time gate:** < 60s for M3 hierarchical; may need relaxation for M6b (unconstrained stick-breaking compiles slower, no benchmark yet).
- **Phase ordering:** P13 → P14 → P15 → P16 → P17 → P18. P15 (M3 POC) is the validation gate before P16 mechanical extension. P17 depends on both P14 (GPU LBA) and P16 (M6b non-centered pattern as template).

### v3.0 Model Decisions (retained for reference)

- Build order: M5 → M6a → M6b → M4 (complexity-ordered; M5 validated pipeline integration pattern)
- M4 gets separate comparison track in `compare_mle_models.py` (joint likelihood incommensurable with choice-only AIC) — **same constraint carries forward into v4.0 Phase 18**
- Parameter recovery r >= 0.80 is a hard gate per model — **replaced in v4.0 by hierarchical shrinkage diagnostic `1 - var_post_individual / var_post_group >= 0.3` plus convergence gate**
- MODEL_REGISTRY in config.py is single source of truth for pipeline scripts
- CHOICE_ONLY_MODELS = ['qlearning', 'wmrl', 'wmrl_m3', 'wmrl_m5', 'wmrl_m6a', 'wmrl_m6b'] — M4 excluded from cross-model AIC comparison
- M6b: stick-breaking decode in objective functions only (not in transform): `kappa = kappa_total * kappa_share`; `kappa_s = kappa_total * (1 - kappa_share)`. **v4.0 HIER-06 uses non-centered offset on the unconstrained scale and decodes inside the likelihood.**

### Pending Todos

- **Re-fit all 7 models on cluster** (3 bugs fixed: argmin NaN, stimulus sampling, reward mapping). See `.planning/todos/pending/2026-04-07-refit-all-models-on-cluster.md`
- Run parameter recovery for all models after re-fit (50 subj / 3 datasets / 20 starts) — superseded by Phase 14 Collins K refit
- Run full cross-model recovery: `python scripts/11_run_model_recovery.py --mode cross-model --model all --n-subjects 50 --n-datasets 3 --n-starts 20 --n-jobs 8`

### Roadmap Evolution

- **Phases 19-20 added (2026-04-12):** GPU-accelerated likelihood via associative scan (Phase 19) and DEER non-linear parallelization research (Phase 20). Motivated by analysis of why naive vmap-over-participants was 7-13x slower (memory-bandwidth bottleneck, not compute-bound). The real opportunity is parallelizing the TIME dimension via O(log T) associative scan, not the participant dimension. Phases 15-16 must log CPU wall-clock timing as baseline for Phase 19 benchmarking. Key references: PaMoRL (NeurIPS 2024), DEER (ICLR 2024), S4/Mamba, Unifying Framework (TMLR 2025).
- **CPU confirmed correct for Phases 15-18:** RLWM Q-value/WM updates have arithmetic intensity ~0.3 FLOP/byte (GPU needs >50 to saturate). CPU L1 cache (~1ns) beats GPU global memory (~200-400 cycles) for 18-float Q-tables. Associative scan changes the algorithm, not the hardware access pattern.
- **Phase 28 added (2026-04-21):** Bayesian-First Manuscript Restructure & Repo Consolidation. Two-pronged scope coupling paper narrative reframing (5-section Bayesian-first Results: summary → Bayesian fitting & winner → hierarchical L2 trauma regression on winner(s) → subscale breakdown → MLE + recovery validation in appendix with Bayesian↔MLE parameter comparison) with repo-wide consolidation (authoritative `src/` models, data-processing 01-04 merged, behavioral 06-08 grouped, 09-11 simulation/recovery unified, 16-21 post-fit scripts consolidated, `figures/`+`output/` reorganized, per-model `cluster/13_bayesian_*.slurm` templates parameterized, stale `validation/`+`tests/` moved to `legacy/`, `environments/` vs `src/` split resolved, docs refreshed). Added via `/gsd:add-phase` so it sits at numeric end (Phase 28 after Phase 27 closure) — ROADMAP.md "Sequencing Consideration" block documents three resolution options (reorder to 23→24→25→28→26→27, spin into new v5.1/v6.0 milestone, or accept post-closure execution). Motivated by user's 2026-04-21 decision after narrative analysis confirmed Bayesian-first framing is methodologically cleaner for this dataset (per-subject MLE retained as appendix validation + shrinkage diagnostic).
- **Phase 21 added (2026-04-18):** Principled Bayesian Model Selection Pipeline. Replaces the MLE-preselected "M6b is the winner" flow with a linear 9-step Bayesian workflow: prior predictive (21.1) → Bayesian parameter recovery (21.2) → hierarchical fit no-scales baseline (21.3) → convergence+PPC audit (21.4) → PSIS-LOO + stacking weights + RFX-BMS/PXP ranking (21.5) → winner(s) refit with L2 scales (21.6) → scale-fit audit (21.7) → model-averaged scale effects and exploratory M6b-subscale arm (21.8) → manuscript tables (21.9). Each step = separate SLURM submission, re-runnable from cold start. Anchored to Baribault & Collins (2023, *Psychological Methods*, DOI 10.1037/met0000554) and Hess et al. (2025, *Computational Psychiatry* 9(1):76–99, DOI 10.5334/cpsy.116). AIC/BIC deprecated for hierarchical-Bayesian comparison. ~70% of existing infrastructure (simulator, generate_data, jax_likelihoods, numpyro_models, fit_bayesian, compute_pointwise_log_lik, recovery structure) is directly reusable; ~30% new (orchestrators `scripts/21_*.py`, SLURM scripts `cluster/21_*.slurm`, RFX-BMS implementation in `scripts/fitting/bms.py`). `validation/compare_posterior_to_mle.py` retained as sanity check only, not selection criterion. Motivated by user flagging that (a) MLE AIC pre-selection of M6b is circular for the subsequent Bayesian test of κ×LEC, (b) step-4 PPC anchored to MLE is MLE-anchored, (c) choice-fit comparison and scale-effect inference were entangled and need separation.

### Blockers/Concerns

- **Compile-time gate on M6b**: constrained `kappa_total`/`kappa_share` under non-centered hierarchical sampling may compile slower than the 60s target. Phase 13 may need to relax the gate specifically for M6b. (From research: PITFALLS.md confidence MEDIUM on this point.)
- **Hierarchical LBA has no NumPyro/JAX precedent.** Phase 17 is effectively a research project nested in the milestone — if the non-centered `log(b - A)` + `post_warmup_state` resume pattern fails at scale, Phase 17 falls back to reporting M4 at MLE only.
- **Pareto-k > 0.7 is near-certain for LBA under NUTS.** Phase 17 MUST include the choice-only-marginal fallback path; M4 cannot sit inside a unified `az.compare` table regardless of how it's fit.
- **IES-R subscale correlations in N=154 not yet audited** — Phase 16 begins with a collinearity audit; if condition number after orthogonalization remains > 30, the orthogonalization strategy must be revisited.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 001 | Setup Quarto scientific manuscript for RLWM trauma analysis | 2026-04-05 | 18637da | [001-setup-quarto-manuscript](./quick/001-setup-quarto-manuscript/) |
| 002 | Pipeline fixes, convergence assessment, recovery config, MODEL_REGISTRY | 2026-04-07 | 3095b92 | [002-pipeline-fixes-convergence-recovery-config](./quick/002-pipeline-fixes-convergence-recovery-config/) |
| 003 | Softcode manuscript: winning model, group names, n_starts from data files | 2026-04-07 | d7ea897 | [003-quarto-softcoded-winning-model](./quick/003-quarto-softcoded-winning-model/) |
| 004 | Pipeline sync: survey data fix (scripts 15/16), uncorrected p-values in manuscript, Bayesian MODEL_REGISTRY | 2026-04-07 | 4df1340 | [004-pipeline-sync-uncorrected-peb-config](./quick/004-pipeline-sync-uncorrected-peb-config/) |
| 005 | Re-run pipeline (N=154), model overview + distribution figures in manuscript | 2026-04-08 | 6b045a4 | [005-rerun-pipeline-analyses-update-quarto-manuscript](./quick/005-rerun-pipeline-analyses-update-quarto-manuscript/) |
| 006 | Post-refit verification: M6b winner, BIC + winner heterogeneity + FDR/Bonferroni + manuscript revision | 2026-04-10 | a01febd | [006-post-refit-verification-recovery-manuscript](./quick/006-post-refit-verification-recovery-manuscript/) |
| 007 | GPU MCMC speedup: chain_method=vectorized on GPU + GPU config logging + vmap-over-participants refactor for wmrl_m3 (POC) | 2026-04-16 | 1ac963c | [007-gpu-mcmc-speedup-vmap-chain-method](./quick/007-gpu-mcmc-speedup-vmap-chain-method/) |
| 008 | Cleanup pipeline + docs per 2026-04-17 audit: prune 7 orphaned scripts, move 3 superseded docs to legacy/, scaffold docs/04_methods + 04_results, fix scale_distributions.png path | 2026-04-17 | 2c4f12a | [008-cleanup-pipeline-and-docs](./quick/008-cleanup-pipeline-and-docs/) |
| 009 | Integrate Burrows thesis content into paper.qmd: line-cited catalog (INTEGRATION_MAP.md), 28 reusable passages (~1,850 words), 9 divergence flags, no auto-edits applied | 2026-04-17 | 8ccdc4b | [009-integrate-burrows-thesis-into-paper](./quick/009-integrate-burrows-thesis-into-paper/) |

## Session Continuity

Last session: 2026-04-22 (Phase 29 Wave 4 execution — 29-05 cluster SLURM consolidation + 29-06 paper.qmd smoke render, parallel)
Stopped at: Plans 29-05 + 29-06 both COMPLETE. Wave 4 finished. 29-05 shipped 2 atomic commits (f81b999 path-update + a7159e6 stage-numbered consolidation) + 1 parallel-race-absorbed metadata commit (955f903). cluster/submit_all.sh is the new canonical master entry (afterok chain 01..06 + --dry-run path validator); cluster/21_submit_pipeline.sh retained as shim. 6 stage-numbered entry SLURMs live. v4 closure 5/5 + pytest 3/3 PASS. Ready for Wave 5 (29-07 closure-guard extension, 29-08 src/fitting vertical refactor).
Resume file: None

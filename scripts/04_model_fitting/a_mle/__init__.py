# Stage folder name starts with a digit: use relative imports within this package (e.g. `from .fit_mle import main`), never `from scripts.04_model_fitting.a_mle.fit_mle` (Python dotted names cannot start with a digit).
"""MLE fitting entry-point and implementation (04a)."""

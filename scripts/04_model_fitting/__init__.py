# Stage folder name starts with a digit: use relative imports within this package (e.g. `from .a_mle.fit_mle import main`), never `from scripts.04_model_fitting.a_mle.fit_mle` (Python dotted names cannot start with a digit).
"""Model fitting stage: MLE (a_mle/), Bayesian (b_bayesian/), Level-2 (c_level2/) (04)."""

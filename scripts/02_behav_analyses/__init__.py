# Stage folder name starts with a digit: use relative imports within this package (e.g. `from .05_summarize import main`), never `from scripts.02_behav_analyses.*` (Python dotted names cannot start with a digit).
"""Behavioral-analysis pipeline scripts (05–08)."""

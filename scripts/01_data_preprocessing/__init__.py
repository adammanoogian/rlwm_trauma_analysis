# Stage folder name starts with a digit: use relative imports within this package (e.g. `from .01_parse_raw_data import main`), never `from scripts.01_data_preprocessing.01_parse_raw_data` (Python dotted names cannot start with a digit).
"""Data processing pipeline scripts (01–04)."""

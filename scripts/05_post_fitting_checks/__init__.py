# Stage folder name starts with a digit: use relative imports within this package (e.g. `from .baseline_audit import run_audit`), never `from scripts.05_post_fitting_checks.*` (Python dotted names cannot start with a digit).
"""Post-fitting convergence checks: Rhat/ESS audit, scale orthogonalization (05)."""

# Stage folder name starts with a digit: use relative imports within this package (e.g. `from .compare_models import main`), never `from scripts.06_fit_analyses.*` (Python dotted names cannot start with a digit).
"""Substantive fit analyses: model comparison, LOO stacking, trauma regressions, manuscript tables (06)."""
